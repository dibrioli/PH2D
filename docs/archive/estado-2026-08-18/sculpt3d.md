# 3D / Sculpt — estado do §5 em 2026-08-18

> **Arquivo histórico.** Este é o texto do `CLAUDE.md` §5 **verbatim**, como estava em
> 2026-08-18, antes de o §5 voltar a ser um roteador. O **estado vivo** (o que está aberto,
> o handoff corrente) fica no [`CLAUDE.md`](../../../CLAUDE.md) §5; o **mecanismo** fica nos
> handoffs que este texto cita. Nada aqui foi editado — só recortado.

---

- **⬛ 3D / SCULPT — a MALHA (W1: M1+M2+M3), `line/sculpt3d` INTEGROU (2026-07-30, 7 commits, smoke aprovado; [ADR-0150](../../architecture/decisions/0150-3d-sculpt-is-a-mesh-that-donates-shading-sculptgl-referenced.md) · cofre [`docs/3D/`](../../3D) · handoff [`docs/3D/handoffs/HANDOFF_INTEGRACAO_line_sculpt3d_W1_2026-07-30.md`](../../3D/handoffs/HANDOFF_INTEGRACAO_line_sculpt3d_W1_2026-07-30.md)):** módulo NOVO, **drop-crate puro** — três crates (`ph2d-mesh` a malha residente + octree + normais · `ph2d-mesh-render` o passe wgpu + matcap procedural · `ph2d-sculpt3d` o kernel de pincel), **zero crate foundational tocada, zero contrato congelado, zero schema, zero id/token/variant**. Referência: **SculptGL (MIT)**, vendorizada como licença. ⚠️ **A navegação orbital mora no SHELL, nunca numa `Tool`** — e é essa decisão que mantém a superfície congelada (`Tool=12`/`RasterEditTool=5`/…) fora do caminho. A feature `sculpt3d` está na lista **`default`** do shell **de propósito**: o `ship.sh` roda clippy **sem** `--all-features`, e atrás de uma feature desligada este código não seria lintado. Sem a env var o `AppGfx.sculpt3d` é `None` e cada porta devolve `false` no primeiro `if` ⇒ **o frame 2D é byte-idêntico**. **⚠️ A APOSTA CENTRAL DO ADR ESTÁ MEDIDA E VALE:** com a **pegada FIXA**, 10× a malha custa **0,79×** (dab) e **1,04×** (refresh de normais) — *o custo é da PEGADA, não da malha*. **⚠️ E os dois kill-criteria disparam — mas o K2 aponta para o LUGAR ERRADO, e isso é o achado da wave:** a 5 M triângulos com pincel de 30% (484k vértices) o dab custa **20,2 ms** contra o K1 de 8; o K2 manda *"recomputar as normais passa de 2 ms ⇒ migre para a GPU"*, e as normais custam **1,66 dos 13,1** ⇒ **seguir o critério ao pé da letra consertaria 13% do problema**. O custo real é **DESCOBRIR A VIZINHANÇA** (11,5 ms = **88% do refresh**), dois passes seriais de dedup. ⚠️ **E o regime que dispara é extremo:** no pincel de **detalhe** (2% do modelo) o mesmo dab custa **0,566 ms**, 14× sob o teto — o K1 só dispara com um pincel que cobre **19% da malha inteira**. **Três caminhos, e a escolha é de PRODUTO** (paralelizar a descoberta — os 32 threads já renderam 6,4× nas normais · migrar o kernel para a GPU, mirando o alvo certo desta vez · **aceitar**, porque é exatamente onde a multiresolução existe para pôr o artista num nível mais baixo). ⚠️ **A porta `sculpt_kernel_device` NÃO foi construída, de propósito:** uma porta com uma resposta só e um variant inalcançável é um controle que não faz nada — ela nasce junto com o caminho de GPU, se ele nascer. ⚠️ **O ADR nasceu 0145 e RENUMEROU para 0150 NA INTEGRAÇÃO** (a 6ª vez no repo): Painter levou 0145·0146·0147, Vector o 0148 e physics o 0149 na mesma janela. ⚠️ **E o rewrite do token é ESCOPADO aos arquivos da LINHA** — o controle mediu **10+ arquivos ALHEIOS** citando `ADR-0145` (o do wet-paint); um rewrite sobre a árvore os corromperia ([[feedback_a_token_rewrite_scopes_to_the_changed_files_not_the_whole_tree]]). Smoke: **`env PH2D_SCULPT3D_SMOKE=1 cargo run -p ph2d-host-desktop --release`** — a cena imprime o que montou (`esfera com 6050 vértices / 6144 faces / 12096 triângulos`); ⚠️ **se essa linha não aparecer, pare**. Julgue: a esfera de barro sombreada · **esquerdo gira** (arrastar para baixo olha de cima) · **meio desloca**, **roda aproxima**, com o mesmo efeito aparente perto e longe · e **rode uma vez SEM a env var** — é a metade do smoke que prova a inércia. Gates de GPU `#[ignore]`, **4/4 na RTX**. **Aberto e nomeado:** o **octree não é atualizado por um dab** (depois de deslocar vértices ele descreve as posições anteriores — invisível enquanto o dab move menos que a folga das caixas frouxas; a atualização incremental é da W2) · **a W2 muda a LEI DO TRAÇO**, não o custo: `apply_dab` lê a normal **VIVA**, correto para um toque isolado e **errado para uma sequência** — é a doença do produto-por-dab que a `line/Painter` curou **quatro vezes**, e o `pre` congelado no pen-down é da W2 · `ph2d-sdf` e `ph2d-light` seguem vazias · o matcap é procedural · **sem culling, de propósito** (casca aberta / winding misto de OBJ de terceiro). **⬛ A DOAÇÃO CHEGA À TINTA — W2+W3, `line/sculpt3d` INTEGROU (2026-08-01, 23 commits no consolidado, smoke aprovado: *"Smoke OK"*; handoff [`docs/3D/handoffs/HANDOFF_INTEGRACAO_line_sculpt3d_W3_2026-07-31.md`](../../3D/handoffs/HANDOFF_INTEGRACAO_line_sculpt3d_W3_2026-07-31.md)):** a **W2** deu o gesto de escultura e a **W3** entregou a razão de o módulo existir — **a malha DOA a normal, e a tinta CHAPADA sai acesa pela FORMA**. Três costuras, **todas aditivas com default neutro**, exatamente as que o `02.3` previu: o passe de luz do impasto aceita um **plano de forma** (`form: Option<&[f32]>`, binding 8, `Globals.has_form` na vaga do `pad0`) e **`None` ⇒ byte-idêntico**; o `shade_over` **compõe duas fontes de normal** e sem forma é a expressão antiga **verbatim**; e a shell ganha `donated_form`/`sculpt3d_canvas_done`. ⚠️ **`ph2d-light` deixou de estar VAZIA e virou o dono do rig de luz** (as lâmpadas, a resolução graus→vetor, o piso ambiente) — o `impasto_rig.rs` do Painter virou **re-export** dela, e o piso da elevação do painel passou a vir de `ph2d_light::MIN_ELEV_DEG` em vez de um literal duplicado: *o rig de luz tem um dono*. ⚠️ **E ela passa a ser NÃO-REMOVÍVEL** — a única exceção ao módulo-removível do `02.3`, decisão já registrada lá. **`PROJECT_SCHEMA` intocado** (nada disto é serializado: a escultura vive num canvas) · registro do `ph2d-ecs` **intocado** (nenhum componente novo) · **nenhum ADR novo** (tudo sob o ADR-0150). Smoke: **`env PH2D_SCULPT3D_SMOKE=2 cargo run -p ph2d-host-desktop --release`**. **⬛ O TRAÇO FICA HONESTO, A MALHA PUXA, A RESOLUÇÃO DEIXA DE SER FIXA, E A CENA VIRA UMA LISTA — W4..W8.2, `line/sculpt3d` INTEGROU (2026-08-02, 32 commits, smokes aprovados wave a wave; handoff [`docs/3D/handoffs/HANDOFF_INTEGRACAO_line_sculpt3d_W4-W8_2026-08-02.md`](../../3D/handoffs/HANDOFF_INTEGRACAO_line_sculpt3d_W4-W8_2026-08-02.md)):** cinco waves. **W4 — o traço fica honesto:** os três defeitos do livro-razão, o plano ajustado sobre o conjunto **FRONTAL**, o raio em **pixels de TELA**, o caminho **PERCORRIDO** entre dois eventos (a lei que esta casa já pagou quatro vezes no relevo do Painter: *o traço é fato do CAMINHO, nunca de quão fino o motor amostrou o caminho*) e a **máscara visível** com as quatro operações. **W5 — a malha puxa:** **Grab · Snake Hook · Twist · Inflate/Magnify**, e o espelho passa a alcançar o gesto. **W6 — a resolução deixa de ser fixa:** a **aresta** (as duas regras de borda do laplaciano), a **subdivisão**, a **multiresolução**, o **refazer**, a **reversão** e o **fechar buraco**. **W7 — o botão REMESH:** malha → campo de distância com sinal → **Surface Nets**, e a `ph2d-sdf` **deixa de estar vazia** (ela tinha 4 linhas e virou a quarta crate do módulo). ⚠️ **O Surface Nets entrou ANTES do marching cubes de propósito:** ele devolve **um vértice por célula** e valência 4 quase em toda parte — a topologia que um escultor quer receber —, enquanto o MC devolve triângulos finos que **subdividem mal**. **W8.1/W8.2 — a cena é uma LISTA:** a `Pose` por peça, o pick que compara em MUNDO, o `ObjectId` durável, e os verbos **acrescentar / duplicar / apagar**. ⚠️ **São TRÊS arquivos fora do módulo, e só três:** `keyboard.rs` (**+4**, dentro do `#[cfg(feature = "sculpt3d")]` que já existia) · `shells/desktop/Cargo.toml` (o `ph2d-sdf` **opcional**, dentro da feature) · e duas arestas **internas de path** no `Cargo.lock` — **nenhuma dep externa nova**. A promessa de removibilidade do `02.3` segue verificável: as quatro crates caem com a feature, e desligá-la não toca em nada do 2D. ⚠️ **`rayon` NÃO entrou na `ph2d-sdf`, e a ausência está escrita no `Cargo.toml` dela com o mecanismo:** o flood fill é **sequencial por semântica** e as caixas de dois triângulos **se sobrepõem**, então a escrita do voxelizador **não é disjunta** — a condição que o ADR-0109 exige. **`PROJECT_SCHEMA` 48 INTOCADO** (nada desta linha é serializado: a escultura vive num viewport solto e **não é salva por nada** — é literalmente a wave seguinte, W8.3), registro do `ph2d-ecs` intocado, **nenhum ADR novo** (as cinco waves rodam sob o **ADR-0150**), contrato congelado **4/4 + 3/3** por gate. ⚠️ **As waves INTERCALAM na história** (a W5.0 é o commit 16, a W6.0 o 17, a W5.1 o 18, a W5.2 só fecha no 25) — foi a ordem em que o Enio smokou, e **não há fronteira limpa de wave para cortar**. Smokes (`--release`): **`PH2D_SCULPT3D_SMOKE=3`** (a REVERSÃO, `J`) · **`=4`** (FECHAR BURACO, `O`) · **`=5`** (TORCER e INFLAR, `T`·`A`) · **`=6`** (o REMESH, `V`) · **`=7`** (a CENA É UMA LISTA). ⚠️ **Três cenas imprimem o número que as torna válidas** — a `=4` quantas arestas de beira a malha tem (*se for zero, PARE*), a `=6` quanto mede a maior aresta (*se não passar de ~0,15, PARE*), a `=7` quantas peças abriu; **se a linha não aparecer, o resto do smoke não diz nada**. E **rode uma vez SEM a env var**: é a metade que prova a inércia. **Aberto e NOMEADO, não escondido:** o documento (W8.3 — o `LayerKind::Sculpt3d` segue **não-apendado** de propósito, porque *um variant que ninguém constrói é um variant morto*) · import/export STL/PLY/OBJ (W8.4) · o **objeto misto** (W8.5, sprite com malha filha — é onde o objetivo 2 existe) · merge e isolate · o marching cubes · ⚠️ **o remesh RECUSA com a pilha de multires montada, e a recusa é nomeada no log** (a alternativa seria achatar a pilha **em silêncio**, que é destruir trabalho autorado sem dizer) · a resolução do remesh não é autorável (o botão usa o default 150) · e o campo **não carrega cor, material nem a MÁSCARA**. **⬛ A ESCULTURA É SALVA, E A MALHA VIRA OBJETO DA CENA — W8.3→W9.1, `line/sculpt3d` INTEGROU (2026-08-04, 29 commits; handoff MESTRE [`docs/3D/handoffs/HANDOFF_INTEGRACAO_line_sculpt3d_MESTRE_2026-08-04.md`](../../3D/handoffs/HANDOFF_INTEGRACAO_line_sculpt3d_MESTRE_2026-08-04.md)):** **`PROJECT_SCHEMA` 50→52** — **DOIS** degraus: **v52** o documento da escultura (`sculpt`, um blob que carrega a própria versão `SCULPT_DOC_VERSION` — o precedente EXATO do `TimelineDoc`, e é ele que deixa o módulo evoluir muitas waves sem tocar este número de novo) e **v53** os canais assados (`baked_forms`: o `base`, a `form` e o RIG de cada objeto que uma malha acendeu). ⚠️ **O campo é `Vec<u8>` INCONDICIONAL, sem `cfg`** — um campo condicional daria DUAS formas de arquivo sob UM número de schema, e é o que tornaria um build sem a feature um **triturador** em vez de um passa-adiante; e o `baked_forms` **não** entrou no blob `sculpt` embora ele já guarde as malhas, porque o parser dele é `cfg(feature = "sculpt3d")` e um objeto assado tem de ser legível **SEM o módulo 3D no build**. Registro do `ph2d-ecs` **46→47** (`BakedForm`) e os espelhos **47→48**. Dois `Cargo.toml` (serde na `ph2d-mesh` + `postcard` dev-dep; serde na `ph2d-light`), **nenhuma dep externa nova**. ⚠️ **E o §3 do handoff avisou o corte que quebrou a linha vizinha:** *o `project.rs` foi PARTIDO, e uma linha que edite o corpo do `project_load_from` funde limpo contra um arquivo de onde a função saiu* — foi exatamente o que aconteceu com o `project_tokens::install` da `line/Vector` (ver a entrada dela). **⬛ A OCLUSÃO DE FORMA — `line/sculpt3d` INTEGROU (2026-08-08, 48 commits, 169 arquivos, +27.750/−1.147; [ADR-0156](../../architecture/decisions/0156-sculpt3d-ao-trace-is-a-per-vertex-gather-rayon-exception.md), handoff MESTRE [`docs/3D/handoffs/HANDOFF_INTEGRACAO_line_sculpt3d_MESTRE_2026-08-08.md`](../../3D/handoffs/HANDOFF_INTEGRACAO_line_sculpt3d_MESTRE_2026-08-08.md)):** **`PROJECT_SCHEMA` 55→56 na linha, CONTADO para 63** (a `line/Vector` levou v56..v62 na mesma janela) — o degrau é da **W10.7**: o `BakedFormDocument` ganhou **`form_occ`**, a oclusão de forma de um objeto assado (cavidade × os dois AOs), um byte por texel. ⚠️ **Ela VIAJA em vez de ser assada no `base`, o que seria de graça em disco:** um re-bake **REUSA** o `base` (`sculpt3d_bake::bake_one`), então pré-multiplicar a oclusão ali a comporia a cada gesto e o objeto **escureceria sozinho** — o defeito exato que o `base` existe para impedir. ⚠️ **E ela nasce VAZIA num documento anterior**, que é a leitura honesta: o neutro da oclusão é `1.0`, e um plano de zeros pintaria de **preto** toda arte já assada. **[ADR-0156](../../architecture/decisions/0156-sculpt3d-ao-trace-is-a-per-vertex-gather-rayon-exception.md)** — o `rayon` entra na **`ph2d-sdf`** porque o traço de AO é um **gather por-vértice** (leitura pura, saídas disjuntas), a condição que o ADR-0109 exige; aceito pelo Enio em 06/08 (*"pode usar rayon. siga"*). ⚠️ **O número 0156 era o próximo livre no `main` de 06/08 e SOBREVIVEU à integração** — nenhuma outra linha desta janela criou ADR (a `line/Painter` reivindicava 0156 para o Liquify e **renumerou para 0157**, a 8ª vez no repo). **Crate nova `ph2d-panel-sculpt3d`** (única adição de `*/Cargo.toml`) · **nenhuma dep externa nova** (o único `+name` do `Cargo.lock` é a própria crate) · registro do `ph2d-ecs` **INTOCADO** · contrato congelado **4/4 + 3/3** · scrollbar id **840** (`SCULPT3D`) · gates de GPU **44/44 na RTX**. Smokes: **`PH2D_SCULPT3D_SMOKE=1..18`** no roteador. **⬛ O EXTRACT, O TRANSFORM, O AMBIENTE COM DIREÇÃO, O ALPHA POR IMAGEM E O PILL — e o carimbo perde a ÂNCORA — `line/sculpt3d` INTEGROU (2026-08-10, 20 commits, 99 arquivos, +10.982/−601; handoff MESTRE [`docs/3D/handoffs/HANDOFF_INTEGRACAO_line_sculpt3d_MESTRE_2026-08-09.md`](../../3D/handoffs/HANDOFF_INTEGRACAO_line_sculpt3d_MESTRE_2026-08-09.md), que **supersede** o de 08/08 apenas como *o que integrar agora* — ⚠️ *o detalhe de mecanismo das waves até a W10.7 continua LÁ*):** a W14→W17 mais a porta de entrada do modo. **(A) W14 — o EXTRACT** (`=22`): a região mascarada vira uma PEÇA, com espessura e passadas de costura autoradas. **(B) W15 — o TRANSFORM** (`=23`): mover / girar / escalar **só a parte mascarada**; ⚠️ o W15b corrigiu o giro para **acompanhar a mão** — o eixo vem do raio que agarrou o barro, não de um ângulo de repouso. **(C) W16 — o AMBIENTE TEM DIREÇÃO** (`=24`): o piso da difusa deixa de ser **um número para toda direção**, com a **média sobre todas as normais inalterada** — o termo *redistribui*, não expõe. ⚠️ **A cena traz LUZ PRÓPRIA, e a fixture anterior não continha metade do termo:** com o rig de todo dia (que vem de CIMA, do mesmo lado em que este ambiente põe o céu) apenas **11,5%** da sombra visível recebe a metade clara — o artista veria *só escurecer*. **(D) W17 — o ALPHA POR IMAGEM** (`=25`): uma **sprite da cena** vira o padrão do pincel. ⚠️ **A escolha e os pixels são o MESMO valor** (`Alpha::Image(Arc<AlphaImage>)`): *`Image` sem imagem é inexprimível* — as duas alternativas (um id numa tabela · a imagem como parâmetro do dab) deixam esse estado nascer, e ele significa *"liso"* **em silêncio**. A lei é `peso = luminância(rgb) × alfa`, convertida **UMA vez na carga**, um byte por texel; ⚠️ **consequência NOMEADA:** um desenho de tinta PRETA sobre transparência vira um alpha **vazio** — é o que o ZBrush faz, pela mesma razão (*branco é cheio*). ⚠️ **E ela fica FORA do `Alpha::ALL`, de propósito:** aquela lista é o que a UI oferece como CHIPS, e um chip é um **nome**; uma imagem é uma coisa para a qual se **aponta** ⇒ o gesto que a arma é um **BOTÃO**. **O `Copy` do `Alpha` morreu, e o preço foi MEDIDO antes da decisão** — o compilador conta **3 derives + cinco `.clone()`** num arquivo de teste, contra a estimativa herdada de *"~20 arquivos"*, que era o que arquivava esta saída. **(E) O PILL SCULPT:** o modo 3D só era alcançável por variável de ambiente. ⚠️ **`ToggleSculpt3d` NÃO é `ActivateTool`, e a distinção é o que o ADR-0150 protege** — a cena 3D **não é uma `Tool`** (a navegação orbital mora no shell de propósito, e é isso que mantém `Tool=12` fora do caminho), então não há `tool_id` que a registry saiba ativar. ⚠️ **E a POSIÇÃO do cluster no topbar é load-bearing, não gosto:** os **sete primeiros** clusters são o grupo da ESQUERDA (o `split` do `paint_top_bar`), então entrar entre eles empurra o vizinho de baixo para o outro lado da tela — o pill entra **depois do FLIP**, e se um merge o mover o layout quebra **sem nenhum gate reclamar**. **(F) E as três rodadas do CARIMBO — a última é a que vale ler.** O Pattern Offset parecia inerte e ⚠️ **o motor sempre esteve certo** (medido: um passo do slider muda **10.159 dos 13.682 vértices**) — cegas eram as **duas superfícies que o mostram**, cujas chaves de cache **enumeravam** `az`/`elev` e não o deslocamento; *uma chave que enumera as entradas de um valor é como a próxima é esquecida* ⇒ as duas passaram a carregar o **`AlphaFrame` inteiro**, que é exatamente o que o `weight_at` consome, então uma entrada nova chega **por dentro do tipo**. Depois a imagem virou **estêncil preso ao viewport** (não girar com o objeto e não mudar com o zoom saíram de **um número só**). ⚠️ **E então o report que fechou a jornada:** *"a tinta da máscara projetada no objeto não corresponde ao que realmente está sendo esculpido"*. **O defeito, medido antes de qualquer hipótese:** o estêncil media *quantas unidades de objeto a altura da tela abrange* — um número que só é verdade **numa profundidade**. Quem o montava tinha de escolher ONDE perguntar, e os dois consumidores escolheram diferente: **o dab no ACERTO, o preview no CENTRO da peça**. A régua vale **3,3456 na frente e 4,1740 no centro** ⇒ o carimbo desenhado saía **24,8% maior** que o depositado, e o erro **mudava conforme o artista andava pela peça** (−16,6% atrás; razão frente/atrás **0,6688×**). ⚠️ **A cura não foi escolher a âncora certa — foi TIRAR A PERGUNTA:** o `AlphaStencil` guarda hoje o **FRUSTUM** (`eye` + `height_per_depth`) e a profundidade entra **por vértice**; a `stencil_for(pose)` **não recebe ponto nenhum**, e os dois consumidores fazem literalmente a mesma chamada — a representação apagando o caso especial, o movimento da bola limitada do Inflate. E ganha-se o que **nenhuma âncora podia dar**: dois pontos que caem no mesmo pixel recebem a mesma coordenada de carimbo, no nariz ou na nuca do modelo. **Três propriedades mantêm isto barato e verificável:** a razão do frustum é **ADIMENSIONAL** (mundo por mundo) ⇒ **não** é dividida pela escala da peça, e a pose entra uma vez só, no olho (dividi-la duas vezes é a mutação que o gate da peça escalada mata, **0,7502** de desvio) · **`eye = [0,0,0]` + `persp = 0` reduzem LITERALMENTE** ⇒ **os nove procedurais atravessam byte a byte**, com gate que passa a vista aos **DOIS** lados (não basta um procedural sem estêncil não mudar — ele tem de ignorar um estêncil que **está** ali) · e o **retrato do painel** amostra o plano `z = 0`, profundidade exatamente `1` contra o olho canônico ⇒ a divisão vira `1.0/(1.0·1.0)` e o swatch sai **sem distorção**. ⚠️ **A razão sai da porta que JÁ EXISTE** (`view_height_per_depth`) e **não** da forma fechada `2·tan(fov/2)` — a fechada seria a segunda cópia das mesmas grandezas, divergindo por um fator que nenhum teste de nenhuma das metades enxerga. **O alpha por imagem JÁ É ASSADO** (pergunta do Enio: *"se assar fica mais otimizado?"*): medido sobre a malha de 426k vértices, **imagem 64² 17,1 ns/vértice · 2048² ~20 · Scratches/Weave 9,1 · Noise 79,8 · Scales/Pores/Cracks ~149** ⇒ **a imagem é o mais barato dos dez** tirando os dois mais ralos, e o tamanho quase não entra (paga-se **cache**, não aritmética) — **não há o que assar**. ⚠️ **E a primeira medição pós-fix acusou +114% na imagem**, com o `Noise` — que esta linha **não toca** — subindo **46% na mesma corrida** (`load average 23,7`); com a máquina em **1,70** o controle volta à linha de base e a imagem mede **17,1 contra 16,8**. *Um número que se move sobre código intocado é a máquina, não o código.* **`PROJECT_SCHEMA` INTOCADO** (`project.rs` com diff **vazio**) · **nenhum ADR** ⇒ a linha ficou **FORA de toda disputa de número** · registro do `ph2d-ecs` **INTOCADO** ⇒ os **três** espelhos também · **nenhuma crate nova** · **nenhuma dep externa nova** · **1 `Cargo.toml`** (`ph2d-panel-sculpt3d` ganha `ph2d-mesh` — **aresta interna de path**, para o painel ler o `Extract::default()` em vez de guardar uma 2ª cópia dos dois números) · contrato congelado **4/4 + 3/3** · **17 ids novos, todos `hash_node_id`** ⇒ nenhum gate de contagem · scrollbar id **nenhum novo** · gates de GPU **54 verdes na RTX** (⚠️ o handoff dizia 45 — o número dele ou envelheceu ou contava um alvo só; **54 é o medido na árvore combinada**). ⚠️ **E a integração teve UM conflito, no arquivo que as DUAS linhas partiram pelo mesmo motivo:** `ph2d-i18n/src/lib.rs` cruzou o teto de LOC nos dois lados da janela — o `main` levou as 186 chaves `panel.vector.*` para o irmão `vector.rs` (a `line/Vector` **avisou disto no handoff dela**) e esta linha levou as `panel.sculpt3d.*` para `sculpt3d.rs`, e as duas reescreveram o **braço de saída** do `match`. Resolvido pelos ESTÁGIOS do índice e **pelo consumidor**: os dois irmãos são consultados **em CADEIA** antes do vazamento (`vector::tr(k).or_else(|| sculpt3d::tr(k)).unwrap_or_else(|| leak_key(k))`), porque ficar com um lado apagaria a família inteira do outro painel — que passaria a **pintar os próprios identificadores na tela, com a suíte verde**. O comentário passou a dizer a lei: **um irmão novo entra nesta cadeia, nunca num segundo `match`.** ⚠️ **E a linha chegou 142 commits ATRÁS do `main`** — a tabela de colisão do handoff descrevia o `main` do fechamento dela, não o do dia: dos **5 arquivos** que ele nomeava como risco fora do módulo, **nenhum** estava na interseção real (o `main` não os moveu), e o único risco era o i18n, que ele não previa. Não muda a tabela (o `main...HEAD` de três pontos é merge-base-relativo, logo imune) — muda o que se espera do rebase [[feedback_integration_order_comes_from_measured_overlap]]. Smokes (`--release`, `env PH2D_SCULPT3D_SMOKE=<n>`): **`=22`** (o EXTRACT) · **`=23`** (o TRANSFORM — o giro acompanha a mão) · **`=24`** (o AMBIENTE — ⚠️ olhe a **metade ESQUERDA**, a que a lâmpada não alcança: topo e beiral de cada degrau têm de se separar) · **`=25`** (o ALPHA POR IMAGEM — arme o sprite, **gire** (o carimbo não acompanha), **dê zoom** (não muda de tamanho) e **pinte olhando o carimbo**: o que a máscara desenha é o que o traço deposita, **em qualquer parte do modelo**). **Aberto, com o preço ao lado:** o **preview no barro recalcula por quadro de órbita** enquanto um carimbo está armado (**0,36–0,46 ms** a 13,7k vértices e **7,9–10,1 ms** a 426k) — é **inerente**: um estêncil muda de lugar no barro a cada movimento da câmera e **nenhum vértice se move**, então um bake por-vértice é invalidado pelo quadro seguinte, por definição (os nove procedurais **não** respondem à câmera, e há gate de CONTROLE afirmando isso) · o K1/K2 do ADR-0150 seguem como o MESTRE de 08/08 os deixou · ⚠️ **e da lista aberta que a entrada de 02/08 desta §5 registrava sobra UM item: o marching cubes** — import/export STL/PLY/OBJ, o **objeto misto** e **merge/isolate** FECHARAM na jornada de 04/08, com smoke, e a frase *"não foi tocado"* sobreviveu ao fato por duas integrações (conferido por CÓDIGO na árvore combinada: `obj.rs`/`ply.rs`/`stl.rs`/`export.rs`/`merge.rs`/`extract.rs` existem; `marching` não aparece em nenhuma linha de `src/`). **⬛ OS KERNELS VIRARAM UM PORTE MEDIDO A UM ULP, E A W6 FECHOU COM QUATRO VERBOS — `line/sculpt3d` INTEGROU (2026-08-15, 91 commits, 191 arquivos, +36.198/−2.932; [ADR-0159](../../architecture/decisions/0159-sculpt3d-the-dab-vertex-loop-is-a-row-disjoint-map-rayon-exception.md), handoff MESTRE [`docs/3D/handoffs/HANDOFF_INTEGRACAO_line_sculpt3d_MESTRE_2026-08-15.md`](../../3D/handoffs/HANDOFF_INTEGRACAO_line_sculpt3d_MESTRE_2026-08-15.md), que **supersede** o de 08-09 apenas como *o que integrar agora* — ⚠️ *o detalhe de mecanismo das waves W14..W17 continua LÁ*):** o módulo deixa de ter kernels *inspirados* na referência e passa a ter kernels **PORTADOS**, medidos contra ela a **um ULP de `f32`**; sobre essa base entram os três modos, o Basic × Pro, os Kelvinlets e quatro ferramentas novas. ⚠️ **A ORDEM não é acidental e vale mais que a lista:** a paridade veio **primeiro** porque *"esculpe horrivelmente"* é uma afirmação sobre **DIVERGÊNCIA**, e não se conserta uma divergência que ninguém mediu. **(A) A cauda do remesh** — a **MÁSCARA e a COR atravessam** (⚠️ *a lei já estava escrita*: o remesh devolve uma malha nova, e um canal autorado que não a atravessa é trabalho do artista destruído em silêncio) · a pilha de multires **ACHATA**, e ⚠️ *o conselho que as recusas davam era o INVERSO* — nem achatar em silêncio (destrói autoria) nem recusar para sempre, mas **oferecer o achatamento como GESTO** · o campo que VAZA é re-amostrado **e só então** recusa · o volume de uma malha fechada sobe para a crate · e a travessia vira **gather PARALELO, 371 → 25 ms** (⚠️ o `clear` do octree era **load-bearing**, coisa que só a paridade byte-a-byte viu). **(B) Os matcaps deixam de ser procedurais** — **8 EXR CC0 do Blender + 2 PNG de pele do SculptGL (MIT)**, com `LICENSES.md` ao lado. ⚠️ **Meio-float da FONTE, com o número medido:** guardá-los em 8 bits erra **~1 nível de 255** de volta em linear. ⚠️ **E eles NÃO são os arquivos originais do Blender** — aqueles são recusados por *layout de canais além de RGBA* e *compressão DWA/DWB*, e **nenhum dos dois motivos é sobre precisão**; o cozimento (`docs/3D/ferramentas/cook_matcaps.sh`) re-embala em RGB/ZIP. **A esfera default passa a ser a do SculptGL** (cubo subdividido, **98.304 quads**) — ⚠️ e o número é **CONSEQUÊNCIA, não escolha**: o pedido era *"pelo menos cinquenta mil faces"*, a escada de subdivisão para ali, e o gate pina o 98.304 contra o pedido. **(C) ⭐ A PARIDADE — o alvo é o JS EXECUTANDO, não uma leitura minha do `Brush.js`:** o **Node roda o próprio SculptGL** (`docs/3D/ferramentas/sculptgl_oracle.mjs` → `.txt`) e o gate compara contra a saída dele. O censo fecha com a tabela **a auto-confirmar-se**: Draw · Clay · **Fill** · **Scrape** · Inflate a `1,00×` com `|dif| = 5,960e-8 = 2⁻²⁴` = **um ULP de `f32`** · Smooth `1,192e-7` · Pinch/**Magnify** `5,776e-4` · Crease `8,087e-4` · Flatten `1,717e-3`. ⚠️ **A tabela PROVA a divergência declarada do Flatten em vez de a afirmar:** o `|dif|` dele é **exatamente o deslocamento máximo do Fill**, e Fill e Scrape — os dois lados unilaterais do mesmo kernel — saem **bit-idênticos** ⇒ a nossa discordância *é*, ao dígito, **o lado que a referência não move**. ⚠️ **E foi a NOTAÇÃO que fechou o último item aberto:** com seis casas o `|dif|` imprimia `0,000000`, que responde *"abaixo do que eu mostro"* e não *"zero"*; em notação científica ele diz `5,960e-8` ⇒ a cadeia de peso em `f64` **está respondida e não há o que construir** — ela custa um ULP, o limite da representação. **O que a wave derrubou pelo caminho:** o **TRAÇO passa a ser função do CAMINHO** (dependência da taxa de eventos **6,485% → 0,000%**, a mesma lei que o Painter pagou quatro vezes no relevo) · o **PLANO passa a ser o da referência** ⇒ Draw/Clay/Inflate **bit-idênticos** · o **`accumulate` era INERTE na família do PLANO** (o plano estava congelado e o knob não movia um vértice) · **`Falloff::Plateau`** entra · a **LEI do carimbo foi construída, MEDIDA e REVERTIDA** uma vez antes de landar na forma certa, *com a medição no doc para ninguém a reconstruir* · e ⚠️ **um doc-comment nosso nomeava a lei do BLENDER sobre um corpo que implementava a do SculptGL**. **(D) Os três modos, o Basic × Pro, os kernels** — **`RefMode::{S,B,L}`** governa **a LEI do kernel**, não um rótulo, e o `L` **fica de fora onde não tem conteúdo, com motivo**. ⚠️ **O achado que REORDENOU o plano: o `s-mode` JÁ EXISTIA** — o porte da (C) *era* o s-mode, e por isso a W1 trocou de lugar com a W3. As **nove curvas do Blender** entram (⚠️ o `B` seguia sem declarar nenhuma), a **DUREZA do dab ganha porta**, a **cerca do Inflate ganha NÚMERO**, e a UI ganha dois níveis por linha. ⚠️ **O `brush.cc` foi trazido e responde METADE:** os defaults por-tool do Blender moram num **`.blend` BINÁRIO**, não no código ⇒ **a W1 e o Draw Sharp viram decisão de PRODUTO do Enio, não dívida de engenharia**. **(E) O Smooth que não encolhe, e o WIREFRAME** — medido, **o Smooth ENCOLHE 3,58% e não satura**; a cura de **Taubin (λ|μ)** custa **0,018%** ⚠️ e **o PAPER mudou na medição** (era HC, virou Taubin). O wireframe vai de **39% → 93%** de continuidade, **remove linha escondida** (⚠️ *e a régua anterior estava inflada*) e **o fio sai de cima de si mesmo** (empurrão lateral de meio pixel). **(F) O agarre vira um CAMPO ELÁSTICO** (Kelvinlets regularizados) — as três famílias afins e o gancho, o campo **ATERRISSA na borda da pegada**, a **largura do campo é um knob**, e o Vinco ganha `l-mode`. ⚠️ **E o `Verb` novo era o item ERRADO:** o *Elastic Deform* do Blender tem 5 tipos, e a medição mostrou que **3 deles são o mesmo verbo com outra família de escalas** e os outros 2 já shipavam — o que faltava era o knob **Field width**. *Um sexto botão cujo conteúdo é um dropdown para verbos que a lista já tem é o item de menu morto que este plano recusa* ⇒ **o alvo de 14 pincéis novos passou a 13**. **(G) ⭐ W6 — os dabs que não são discos: QUATRO verbos (16 → 20)** — **Clay Strips** (**o dab deixa de ser um DISCO**: `Footprint::Square`) · **Blob** (o Crease com **o aperto invertido**) · **Clay Thumb** (`=30`, o primeiro verbo cujo alvo depende de **quantos dabs já passaram** — o ângulo ACUMULA) · **Multiplane Scrape** (`=31`, o único com **DOIS planos**, `Footprint::Blade`). ⚠️ **A faixa custou QUATRO correções e cada uma tem número:** saía **REDONDA** (*eu deixei o fallback definir o produto*) · não **NIVELAVA** (o lift do plano decidia copiar o relevo) · corria **a lei do SculptGL, que não tem esta tool** · e estava **7,5× mais fraca** (o `reach` também era do SculptGL). ⚠️ **E o report da família que APERTA** (BUGS #2/#3): o chip `B` do Pinch **vestia a lei de OUTRA ferramenta**, e o gate que devia pegar isso *afirmava a coisa certa sobre o lugar errado*. **A superfície de colisão, MEDIDA pelo integrador na árvore combinada:** `PROJECT_SCHEMA` **84 INTOCADO** (`project.rs` **e** `project_schema.rs` com diff vazio) · contrato congelado **INTOCADO** (`git diff` vazio) · registro do `ph2d-ecs` **INTOCADO** ⇒ os **três** espelhos também · **nenhuma crate nova** · **nenhum pacote externo novo** (o `Cargo.lock` não ganha um `+name`) · **3 `Cargo.toml`** (`ph2d-sculpt3d` ganha `rayon` + `libm`, `ph2d-mesh-render` ganha as crates de imagem, `ph2d-mesh` só a prosa) · ids novos **todos `hash_node_id`** ⇒ nenhum gate de contagem · scrollbar id **nenhum novo** (o do painel segue **840**) · i18n **só o irmão `sculpt3d.rs`** (+18; o `lib.rs` **não** é tocado, então a cadeia que a integração de 08-10 instalou fica intacta) · cenas de smoke **26..31** (⚠️ **próxima livre: 32**). ⚠️ **O `libm` não é gosto, é MEDIÇÃO:** o porte do `Twist` precisa de `atan2`, e sobre 20.000 amostras o `atan2` do `libm` é **exato** contra o Node (0 divergências) enquanto o do sistema erra em **18,6%** delas — o ECMAScript declara os quatro transcendentais *implementation-approximated*, então não há resposta exata a espelhar; há a biblioteca que chega mais perto, e a tabela está no `ref_twist.rs`. ⚠️ **E `ph2d-imageio`, não o `image` cru** — aquela crate já é a dona da pergunta *"que formatos este app abre"*, e um segundo decoder de PNG aqui seria a segunda resposta, que diverge no dia em que a política de perfil de cor mudar. ⚠️ **O [ADR-0159](../../architecture/decisions/0159-sculpt3d-the-dab-vertex-loop-is-a-row-disjoint-map-rayon-exception.md) nasceu 0158 e foi CONTADO na integração — a 10ª vez no repo, e a PRIMEIRA que estava PREVISTA:** a §5 já escrevia, na entrada da `line/Painter` de 2026-08-15, que *"a `line/sculpt3d` renumera para 0159"*. O rewrite foi **ESCOPADO aos arquivos da LINHA e ao TOKEN**, nunca ao número nu sobre a árvore — ⚠️ o `06.1-Waves` carrega um **`0,0158` que é uma MEDIÇÃO**, e o handoff de integração ficou **de fora de propósito** (ele *narra* a colisão: reescrevê-lo apagaria a própria instrução). ⚠️ **E não existe lista compartilhada de exceções de `rayon` a atualizar** — a linha ia afirmar que o ADR-0109 a mantém, **grepou antes de afirmar e ele não a tem**: cada exceção (0145 · 0147 · 0156 · **0159**) é um ADR que se sustenta sozinho, e a **cerca mora no `Cargo.toml` da crate que a paga**. ⚠️ **E a integração teve UM conflito, no `CLAUDE.md`, cuja resolução ingênua teria APAGADO uma correção verdadeira:** a linha corrigia, na própria entrada de 3D, a frase *"o resto da lista aberta do módulo (import/export, objeto misto, merge/isolate, marching cubes) não foi tocado"* — e ela é **FALSA desde 04/08** (conferido por código na árvore combinada: `obj.rs`/`ply.rs`/`stl.rs`/`export.rs`/`merge.rs`/`extract.rs` **existem**, e `marching` **não aparece em nenhuma linha de `src/`**) ⇒ **sobra UM item: o marching cubes**. Resolver *"fique com o `main`"* era limpo, compilável e **perderia a correção**; quem pegou foi a **sonda que a resolução carregava** (*a correção da linha já está no `main`?* → **False**), não o diff nem a suíte. *Um lado escolhido não é um conflito resolvido: o conteúdo tem de ser conferido, e a pergunta certa é sobre a FRASE, não sobre o arquivo.* ⚠️ **E a varredura de marcadores tem de ser ancorada em INÍCIO DE LINHA** — a §5 hoje cita `<<<<<<< HEAD` e `|||||||` **em prosa** (a lição de merge da `line/Vector`), então um `in`-de-substring acusa quatro marcadores num arquivo limpo. ⚠️ **E a tabela §3 do handoff ENVELHECEU entre o fechamento e a ordem, exatamente como na integração anterior desta linha:** ela media o `main` **203 commits** à frente do fork e nomeava **3** arquivos na interseção; medido no dia, o `main` andou **298** e a interseção é **5** — os dois extras são `ph2d-panel-sculpt3d/src/{event,paint}.rs`, que a **`line/Vector`** tocou nas waves de *UI viva* (o polegar que acende, o chevron que roda) e que esta linha reescreveu por dentro. **Isso não muda a tabela de colisão** (o `main...HEAD` de três pontos é merge-base-relativo, logo imune) — muda o que se espera do rebase [[feedback_integration_order_comes_from_measured_overlap]]. **Dez mudanças de comportamento, nomeadas** (é a parte que um smoke de outra linha pode estranhar): ⚠️ **a malha default muda** (a esfera passa a ser o cubo subdividido do SculptGL, 98.304 quads ⇒ **toda cena do módulo nasce com outra malha**) · ⚠️ **os matcaps mudam** (deixam de ser procedurais e viram imagens autoradas; **o default passa a ser um deles**, o *Skin Haz 2*) · ⚠️ **seis kernels mudam de desenho onde divergiam** — é a entrega, e o atlas mede quanto · o traço deixa de depender da taxa de eventos · o `accumulate` passa a funcionar na família do plano · o **Grab é carimbado uma vez por QUADRO** (16 dabs intermediários custavam 17,9 ms onde um custa 1,2, e eles eram **byte-idênticos ao último**) · o Smooth em `l-mode` para de encolher · o wireframe remove linha escondida · o `Pinch` em `B` passa a ser o `pinch.cc` e o `l-mode` **sai** da família que aperta · e **quatro chips novos** no seletor de verbo. **Smokes** (`--release`, `env PH2D_SCULPT3D_SMOKE=<n> cargo run -p ph2d-host-desktop --release`): **`=26`** (a história inteira do remesh: máscara e cor **atravessam**, a pilha achata) · **`=27`** (**o canal que se constrói esfregando** — o `accumulate` que era inerte) · **`=28`** (o campo elástico e as três famílias afins) · **`=29`** (**a FAIXA** — largura, nivelamento, auto-limite) · **`=30`** (**O POLEGAR** — o plano que se inclina ao longo do traço) · **`=31`** (**A LÂMINA EM V** — ⚠️ o **CONTROLE é o Scrape primeiro**, e com o ângulo em **zero** ela tem de fazer **NADA**). ⚠️ **As cenas `=2..25` têm de continuar iguais**, exceto pelas duas mudanças de default nomeadas acima (a malha e o matcap), que aparecem em **todas** elas. ⚠️ **Flake conhecida, de CARGA:** `measure_brush_kernel` (`ph2d-sculpt3d`) é kill de relógio e já reprovou sob `load average 26`, passando isolado — *nenhuma leitura de relógio desta workstation significa coisa nenhuma acima de `load ~5`*. **Aberto — e a metade que é DECISÃO DE PRODUTO está separada da que é trabalho:** ⛔ os **defaults do `B`** (a W1) e o **Draw Sharp** não têm o que construir até o Enio decidir de onde vem o número (o §7.0 mediu que ele mora num `.blend` binário) · ⛔ o **atalho de teclado** do Clay Thumb e do Multiplane Scrape (os dois estão `CHIP_ONLY` de propósito) · ⛔ as **três divergências DECLARADAS** da referência (o `Flatten` bilateral e a projeção tangencial de `Pinch`/`Crease`, `5,8e-4`/`8,1e-4`), cada uma com **gate próprio defendendo a nossa posição** — elas só fecham se o Enio abrir mão delas. **Trabalho nomeado:** a outra metade da **W4** (Slide Relax · Surface Smooth como pincel próprio · o laplaciano por cotangentes) · **W7** o plano MLS · **W8** Layer (⚠️ ele traz um **plano por-vértice novo** ⇒ a lei do repo: *ao adicionar um plano, adicione-o ao snapshot de undo no MESMO commit*) · **W9** Mesh Filter (⚠️ **o mais barato da lista inteira** — o precedente do *Filter Layer* do Painter diz que **não há kernel novo**) · **W10** Cloth · **W11** handles · **W12** a geodésica · e o **marching cubes**, o único item que sobra da lista aberta de 02/08. ⚠️ **NOMEADO e NÃO corrigido:** os gates do **Clay Thumb** e do **Clay Strips** herdam a cegueira que a lâmina em V expôs — a fixture leva o `accumulate` do `Brush::default()` (o do Draw, `true`), então **o ramo do plano congelado é inalcançável neles**; mexer nas barras deles é wave de quem os possui. **Placar: 20 verbos** contra os 16 de que a linha partiu, e o alvo honesto é **29** (16 + 11 pincéis + 2 filtros), depois de o Elastic Deform ter sido respondido **sem verbo** e o Draw Sharp ter saído **com motivo**. **⬛ O ALISAMENTO, A SUPERFÍCIE, A DEMÃO PORTADA DO `layer.cc` E OS CINCO PEDIDOS — `line/sculpt3d` INTEGROU (2026-08-17, 41 commits, 117 arquivos, +18.312/−1.545; handoff MESTRE [`docs/3D/handoffs/HANDOFF_INTEGRACAO_line_sculpt3d_MESTRE_2026-08-17.md`](../../3D/handoffs/HANDOFF_INTEGRACAO_line_sculpt3d_MESTRE_2026-08-17.md), que **supersede** os dois handoffs de LAYER de 16/08 apenas como *o que integrar agora* — ⚠️ *o mecanismo do porte do `layer.cc` continua LÁ*):** quatro blocos, e ⚠️ **os três primeiros são exactamente os itens que a entrada de 15/08 desta §5 deixou abertos** (*a outra metade da W4* · *W7 o plano MLS* · *W8 Layer*) — a lista aberta de uma jornada virou a lista fechada da seguinte. **(A) W4 — o alisamento que não encolhe:** **Slide Relax** (redistribui a malha sem mudar a forma, **71×** menos deformação) · **Surface Smooth (HC)** · o **laplaciano por COTANGENTES** · e ⚠️ o `lambda` do Taubin, que era um **PALPITE** (o `l-mode` alisa **2,2×** mais). **(B) W7 — o plano vira SUPERFÍCIE**, e o `offset` era um **knob morto**. **(C) W8 — a DEMÃO, portada do `layer.cc`** — e ⚠️ **a auditoria que ela puxou é maior que ela**: ler o catálogo inteiro derrubou **OITO defeitos vivos, todos ANTERIORES à wave**. **SETE dos 23 verbos nasciam com a força da referência ERRADA**, e ⚠️ **o preço não era o chip apagado, era a FORMA do barro**: `profile(S, Layer)` é `None` ⇒ o slider virava o peso **cru** onde o `layer.cc` o **eleva ao quadrado** (`0,5000` contra `0,2500`, o dobro da taxa) · o catálogo de falloff **dizia o nome do Blender sobre outra curva**, e o `Sharper` virou um **CONE** · o `auto_smooth` era a força de um lerp e é um **ORÇAMENTO de passadas** · o **Möller-Trumbore do PICK não era estanque** (na `ph2d-mesh`) e a justificativa escrita era falsa · o shell tomava **METADE do par do SculptGL** (a âncora apagava os dabs que o `break` pulou), ⚠️ e a lei da âncora **REFUTOU a hipótese da própria linha** — ela **AVANÇA num miss**, que é o que as duas referências dizem e o oposto do que estava escrito · **TRÊS verbos shiparam sem ninguém decidir como o artista os pega**, com o gate que devia pegar isso **nomeando um por corrida** · e o arch-gate do transform **PANICAVA sobre produto correto**. ⚠️ **Nada disso é escopo inventado:** cada um estava *dentro* do caminho de tornar a demão idêntica à fonte, e deixá-los de pé faria a demão ser conferida contra um catálogo que mente. **(D) OS CINCO PEDIDOS do Enio** — ⚠️ e o resultado honesto é **três construídos, um REFUTADO por medição e um MEDIDO e não construído de propósito**. **(1)+(5) cada ferramenta lembra a própria afinação:** ⚠️ a lei antiga só podia acertar **enquanto o artista não mexesse em nada** — o `arm_verb_defaults` levava o pincel VIVO para o verbo novo e re-armava campo a campo sob *"arma se o artista ainda não tocou"*, uma heurística sobre **intenção**; agora a tabela guarda o **`Brush` INTEIRO por verbo** (`VerbSlot` + o `radius_px`, que é a régua da TELA) e trocar de ferramenta é **salvar o slot que sai e carregar o que entra**. *O slot sabe.* O **Layer** nasce em **`0.7 / 0.4 / 0.0`** ⚠️ e a fábrica dele é **NOSSA** (`profile(RefMode::S)` devolve `None` — o SculptGL não tem essa ferramenta), que é o que dá aos números do smoke onde morar sem divergir de fonte nenhuma. ⚠️ **E um arch-gate apanhou um defeito que ESTA wave introduziu:** enquanto a tabela guardava só o modo, nada por-verbo podia envelhecer; com o pincel inteiro no slot, carimbar uma referência deixava o `falloff` daquele verbo resolvido contra a **ANTERIOR** — o chip diria *Blender* e o kernel rodaria a quártica do SculptGL ⇒ **uma lei (`reconcile_mode`), dois chamadores**. **(4) o cursor deita na superfície:** ⚠️ **a cerca anterior media a coisa errada, e a distinção é GEOMÉTRICA** — o doc afirmava que *"um círculo de raio `radius_px` no plano da tela **é** a figura dele"*, mas a pegada é uma **BOLA de MUNDO** cujo raio deriva dos pixels **na profundidade do acerto**, o círculo de tela é a **SILHUETA** dela, e quem recebe tinta é a **interseção com a superfície**, que num plano inclinado de `θ` projeta uma elipse de eixo menor `r·cos θ` **inscrita** na silhueta (medido: **0° → 80,00 px nos dois** · 60° → 37,98 · 85° → **6,57 contra 80,00, 12×**). ⚠️ **É a linha do 0° que torna a troca segura** — no caso comum as duas figuras são a mesma, então o cursor **não pisca** ao encarar a câmera. ⚠️ **A normal vem das normais SUAVES** (`Mesh::normals()`, o `base_nrm` que o dab consome ⇒ *o cursor concorda com o kernel por construção*) e **NÃO do `Hit::normal`**, que carrega uma lacuna nomeada (quad "gravata" → `[0,0,0]`) cujo gatilho declarado é *"o primeiro leitor de produto"* — adotá-lo importaria a lacuna para dentro da feature; e o círculo de tela **fica como recuo honesto**, com a fixture a precisar de superfície **de perfil (89°)** para alcançar o segundo caso. ⛔ **E O QUE FOI MEDIDO E NÃO CONSTRUÍDO fica escrito, para ninguém reconstruir.** **(2) O espigão do zoom é REAL e é a lei da REFERÊNCIA:** pela porta do produto a esbeltez `altura/raio` vai de **0,135** com a câmera longe a **5,857** a 4× de zoom — o raio nasce dos **pixels** e encolhe, a altura é de **MUNDO** e não encolhe. ⚠️ **E é HERDADO, em três fatos do fonte:** `rna_brush.cc:3230` declara `height` como **`PROP_DISTANCE`** (distância de mundo, não fração) · `layer.cc:101` a multiplica **crua** · e o `cache.radius` de lá **também** sai dos pixels; ⚠️ **o default do Blender é `0.5` contra o nosso `0.1`**, então no mesmo zoom o Layer dele espiga **cinco vezes mais**. ⛔ A cura que achataria a curva é a lei do **SculptGL** (`Brush.js:62`, o deslocamento escalando com o raio) e **DIVERGE da referência**, contra a ordem permanente *"idêntico ao Blender"* ⇒ **decisão do Enio, com o número na mão**, e a cerca é **executável** (`the_coat_height_is_a_world_distance_and_does_not_follow_the_radius`) com mutação que sangra. ⚠️ **E a SEGUNDA causa não é de lei nenhuma:** a 4× de zoom o pincel cobre **1,45 arestas medianas**, e *nenhuma lei de deslocamento conserta um pincel mais estreito que a malha* — quem conserta é **subdividir**, e o gesto já shipa; confundir as duas faria trocar a lei e continuar sem resolução. **(3) A incidência: a nossa lei JÁ É a do Blender** — o pedido supunha um vão e **não há**: a normal que o dab consome é o **`base_nrm` congelado no pen-down**, que é *exactamente* o `orig_normals[i]` do `layer.cc:101` · a pegada é uma **esfera de mundo** (`PAINT_FALLOFF_SHAPE_SPHERE`), não um cilindro de tela — é isso que faz o flanco de uma esfera receber tinta correta · e o vazamento para trás começa em **85-90°**, ⚠️ **com o Blender a ter o mesmo** (lá o front-face é **opt-in**, `BRUSH_FRONTFACE`, e nenhuma linha do Blender inteiro o liga por omissão) ⇒ **nada a construir**. ⚠️⚠️ **E A INTEGRAÇÃO ACHOU DOIS ARCH-GATES VERMELHOS NO TIP DA LINHA, sobre produto CORRETO — o achado que vale mais que a lista acima.** Os dois vivem em `shells/desktop/tests/` e **afirmavam a GRAFIA em vez da propriedade**, e a wave §2.2 reescreveu o `sculpt3d_cursor.rs` debaixo deles: **(1)** `the_ring_is_centred_on_the_reprojected_hit` pedia o literal `hit.unwrap_or((x, y))` e o recuo do vazio passou a ser `.map_or((x, y), …)` — *ele continua exactamente onde estava, só mudou de combinador* ⇒ a âncora virou a **FAMÍLIA** (`_or((x, y)`), que casa com os dois e com o que vier depois deles · **(2)** `the_brush_radius_is_screen_pixels_converted_against_the_camera` contava **UM** sítio de `world_radius_for_screen_px` e agora há **DOIS**, porque o anel conformado precisa do raio de mundo na profundidade do acerto. ⚠️ **A pergunta não se responde por opinião, e foi MEDIDA:** a distinção que torna o segundo sítio legítimo é o **CONSUMIDOR**, não a pergunta — o dab (`armed_brush`) pede o raio de mundo e **o divide pela escala da pose** (ele mede na régua da MALHA), o anel o consome em **MUNDO** porque é projetado; mesma ajudante, mesmo ponto de acerto, mesmo `radius_px()` ⇒ **concordam por construção**, que é a propriedade que aquele gate existe para defender. O segundo sítio ganhou **nome e linha** — que é literalmente o que a mensagem do próprio gate prescrevia (*"ou é uma pergunta nova, e ela precisa de nome e de linha aqui"*) — e ⚠️ **a metade que a CONTAGEM não sabe afirmar virou asserção própria**: *o anel tem de perguntar com o MESMO tamanho de pincel do dab*, sem a qual os dois podem divergir **sem mover o número** e o cursor passaria a desenhar uma pegada que a tinta não deposita. **3 mutações, 3 sangram** (apagar o recuo do vazio · o anel a perguntar com outro tamanho · um terceiro sítio). ⚠️ **E o handoff §5 afirmava `ph2d-host-desktop: 2799 passed, 0 failed` no tip `75df717c4`** — medido por `git log -S`, os dois proxies quebraram em **`db1f494fa`**, o **38º de 40**, **dois commits ANTES** daquele tip: *a tabela estava errada, e nenhum `cargo test -p` da linha a desmentiu porque ela nunca correu naquele estado*. ⚠️ **DUAS imprecisões do handoff, as duas benignas e medidas:** ele dá o tip como `75df717c4` / 39 commits / 115 arquivos, e o medido é **`77c7db4f9` / 40 / 116** — a diferença é **exactamente o commit do próprio handoff**, que não pode saber o próprio hash (consistente) · e ele diz que *"a `ph2d-mesh` é a única crate tocada FORA do módulo"* quando **`crates/ph2d-editor-core/src/ids/chrome/sculpt3d.rs` também é** (+46/−7, e a `editor-core` é foundational). ⚠️ **O risco ali é NULO e foi conferido:** o arquivo já existia, é sculpt3d-específico pelo nome, e o `ids/chrome/mod.rs` **não foi tocado** ⇒ não há lista compartilhada no diff. **A superfície de colisão, MEDIDA pelo integrador com CONTROLE POSITIVO** (⚠️ a primeira varredura usou `shells/desktop/tests/project_schema_tests.rs`, um caminho que **NÃO EXISTE** — o arquivo mora em `src/` —, e uma busca negativa sobre caminho vazio devolve *"intocado"* para qualquer coisa): `PROJECT_SCHEMA` **84 INTOCADO** — a família `shells/desktop/src/project*` inteira com diff **vazio**, os quatro arquivos **conferidos por `ls` antes do diff**, a tripla **`(84, 13, 14)`** viva, e o **controle negativo** provando que o diff enxerga a shell (12 arquivos mudados, nenhum deles `project*`) · contrato congelado **INTOCADO** (`ph2d-nodegraph/` e `ph2d-core/src/tool.rs` com diff vazio) · registro do `ph2d-ecs` **INTOCADO** ⇒ os **três** espelhos também · **ZERO `Cargo.toml`/`Cargo.lock`** ⇒ nenhuma crate nova, **nenhuma dep externa nova**, nenhuma aresta interna · **nenhum ADR** ⇒ a linha ficou **FORA de toda disputa de número**, e **0160** é o próximo livre · `ph2d-i18n` só o **irmão** `sculpt3d.rs` (**+5**), com o `lib.rs` intocado ⇒ a cadeia `vector::tr(k).or_else(sculpt3d::tr)` da integração de 10/08 fica intacta (conferida nas linhas 414-415) · **12 acréscimos de `hash_node_id` e ZERO literal `NodeId(<número>)`** ⇒ nenhum gate de contagem em risco · scrollbar id **nenhum novo** (o do painel segue **840**) · `VEC_SCENE_SCHEMA` **14** e `FLIP_SCHEMA` **13** intactos · **nenhum uso novo de `rayon`** · e as **cinco crates de GPU com diff VAZIO** (`ph2d-mesh-render` · `ph2d-render` · `ph2d-gpu-cook` · `ph2d-paint-gpu` · `ph2d-flip-render`) ⇒ **esta linha não alcança os gates de adapter**. ⚠️ **`SCULPT3D_VERB` 20 → 23**, e o tamanho é o do `Verb::ALL` (**23**, conferido nos dois arquivos, com gate que os compara em `seam.rs`). ⚠️ **A ISOLAÇÃO é melhor do que o handoff descreve, e foi medida por diff:** *todo* arquivo de shell tocado é `sculpt3d_*`, e fora das três crates do módulo existem **DOIS** arquivos — `editor-core/src/ids/chrome/sculpt3d.rs` e `i18n/src/sculpt3d.rs` —, os dois **nomeados pelo módulo**, pré-existentes, e **nenhum é lista compartilhada**: é o *"projete o foundational para ISOLAMENTO"* do ADR-0107 a funcionar. ⚠️ **Cenas de smoke — censo PRÓPRIO do integrador: 32 reivindicações, 32 DISTINTAS, zero duplicata, maior 33 ⇒ próxima livre 34**; e ⚠️ **o roteador do sculpt3d NÃO é um `match`** (cada arquivo testa a env var por conta, `== Some("33")`), então um número repetido **não** é `unreachable pattern` do compilador — mas ⚠️ **isto é MAIS forte do que o handoff diz: existe gate** (`no_two_sculpt3d_scenes_claim_the_same_level`), e é ele, não uma nota, que impede a cena inalcançável em silêncio. ⚠️ **O ÚNICO ponto de merge sensível é um CORTE, e ele é da própria linha:** o `crates/ph2d-panel-sculpt3d/src/state.rs` cruzou o teto de **600** LOC e foi partido — a memória por-verbo (o `VerbSlot` e as suas leis) mudou-se para o irmão **NOVO** `slots.rs` (**544 + 210**, medido). ⚠️ **O CAMINHO não mudou, de propósito:** o `state.rs` **re-exporta** a superfície movida (`VerbSlot` · `arm_mode_defaults` · `reconcile_mode` · `switch_verb` · `switch_verb_parts` · `verb_index`), porque um arch-gate da shell lê o **FONTE do nascimento** atrás de `VerbSlot::for_verb` e o shell endereça `state::switch_verb` em vários sítios. *Uma linha que edite `state.rs` procurando o `VerbSlot` funde **limpo** contra um arquivo de onde ele saiu* — o modo de falha que o corte do `project.rs` já produziu duas vezes neste repo. ⚠️ **E o gate de LOC que passou não provava nada:** o `architecture_workspace_file_loc_cap` (700) **EXCLUI `ph2d-panel-*`**; quem é dono daquele arquivo é o `architecture_panel_loc_cap` (600), e era **ele** que estava vermelho — *um teto verde do gate errado é a família do vermelho-latente*. **Gate da árvore combinada, rodado pelo integrador** (⚠️ `main` a **ZERO commits** do fork ⇒ a árvore da linha **É** a combinada, e o merge foi fast-forward trivial — a caixa que o próprio handoff avisa que ENVELHECE, e que desta vez não envelheceu): `cargo fmt --all -- --check` **EXIT 0** · `typos` **EXIT 0** (⚠️ conferido pelo **ESTADO** e nunca pelo exit de um pipe — ⚠️ *e o shell desta máquina é **zsh 5.9.2**, não o `fish` que o ambiente ANUNCIA — medido, e a distinção custa comandos: o `PIPESTATUS` é bash-ism e devolve **vazio em silêncio** (quem existe ali é o `pipestatus` **minúsculo**, que funciona), um `for … end` morre com `parse error near 'end'` e um glob sem match aborta o comando inteiro com `no matches found`; a assinatura para diagnosticar é o prefixo **`(eval):1:`** das mensagens de erro*) · clippy `--workspace --all-targets --features ph2d-spike/bevy_ecs -- -D warnings` **EXIT 0, zero warnings** (⚠️ **mais do que a linha rodou** — o handoff §5 registra clippy nas *5 crates tocadas*) · codegen sync **no-op** (`ph2d-tool-sync` + `ph2d-node-sync` deixam a árvore limpa: 15 tool-crates, 123 node-crates) · varredura de marcadores de conflito **incluindo `|||||||`** (diff3) sobre os 116 arquivos: **zero** · e `nextest --workspace --cargo-profile ci-test --no-fail-fast` **16.315 correram, 16.315 passaram, 1.652 skipped, EXIT 0** ⇒ **zero vermelho e ZERO flake nesta corrida**, com a máquina em `load 0,00`–3,5 (⚠️ *nenhuma leitura de relógio desta workstation significa coisa nenhuma acima de `load ~5`*). **Seis mudanças de comportamento, nomeadas:** ⚠️ **cada ferramenta lembra a própria afinação** — trocar de verbo **não** carrega mais força/curva/dureza/raio da anterior, e isto alcança os **23** verbos · **o Layer nasce em `0.7 / 0.4 / 0.0`** (era o genérico `0.5 / 0.0 / 0.0`) · ⚠️ **o cursor deixa de ser um círculo de tela** (a 0° é indistinguível do anterior; em superfície inclinada ele **encolhe para o que a tinta de facto cobre**, 12× a 85°) · **o facing deixa de correr por omissão** em todo verbo cujo modo declare `FrontFace::Continuous` — **é o que a fonte faz** · **trocar de verbo pelo teclado** passa a armar o `front_faces_only` · e as cenas **`=32`** e **`=33`** são novas, com a `=33` a ganhar onze passos. Smokes (`--release`): **`env PH2D_SCULPT3D_SMOKE=33`** (⭐ **a DEMÃO** — ⚠️ **a cena imprime os onze passos; se a lista não aparecer, PARE**; os passos **1-8 são o CONTROLE**, aprovados antes e obrigados a continuar iguais, e o **11** é o único que precisa da esfera **CURVA**: deite a demão sobre o **flanco**, não no topo achatado, senão os dois estados desenham a mesma coisa) · **`=32`** (a SUPERFÍCIE, W7) · e ⚠️ **as duas perguntas da última wave são de OLHO e não têm cena própria** (elas alcançam toda ferramenta): *o CURSOR* — aponte para o **flanco** de uma esfera, o anel tem de **deitar** e encolher, e encarando a câmera tem de ficar **igual ao de sempre** (se piscar ao atravessar a curvatura, a fiação regrediu) — e *a PROPAGAÇÃO* — afine o **Draw**, pegue o **Clay**, afine diferente, **volte ao Draw**: ele tem de estar como você o deixou. ⚠️ **Rode a suíte do módulo também em DEBUG** (precedente registado nesta casa: o `ph2d-flip-colorize` panicava só ali). **Aberto, com o preço ao lado — e a metade que é DECISÃO DE PRODUTO está separada da que é trabalho:** ⛔ **a lei do zoom** (adotar o `Brush.js:62` achata a curva e **diverge da referência**; o número está na cerca) · ⛔ **a W1 (os defaults do `B`) e o Draw Sharp** seguem decisão de **PRODUTO**, não dívida (os defaults por-tool do Blender moram num `.blend` **binário**) · ⛔ **NÃO persiga o pente** da demão — medido, **0,0093 de UMA aresta**: é a parede a escadear pela grade, e a referência escadeia igual · ⛔ **NÃO "melhore" a dureza alta** — *a do Blender também é feia; o alvo é a violência dele, e se o resultado ficar bonito e diferente ele está errado*. **Trabalho nomeado:** o **falloff, a referência e o raio no atalho de teclado** (os três precisam do `Sculpt3dUi`, e trazê-los para o motor exigiria dar a esta crate um tipo de painel — divergência real, escrita nos dois lados) · o **`space attenuation`** segue não conferido, e ele é **TAXA e não forma** (muda em quantos dabs a demão fecha, nunca a espessura final) · a lacuna do **`Hit::normal`** (quad "gravata" → `[0,0,0]`) segue nomeada e **sem leitor de produto** — o cursor **não** a adotou, de propósito · e **W9 (Mesh Filter)** segue o item mais barato da lista, pelo precedente do *Filter Layer* do Painter: **não há kernel novo**.
