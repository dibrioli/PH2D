[package]
name = "ph2d-pan-diag"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
license.workspace = true
authors.workspace = true

[lib]

[dependencies]
ph2d-host = { path = "../ph2d-host" }
ph2d-render = { path = "../ph2d-render" }
# ⚠️ A sonda compara a rota do Flip com a das sprites e a do Vello, e a câmera do Flip mora na crate
# do renderizador desde a auditoria de arquitectura A1 (2026-09-12) — antes, para a perguntar, esta
# folha dependia da família Flip inteira, e uma folha não depende de uma família.
ph2d-flip-render = { path = "../ph2d-flip-render" }

[lints]
workspace = true
