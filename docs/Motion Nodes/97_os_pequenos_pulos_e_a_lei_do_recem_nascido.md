# 97 — Os pequenos pulos do L-System, e a lei do recém-nascido

> **Report do Enio, 2026-08-31:** *"Dá pequenos pulos, não é perfeitamente liso"* (decisão nº 2 das
> três daquela mensagem).
>
> **Estado:** ✅ **CURADO e medido.** A tinta que uma viragem de geração acrescentava de uma vez
> passou de **`67 %`** para **`0,27 %`**, e o discriminador salto/movimento passou de `0,966×`
> (descontinuidade pura) para `0,125×` (o valor **teórico** de movimento). Seis dos oito moldes
> saem **byte-idênticos**.
>
> ⭐ **E o report SEGUINTE, no mesmo dia** — *"está mais suave mas não é perfeitamente linear"* —
> é a **§10**: outra lei, outra causa (duas, uma por família), e o desvio da recta cai de
> **`+21,3 %`** para **`≤1,35 %`** no corpus inteiro.

---

## §1 — O que ele viu, e o que eu tinha medido antes

Na jornada anterior eu tinha respondido a este report com dois números:

- o **tamanho** cresce liso — ondulação `1,00`;
- a **quantidade de traço** salta `6,17×` no primeiro instante, e as `625` peças da geração
  seguinte aparecem todas juntas.

E tinha fechado com uma hipótese refutada (trocar o factor pela fracção: piora, §7.1) e com a
conclusão de que *«o salto é a fronteira de geração em si»*. ⛔ **A conclusão estava certa sobre
o SÍTIO e errada sobre a NATUREZA:** não é a contagem de módulos que se vê, é a **tinta**, e ela
não tinha de saltar coisa nenhuma.

---

## §2 — O estado da arte: o Houdini tem o mesmo defeito, e a resposta é anterior a ele

### 2.1 — O que a referência comercial faz

O [L-System SOP do Houdini](https://www.sidefx.com/docs/houdini/nodes/sop/lsystem.html) tem
exactamente os dois interruptores que nós temos, e a documentação dele diz o que eles fazem:

> *"If you specify a fractional number and Continuous angles and/or Continuous length are on,
> Houdini **scales the geometry generated by the last substitution** to give smooth growth between
> generations."*
>
> *Continuous Angles: the angles rotated by the **last generation's** turtle operations will be
> scaled by the amount into the generation.*
> *Continuous Length: the lengths taken by the **last generation's** turtle operations will be
> scaled by the amount into the generation.*

⚠️ **É palavra por palavra o que o nosso `build` fazia** — e a lei é *«escala a geração inteira
pelo mesmo número»*. Ela não distingue o material que **retraça** o que já estava desenhado do
material que é **novo**. ⇒ *não havia nada a copiar da referência: ela tem a mesma dor.*

### 2.2 — A resposta está no livro, e é de 1990

[*The Algorithmic Beauty of Plants*, Prusinkiewicz & Lindenmayer, capítulo 6 — «Animation of plant
development»](https://algorithmicbotany.org/papers/abop/abop-ch6.pdf). O capítulo existe
precisamente porque o modelo discreto **não** anima:

> *"A developmental process is viewed as consisting of periods of continuous module expansion
> delimited by instantaneous module divisions. **Special conditions are imposed to preserve the
> shape and growth rates of the organism during these qualitative changes.**"*

O formalismo é o **tDOL-system** (§6.1): cada letra carrega uma **idade** `τ`, cada produção
declara a **idade terminal** `β` do predecessor e a **idade inicial** `αᵢ` de cada sucessor. O
comprimento de um módulo é uma **função de crescimento** `g(a, τ)`, e as condições de continuidade
(§6.2) são:

| condição | o que diz |
|---|---|
| **R1** | `g(a, τ)` é contínua em `[αmin, β]` |
| **R2**, eq. (6.3) | `g(a, β) = Σᵢ g(bᵢ, αᵢ)` — **no instante da divisão o comprimento do pai é igual à SOMA dos comprimentos dos filhos** |
| eq. (6.6) | `g⁽ᵏ⁾(a, β) = Σᵢ g⁽ᵏ⁾(bᵢ, αᵢ)` para `k = 0..N` — a mesma coisa para as **derivadas** |

E para estruturas **ramificadas** (§6.2.2) o livro escreve as três equações do caso com colchetes:

| eq. | o que diz |
|---|---|
| (6.10) | `g(a, 1) = g(s, 0) + g(a, 0)` — continuidade **ao longo do eixo existente** quando nasce um entrenó |
| **(6.11)** | **`g(b, 0) = 0`** — *«a newly formed lateral apex has zero length and zero growth rate»* |
| (6.12) | `g(b, β) = g(a, 0)` — a transformação do ápice lateral em ápice normal é suave |

⭐⭐⭐ **A eq. (6.11) é a cura, e ela não existia no nosso código nem no do Houdini.**

⚠️ O livro também nomeia o sintoma da versão sem as derivadas (a fig. 6.3a, crescimento linear):
*"the rate of organism growth suddenly increases with each cell division. **This effect is
disconcerting in animation.**"* — que é a frase do Enio, escrita 36 anos antes.

### 2.3 — O que ficou de fora, e porquê

| ideia da literatura | porquê não |
|---|---|
| **tDOL-systems completos** (idade contínua por módulo, idade terminal por produção) | Muda o **modelo de autoria**: a gramática passaria a declarar `β` e `αᵢ` por regra, e nenhum tutorial de L-System do mundo escreve isso. A nossa derivação é síncrona (todo módulo divide na mesma viragem), e nessa restrição a R2 e a (6.11) reduzem-se a **duas fracções em vez de uma** — que é o que se implementou |
| **dL-systems** (Prusinkiewicz, Hammel & Mjolsness, SIGGRAPH 93 — EDOs entre eventos discretos) | Resolve o mesmo problema com um solver de EDO por módulo. É o padrão-ouro para *simulação* de desenvolvimento; aqui o `Growth` é um **slider que o artista arrasta para trás e para a frente**, e um estado integrado não é função de `t` |
| Funções de crescimento **exponenciais** (eq. 6.7, com `B = ln φ ≈ 0,4812`) | Elas compram continuidade de ordem **infinita** (eq. 6.8), mas fixam a FORMA da rampa. O nosso `Growth` já resolve `r^g = r + t·(r^G − r)` para o **tamanho** sair numa recta, que é o que o Enio pediu em 29/08 — e sobrepor uma exponencial por módulo brigaria com essa lei |

---

## §3 — ⛔⛔ Por que TODAS as réguas desta linha diziam «liso»

O `probe_flicker` e o `probe_drift` medem **um escalar de TAMANHO** — a largura média de Cauchy, o
span de eixo, o centroide — e perguntam se a série dele é uma recta.

⚠️ **A lei do `build` normaliza exactamente essa grandeza.** Ela mede a figura a cada instante e
resolve o factor de comprimento que a põe na rampa recta entre a geração anterior e a nova. ⇒ *a
régua partilhava a lei do produto*, e um oráculo que partilha a lei do que julga é um espelho:
eles concordam **no errado**.

A grandeza que o produto **não** normaliza é a **TINTA** — a soma dos comprimentos desenhados. E a
imagem (que células o esqueleto toca) também não a vê: `5` segmentos colineares sobre o caminho do
pai tocam as **mesmas** células. ⇒ ⭐ *a cobertura é cega à sobreposição, e era sobreposição que
aparecia.*

A sonda nova é [`examples/probe_pops.rs`](../../crates/ph2d-node-source-lsystem/examples/probe_pops.rs),
com três modos:

| modo | o que mede |
|---|---|
| (sem argumento) | rasteriza o esqueleto e mede a fracção da tinta que MUDOU entre dois passos do arrasto (Jaccard), em duas molduras — mundo e forma |
| `--refine` | o **discriminador salto/movimento** através de cada fronteira de geração, contra um controlo no meio da geração, em dois passos |
| `--turn` | o salto relativo da tinta ao atravessar um `G` inteiro, em dois `ε` — a forma que os gates usam |

⭐ **O discriminador é o único que decide.** Uma diferença grande entre dois quadros é ou um
**salto** ou **velocidade**; as duas leem-se iguais numa amostragem só. Afina-se o passo e
pergunta-se o que aconteceu: movimento encolhe com o passo, um salto **não**.

---

## §4 — O mecanismo, e a correlação é perfeita

Nas quatro gramáticas que **refinam**, a tinta salta exactamente nas duas que têm material que
ninguém retraça:

| molde | gramática | tem tinta NOVA? | salta? |
|---|---|---|---|
| `Bush` | `F -> F[+F][J]F[-F]F` | **sim** — os `[+F]` e `[-F]` desenham e são laterais | **sim** |
| `Weed` | `X -> F[+X][J]F[-X]+X ; F -> FF` | **sim** — o `X` é um ápice que **não desenha**, e os `F` que ele produz são tinta que não estava lá | **sim** |
| `Koch` | `F -> F[J]+F-F-F+F` | não — só o `[J]`, que pousa uma marca e não desenha | não |
| `Dragon` | `F -> F[J]+G ; G -> F-G` | não — idem | não |

⚠️ **Atenção ao `[J]`:** as quatro têm colchetes. O que separa as duas famílias é *o colchete
DESENHA?* — uma marca de instância pousa um objecto, não traça um osso.

⇒ **A pergunta é: este módulo da geração nova RETRAÇA o que já estava desenhado, ou é tinta nova?**
E ela tem **duas** metades, porque a estrutura da cadeia só responde a uma:

1. **Lateral** — estou dentro de um `[` que a geração mais nova acabou de abrir. Responde-se na
   travessia, e de graça: o `[`/`]` já empilha o estado inteiro, então a bandeira herda-se para
   dentro e restaura-se na saída.
2. **Produtor mudo** — o meu produtor não desenhava. ⛔ **Isto a cadeia NÃO responde:** um `F`
   nascido de outro `F` e um `F` nascido de um `X` têm o mesmo símbolo, a mesma geração e a mesma
   profundidade de encaixe. Viaja no módulo (`Module::from_drawing`), escrito pelo reescritor, que
   é o único sítio onde o predecessor existe.

---

## §5 — A cura

Uma terceira fracção no `Setup` (`newborn`), ao lado das duas que já lá estavam:

| fracção | quem a recebe |
|---|---|
| `youngest.1` | quem **continua o eixo** do módulo que o produziu — retraça, logo chega com o comprimento do pai repartido |
| `angle_frac` | a viragem da geração mais nova |
| **`newborn`** | **quem não retraça nada** — nasce a zero e cresce |

⭐ **A lei entra MULTIPLICANDO o factor do eixo** (`newborn = len_frac · frac`) e não como um
número independente. Isso mantém a largura medida **homogénea** em `len_frac`
(`W(s·A, s·A·lat) = s·W(A, A·lat)`), e portanto o solve da normalização continua a ser **uma
divisão**, como sempre foi. *Uma lei que quebrasse a homogeneidade trocaria a normalização por uma
raiz.*

⚠️ **Três dos quatro braços do `build` passam `lat = 1`**, o que torna a lei **inerte** neles:
geração inteira · quem cresce pela PONTA (ali tudo o que é novo já estica de zero, e um `lat`
daria `frac²` — outro perfil, não outra lei) · `Grow Angle` desligado (o degrau inteiro de
sempre). Só o braço que **refina com o `Grow Angle` ligado** as separa.

⚠️ **A travessia que MEDE corre a mesma lei da que desenha.** O factor de comprimento sai de
`alvo / agora`, e o `agora` é medido com o material novo a zero — medi-lo a cheio devolveria o
factor de uma figura que o produto nunca desenha. O mesmo vale para a porta de sonda
`probe_anchor`.

---

## §6 — O que a medição deu

### 6.1 — O discriminador salto/movimento (`--refine`), fronteira ÷ controlo

| molde | antes: `h` → `h/4` | depois: `h` → `h/4` |
|---|---|---|
| `Bush` | **`38,5×` → `150,3×`** (×3,9 ⇒ SALTO) | `1,75×` → `2,28×` (×1,3 ⇒ movimento) |
| `Weed` | **`42,4×` → `166,5×`** (×3,9 ⇒ SALTO) | `1,30×` → `1,42×` (×1,1 ⇒ movimento) |
| `Koch` · `Dragon` | `1,08×` → `1,17×` · `1,43×` → `1,55×` | **os mesmos números** |
| `Tree` · `Fern` · `Wild` · `Sprig` | `1,31×` · `1,40×` · `1,81×` · `0,78×` | **os mesmos números** |

### 6.2 — A viragem nua (`--turn`), salto relativo da tinta ao atravessar `G = 4`

| molde | antes: `ε=8e-3` → `ε=1e-3` | encolhe | depois | encolhe |
|---|---|---|---|---|
| `Bush` | **`0,6933` → `0,6699`** | **`0,966×`** | `0,0214` → `0,0027` | `0,125×` |
| `Weed` | **`0,5587` → `0,5467`** | **`0,979×`** | `0,0134` → `0,0017` | `0,125×` |
| `Koch` | `0,0161` → `0,0020` | `0,125×` | **os mesmos bits** | `0,125×` |
| `Dragon` | `0,0037` → `0,0005` | `0,124×` | **os mesmos bits** | `0,124×` |
| `Tree`/`Fern`/`Wild`/`Sprig` | `0,0011`–`0,0049` | `0,125×` | **os mesmos bits** | `0,125×` |

⭐⭐ **`0,125` é o valor teórico** (o passo caiu `8×`), e seis dos oito já o davam. Os dois que
davam `0,97` tinham **67 %** e **55 %** da tinta a aparecer num intervalo de largura `1e-3` — e
nenhuma taxa de quadros esconde isso.

⭐ No `Bush` o salto foi de `0,6699` para `0,0027`: **248× menor**.

---

## §7 — O que a cura NÃO faz, e o que sobra

### 7.1 — ⛔ A hipótese barata continua refutada

Trocar o factor de normalização pela fracção crua (`grow = frac` para quem refina) baixa a
ondulação da tinta de `6,17` para `4,75` — **continua a saltar** — e **explode a do tamanho de
`1,00` para `7,61`**. A planta passaria a dar solavancos de tamanho para curar metade do outro
defeito.

### 7.2 — ⛔ O cruza-fade entre gerações continua recusado, e por outro motivo

Já estava na tabela de recusas do [estudo 95](95_estudo_ramificacao_continua_e_instancias.md):
desenhar as duas gerações com pesos `f` / `1 − f` fazia **cada folha encolher até sumir** durante o
crescimento, e o veredito do Enio foi *«bem bizarro»*. ⚠️ Aquela recusa respondeu a uma pergunta
sobre **folhas**; esta é sobre **tinta**, e a cura que se achou não precisa dela.

### 7.3 — ⏳ O que fica aberto, nomeado

- **O resíduo de `0,0027`** no `Bush` é movimento genuíno (encolhe com o passo), mas não é zero: a
  normalização iguala o **tamanho** da figura, e o eixo de uma gramática que não é exactamente
  auto-semelhante não retraça o pai ao bit. Curá-lo exigiria a R2 **por produção** (a repartição do
  comprimento do pai pelos filhos do eixo, proporcional ao comprimento final de cada um) em vez de
  um escalar global — mais uma coluna no módulo, e o ganho medido seria de `0,27 %` para `~0`.
- **A `Custom`** (a gramática que o artista escreve) herda a lei automaticamente: ela é estrutural,
  não uma lista de moldes.

---

## §8 — Os gates, e as duas mutações que os obrigaram a existir

[`tests/newborn_law.rs`](../../crates/ph2d-node-source-lsystem/tests/newborn_law.rs) — quatro:

| gate | o que afirma |
|---|---|
| `no_grammar_makes_its_ink_appear_when_a_generation_turns` | a **assinatura**: o salto encolhe com o passo (barra `0,5`, medido `0,125`, defeito `0,97`) |
| `the_ink_that_a_generation_turn_adds_is_a_sliver_not_a_step` | o salto **absoluto** (barra `0,04` = média geométrica das duas populações) |
| `the_tip_stretches_linearly_and_not_slowly_then_fast` | quem cresce pela PONTA acrescenta tinta **linearmente** (`0,2500`/`0,5000`/`0,7500`, exacto) |
| `with_grow_angle_off_a_fractional_generation_draws_the_whole_next_one` | com o `Grow Angle` desligado, quem refina desenha a geração seguinte **inteira, ao bit** |

⚠️ **Os dois últimos nasceram de mutações SOBREVIVENTES.** Eu tinha escrito a inércia dos três
braços em **comentários**, e a suíte inteira ficava verde ao:
- pôr o braço da PONTA a usar a lei (perfil `frac²`);
- pôr `lat = 0` no braço do `Grow Angle` desligado — o que **apaga os ramos laterais da figura**.

*Uma inércia declarada sem gate é uma afirmação, não uma propriedade.*

⚠️⚠️ **E a 1.ª redacção dos dois varria os oito moldes: os dois reprovaram, cada um sobre a
família do outro.** A família **sai do produto** (`probe_grows_by_refining`), nunca de uma lista
escrita à mão — a mesma lição que a `probe_growth_ratio` já tinha pago.

**Nove mutações, nove mortas:** reverter a lei · só metade dela · a travessia de medição a correr
outra lei · o braço que refina a passar `lat = 1` · o colchete a deixar de marcar lateral · o
produtor mudo a deixar de contar · e as três dos braços inertes.

---

## §9 — Smoke

```
cd /home/enio/Documentos/Projetos/PH2D/Worktrees/line-motion-value && env PH2D_GPU_COOK_DEMO=108 cargo run -p ph2d-host-desktop --release
```

No painel do nó, arraste **Growth** de ponta a ponta com o molde **Bush** e depois com o **Weed**.
A planta tem de engrossar **sem saltos**. Os outros seis moldes desenham exactamente o que
desenhavam.

---

## §10 — E o report seguinte: *"está mais suave mas não é perfeitamente linear"*

> **Enio, 2026-08-31**, depois de smokar a lei do recém-nascido.

⚠️⚠️ **Suave e linear são coisas diferentes, e ele nomeou a distinção.** A primeira é a ausência
de degraus (a derivada existe); a segunda é a derivada ser **constante**. A lei do §5 curou a
primeira; esta é a segunda.

### 10.1 — Medido: DUAS causas, uma por família

A régua é o desvio do tamanho em relação à recta que une as duas pontas do arrasto
(`probe_pops.rs --linear`), e a coluna que decide é **onde** o desvio está:

| família | desvio **na fronteira** de geração | no MEIO |
|---|---|---|
| refinam (`Koch`, `Bush`, `Weed`, `Dragon`) | **`−0,3 %`** ⇒ acerta | **`+9,5 %`** |
| ponta (`Tree`, `Fern`, `Wild`, `Sprig`) | **`+7,2 %` · `+10,9 %` · `+8,3 %`** | idem |

⭐⭐ **Quem refina erra DENTRO da geração e acerta nas inteiras — é a composição de duas leis.**
O remap resolvia `g` supondo `tamanho = r^g`; o `build` entrega a **CORDA** entre `r^k` e
`r^{k+1}`, porque a normalização dele é linear em `frac`. Uma corda está sempre **acima** da
exponencial que une os mesmos dois pontos: a `r = 3`, a meio, `(1 + 2)/√3 = 1,155` ⇒ **`+15,5 %`**
local. *Duas leis, cada uma certa sozinha.*

⭐⭐ **Quem cresce pela PONTA erra nas PRÓPRIAS gerações inteiras — ninguém os linearizava.** A
`measure_ratio` devolvia `1,0` para eles, o remap virava a rampa linear em `g`, e o tamanho deles
é **côncavo** (a razão converge para `1`).

⚠️⚠️ **E a nota que justificava essa isenção mediu a régua ERRADA.** Ela dizia *«o modelo
exponencial piora-os — o Tree foi de `0,5×` para `0,8×` de ondulação»* — e **ondulação é
suavidade**. *A recusa respondeu a outra pergunta, e viveu dois dias como se tivesse respondido a
esta.*

### 10.2 — A cura: inverter o que o produto ENTREGA, medido

⛔ Nenhum modelo — nem exponencial, nem convergente, nem limiar. Mede-se a **escada de tamanhos**
(`growth::size_ladder`: o tamanho em cada `g` da grelha) e **inverte-se**. A mesma lei serve as
duas famílias, e a densidade da escada sai da **família**, medida na própria cadeia
(`derive::refines_at`):

| família | degraus por geração | porquê |
|---|---|---|
| refina, `Step Scale = 1` | **1** | a normalização já força a corda ⇒ um degrau descreve-a a `±0,02 %` |
| cresce pela ponta | **3** | não há normalização; a curva curva-se dentro da geração |
| refina, `Step Scale ≠ 1` | **3** (2 travessias cada) | o `build` mede as duas pontas no factor do instante ⇒ a corda entre degraus não é a curva |

### 10.3 — O que a medição deu

| molde | desvio da recta ANTES | DEPOIS |
|---|---|---|
| `Tree` | `+11,00 %` | **`−0,26 %`** |
| `Fern` | `+6,88 %` | **`+0,11 %`** |
| `Bush` | `+9,75 %` | **`+0,01 %`** |
| `Weed` | `+5,73 %` | **`+0,02 %`** |
| `Wild` | `+21,33 %` | **`+0,13 %`** |
| `Koch` | `+9,76 %` | **`−0,01 %`** |
| `Dragon` | `+3,76 %` | **`−0,46 %`** |
| `Sprig` | `+14,17 %` | **`+1,35 %`** |

**Custo** (`probe_cost.rs`, load `1,1`): arrastar o `Growth` custa `0,017`–`0,085 ms` em sete
moldes e **`0,628 ms` no `Dragon`** (`3,8 %` de um quadro), e só enquanto o slider está abaixo de
`1`. ⭐ É **mais barato** que a lei que substituiu: o Bush ia a `1,124 ms` e a Koch a `0,995 ms`,
porque aquela derivava sempre até `g = 6` — a escada deriva até ao **topo do arrasto**, que é
`4` neles. ⏳ O `Dragon` é a excepção (`0,226 → 0,628`): ali o topo é `12`.

### 10.4 — Três defeitos que só a construção revelou

1. ⛔ **A escada derivava com a semente `1` fixa.** Numa gramática **estocástica** a semente
   escolhe a planta ⇒ a escada media **outra planta**. Medido no `Wild` (o único do corpus com
   pesos): `−7,69 %` → **`−0,29 %`**. *As determinísticas são byte-idênticas a qualquer semente,
   e é por isso que o defeito tinha um sujeito só.*
2. ⛔⛔ **O `Sprig` tem um PATAMAR, e ele é da FIGURA.** A ponta mais alta da planta é um galho
   **lateral**; o rebento novo tem de o ultrapassar antes de a silhueta crescer. Medido: no
   primeiro quarto de cada geração o `y` máximo fica **exactamente** preso em `0,6903` enquanto a
   **tinta** cresce a passo constante (`+0,0356`). ⛔ Um intervalo plano não tem inversa única, e
   colapsá-lo faria **`20 %` da tinta aparecer de uma vez** — o defeito do §5. ⇒ `3` sub-degraus
   (o último valor antes de o patamar tornar a curva ambígua, medido por força bruta: `M=1`
   `−11,51 %` · `M=2` `−3,33 %` · **`M=3` `−1,40 %`** · `M=4` não-invertível) **mais** a rede que
   descarta o degrau que não sobe.
3. ⛔ **O `Step Scale` fora do neutro quebrava a lei, e é PRÉ-EXISTENTE.** Medido na `Koch` com
   `0,5`: `0,3737` de desvio com a lei de 29/08 → `0,1540` com a escada de um degrau →
   **`0,0244`** com os sub-degraus. ⏳ Fica **nomeado**: `2,4 %` contra os `≤1,35 %` do neutro.

⚠️ **Os oito moldes deixam o `Step Scale` no default**, então um corpus inteiro no ponto neutro
de um param **não testa esse param** — a mutação que o tirava da escada sobrevivia. A fixtura é
`the_step_scale_reaches_the_size_ladder`.

### 10.5 — Os gates, e as duas fixturas que uma mutação obrigou

| gate | o que afirma |
|---|---|
| `the_whole_growth_drag_is_a_straight_line_for_every_preset` | o desvio da recta `< 2,5 %` no corpus inteiro |
| `the_remap_straightens_the_tip_growers_it_used_to_leave_bent` | com a escada ÷ sem ela `< 0,3` (medido `0,006`–`0,123`) |
| `the_size_ladder_is_measured_with_the_artists_seed` | a escada muda com a semente, **e o arrasto também** |
| `a_plant_whose_size_stalls_for_a_whole_generation_still_drags_forward` | a rede do patamar, sobre uma fixtura **construída pelo mecanismo** (galhos de `0,9·s` contra um rebento de `0,8·s` ⇒ ele nunca os ultrapassa) |
| `the_step_scale_reaches_the_size_ladder` | o factor chega à escada (`< 0,06`) |

⚠️ **As duas últimas nasceram de mutações SOBREVIVENTES**, e as duas pela mesma causa: *uma
guarda cujo fenómeno não existe no corpus não é testada por ele*. **Nove mutações, nove mortas.**

---

## ⛔ Recusas MEDIDAS (deste doc)

| Item | Motivo |
|---|---|
| `grow = frac` para quem refina | Baixa a ondulação da tinta de `6,17` para `4,75` (**continua a saltar**) e explode a do tamanho de `1,00` para `7,61` ([§7.1](#71--️-a-hipótese-barata-continua-refutada)) |
| Cruza-fade entre as duas gerações | Faz cada folha encolher até sumir durante o crescimento; veredito do Enio *«bem bizarro»* ([§7.2](#72--️-o-cruza-fade-entre-gerações-continua-recusado-e-por-outro-motivo)) |
| Copiar a lei do Houdini | ⛔ **Ela é a que já tínhamos** — a doc dele diz *«scales the geometry generated by the last substitution»*, sem distinguir material novo de material retraçado ([§2.1](#21--o-que-a-referência-comercial-faz)) |
| tDOL-systems completos (idade terminal por produção) | Muda o modelo de **autoria**: a gramática passaria a declarar `β`/`αᵢ` por regra, e nenhum tutorial de L-System escreve isso ([§2.3](#23--o-que-ficou-de-fora-e-porquê)) |
| dL-systems (EDOs entre eventos) | O `Growth` é um slider que se arrasta nos dois sentidos; um estado integrado não é função de `t` ([§2.3](#23--o-que-ficou-de-fora-e-porquê)) |
| Funções de crescimento exponenciais (ABOP eq. 6.7) | Compram continuidade de ordem infinita mas **fixam a forma da rampa**, brigando com a lei do `Growth` que o Enio pediu em 29/08 ([§2.3](#23--o-que-ficou-de-fora-e-porquê)) |
| R2 **por produção** (repartição proporcional do comprimento do pai) | Custa mais uma coluna no módulo e o ganho medido é de `0,27 %` para `~0` ([§7.3](#73---o-que-fica-aberto-nomeado)) |
| Réguas de TAMANHO para acusar este defeito | ⛔ O produto **normaliza** o tamanho — a régua partilhava a lei do que julgava ([§3](#3---por-que-todas-as-réguas-desta-linha-diziam-liso)) |
| Réguas de COBERTURA (imagem rasterizada) | Cegas à **sobreposição**: `5` segmentos colineares sobre o caminho do pai tocam as mesmas células ([§3](#3---por-que-todas-as-réguas-desta-linha-diziam-liso)) |
| O modelo `r^g` no remap | ⛔ Ele é o modelo do REMAP, não o do PRODUTO — o `build` entrega a CORDA, e o erro a meio é `+15,5 %` a `r = 3` ([§10.1](#101--medido-duas-causas-uma-por-família)) |
| Isentar da remapagem quem cresce pela ponta | ⛔ **Refutado**: a nota media ONDULAÇÃO (suavidade) e a pergunta é LINEARIDADE — sem remapagem eles iam `+6,9 %` a `+21,3 %` adiantados ([§10.1](#101--medido-duas-causas-uma-por-família)) |
| Colapsar o patamar do `Sprig` para o tornar invertível | ⛔ Faria **`20 %` da tinta aparecer de uma vez** — o defeito do §5 ([§10.4](#104--três-defeitos-que-só-a-construção-revelou)) |
| Mais de `3` sub-degraus por geração | ⛔ A `M = 4` a curva do `Sprig` tem um degrau **plano** e a inversão deixa de ter resposta única ([§10.4](#104--três-defeitos-que-só-a-construção-revelou)) |
| Sub-degraus para quem refina no `Step Scale` neutro | ⛔ A normalização já força a corda: um degrau por geração descreve-a a `±0,02 %`, e sub-degraus custariam **2 travessias cada** ([§10.2](#102--a-cura-inverter-o-que-o-produto-entrega-medido)) |

---

## Fontes

- [L-System geometry node — SideFX Houdini docs](https://www.sidefx.com/docs/houdini/nodes/sop/lsystem.html)
- [*The Algorithmic Beauty of Plants*, cap. 6 «Animation of plant development» — Prusinkiewicz & Lindenmayer](https://algorithmicbotany.org/papers/abop/abop-ch6.pdf)
- [«Animation of plant development» — Prusinkiewicz, Hammel & Mjolsness, SIGGRAPH 93](https://algorithmicbotany.org/papers/animdev.sig93.pdf)
- [«Generative Modelling with Timed L-Systems» — Jon McCormack, DCC 2004](https://users.monash.edu/~jonmc/research/Papers/McCormack_DCC04.pdf)
