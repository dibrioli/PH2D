---
titulo: "O plano — waves do tamanho de uma feature, com o arquivo-fonte e o tamanho de cada uma"
tags: [modulo/3d, tipo/plano, assunto/waves, assunto/performance, assunto/risco, status/aceito]
status: aceito
modulo: 3D
atualizado: 2026-08-01
resumo: "A ordem de construção re-cortada pelo censo medido do SculptGL: cada wave é UMA coisa que o artista abre e usa, nomeia os arquivos-fonte que traduz e traz o tamanho em linhas. Os três CANAIS que multiplicam, os riscos com solução, e os alvos a medir."
relacionados: ["[[03.4-Referencia-SculptGL]]", "[[03.7-Oraculo-de-fidelidade]]", "[[03.5-Onde-roda-o-motor]]", "[[04.1-Pinceis]]"]
---

# O plano

## A regra que governa o corte

> **Cada wave deixa UMA coisa que o Enio abre e usa.** Nada de três meses de fundação antes do primeiro
> pixel — e nada de uma "wave" que na verdade são quatro.

⚠️ **E o plano anterior violava a segunda metade, medido.** Ele tinha uma linha chamada *"W4 —
TOPOLOGIA: voxel remesh, subdivisão/multires, decimação"*. Isso são **~4.100 linhas de JS**, seis
algoritmos independentes e três pré-requisitos que ele não nomeava. Não é uma wave; é um trimestre com
nome de linha de tabela. O censo de 2026-08-01 mediu cada bloco, e este plano é o re-corte.

## O que o censo mudou, e por quê

| Antes | Agora | Motivo (medido) |
|---|---|---|
| W4 = "topologia" (um bloco) | **W6 · W7 · W9**, três waves com entregas diferentes | ~4.100 linhas de JS e três pré-requisitos distintos; subdividir, remeshar e dyntopo são três produtos |
| W6 = verbos que deformam, **depois** da topologia | **W5**, logo depois do traço | eles dependem só do **canal de gesto** (`medium`). Nenhum depende de topologia — só o Snake Hook *melhora* com ela |
| W8 (O2) **dependia do shader** | **W8 depende da CENA VIRAR LISTA** | o objeto misto precisa de *transform por malha*, não de PBR. A W3 já provou que a malha renderiza dentro da cena 2D. ⚠️ Isto PUXA o segundo objetivo para mais cedo |
| a máscara estava "pronta" (W2) | **W4.2 — ela é INVISÍVEL** | construída em W2 e o artista não a vê: falta o canal de cor até a GPU |
| — | **W4.0 — três defeitos** | a auditoria achou código que mente (`honours_invert`), uma normal fabricada e um import que embaralha índices em silêncio |

## Os TRÊS CANAIS — a espinha, e por que a ordem é esta

O censo achou três pré-requisitos, cada um `medium`, e **cada um destrava uma família inteira**. Eles são
o melhor negócio do módulo e por isso abrem as três waves seguintes:

| Canal | Custa | Destrava |
|---|---|---|
| **o dab completo** (espaçamento · raio de tela · culling · direção do olho) | medium | o traço sem buracos · o pincel de tamanho constante · o plano ajustado só nos frontais |
| **o canal de COR até a GPU** | medium | a máscara **visível** (já construída!) · o Paint · o material por vértice |
| **a ARESTA** (+ `vertOnEdge`) ✅ | medium | a borda do Smooth · fechar buraco · subdividir · decimar · collapse |

---

# As waves

| W | Entrega que o artista abre | Traduz de | Linhas JS | Depende |
|---|---|---|---|---|
| **W1** ✅ | **A MALHA** — buffers, adjacência CSR, octree, import OBJ, render | `mesh/Mesh.js` · `math3d/Picking.js` | 2347 | — |
| **W2** ✅ | **O BARRO** — a lei do traço + 12 verbos + máscara + simetria | `editing/tools/*` | 2234 | W1 |
| **W3** ✅ | ★ **A DOAÇÃO (O1)** — a tinta chapada sai acesa pela FORMA | *(nada — é nosso)* | — | W1 |
| **W4** ✅ | **O TRAÇO FICA HONESTO** — sem buracos, tamanho constante na tela, e a máscara **aparece** | `SculptBase.js` · `Picking.js` · `Masking.js` | ~800 | W2 |
| **W5** ✅ | **A MALHA PUXA** — Move ✅ · Snake Hook ✅ · Twist ✅ · Local Scale ✅ | `Move.js` · `Drag.js` · `Twist.js` · `LocalScale.js` | ~550 | W4 |
| **W6** ✅ | **A RESOLUÇÃO DEIXA DE SER FIXA** — subdividir ✅, multires ✅, des-subdividir ✅, fechar buraco ✅ | `Subdivision.js` · `multiresolution/` · `Reversion.js` · `HoleFilling.js` | ~1900 | W1 |
| **W7** | **O BOTÃO REMESH** — voxelizar e reconstruir | `Remesh.js` · `SurfaceNets.js` · `MarchingCubes.js` | ~1370 | W6 |
| **W8** | ★ **O OBJETO MISTO (O2)** + a cena com mais de um objeto, import/export, o documento | `Scene.js` · `files/*` | ~2400 | W4 |
| **W9** | **DYNTOPO** — a malha ganha detalhe onde o pincel toca, sem rasgar | `mesh/dynamic/*` | 1290 | W6 |
| **W10** | **O SHADER** — PBR · SSS · AO · Cavity · IBL · alphas/stencils · mask extract | `shaders/pbr.glsl` (referência) | — | W3 |

⚠️ **W3 e W8 são os objetivos** ([[01.1-Missao-objetivos-e-nao-objetivos]]). Tudo antes existe para
sustentá-los; tudo depois os amplia. **Se o orçamento acabar na W8, o módulo entregou o que foi pedido** —
e agora isso custa menos do que custava, porque o O2 deixou de esperar o shader.

---

## W4 — O TRAÇO FICA HONESTO

> *"Eu esculpo depressa e o traço não tem buracos; aproximo a câmera e o pincel continua do mesmo tamanho
> na tela; eu mascaro e VEJO a máscara."*

⚠️ **Quem vai IMPLEMENTAR esta wave lê [[03.8-HANDOFF-implementador-W4]] primeiro** — ele traz, item a
item, o `arquivo:linha` verificado, a cura exata, o gate red-first **com o número que ele dá hoje**, a
fixture e a armadilha; os seis mapeamentos foram auditados por um cético e **os seis voltaram
corrigidos**. O que está aqui embaixo é o *corte* da wave; lá está *como fazê-la sem repetir os erros
que já foram medidos*.

**W4.0 — os três defeitos** (fecham antes de qualquer feature; §livro-razão ⛔)

- ✅ **FECHADO** — `honours_invert()` devolvia `true` para Flatten/Fill/Scrape e o `invert` **nunca chegava
  neles** (o alvo dos três é `project(base, plane)`, que não toca `reach`). A cura foi a função **dizer a
  verdade** — whitelist `Draw·Inflate·Clay·Crease·Mask` —, não "fazer o invert funcionar". ⚠️ O gate antigo
  não podia falhar (o oráculo dele era o `reach`, que **é** o predicado composto com um sinal); o
  substituto mede o **estado da malha**.
- ✅ **FECHADO** — `face_normal` de face degenerada devolvia **`[0,1,0]` unitário fabricado**, que entrava
  na média com peso cheio (**37,16°** de inclinação medidos); agora contribui o vetor zero, como no
  original. ⚠️ E a prova de que o `ray.rs` era seguro estava **errada para QUADS**: o gap do quad-gravata
  ficou nomeado e com gate, sem leitor de produto.
- ✅ **FECHADO** — `obj.rs`: uma linha `v` malformada fazia `continue` **sem empurrar vértice** ⇒ todo
  índice seguinte deslocava, e um arquivo corrompido virava geometria embaralhada em silêncio. São
  **quatro canais de duas classes** (descarta ⇒ desliza · fica ⇒ corrompe), mais `inf`. ⚠️ E o original
  **não descarta o arquivo**, como a nota dizia: ele empurra `NaN` e nunca desliza — divergimos de
  propósito, porque `NaN` em posição envenena octree e AABB em silêncio.
- ✅ **FECHADO** — o `fallOff^5` do Crease. Sem ele o verbo era um Draw invertido **ao bit** (razão de
  afiação 1,000 → **2,073**). ⚠️ E a cura de UMA LINHA (elevar o `accum`) compila, fica verde em toda
  fixture — todas usam `strength: 1.0`, onde `accum == shape` — e entrega **3,1%** do alcance com
  meia força. O gate que a mata mede a linearidade na FORÇA ao lado da quinticidade na MÁSCARA.

**W4.1 — o DAB completo** · ✅ **FECHADO** · `SculptBase.js:92-152` · `Picking.js:378-392`

Os quatro vinham do mesmo `Dab` enriquecido — e a leitura da referência mostrou que a razão de serem
**uma** fatia é outra e mais forte: os três primeiros pedem a MESMA superfície nova (câmera + pick),
porque o `makeStroke` **re-pica a cada ponto interpolado** e usa isso para `break`. Interpolar em MUNDO
seria outro algoritmo: ele carimbaria através do vão onde a superfície não está.

- ✅ **espaçamento + interpolação com CARRY** — `ph2d-sculpt3d::spacing`, puro e sem câmera (a unidade é
  a do chamador). ⚠️ O oráculo é o **teto do vão** (`≤ 2·min_spacing` em qualquer polling) e não uma
  barra de volume: a lei põe o espaçamento em `[ms, 2·ms)`, então uma barra de 5% sobre volume é flake
  por construção. ⚠️ E o índice do laço é INTEIRO: o acúmulo em float do original **perde um dab em
  1923 de 4000** valores de `n` (medido);
- ✅ **raio em pixels de TELA** — `Camera3d::world_radius_for_screen_px`, derivado por dab. ⚠️ Os dois
  limites da faixa **mudaram de mecanismo** ao mudar de unidade, e por medição (o piso é a TELA, não a
  aresta da malha; o teto é fração da ALTURA do viewport, porque um teto fixo em px cobre 91% do modelo
  a 720p e 45% a 1440p). ⚠️ A **pressão do tablet** entra AQUI, e a shell não a recebe — vão nomeado;
- ✅ **direção do olho** no pick (`Picking.js:221-223`) — o `Dab.eye`, e o espelho da simetria acontece
  **antes**, então o pick espelhado tem `eyeDir` espelhado;
- ✅ **conjunto FRONTAL** (`getFrontVertices`) alimenta a normal de área e o ajuste do plano. Só esta
  metade é incondicional no original; filtrar *o que se MOVE* é um checkbox `false` por default e **não**
  foi portado. Medido: um bojo assimétrico invisível girava o Draw em **1,456°**, agora **0,207°**.

✅ **E a frase do `04.1` deixou de ser falsa.** A lei do envelope protegia contra *passar devagar deposita
mais* e não contra *passar rápido deposita MENOS*, porque nos vãos não havia dab nenhum. Com o caminho
percorrido, a promessa de independência de amostragem vale — no limite exato que a lei entrega.

**W4.2 — o canal de COR até a GPU** · ✅ **FECHADO** · `Masking.js` · `RenderData.js`

A máscara existia desde a W2 e **era invisível**. Agora ela tem um buffer de vértice próprio, sobe pelo
mesmo caminho incremental da geometria e é tingida no fragment.

- ✅ **o canal** — um `f32`, e **não** o `vec3` de material do original: rugosidade e metalness não têm
  quem as escreva nem quem as leia, e portá-las agora seriam 12 B/vértice e dois controles mortos. O
  material chega com o Paint, e o canal é aditivo;
- ✅ **a costura que decidia a wave** — um traço de máscara **não move geometria**, então ele não refresca
  normal nenhuma, e um upload guiado por *"o que refresquei?"* não subiria byte algum. A pergunta virou
  `SculptStroke::last_gpu_dirty` (*o que a GPU precisa RE-LER, em qualquer canal*);
- ✅ **a saturação** — MEDIDA: com a força de geometria (0,5) esfregar oito dabs chega a `0,5000 e para`
  (o envelope é um `max`), e `keep = 1 − mask` deixava metade de todo dab atravessar a proteção. O
  pincel de máscara passa a **nascer em 1,0**; a lei do traço fica intacta;
- ✅ **as quatro operações** (`clear` · `invert` · `blur` · `sharpen`), com gesto e undo. Elas **não são
  verbos**: um verbo fica na mão, estas executam e acabam.

⚠️ **E a máscara NÃO entra no G-buffer da doação**, com gate byte-a-byte: ela é chrome de AUTORIA, e se
vazasse a tinta que o Painter acende por baixo sairia azulada onde o escultor protegeu — a ferramenta de
trabalho dentro da obra.

---

## W5 — A MALHA PUXA

> *"Eu pego o barro e ele vem comigo."*

**W5.0 — o GRAB** · ✅ **FECHADO** · `Move.js`

O canal de gesto (`Dab::pull`, o deslocamento TOTAL desde o pen-down em mundo) e o verbo que o consome.

⚠️ **A exceção ao envelope é MAIS ESTREITA do que este plano supunha, e a diferença importa.** O que o
Grab precisa não é *composição de mapas* — é que o **early-out** do envelope não valha para ele
(`Verb::pulls`): a pegada é presa no pen-down, então o peso de cada vértice é o do primeiro dab e nunca
mais sobe, e o early-out congelaria o alvo (o barro andaria um evento e pararia com o cursor seguindo em
frente). **A lei continua de pé**: o alvo é `base + pull·falloff`, função do `pre` congelado, então puxar
de volta devolve o barro ao lugar e re-carimbar não intensifica nada.

⚠️ **E dois traços compõem sem que nada componha mapas** — cada `begin` congela um `pre` novo. Medido:
0,2 e depois 0,2 dão **0,2996 e não 0,4**, porque o vértice que o primeiro traço levantou ficou mais
longe do centro e o falloff do segundo o pega mais fraco. É o que qualquer app faz (a pegada é sobre a
superfície onde ela ESTÁ), e foi o oráculo ingênuo — somar os dois puxões — que falhou, não o produto.

⚠️ **A composição de mapas continua sendo o desenho do Snake Hook**, e a razão agora tem nome: nele a
**PEGADA anda**, e é isso que estica a malha.

**W5.1 — o SNAKE HOOK** · ✅ **FECHADO** · `Drag.js`

O verbo que **estica**, e ele obedece a **outra LEI**. O envelope (`accum ← max`, alvo função do `pre`) dá
idempotência sob re-stamp; um espinho é o oposto disso — ele **transporta matéria**, então o que sobrevive
a `n` dabs *tem* de ser uma soma sobre a lista. A lei nova é um **revezamento**: cada dab entrega ao
seguinte, o alvo é `posição VIVA + incremento·peso`, e a pegada ANDA com o cursor.

⚠️ **E ela cabe no MESMO aplicador**, o que é o desenho inteiro: com `accum = 1`,
`lerp(base, target, 1) == target`, então não há uma segunda rota de escrita de posição para divergir da
primeira no dia em que uma ganhar um caso especial. A escolha *qual lei este verbo obedece?* é feita
**uma vez**, pelo [`Grip`] — `Stamp` (onze verbos) · `Hold` (o Grab) · `Hook` —, e os consumidores casam
exaustivamente: um quarto grip não compila até alguém dizer o que ele significa.

- **o que se MANTÉM** — o undo continua trivial (`base` é o `pre` congelado de todo vértice que a pegada
  tocou em qualquer momento, e a janela do Hook é **maior** que a pegada atual: 66 contra 39, medido) · e
  a independência de amostragem sobrevive **como INTEGRAL DE LINHA**, porque o walk fixa o passo na
  geometria: o mesmo caminho entregue em 1, 2, 4, 8, 32 e 128 eventos move a ponta **1,19475 · 1,19475 ·
  1,19475 · 1,19792 · 1,19792 · 1,17583** — espalhamento de **1,6%**;
- **o que CAI** — a idempotência sob re-stamp, e nada a consome hoje (nenhuma rota deste módulo
  re-carimba um traço). Nenhum app entrega um Snake Hook idempotente: seria a ferramenta desfazendo o
  gesto.

⚠️ **Snake Hook funciona sem dyntopo** — ele estica os triângulos que existem, como o `Drag.js` deles. O
dyntopo (W9) é o que o impede de degenerar; não é pré-requisito para o verbo existir.

⚠️ **E medir o Grab para escrever esta wave achou DUAS dívidas nele**, ambas invisíveis aos gates que já
existiam porque os dois mediam o MIOLO da pegada — e no miolo `fall == 1`, onde os defeitos somem:

| Dívida | O número |
|---|---|
| o falloff entrava **duas vezes** (o alvo já trazia o peso, e o aplicador o aplica de novo) | a meio raio a referência move **0,22500** e nós movíamos **0,12226** — `pull·fall²` ao milésimo. O pincel saía pontudo |
| a pegada era **re-consultada** a cada dab, sobre posições vivas | um vértice puxado além do raio SAÍA da esfera e congelava: raio 0,4 puxado a 0,6 e trazido de volta deixava **0,52994** de resíduo, no verbo que promete *"puxar de volta devolve o barro ao lugar"* → **0,00000** |

**W5.2 — Twist e Local Scale** · ✅ **FECHADO** · `Twist.js` · `LocalScale.js`

Os dois verbos que faltavam, e com eles o **quarto grip**: `Grip::Turn`, que gira (ou escala) em torno de
uma **ÂNCORA**. O canal é UM campo escalar no dab (`Dab::amount`), com a unidade nomeada pelo construtor —
`turning` entrega radianos, `scaling` entrega uma fração.

- ✅ **A lei do traço fica INTEIRA, e é a divergência que decide a wave.** O original aplica o incremento de
  cada evento sobre o resultado do anterior (`Twist.js:47` lê `_lastMouseX`) — o *produto sobre a lista de
  dabs* que este módulo existe para não ter, e que no Local Scale morde de verdade
  (`Π(1 + sᵢ·wᵢ) ≠ 1 + Σsᵢ·wᵢ`: arrastar devagar cresceria mais que arrastar rápido pelo mesmo caminho).
  Aqui o gesto que chega é o **TOTAL** desde o pen-down e o alvo é função do `pre` congelado ⇒ o mesmo
  gesto entregue em **1, 2, 8 e 64 eventos dá a mesma forma a menos de 1e-5**, e varrer de volta devolve o
  barro ao lugar;
- ⚠️ **o peso entra no ÂNGULO (`accum = 1`), nunca no lerp** — interpolar entre o `base` e a posição girada
  cortaria pela **CORDA** do arco, e o barro encolheria na direção da âncora quanto maior o giro (o defeito
  que o tween do Flip pagou antes de trocar o lerp pela espiral). Medido: o raio ao eixo muda **< 0,01%**
  numa varredura de 2 rad;
- ⚠️ **os quatro grips viraram uma TABELA** em `dab_core` (`frozen` · `from_live` · `unit_accum` ·
  `early_out`), porque o `Turn` congela a pegada como o `Hold` e carimba `accum = 1` como o `Hook` — as
  combinações se cruzam, e três predicados soltos fariam um grip novo nascer com a mistura de quem alguém
  lembrou de atualizar. O `Verb::pulls()` virou **`anchors()`**: um redemoinho não puxa nada;
- ✅ **o clamp do Local Scale em zero não é um palpite** — é onde a operação deixa de estar definida: um
  fator negativo não encolhe mais, ele **reflete** a pegada através da âncora e vira aquele pedaço da malha
  do avesso;
- ⚠️ **o SENTIDO nasceu invertido, e ele não se argumenta — mede-se na TELA.** O `Dab::eye` aponta *para
  dentro* da tela, e girar em torno dele pela regra da mão direita sai **horário** para quem olha: medido,
  um vértice à direita da âncora descia `−0,1063` com um gesto positivo, então varrer no anti-horário
  torceria o barro ao contrário. O eixo é **menos** o olho (é o que o `Twist.js:41` faz), e o gate mede o
  barro na tela em vez de argumentar sobre sinais — o remédio que o smoke da órbita já tinha ensinado;
- ⚠️ **a força do pincel entra nos dois**, divergindo do original (que multiplica só por falloff e máscara).
  Um slider vivo em catorze verbos e morto em dois é um controle que mente.

**⚠️ E o Twist expôs um defeito MEDIDO na SIMETRIA, que fecha junto:** o espelho alcançava só o *centro* do
dab. O `pull` e o `eye` atravessavam sem ser tocados, então um **Grab com espelho puxava as duas metades na
MESMA direção de mundo** — `0,343574` de erro de simetria num pincel de raio 0,4. O `eye` errava
`0,047`–`0,094` **apenas onde a pegada atravessa a terminador**, e `0,000001` em qualquer outro lugar: era a
fixture que não continha o fenômeno, não o defeito que era pequeno. Agora o espelho alcança o dab inteiro, e
cada canal pela espécie geométrica dele — ponto, vetor, e o **ângulo como PSEUDOESCALAR** (um redemoinho no
espelho gira ao contrário; o sinal vem do determinante do trio, então espelhar os três eixos, que é uma
rotação de 180°, devolve `+1` sozinho).

⚠️ **A única linha que paga transcendental neste módulo** é a rotação de Rodrigues (um `sin_cos` por vértice
por dab). Ela não é aproximável por tabela: quantizar o ângulo faria a torção sair em degraus, e interpolar
entre nós devolveria uma rotação de norma < 1 — o encolhimento de novo, por outra porta.

⚠️ **E o gate que a wave trouxe achou um VERBO MORTO que ela não criou:** o **Magnify** não tinha tecla
nenhuma. Onze verbos de carimbo queriam dez dígitos e ele foi o que transbordou — existia no enum, tinha
braço de alvo, era varrido por todos os gates de verbo, e o artista **não conseguia pegá-lo**. Não era cerca
de Chesterton, era capacidade, e nada dizia. Ele foi para o `A` (de *amplify*), e o
`every_verb_is_reachable_from_the_keyboard` — que lê o `Verb::ALL` da FONTE do kernel, para a lista nunca ser
do gate — faz o verbo dezessete nascer impossível de esquecer.

**Smoke: `PH2D_SCULPT3D_SMOKE=5`** — uma esfera com uma CRUZ de cristas, porque numa esfera lisa um giro em
torno do eixo da vista **não se vê** (ela é invariante por rotação).

---

## W6 — A RESOLUÇÃO DEIXA DE SER FIXA

**W6.0 — A ARESTA** · ✅ **as três regras FECHADAS** · `Mesh.js:890-1000`

**`vertOnEdge`** virou `Adjacency::is_border`, e ⚠️ **sem campo e sem memória nova** — o dado já estava
nos dois CSRs (`vert_faces.len() != vert_verts.len()`), e a pergunta é `O(1)`. Com ele saíram as **duas
regras de borda do laplaciano** e o congelamento de **valência ≤ 2**.

⚠️ **E este é o consumidor das fixtures que a jornada abriu:** até aqui `open_tube3` e `pillow` eram
infraestrutura sem quem as usasse. Medido com a regra de borda desligada, seis passes de Smooth levam a
altura do tubo de **2 para 1,3597** — as bocas são sugadas para o miolo.

⚠️ **O grafo de ARESTAS (`_edges`/`_faceEdges`) não foi construído AQUI**, e as três regras não
precisaram dele — ele entrou com o primeiro consumidor de verdade, que é a **W6.1**.

**W6.1 — Subdivisão uniforme** · ✅ **FECHADA** · `editing/Subdivision.js` (588)

Tecla **K**: quatro faces onde havia uma, `V + E + Q` vértices, e a forma **alisa**.

⚠️ **A regra é EXATA, e isso foi conferido em vez de acreditado.** Os pesos do original são **Loop**
para triângulo e **Catmull-Clark** para quad: expandindo a fórmula publicada `(F + 2R + (n−3)V)/n` nos
anéis para valência 4 saem os três literais do `Subdivision.js` (`0,5625 / 0,09375 / 0,015625`), e o
ponto de aresta de quad é a média `(v1 + v2 + f1 + f2)/4` com os centroides abertos. **Nenhuma
divergência de MODELO** — só três de forma, todas para tirar caso especial (§livro-razão).

⚠️ **O GRAFO DE ARESTAS não é guardado na malha**, e é decisão de custo: a adjacência e o octree são
reconstruídos em todo `rebuild` porque todo dab os consulta; o grafo tem **um** consumidor, que roda
quando o artista aperta um botão. Guardá-lo cobraria a construção em cada undo para responder uma
pergunta que ninguém faz naquele instante.

⚠️ **E o custo NÃO está onde a intuição põe** (sonda `measure_subdivide`, malha de 24 mil vértices):
o gesto inteiro custa **7,3 ms**, dos quais **6,4 são reconstruir adjacência, octree e normais** da
malha quatro vezes maior — o plano e os canais são **0,7**. Otimizar a aritmética dos pesos não
compraria nada. É linear: **~80 ns por vértice de SAÍDA**, 220 mil vértices em 17,8 ms.

⚠️ **Sem teto escrito, e o motivo é o número:** o que aperta primeiro é a MEMÓRIA (~100 B por vértice
de saída, ×4 por nível), não o tempo. O log imprime a contagem nova a cada toque.

⚠️ **O UNDO ganhou uma CLASSE nova.** Uma mudança de topologia não tem janela — a contagem de
vértices é outra, e um índice de antes não nomeia nada de agora —, então a entrada guarda a malha
INTEIRA. Com isso o `StrokeUndo` deixou de ser um struct com bandeira e virou **enum de três casos**
(a janela de um traço · o plano de máscara · a topologia): com duas formas um `bool` bastava; com a
terceira, as quatro combinações incluiriam duas que não significam nada.

**Ainda ABERTO desta linha:** a esfera de partida virar um **cubo subdividido** (todo quad, sem
polos) — ela agora é possível de graça, e é mudança de LOOK da cena de smoke, então é do Enio.

**W6.2 — Multiresolução** · ✅ **FECHADA** · `mesh/multiresolution/` (`Multimesh.js` · `MeshResolution.js`)

Teclas **`,`** e **`.`**: o artista esculpe o detalhe fino no nível 3, **volta ao 0** para mover a
forma GRANDE, e sobe de novo com o detalhe intacto. É a única maneira de corrigir uma proporção
depois de já ter feito a pele — e é o que o `06.1` sempre nomeou como a razão de a W6 existir.

⚠️ **O detalhe é um DESLOCAMENTO num frame LOCAL, não uma posição.** Guardar posições seria inútil
(mover a base embaixo não as moveria); o que se guarda é a diferença entre onde o vértice ESTÁ e
onde a subdivisão o PORIA, escrita nos eixos `(normal, tangente, binormal)` daquele vértice — então
entortar a base **gira o detalhe junto**.

⚠️ **A ida e a volta são EXATAS quando nada muda embaixo**, e não é aspiração: `previsão +
(topo − previsão) = topo` ao bit, desde que o frame seja o mesmo dos dois lados. Daí o frame ter
**uma porta só** — encode e decode são as duas metades de uma inversa, e escritos duas vezes eles
divergem de um jeito que não aparece como erro, aparece como a escultura **escorregando** um pouco
a cada viagem. Medido: erro < 1e-5 em uma viagem **e em vinte**.

⚠️ **A NORMAL do frame não é recomputada entre descer e subir** — é isso que torna a viagem exata.
O preço foi **medido antes de ser aceito**: dobrando a base com um cisalhamento de 0,50 (que move o
topo da esfera meio raio de lado) o detalhe **inclina 1,5°** e o comprimento dele não muda um dígito
(0,26726 em toda a varredura) ⇒ a divergência do original **não se justifica**, e portar é o certo.

⚠️ **A metade barata da subdivisão virou porta:** `predict` devolve os CANAIS sem construir a malha
(o `partialSubdivision` do original). Trocar de nível custa **2,2 ms para descer e 4,1 para subir**
num topo de 55 mil vértices — contra 4,2 de subdividir; sem ela as duas seriam 4,2, porque **86% do
custo de subdividir é o `rebuild`** e a troca de nível não muda topologia nenhuma.

⚠️ **Os DETALHES custam 1,43 MB** para um topo de 95 mil vértices (12 B/vértice acumulados pela
pilha) — 15% da malha do topo, e a pilha inteira soma ~1,33× o nível mais alto.

⚠️ **O UNDO ficou mais barato, e a pilha é o motivo:** desfazer uma subdivisão deixou de guardar a
malha inteira (`Topology(Box<Mesh>)`) e virou **descartar o topo** — o nível de baixo nunca foi
tocado. E toda entrada de EDIÇÃO passou a carregar o **NÍVEL** em que aconteceu: sem isso, desfazer
um traço do nível 0 de pé no 2 escreveria as posições certas **nos vértices errados**, em silêncio.

⚠️ **Dois defeitos achados por MUTAÇÃO, os dois reais:** apagar a cópia dos vértices compartilhados
**sobrevive a todos os gates de viagem** (a ida e volta continua exata; só descer passa a mostrar
uma malha que ignora tudo o que foi esculpido em cima — *o trabalho fica guardado no detalhe e
invisível no lugar onde o artista foi procurá-lo*) · e desfazer uma subdivisão **de pé noutro
nível** era um **no-op silencioso**, porque descartar o topo só é possível estando nele.

⚠️ **E o smoke da W6.2 reprovou o REFAZER** (Enio: *"redo não funciona"*), que era pior que um botão
inerte: o `Ctrl+Shift+Z` caía no **mesmo braço** do `Ctrl+Z` porque o `shift` nunca chegava à cena —
o atalho de refazer **desfazia mais um passo**, destruindo trabalho em vez de não fazer nada.

Agora **desfazer e refazer são a MESMA operação.** Toda entrada é uma **troca**: aplicá-la instala o
estado que carrega e devolve, na mesma passada, o que estava ali — que é exatamente a entrada
inversa. Uma porta (`apply_entry`), duas filas; as direções diferem só em de qual se tira e para qual
se empurra. ⚠️ E a colheita é `mem::replace`, não uma segunda leitura: ler *depois* de escrever
devolveria o valor recém-instalado, a fila oposta guardaria um estado que **nunca existiu**, e o
artista só descobriria no **segundo** Ctrl+Shift+Z.

⚠️ **Refazer uma subdivisão RECOLOCA o nível, nunca o recomputa** — e a diferença está medida. A rota
barata (`add_level` de novo) só reproduz o nível enquanto o de baixo estiver byte-a-byte como estava,
e **descer escreve no de baixo** (o carimbo): depois de UMA viagem para baixo COM trabalho, a recomputação
desvia **0,236 numa esfera de raio 1** — 24% do raio, uma malha *parecida*, que é a pior forma de
errado porque ninguém a vê. Então `drop_top` **entrega** o que tira (`DetachedLevel`), e isso **não
custa pico de memória**: o nível é MOVIDO para fora e MOVIDO de volta, nunca clonado. ⚠️ Achado ao
construir: é literalmente o que o original faz (`StateMultiresolution.redo` chama `pushMesh(this._mesh)`),
então a convergência é confirmação, não coincidência.

⚠️ **Uma edição nova mata o futuro guardado, e a lei mora na PORTA que grava** (`record`), não numa
lista dos sítios que editam — enumerá-los (hoje três) é a lista que nasce incompleta no dia em que
aparece o quarto. **Trocar de nível não passa por ela:** olhar não é editar, e não pode apagar um
refazer.

⚠️ **Uma mutação SOBREVIVE e fica documentada em vez de gateada:** o `details` que um nível destacado
carrega não é lido por ninguém — `details[i]` só é consultado ao ENTRAR no nível `i`, o que exige um
`lower` antes, e o `lower` o **reescreve**. Ele fica porque *um nível É malha e detalhe*, e carregá-lo
é um move.

### ⬛ E TROCAR DE RESOLUÇÃO ERA UMA MUTAÇÃO SEM INVERSO — o artefato, medido

Report do Enio: *"UNDO/REDO não estão guardando cada etapa como aumentar e reduzir a resolução da
malha. isso provoca artefatos na malha. Faça gravar tudo que o usuário fizer"*. Ele está certo, e a
medição achou **duas** metades — a segunda é a causa do artefato e não estava no pedido.

⚠️ **(A) A DESCIDA ENCOLHIA O MODELO, e o número é do produto.** `K` seguido de `,` — **sem esculpir
nada** — deslocava a base 0,038 e levava o raio médio de **1,000 para 0,972**. E **compunha**: 2,81%
num ciclo de resolução, 3,32% em dois, 3,45% em três, convergindo para a superfície-limite. Causa: a
descida copiava `topo[..V]` verbatim para a base, e `topo[i]` num vértice par é a **regra par**
aplicada à base — um alisamento. ⚠️ **É fiel à referência** (`copyDataFromHigherRes` faz o mesmo) e
mesmo assim é um defeito: *o artista perdia o modelo dele por ter olhado*. Agora o que desce é a
**DIFERENÇA contra a síntese** — só o que ele moveu —, e um passeio ocioso não move **um bit**.

⚠️ **A régua é a SÍNTESE, não a previsão.** Comparar contra `predict` faria a base absorver o
DETALHE a cada volta (subir e descer carimbaria a pele na forma grande). A pergunta certa é *o que
mudou desde que cheguei aqui*, e a resposta é `topo − sintetizado(base, detalhe)`.

⚠️ **E ela vem com um PISO medido** (`STAMP_FLOOR`, 1e-6 da maior aresta do modelo): a régua é
recomputada, e o round-trip do frame ortonormal erra **1,49e-8**. Sem piso, *todo* passeio mediria um
carimbo de um ulp — e a entrada de desfazer de quem só foi olhar custaria uma cópia inteira da base.
⚠️ O gate do passeio ocioso **não pega um piso zerado** (sem escultura o detalhe é zero e a régua
acerta ao bit); quem pega é o irmão que esculpe primeiro.

⚠️ **(B) E a troca de nível entrou na história.** O doc de `change_level` dizia *"não é uma edição: o
gesto é o próprio inverso"* — a segunda metade é falsa, porque **descer escreve na malha de baixo**.
Agora são duas entradas distintas (`Descended` carrega o carimbo a devolver; `Ascended` não carrega
nada, porque subir só escreve o que base e detalhe já determinam) e elas são a inversa uma da outra.
⚠️ **Desfazer uma descida restaura base E DETALHE**, e a primeira versão dizia que o detalhe não
precisava: re-encodar reproduz a forma e **perde a distinção** entre *o que o artista moveu* e *o que
sempre foi detalhe* — o sintoma, achado por gate, é que descer de novo à mão deixava de carimbar.

⚠️ **Consequência de produto, nomeada:** uma troca de nível agora **limpa a fila de refazer**, como
qualquer edição. É a lei uniforme (uma porta, sem caso especial) e ela tem razão de correção — a
descida É uma edição —, mas o artista que desfaz, desce para olhar e volta perde o redo. **Decisão
reversível, se o smoke reprovar.**

### W6.3a — A REVERSÃO ✅ (`Reversion.js`, 580)

⚠️ **`Reversion.js` NÃO é undo** — a nota antiga o listava entre as recusas por causa do nome. É *reverse
Catmull-Clark*: reconstruir o nível de baixo de um OBJ denso. **É o único jeito de uma malha IMPORTADA
ganhar multiresolução**: a `Multires` só sabe acrescentar níveis para CIMA, então um OBJ denso nasce com
um nível — dá para esculpir a pele e **não** para mover a forma grande.

**A lei que a torna possível:** todo vértice ÍMPAR de uma subdivisão é **regular por construção** (6 no
interior de triângulos, 4 no de quads), logo todo **extraordinário é par** — e é dele que a varredura
parte. Depois é local: os vizinhos de um par são todos ímpares, e atravessando um ímpar chega-se ou ao
próximo par ou ao **ponto de face**, que se distingue por tocar **dois** ímpares do mesmo anel.

⚠️ **As posições grossas são CÓPIAS, e isso está certo.** Copiar a posição de um par de volta não é a
inversa aritmética de Catmull-Clark — é uma decimação. Quem restitui a forma é o **detalhe**, e o
round-trip fica dentro do `STAMP_FLOOR` (5,96e-8 medido, o épsilon de `f32`), que é por que a descida
seguinte carimba **exatamente nada**.

⚠️ **A metade cara é a RENUMERAÇÃO.** A `Multires` inteira se apoia em *o vértice `i` de baixo É o `i` de
cima*, e os pares de uma malha importada estão espalhados. O SculptGL guarda um `vertexMapUp` que todo
consumidor honra; nós **renumeramos a malha fina uma vez** — porque um mapa persistente seria uma segunda
numeração viva. Isso desce em **CASCATA** por toda a pilha, e a cascata sai das **FACES** (o canto
`(f, c)` de antes e o de depois descrevem a mesma aresta), nunca de aritmética de índices.

⚠️ **O desfazer guarda PERMUTAÇÕES (4 B/vértice/nível), não malhas**, e o **refazer não guarda nada**:
reverter é função pura da malha e desfazer a devolve ao bit, então refazer é chamá-la de novo. É o oposto
exato do `DroppedLevel`, que TEM de carregar o nível porque recomputá-lo dependeria de uma base que o
carimbo já mudou.

⚠️ **DUAS mutações sobreviveram e a causa das duas era a mesma: a fixture não continha o fenômeno.**
`subdivide(cube)` numera os originais primeiro, então reverter devolve a permutação **IDENTIDADE** — e a
renumeração inteira, cascata inclusive, ficava invisível a todo gate. A fixture honesta é a que o gesto
serve: uma malha **embaralhada** (`shapes::shuffled`), que é o que um OBJ de terceiro é. Com ela, as
mutações sangram e um gate novo mede a própria fixture (*a maioria dos vértices MUDA de índice*).

⚠️ **E uma terceira sobreviveu por outro motivo, com o oráculo errado:** um nível recém-subdividido tem
detalhe **zero**, e permutar zeros é no-op; pior, o `higher` **re-encoda** no fim, então uma pilha que
subiu errado fica auto-consistente e o gate do carimbo passa sobre forma destruída. O que resta
observável é **onde a escultura está** — um oráculo geométrico, por posição e não por índice.

⚠️ **Uma defesa em camada fica DOCUMENTADA em vez de gateada:** a verificação de bijeção é uma rede sobre
entrada não-confiável cuja própria falha nenhuma fixture de hoje alcança (buraco é estruturalmente
impossível; índice repetido exige topologia adversária que o `shapes_open` não produz). Ela fica pelo
mesmo motivo que o `Mesh::from_parts` valida índices.

Tecla **`J`** (o vizinho do `K`: um acrescenta acima, o outro reconstrói abaixo). Smoke:
**`PH2D_SCULPT3D_SMOKE=3`**.

### W6.3b — Fechar buraco ✅ (`HoleFilling.js`, 217)

Duas razões, e a segunda é a que aperta: o artista importou um modelo com furos (o pincel atravessa por
eles, a silhueta vaza, a simetria não tem o que espelhar); e **voxelizar exige malha FECHADA** — o flood
fill assinado do **W7** não tem resposta numa superfície com beira.

O contorno é uma **cadeia de arestas de valência 1**, colhidas na ordem em que a face que as possui as
percorre. O remendo é um **leque** a partir do centroide, que é a escolha do original — e para uma malha
de escultura ele tem a vantagem que as alternativas não têm: **é manifold e orientável por construção**
(um *ear clipping* não acrescenta vértice, mas num furo torto ele se auto-intersecta).

⚠️ **O ORIGINAL EMITE O REMENDO VIRADO, e isto foi conferido lendo o código dele.** Duas faces
consistentes percorrem a aresta partilhada em sentidos **opostos**, e o `HoleFilling.js` empurra
`(iv1, iv2, last)` com `iv1→iv2` já na ordem da face vizinha — a tampa nasce com a normal para dentro.
Aqui é `(b, a, centro)`, **e o gate é de ORIENTAÇÃO**: a contagem de faces, a de vértices e a valência
das arestas ficam todas certas com o remendo de cabeça para baixo. Nenhum gate de contagem o pega, e a
mutação que reinstala a lei do original derruba **dois** gates.

⚠️ **Divergência declarada: um contorno de TRÊS arestas vira UM triângulo**, sem vértice novo. O leque ali
poria um vértice de valência 3 no meio do que já era um triângulo — uma verruga de topologia que o
artista teria de limpar depois.

⚠️ **Fechar só ACRESCENTA**, e isso não é conveniência: é uma propriedade do algoritmo (um remendo é
geometria nova colada na beira). É ela que faz o desfazer custar **dois `usize`** — truncar — em vez de
uma cópia do documento, e há gate afirmando a propriedade em vez do atalho: no dia em que o
preenchimento mexer num vértice antigo, é ele que sangra, e o truncar deixa de ser correto no mesmo
instante.

⚠️ **Com a pilha montada ele RECUSA, e a recusa é estrutural:** tapar muda a TOPOLOGIA da base, e todo
nível acima é `subdivide` dela — o detalhe deles passaria a descrever uma malha que não existe mais.
Descer não resolve; o que resolveria é reconstruir a pilha, que é outra operação. O log manda tapar
ANTES de subdividir.

⚠️ **O centroide AFUNDA numa superfície curva**, e não é corrigido: empurrá-lo pela normal média exige um
número que ninguém mediu, e inventá-lo seria escrever um limite sem a tabela ao lado (CLAUDE.md §0). O
que se faz depois é o que se faria a qualquer tampa — um Smooth. **O smoke decide se isso basta.**

Tecla **`O`**. Smoke: **`PH2D_SCULPT3D_SMOKE=4`** — e ⚠️ **a cena DECLARA o furo que montou** (quantas
arestas de beira ela abriu): um smoke de fechar buraco sobre uma malha fechada é indistinguível da
feature quebrada.

**Com isto a W6 fecha.**

---

## W7 — O BOTÃO REMESH ✅ (Surface Nets; o marching cubes fica)

`Remesh.js` (483) → **o campo** (voxelização + flood fill assinado) → `SurfaceNets.js` (210, sai em quads).
A crate `ph2d-sdf`, que tinha **4 linhas**, virou a **quarta do módulo**. Tecla **`V`**; smoke
**`PH2D_SCULPT3D_SMOKE=6`**.

⚠️ **O `MarchingCubes.js` (680) NÃO entrou**, e a ordem não é acidente: Surface Nets põe **um vértice por
célula** e liga os vizinhos, então a saída é uma grade deformada, com valência 4 quase em toda parte — que é
a topologia que um escultor quer receber de volta. O marching cubes devolve triângulos finos e valências
irregulares, e a malha resultante **subdivide mal**. Ele existe para quem precisa da garantia de
*manifold* em célula ambígua, e é wave própria.

### A geometria que faltava, e o Möller–Trumbore que já existia

`ph2d-mesh::TriEdges` traz as duas peças do `math3d/Geometry.js`: distância ponto→triângulo (o
particionamento de 7 regiões do Eberly) e a travessia de raio. ⚠️ **E ela é o SEGUNDO Möller–Trumbore da
crate, com o viés OPOSTO ao do primeiro** — o do *picking* recusa em bordas exatas (um falso positivo na
aresta partilhada elege o triângulo vizinho, e o artista não distingue), enquanto aqui o falso **negativo**
é que custa: uma travessia de aresta perdida é o furo por onde o flood fill escapa e a malha sai do avesso.
A referência diz isso no próprio comentário. Os dois moram lado a lado com um gate pinando exatamente onde
discordam.

⚠️ **E a folga NÃO pôde ser copiada:** lá ela é `1e-15`, num motor de `f64`. Em `f32`, `1.0 + 1e-15` **é**
`1.0` — o literal daria uma função estrita com um comentário dizendo que é tolerante. Há gate pinando que
o número do original seria inerte.

### O que foi medido (e o `RESOLUTION = 150` da referência sobrevive)

Esfera de 13.682 v, `--release`, pela porta do produto:

| resolução | tempo | células | campo | saída |
|---:|---:|---:|---:|---|
| 32 | 15,8 ms | 50 k | 0,3 MB | 4,9 k v |
| 64 | 27,6 ms | 329 k | 2,2 MB | 19 k v |
| 100 | 46,9 ms | 1,16 M | 7,7 MB | 47 k v |
| **150** | **96,2 ms** | 3,72 M | 24,9 MB | 106 k v |
| 200 | 167,3 ms | 8,6 M | 57,5 MB | 188 k v |

Decomposição a 150: **voxelizar 52,4 · flood 21,7 · extrair 21,1 · tapar 1,4 · alocar 1,9** — soma 98,5
contra os 96,2 medidos, reconcilia. **Nenhum `MAX_*` foi escrito**: o botão é um CLIQUE, não um quadro, e o
que aperta primeiro é a memória (57 MB a 200), não o tempo. Sonda: `measure_remesh`.

### Quatro divergências declaradas

1. **O campo não carrega cor nem material** (a referência arrasta os dois, 24 B extra por célula contra os
   7 B que gastamos). ⚠️ **E a MÁSCARA de escultura também se perde** — carregar qualquer um dos dois é um
   plano a mais no campo com o ciclo de vida inteiro atrás. **Aberto e nomeado.**
2. **Não há re-alinhamento no fim.** A referência emite vértices em coordenadas de GRADE e re-escala pela
   *diagonal* da caixa; aqui a extração já emite mundo, então some o passo e some o erro de uma escala
   derivada de uma diagonal.
3. **A vizinhança do flood é calculada em `(x, y, z)`**, não por deslocamento cru no índice linear. No
   original, `cell ± 1` **envolve de uma linha para a outra**; lá é inofensivo porque a folga da caixa faz
   o fora encostar no fora — uma propriedade da MARGEM segurando um erro de indexação.
4. **A célula-semente é marcada ao ser semeada.** No original ela entra na pilha sem rótulo, e se nenhum
   vizinho a re-empurrar ela termina contada como **interior** — de novo, invisível só pela folga.

Mais uma guarda que a referência não tem: no `interpolate`, dois cantos infinitos de sinais opostos dão
`inf / inf` = **NaN**, e o teste `|den| < 1e-7` do original não o pega (`NaN < x` é falso). A finitude é
conferida antes.

### O que a wave deixa em aberto

- **Marching cubes** (o *manifold*), acima.
- **A recusa com a pilha de multires montada**, e ela é a MESMA lei do tapar-buraco: um remesh troca a
  topologia da base, e todo nível acima é `subdivide` dela. ⚠️ A alternativa seria **achatar a pilha em
  silêncio**, que é destruir trabalho autorado sem dizer — o log nomeia a recusa. Falta um verbo de
  *achatar* explícito, e ele é decisão de produto.
- **A resolução não é autorável**: o botão usa o default. Um slider é UI, e a tabela acima é o que ele
  precisaria mostrar.
- **Serial, e a ausência de `rayon` é deliberada**: o flood fill é sequencial por semântica e as caixas de
  dois triângulos se sobrepõem, então a escrita do voxelizador não é disjunta — a condição que o ADR-0109
  exige. O eixo honesto, se a medição pedir, é a fatia em Z.

---

## W8 — ★ O OBJETO MISTO (O2), e a cena que o permite

> **É aqui que o objetivo 2 existe, e o jogo ganha.**

**A cena vira uma LISTA com transform por malha** (`Scene.js` · `TransformData.js`) — o pré-requisito que o
plano anterior escondia dentro de "blocagem".

### W8.1 — A CENA É UMA LISTA ✅ (2026-08-02)

O primitivo é a **`ph2d_mesh::Pose`**: translação + **escala uniforme**, com quatro portas
(`point_to_world` / `point_to_local` / `vector_to_world` / `vector_to_local`) e uma quinta que é a que
decide a correção — **`ray_to_local`**.

| decisão | por quê |
|---|---|
| **o RAIO desce ao espaço da malha**, e não a geometria ao mundo | o caminho oposto custaria uma cópia da malha por consulta e invalidaria o octree, que é construído em local. Assim a consulta é a MESMA que a W1 gateou, e o acerto já volta nas coordenadas em que o dab escreve |
| **o pick compara em MUNDO** | o `t` de cada acerto mede na régua LOCAL do seu objeto (o `Ray` normaliza a direção), então uma peça a metade do tamanho devolveria um `t` duas vezes maior para o mesmo lugar — e a peça de trás ganharia o clique |
| **o pincel atravessa a pose DUAS vezes** | a câmera só responde sobre o mundo, então o ponto sobe para a pergunta e o raio desce (`/ scale`) para a resposta. Sem a segunda metade um objeto ao dobro receberia metade do diâmetro aparente de pegada |
| **`uploaded`/`dirty` são POR OBJETO** | subir a malha de um não limpa a do outro; um par compartilhado deixaria o segundo desenhado com a geometria de antes do dab, com todos os gates de CPU verdes |
| **um traço pertence a UMA peça** | ⚠️ **e isto é a diferença entre desenhar e PANICAR.** O `SculptStroke::begin` dimensiona os planos por-vértice na malha em que o traço começou; um dab noutra peça os indexa com os índices dela — mudo enquanto a nova for menor, **estouro** assim que for maior. Medido no smoke do Enio: tocar o cubo (8 vértices) e depois a esfera (6050). A peça é escolhida **uma vez, no pen-down** (`aim`), e os gestos consultam a ATIVA (`pick_active`) |
| **a entrada de undo carrega o OBJETO** | esculpir A, esculpir B, Ctrl+Z: sem ela a janela de A é aplicada na malha de B — índices certos, malha errada, sem erro nenhum |
| **`model` num bind group PRÓPRIO (grupo 1)** | a câmera e o rig são da CENA (um write por frame); a pose é do objeto (um bind por desenho). Juntá-los daria um bind group por objeto carregando cópias da câmera |

⚠️ **Sem ROTAÇÃO, e é decisão, não meio caminho.** Girar um objeto inteiro é um GESTO, e esse gesto não
existe (não há gizmo 3D): um campo de rotação hoje seria um número que ninguém consegue autorar, guardado,
multiplicado em todo frame e sempre igual à identidade — a *capacidade sem porta* que este módulo já
recusou uma vez (a `sculpt_kernel_device` da W1). Quando o gizmo chegar, ela entra **dentro das duas
portas** e nenhum call site muda. Corolário medido, e escrito nos dois lugares onde vale: hoje o
`obj.model` da linha da NORMAL (`mesh.wgsl`) e o `dir_to_local` da cena são **inertes** — tirá-los deixa os
22 gates de GPU verdes.

⚠️ **E a escala é uniforme por uma razão que atravessa o shader:** a normal viaja por `view * model` com
`w = 0` e só é normalizada, o que é exato enquanto a parte linear for uma **similaridade**; uma escala
por-eixo exigiria o inverso-transposto, e o sintoma de esquecê-lo é uma luz sutilmente torta que ninguém
sabe nomear. É decisão de RENDERIZAÇÃO, não um campo a mais.

**Gates:** 8 na `Pose` (ida-e-volta, ponto × vetor, o raio na esfera deslocada **com controle**, o piso da
escala degenerada, a coluna-major) · 2 no motor de traço (o repro **device-free** do pânico + o controle) · 3 de GPU (`two_objects_are_drawn_where_their_poses_put_them` ·
`the_pose_scale_grows_the_silhouette_without_tilting_the_light` ·
`truncating_the_list_stops_drawing_what_left_the_scene`) · 5 arch-gates de fonte. **12 mutações, 11 sangram**
— a nona (tirar o `obj.model` da normal) é a inerte acima, documentada em vez de gateada.

⚠️ **Uma medição derrubou uma prosa minha no ato:** eu escrevi que a SIMETRIA entre as duas peças pegava
uma matriz transposta. Não pega — as duas são imagens espelhadas uma da outra, então a razão fica em 1,0
(0,314 contra 0,320) com o defeito instalado. Quem pega é o **TAMANHO**: a transposição põe a translação na
linha `w`, o `w` de cada vértice deixa de ser 1, e as peças **inflam** — 0,052 contra 0,314 de tela por
terço, seis vezes de tinta.

**Smoke:** `env PH2D_SCULPT3D_SMOKE=7 cargo run -p ph2d-host-desktop --release`.

### W8.2 — OS VERBOS DA LISTA ✅ (2026-08-02)

A porta que faltava: até aqui só uma **fixture** conseguia pôr peça na cena. Agora o artista põe,
duplica e tira — `Shift+1..4` (esfera · cubo · **cilindro** · **toro**), `Shift+D`, `Delete`.

| decisão | por quê |
|---|---|
| a identidade de um objeto é um **`ObjectId`**, não o índice | apagar a peça 1 de três faz a antiga 2 virar 1, e toda entrada de undo que apontava para 2 passa a nomear **outra peça** — em silêncio, com o índice ainda válido. É a lição que a timeline pagou no `wire_id` e a física no `stable_name_id` |
| a peça nova nasce no **ALVO da câmera**, com tamanho **derivado da cena** | não há cursor 3D; o alvo é o único ponto que o artista escolheu. Um número fixo seria invisível numa cena grande e dominante numa pequena |
| a cópia nasce **ao lado NA TELA** | *"ao lado"* é palavra sobre o que se vê: um deslocamento em `+X` de mundo esconde a cópia atrás da original conforme a câmera, e o gesto lê como *"não duplicou"*. Usa a MESMA porta do arrasto de barro (`screen_delta_to_world`) |
| o delete **carrega a peça** na entrada | a saída que a W8.1 anotou (*apagar limpa a fila*) trocaria um trabalho perdido por outro: o artista recuperaria a peça e perderia a história de todas as outras. Custa um `Box` e devolve pilha de níveis, máscara e pose |
| a **última** peça não é apagável | a lista nunca-vazia é a invariante que torna `obj()` total; e uma tela preta sem nada dizendo por quê é pior que uma recusa que fala |
| as quatro primitivas cabem na **mesma esfera unitária** | senão a escala da pose dependeria da forma, e o cubo nasceria de outro tamanho que a esfera pelo mesmo gesto — com o mesmo número na pose. (Daí a aresta do cubo ser `2/√3`: quem mede 2 é a DIAGONAL) |

⚠️ **E o gate pegou um defeito meu no ato.** O delete precisa gravar com a peça que **saiu** (não com a
ativa), e a primeira versão escreveu um `push` paralelo na fila — a **segunda porta** para *quando o
refazer morre*, que é exatamente o que o `assert_eq!(matches("self.redo.clear()"), 1)` existe para pegar. A
cura foi `record_for`, com o `record` delegando.

⚠️ **E uma contagem minha da W8.1 expirou uma wave depois:** o gate afirmava *"só o `aim` e o `step`
escrevem no ativo"* contando `self.active =`. Os verbos da lista escrevem nele por definição, e todos
**fora** de um traço — a lei que importa é *não repicar a LISTA no meio de um gesto*, e ela se afirma no
`pick`, que tem **um** chamador. Proxy trocado pela propriedade.

**Gates:** 4 nas primitivas (`ph2d-mesh`: sólido fechado, quad puro e valência 4 no toro, a caixa, o piso
de fatias) · 2 arch-gates dos verbos. **5 mutações, 5 sangram.**

**Smoke:** a mesma `=7`, passos (7)–(10) — e o (10) é o duro: esculpir A, acrescentar B, esculpir B, apagar
B, e desfazer tudo **na ordem inversa, cada passo na peça certa**.

### W8.3 — O DOCUMENTO ✅ (2026-08-03, **smoke OK 2026-08-04**)

A escultura **sobrevive a fechar o app**: ela viaja num blob em `ProjectFile.sculpt`, **fora** do
`ProjectState` (que é a unidade do undo GLOBAL — o mesmo lugar, pelo mesmo motivo, de `motion`,
`timeline` e `physics`), e carrega a própria versão (`SCULPT_DOC_VERSION`), o precedente exato do
`TimelineDoc`. **`PROJECT_SCHEMA` 50→51 bumpa UMA vez**: daqui em diante o módulo evolui contra o
número de dentro do blob.

⚠️ **Só a metade AUTORADA viaja.** Uma `Mesh` tem nove campos e o artista autorou **quatro**
(posições, faces, cor, máscara); normais, normais de face, adjacência CSR, octree e caixa são
**função** deles, e o load os deriva pelo mesmo `from_parts` que um OBJ de terceiro atravessa.
Guardar um derivado não é desperdício de bytes, é uma **segunda cópia de um fato** — um octree
gravado hoje e lido por um binário cujo critério mudou descreveria uma partição que a malha não
tem, e nada na tela diria por quê. É a lei do ADR-0131 D2 (*o mundo é derivado dos componentes*).

⚠️ **A PILHA inteira viaja, e o `details` com ela** — o que a wave descobriu MEDINDO: o `lower`
**reescreve** `details[i]` a partir do nível vivo, então uma pilha salva **no topo** reconstrói o
detalhe sozinha, e apagá-lo na escrita passava em sete gates. A janela em que ele é a ÚNICA cópia
do trabalho é `sel == 0` com um nível acima — *salvei trabalhando na base* —, e é essa a fixture.
Medido: com o detalhe, o pico volta a **0,2455** contra os 0,25 autorados (1,8%, o custo da síntese);
sem ele, a **0,15625** — a predição lisa, 37,5% abaixo.

⚠️ **Uma escultura ilegível RECUSA o load inteiro** (a lei da timeline): abrir *sem* ela mostraria
uma cena que parece certa e o **próximo Ctrl+S gravaria o vazio por cima do arquivo** — a obra não
sumiria por um bug, sumiria porque o app abriu, mentiu e salvou. O parse roda **antes** de qualquer
mutação da sessão, com arch-gate sobre a ordem.

⚠️ **E um build sem o módulo é PASSA-ADIANTE, não triturador:** `ProjectFile.sculpt` é `Vec<u8>`
**incondicional** (um campo com `cfg` daria duas formas de arquivo sob um número de schema), e o
save devolve os bytes que o load trouxe quando não há cena viva — que é também o caso de um projeto
aberto antes de a GPU aparecer.

**Aberto:** o `LayerKind::Sculpt3d` segue **não-apendado** de propósito (um variant que ninguém
constrói é um variant morto).

**Smoke:** `PH2D_SCULPT3D_SMOKE=8` — três peças (a esfera com CRISTAS ao centro, e é isso que
separa *voltou* de *nasceu*), esculpir, `K` + detalhe fino + `,`, Ctrl+S, fechar, reabrir, Ctrl+O,
e `.` para conferir o detalhe.

### W8.4 — A PORTA DE ENTRADA ✅ (2026-08-03, **smoke OK 2026-08-04**)

⚠️ **O `import_obj` existia e NÃO tinha chamador nenhum** (conferido por grep): o leitor de OBJ
shipava desde a W1, com onze gates, e o artista não tinha um gesto que trouxesse um arquivo. Agora
**soltar um `.obj` na janela o traz** — e soltar uma malha **ARMA o módulo**, a mesma lei do load de
projeto da W8.3.

⚠️ **As malhas saem da fila ANTES do filtro de imagem**, e a ordem é a asserção inteira: o filtro
emite *"Skipped non-image"* por arquivo que não reconhece, então sem o desvio um `.obj` produziria
**um aviso de que foi ignorado** — a resposta errada com a confiança da certa.

**As duas dívidas de import estão PAGAS, e cada metade tem uma razão diferente:**

- **CENTRAR é exigido por um MECANISMO.** O espelho reflete negando uma coordenada
  (`Symmetry::signs`), então **o plano de simetria É a origem local**: uma malha que o autor deixou
  longe do zero espelha em torno de um plano que não passa por ela. A `Pose` não resolve — ela move o
  objeto no MUNDO, não a origem local em relação à geometria. A geometria é reescrita
  (`Mesh::recenter`) e o deslocamento retirado vai para a `Pose`, onde **preserva o ARRANJO** do
  arquivo: uma cabeça e um corpo continuam onde o autor os pôs.
- **NORMALIZAR não é exigido por nenhum, então mora na `Pose`.** O pincel mede PIXELS DE TELA (W4) e
  a câmera enquadra, então o tamanho absoluto não alcança gesto nenhum; o que ele alcança é a
  **convivência** — desde a W8.1 a cena é uma lista, e uma peça de 300 unidades ao lado de uma
  esfera de 1 torna a segunda invisível. **Um fator por ARQUIVO**, na escala da pose: os tamanhos
  relativos das peças sobrevivem e os números do autor não são reescritos.
- **`o <nome>` vira uma PEÇA.** ⚠️ O pool de vértices é do ARQUIVO (os índices de `f` são globais e
  podem apontar para antes do `o`), então cada peça é **compactada** — só os vértices que ela usa,
  com os índices remapeados. Sem isso cada peça de um arquivo de dez objetos carregaria os vértices
  dos outros nove, e nasceria enquadrada no arquivo inteiro em vez de nela mesma.

⚠️ **Um arquivo ilegível não derruba os outros** — aqui NÃO vale a lei da timeline que o load de
projeto segue: lá o risco é o próximo Ctrl+S gravar o vazio por cima da obra; aqui cada arquivo é um
gesto independente, e recusar o lote puniria os que estão bons.

⚠️ **`import_obj` mudou de forma:** devolve `Vec<ImportedPiece>`. Um arquivo sem `o` dá exatamente
uma peça, com gate pinando que esse caminho não se moveu.

**Smoke:** `PH2D_SCULPT3D_SMOKE=9` — a cena **escreve um OBJ-fixture** (dois objetos, a 400 unidades
da origem, medindo ~450) e imprime o caminho; o artista o arrasta para dentro da janela. Ela fabrica
o arquivo porque precisa de um que CONTENHA o fenômeno: um `.obj` qualquer poderia já vir centrado.

### W8.5 — A PORTA DE SAÍDA ✅ (2026-08-03, **smoke OK 2026-08-04**)

A escultura **sai**: OBJ, PLY e STL de saída, e STL/PLY de entrada — o par exato da W8.4, que
sem isto deixava o trabalho entrando e salvando num formato que só este app abre.

⚠️ **A geometria sai em MUNDO, e é a decisão espelho do import.** Desde a W8.1 a posição de cada
peça vive na `Pose`, não na geometria (foi a W8.4 que estabeleceu isso ao CENTRAR cada peça
importada) — escrever a geometria local seria escrever **todas as peças empilhadas na origem**.
Toda posição atravessa `Pose::point_to_world`.

⚠️ **O que cada formato perde é uma pergunta de CÓDIGO, não de prosa** (`MeshFormat::keeps_colour`
/ `keeps_pieces`): quem escreve o arquivo e quem AVISA o artista consultam a mesma tabela. Duas
cópias dariam um toast dizendo *"cor preservada"* sobre um STL — a resposta errada com a confiança
da certa. A **máscara** não sobrevive a nenhum dos três, então ela não é pergunta: é dita sempre.

⚠️ **A EXTENSÃO decide o formato, e não há um segundo seletor.** Duas portas para *"que formato é
este?"* divergem no primeiro `retrato.obj` salvo com "STL" escolhido ao lado; e uma extensão
desconhecida é **recusada com nome**, nunca dobrada num OBJ disfarçado — um default calado faz o
próximo programa dizer que **o arquivo** está corrompido, apontando para o lugar errado.

⚠️ **A SOLDA do STL não é otimização.** Um STL de um cubo traz 12 triângulos × 3 = 36 posições, das
quais 8 são distintas; sem soldar, **toda ferramenta de escultura fica inerte na malha** — suavizar,
subdividir, espelhar e fechar-buraco leem adjacência, e cada triângulo seria uma ilha. A malha
desenharia certo, que é a forma mais cara de um import estar errado. A chave são os **bits** do
`f32` (um épsilon fundiria vértices que o autor pôs perto de propósito), com o **zero normalizado**:
`-0.0` e `+0.0` são o mesmo ponto, e sem isso a costura abre exatamente sobre o plano de simetria.

⚠️ **O cabeçalho do PLY é uma DECLARAÇÃO, e é preciso lê-la.** Um leitor que assuma
`x y z red green blue` porque é isso que ele mesmo escreve devolve, num arquivo com `nx ny nz` no
meio, **as normais como cor** — e não falha, porque os números são todos válidos.

⚠️ **E a recusa por truncamento é o ÚLTIMO recurso, não o primeiro** — a primeira versão do leitor de
STL a punha antes do ASCII e **engoliu um arquivo ASCII legítimo**: num texto de 109 bytes os bytes
80..84 são letras, leem como uma contagem enorme, e a condição *"menor que o esperado"* casa.

**Os leitores vieram JUNTO com os escritores de propósito:** o oráculo desta wave é o **round-trip**,
e ele só existe dentro do app se houver quem leia de volta. Um smoke que precisa do Blender para
julgar não é um smoke, é uma tarefa.

**Smoke:** `PH2D_SCULPT3D_SMOKE=10` — três peças COLORIDAS em poses diferentes (a fixture tem de
conter as três coisas que um formato pode perder), `Ctrl+Shift+E` para escrever e `Ctrl+Shift+O`
para trazer de volta, nos três formatos.

### W8.6 — ★ O OBJETO MISTO, ROTA ASSADA ✅ (2026-08-03, **smoke OK 2026-08-04**)

**É aqui que o objetivo 2 existe.** Um gesto (`Shift+B`) assa a forma da malha num **sprite da
cena**: o objeto passa a ser desenhado pela luz da escultura, e **continua aceso depois de a malha
sair**. Cena `=11`.

⚠️ **A LEI da luz é UMA, e é isso que decidiu a fatia.** Quem acende é o `ImpastoLightPass` — o
mesmo passe que acende a tinta do Painter. Um kernel de iluminação no módulo 3D seria a segunda
resposta a *como uma normal vira luz*, e as duas divergiriam no primeiro material que alguém
acrescentasse. O preço de reusar é que o passe fala o vocabulário da TINTA, e os três planos que um
sprite não tem são fabricados neutros — cada um com o motivo LIDO no shader:

| plano | valor | por quê |
|---|---|---|
| relevo | `0` | sem gradiente, a composição das duas normais devolve **a da forma, intocada** |
| cobertura | o **alpha** do sprite | o shader faz `gloss = cobertura` — **sem `max`** —, então ela decide o **REALCE**: ele segue o alpha do desenho em vez de brilhar sobre o recorte vazio ⚠️ (a v1 desta linha dizia *"onde ela importa é a borda da silhueta, onde o peso é parcial"*, e a **medição a derrubou** — ver abaixo) |
| material | **o do BARRO** | ⚠️ a v1 usava `Material::NEUTRAL` e **o smoke a reprovou** — ver abaixo |

⚠️ **O que torna isto RELUMINÁVEL, e não um carimbo:** o bake guarda o `base` (os pixels ANTES da
luz), a `form` (o G-buffer, que **não depende do rig**) e o `texture_id`. Mover a lâmpada re-acende
a partir do `base` **sem re-rasterizar a malha** e **sem alocar textura nova**. Um bake que só
escrevesse pixels acesos entregaria uma sprite que o artista não pode mais iluminar — e iluminar é
a palavra inteira do objetivo 2.

⚠️ **Um defeito que o ROTEIRO do smoke expôs antes do código rodar:** escrever o passo *"assar de
novo não pode escurecer"* mostrou que `bake_one` relia os pixels do sprite — que depois do primeiro
bake **já estão acesos**. Re-assar compunha a luz. Hoje um sprite já assado reusa o `base` e o slot.

⚠️ **A ausência é NOMEADA:** o bake **não sobrevive a fechar o app**. Onde os dois planos moram no
documento é a pergunta da rota A completa (*canais no sprite*, `02.2` §Rota A), e respondê-la agora
seria escolher pelo que é fácil de escrever em vez de pelo que o runtime lê.

#### O smoke reprovou a LUZ, e a projeção estava inocente

Report: *"a luz não é idêntica no vivo e no assado; o modelo vivo parece em perspectiva, o assado
parece isométrico"*. ⚠️ **A segunda metade parecia geometria e não era** — a sonda
`measure_bake_framing` (`ph2d-mesh-render`, `--ignored`) mede a MESMA câmera nos dois aspectos:

| | silhueta | fração do quadro | redondeza |
|---|---|---|---|
| viewport largo (o que o artista vê) | 442×442 | 0,276 × **0,491** | **1,000** |
| sprite quadrado (o que o bake grava) | 503×504 | 0,491 × **0,492** | **0,998** |

A esfera sai **redonda nos dois** e a fração **vertical** é a mesma ⇒ a projeção é idêntica, e só a
largura do quadro muda. ⇒ **as duas metades do report eram UMA**, e a causa é o material: o
`Material::NEUTRAL` do Painter tem `shine = 0`, o realce do passe é `shine × spec × gloss`, e o
objeto assado saía **sem especular** enquanto o barro tem `CLAY_SHINE = 0.35`. Numa esfera lisa o
realce **é** o cue de volume.

⚠️ **A frase *"o barro ainda não tem material"* — que o shader e o bake diziam — estava ERRADA:** ele
não tem material POR PIXEL, mas tem dois números fixos. Eles viraram
`ph2d_mesh_render::{CLAY_SHINE, CLAY_EXPONENT}` (públicos, com gate WGSL↔Rust) e o bake veste o
material do barro. ⚠️ **A rugosidade não precisou de conversão:** `CLAY_EXPONENT = 24` é
`√(6 × 96)`, exatamente o que a rugosidade neutra da tinta produz — os dois caminhos **já**
concordavam sobre a largura do realce, e só discordavam sobre a intensidade.

#### 2ª rodada: *"não coincide perfeitamente"* — e o ARO está INOCENTE

Report com duas fotos e as setas no **ARO** da esfera. Um aro é onde a normal deita, e é onde duas
leis podem divergir sem que o miolo mude — então a pergunta não era *quanto* difere e sim **ONDE**,
porque um erro de borda e um erro de corpo têm curas opostas.

A sonda `sculpt3d::bake::light::measure_the_two_lights_over_the_same_form` roda **as duas leis
reais** (o `MeshRenderer::render` do barro e o `ImpastoLightPass` do bake) sobre o **mesmo
G-buffer**, e bucketiza a diferença por **distância à silhueta**. ⚠️ **Com o CONTROLE embutido** —
o sprite vestindo o albedo do próprio barro, sem o qual a tabela não vale nada:

| distância à borda | 0 | 1 | 2 | 3 | 4-7 | 8-15 | 16-31 | 32+ |
|---|---|---|---|---|---|---|---|---|
| `|dif|` média | **0,0020** | 0,0019 | 0,0019 | 0,0019 | 0,0019 | 0,0019 | 0,0029 | 0,0158 |

**PLANA na distância, e ruído no aro.** ⇒ **não há aro**: as duas leis concordam a meio por cento
exatamente onde o report apontava. O gate `the_two_lights_agree_where_the_form_turns_away` pina isso
(3 mutações rodadas: largar a `dir` na tradução do rig → 0,3002; `form: None` → 0,3514; e o
`Material::NEUTRAL` fica **VERDE e está certo** — o realce vive no miolo, e quem sangra por ele é o
irmão `a_baked_object_wears_the_clays_highlight`).

**O que difere é o ALBEDO, e o mecanismo é o TETO.** O passe leva `m` até **1,65**
(`AMBIENT + (1−AMBIENT)·2`) e clampa `albedo × m` em 1,0; o barro vivo escreve **HDR**
(`Rgba16Float` — 28.913 canais acima de 1,0 nesta cena) e deixa o tonemap fazer o roll-off. Um
sprite é `unorm8` e não tem esse teto para gastar:

| albedo do sprite | texels ESTOURADOS (gradiente ZERO — a forma some ali) |
|---|---|
| **255** (a tela branca do smoke) | **43,6 %** |
| 224 | 34,1 % |
| 192 | 20,9 % |
| 189 (`CLAY` como cinza) | 19,5 % |
| 160 | 3,6 % |
| **128** | **0,0 %** |

⚠️ **É isso que faz o assado ler como "chapado":** sobre branco quase metade da esfera vira
`(255,255,255)`. Não é a luz — é o albedo não ter alcance para a luz caber. **É o mesmo
comportamento da tinta branca sob o impasto**, então a lei está sendo honrada, não violada.

⚠️ **DECISÃO DE PRODUTO, devolvida ao Enio com o número:** a cura de zero código é **assar sobre
arte que não seja branca** (a 128 o estouro é ZERO). Um *ganho* que comprimisse o alcance da luz
dentro de `[0,1]` seria uma **segunda lei de luz** — exatamente o que esta fatia recusou — e **não
é recomendado**. O banner da `=11` passou a NOMEAR o número, para o artista não inferir.

⚠️ **E a medição derrubou uma afirmação minha:** o doc dizia que a cobertura importa *"na borda da
silhueta, onde o peso da forma é parcial (o antialiasing do G-buffer)"*. Medido, texels com
`0 < w < 1`: **ZERO** — o G-buffer é `sample_count: 1` e o `fs_gbuffer` escreve `w = 1.0`, então o
peso é **binário**. O que a cobertura de fato compra é o `gloss` (o realce segue o alpha do
desenho); dentro da silhueta o `body` não depende dela, e fora o early-out de tinta plana dispara
antes. Doc e tabela corrigidos.

**Gates:** 6 (o carimbo do rig por bits · os planos · **o passe ACEITA o que o bake entrega** — a
falha muda da wave · a re-acendida, com o fracasso que não carimba · **o assado veste o realce do
barro**, nas duas metades: intensidade e largura · o número do barro é UM no shader e no Rust).
**7 mutações, 7 sangram.**

**LOC:** dois arquivos cruzaram o teto e os dois cortaram por RESPONSABILIDADE — `sculpt3d_pull.rs`
(os quatro verbos que PUXAM, que obedecem a uma lei própria) e `sculpt3d_keys.rs` (*o que a mão
ESCOLHE com o teclado* × *o que ela faz com o PONTEIRO*).

**Smoke:** `PH2D_SCULPT3D_SMOKE=11` — a cena imprime o roteiro; os passos (4) e (5) são a wave.

### W8.7 — ★ OS CANAIS NO DOCUMENTO ✅ (2026-08-04, **smoke OK 2026-08-04**)

Um objeto assado sobrevive a **fechar o app**, e re-acende **sem o módulo 3D no build**. Três peças,
o padrão exato do `project_painter` — e a do meio é de novo a que faltava:

| peça | o quê |
|---|---|
| identidade estável | `ph2d_ecs::BakedForm(u32)`, carimbada pelo gesto de assar. Registro **46→47**, e os DOIS espelhos **47→48** (o contador é TRÊS) |
| o DOCUMENTO no arquivo | `base` (pixels antes da luz) · `form` (o G-buffer) · **o RIG**. `PROJECT_SCHEMA` **51→52**, campo de SPRITE |
| a textura re-materializada | slot vazio no load, e quem acende é o passe de re-acendida do frame — a **porta única** |

⚠️ **Os canais NÃO entraram no blob `sculpt`**, embora ele já guarde as malhas: o parser dele é
`#[cfg(feature = "sculpt3d")]`, e um objeto assado tem de ser legível **sem** o módulo. Guardá-los
ali teria feito a promessa da rota A virar prosa.

⚠️ **A forma viaja como IMAGEM RGBA8, e a escolha é MEDIDA** (`baked_form::probe`):

| lado | base f32 | base RGBA8 | forma RGBA8 | doc postcard |
|---|---|---|---|---|
| 512 | 4,00 | 1,00 | 1,00 | **2,00 MiB** |
| 1024 | 16,00 | 4,00 | 4,00 | **8,00 MiB** |
| 2048 | 64,00 | 16,00 | 16,00 | **32,00 MiB** |

**4× menos disco na forma** por **≤ 3 de 255** no pixel aceso (média 0,25). É também o que a
indústria inteira shipa — as *Secondary Textures* da Unity, o `normal_texture` da `CanvasTexture` do
Godot, o bake-to-texture do Blender, o Sprite DLight, o Spine —, e **nenhum deles guarda a malha**.
⚠️ A decodificação **RENORMALIZA**: `n × 0,5 + 0,5` quantizado não devolve um vetor unitário, e a luz
lê a normal como DIREÇÃO.

**O corte estrutural**, e é ele que torna a promessa verificável: `baked_form.rs` (**não gateado**) é
*o que um objeto assado É*; `sculpt3d_bake.rs` (gateado) é só *o GESTO*, que precisa da malha. O mapa
saiu da `Sculpt3dScene` para o `AppGfx` — um objeto assado sobrevive à peça, e tinha de sobreviver ao
módulo. E o `CLAY_SHINE`/`CLAY_EXPONENT` mudaram para a `ph2d-light` (a única peça não-removível),
com re-export na `ph2d-mesh-render`: **zero churn de chamada**, o precedente da W3.

⚠️ **`ph2d-light` ganhou `serde`** — o doc do `LightRig` sempre disse *"o rig inteiro, como o
documento o guarda"*, e uma lista de campos re-escrita no shell seria a segunda representação do
mesmo rig.

**5 arch-gates + 12 de unidade · 4 mutações, 4 sangram.** Smoke: **`PH2D_SCULPT3D_SMOKE=12`**.

⚠️ **ABERTO e nomeado por ser COMPARTILHADO:** o mapa é keyed por bits de entidade, e o undo global
respawna — depois de um Ctrl+Z a entrada fica órfã (a tela não muda; a lâmpada e o save deixam de a
alcançar). **O `PaintedDoc` tem a mesma forma** (o `restore_painted_docs` só é chamado pelo LOAD,
conferido por grep), então curar um e não o outro seria duas respostas para *como um documento
reencontra o seu objeto depois de um respawn*. É wave própria, do dono dos dois lados.

### W8.8 — ★ FUNDIR e ISOLAR ✅ (2026-08-04, cena `=13`, **SMOKE OK**, integrada)

`Shift+J` funde as peças **À VISTA** numa só; `Shift+I` isola a ativa e tira as outras da tela.

**A escolha que decide o resto:** a fusão age sobre o que se **VÊ**, não sobre uma seleção — porque
seleção múltipla não existe nesta cena, e inventá-la aqui seria um segundo modelo de *"em quem este
gesto pega"*. De quebra os dois verbos **compõem**: isole, olhe, funda o que sobrou.

**O motor mora na malha** (`ph2d_mesh::merge`), porque concatenar é operação de malha e porque ali ele
é gateável sem device — o assunto é aritmética de **ÍNDICE**, cujo modo de falha (uma face apontando
para o vértice de outra peça) **desenha** em vez de dar erro. Três decisões escritas no doc dele: a
saída é em **MUNDO** (espelho do que o `export` já decide) · ele **não solda** (costurar cascas é
trabalho do remesh) · e os planos opcionais só se materializam se **alguém** os tinha.

⚠️ **O isolamento é VISTA, nunca história** (`isolated: Option<ObjectId>`, o precedente do onion da
timeline): ele não move um vértice, então um passo de undo aqui gastaria um Ctrl+Z para devolver
pixels e nenhum trabalho. Ele guarda o **ID** e resolve o **índice** a cada pergunta ⇒ peça isolada que
morreu vira *sem isolamento*, e não uma tela preta.

⚠️ **O refazer da fusão NÃO carrega o resultado** — ele re-roda a mesma porta (`fuse_visible`), que é
função pura da lista, o precedente do `UnfilledHoles`. A metade que o faz funcionar é o **id**: o
refazer reusa o `ObjectId` que a entrada nomeia, senão a inversa fica órfã.

⚠️ **E a wave achou um BUG VIVO lendo o código, não por um teste:** a sincronização perguntava *"eu já
mandei esta malha?"* (`uploaded`, memória da **CENA**) para decidir o que o **DEVICE** tem — apagar a
peça 0 de três desloca as sobreviventes para os slots 0 e 1 com `uploaded == true`, e a guarda antiga
(`object_count() <= i`) não pega: os slots **existem**, só descrevem outra peça. A sobrevivente era
desenhada com a geometria da peça **morta**, na pose dela própria, **invisível numa cena de primitivas
iguais** — que é a cena que os smokes montam. A cura é a **testemunha** (`slots: Vec<ObjectId>`), e ela
mata a classe inteira de uma vez: o delete que desloca, o isolamento que compacta, a fusão que troca N
por uma, o undo que recoloca no fim. `plan_slots` é **função livre e pura** (o precedente do `place` do
import) porque uma `Sculpt3dScene` não nasce sem `wgpu::Device`.

**19 gates · 15 mutações, 15 sangram.** ⚠️ **Dois arch-gates ficaram vermelhos sobre produto CORRETO** e
os dois pelo mesmo motivo — o **proxy** envelheceu, não a afirmação: `braced_block(key, "if code ==
K::KeyJ")` passou a achar o `Shift+J` (as duas famílias de verbo dividem letras **de propósito**), e a
primeira cura tinha a **mesma doença um nível acima** (`"if shift"` acha o `if shift && … KeyB`).
*"O primeiro X"* é posição, e posição expira. O helper novo (`branch_containing`) faz a pergunta **do
corpo para fora**: dada a chamada que só existe uma vez, *que ramo a guarda?*

### W8.8 — o que ainda falta

- ⚠️ **Fundir NÃO solda** — a costura é do remesh (`V`), e a mensagem do log diz isso. Um *merge by
  distance* seria outra operação, com tolerância própria.
- A fusão **recusa** com a pilha de multires montada (a base mudaria sob níveis que são subdivisão
  dela). Achatar em silêncio destruiria trabalho autorado; a recusa é nomeada no log.

⚠️ **A dívida que a W8.1 nomeou está PAGA, e por outra via:** ela previa *apagar limpa a fila*; a W8.2
mostrou que a cura certa é a peça viajar DENTRO da entrada, e o índice clampado deu lugar ao `ObjectId`.

---

## W9 — DYNTOPO

### W9.1 — ★ O TRAÇO ADENSA ONDE O PINCEL TOCA ✅ (2026-08-04, cena `=14`, **SMOKE OK em 2 rodadas**, integrada)

`P` liga, `U` cicla o detalhe. Uma **FACE** cujo centroide cai na esfera do dab e cuja aresta mais
longa passa do alvo tem **todas as arestas longas partidas**; a marcação é então **fechada pela
aresta mais longa** (a *LEPP* de Rivara, que propaga para a vizinha) e o resultado é **relaxado por
troca de diagonal** (`dyntopo_flip.rs`). Toda face que toca uma aresta partida é re-triangulada.

⚠️ **A 1ª RODADA DO SMOKE REPROVOU O ASPECTO** (*"o aspecto da escultura depois o P fica
horrível"* — agulhas), e a lei acima é a 2ª. O que ela ensina vale para além desta wave:

**NENHUMA MÉTRICA DE POSIÇÃO VIA O DEFEITO.** Pelo desvio de vértice o produto media **0,7131** e a
correção que eu tinha feito primeiro — a herança do `pre`, que está certa — deixava **0,7158**.
Igual. Só medindo **ÂNGULO DE TRIÂNGULO** ele apareceu: **21,21° no controle contra 1,53°, com 15%
da malha abaixo de 10°**. *Uma lasca não desloca vértice nenhum* — ela dá uma normal por-vértice sem
direção, e é a LUZ que a desenha como espinho.

**Mecanismo:** partir UMA aresta de um triângulo o divide em dois com metade do ângulo daquele
canto (o corte *verde*). Com a esfera do dab a ANDAR, cada dab escolhia outra aresta da mesma face:
30 → 15 → 7 → 3,5 → 1,7. As três peças, **cada uma com o seu número na ablação**:

| | pior ângulo mínimo | abaixo de 10° | vértices no hemisfério NÃO tocado |
|---|---|---|---|
| controle (sem refino) | 21,21° | 0,0% | 57 |
| escolha por FACE, sozinha | 0,59° | 48,0% | 57 |
| + fecho de aresta mais longa | 2,43° | 1,5% | 57 |
| + **flip de aresta** | **16,85°** | **0,0%** | 57 |

⛔ **MEDIDO E REJEITADO, não refaça:** promover a vizinha a 1→4 também dá qualidade perfeita
(20,47°) e **cascateia pela malha inteira** — 57 → **846** vértices onde o artista nunca passou, o
oposto exato da promessa deste modo. *Qualidade é global; a pegada não pode ser.* O flip fecha a
tensão porque é **local e não propaga**.

⚠️ **O refino alcança um pouco ALÉM do pincel** — é o preço da propagação, e o gate diz o número
medido em vez de uma barra escolhida: **1,66× · 1,31× · 1,38× · 0,93×** o raio em quatro densidades
de esfera; ele **encolhe** quando a malha já é fina.

⚠️ **A MEDIÇÃO MUDOU O DESENHO ANTES DA PRIMEIRA LINHA, e é o §0 a funcionar.** Este plano dizia que
a wave *"não pode herdar"* dois pré-requisitos — anéis mutáveis e octree de folhas mutáveis. Isso é
uma afirmação sobre um NÚMERO, e a sonda (`ph2d-mesh/tests/measure_dyntopo.rs`) a derrubou:

| vértices | anéis | octree | normais | **rebuild** | cabe no dab (8 ms)? |
|---|---|---|---|---|---|
| 6 050 | 0,19 | 0,09 | 0,31 | **0,59 ms** | sim |
| 24 386 | 0,84 | 0,45 | 0,27 | **1,55 ms** | sim |
| 97 922 | 3,37 | 1,99 | 0,15 | **5,50 ms** | sim, apertado |

⇒ o refino nasce sobre o `rebuild` que já existe, e a estrutura mutável é a **W9.2**, com um gatilho
que é um número medido. O mesmo dab toca **0,33% das faces** a 98k (razão 307×): é esse o tamanho do
ganho que ela vai comprar, e é ele que a justifica quando o artista cruzar ~100k — que é exatamente o
que a topologia dinâmica existe para fazer.

**O alvo de aresta é uma FRAÇÃO DO PINCEL**, pela fórmula da referência (`SculptBase.js`,
`d2Max = r²(1.1−d)·0.2`): pincel pequeno detalha fino. `edge_target` é a porta única — quem faz a
conta duas vezes tem um log que discorda da geometria.

⚠️ **Ligar TRIANGULA a peça** (`Mesh::triangulate`, no ARM e não no primeiro dab): partir a aresta de
um quad devolve um triângulo e um pentágono, e é por isso que o `MeshDynamic` do SculptGL é
*"triangles only"* na primeira linha. ⚠️ **O desfazer do traço passa a ser a malha inteira**, e a
pergunta é sobre a CONTAGEM e não sobre o modo. ⚠️ **A pilha de multires RECUSA**, e a recusa é
nomeada no log.

⚠️ **A peça que faz a wave ser correta é o `SculptStroke::grow_with`.** Chamar `begin` depois de
refinar é o caminho óbvio e é a doença do produto-por-dab que esta casa curou **quatro vezes** no
relevo do Painter — o traço fica mais forte quanto mais o refino disparar, **sem quebrar nada
visivelmente**. Ele só é correto porque o refino **APENDA**.

⚠️ **E crescer não basta: o vértice novo HERDA o `pre` dos PAIS** (`Birth`, parâmetro obrigatório do
`refine_in_sphere`). Ele nasce no ponto médio de dois pais que este mesmo traço já mexeu, então
capturar essa posição como `pre` conta o deslocamento **duas vezes**. O doc da primeira versão
afirmava que a posição de nascimento era *"a única resposta que existe para onde ele estava
antes"* — **há outra, e é a certa**. São **quatro gestos** que fazem um vértice nascer no meio de um
traço, e cada um vê um erro que os outros não veem: **puxar** (a agulha, 0,053 × 0,720) · **varrer**
(a cratera do `accum` herdado em zero, 0,108 × 0,446) · **afinar o detalhe com `U`** · **sob
máscara** (0,108 × **0,867**).

**17 gates · 18 mutações, 18 sangram** (1 documentada e não-sangrante: a varredura de todas as faces,
que é camada). ⚠️ **E três delas acusaram AFIRMAÇÕES minhas em vez de buracos** — a varredura não é o
que fecha a rachadura (é o PADRÃO, total sobre qualquer subconjunto) · o `sort` das arestas não é o
que torna o refino determinístico (é o `pending` + a travessia do octree) · e um gate media
`capacity_bytes` para julgar um encolhimento que `Vec::resize` não devolve.

### W9.2a — ★ O REFINO PERGUNTA SÓ PELA REGIÃO ✅ (2026-08-05, **pendente de smoke**)

As duas metades do refino que perguntavam à malha INTEIRA o que só a região podia responder. Nenhum
vértice novo, nenhuma lei nova: a mesma resposta, perguntada onde ela mora.

**(A) A NEGATIVA NÃO CONSTRÓI O GRAFO DE ARESTAS.** Depois que a região alcança a densidade pedida,
todo evento de ponteiro chega ao refino para ouvir *não há o que partir* — e essa resposta era paga
com um `Edges` inteiro. ⚠️ **Medido, ele era 86% do piso** (1,84 de 2,14 ms a 113k). Agora a escolha
por face roda ANTES do grafo, sobre posições e faces (um comprimento sai de duas posições, um
centroide de três); o NÚMERO da aresta só é preciso para MARCAR, e marcar só acontece se houver o
que marcar. A ordem de `marked` é preservada exatamente — ela decide os índices dos vértices novos.

**(B) O FLIP RELAXA A REGIÃO, NÃO A MALHA.** Ele varria **toda aresta** por rodada: 33,2 de 66,1 ms
num dab a 98k, com a 2ª rodada gastando 17,3 ms para achar **ZERO**. ⚠️ **E a hipótese óbvia foi
REFUTADA pela medição:** o desperdício era de VARREDURA, não de mudança — por faixa de distância ao
dab, **todas** as faces alteradas caem dentro da esfera do pincel e **nenhuma** fora (3648 dentro, 0
fora, a 28k). O corte passou a reportar as faces que mexeu; a rodada seguinte recebe as que a
anterior flipou. ⚠️ **Ele também não constrói mais o `Edges`:** as duas perguntas que ele fazia
àquele grafo — *quem é a outra face desta aresta* e *a diagonal nova já existe?* — são as duas
respondidas pela `Adjacency`, que a malha já carrega (`id_of` era, literalmente, uma varredura do
anel de `c` atrás de `d`).

| medida (98k, salvo indicado) | antes | depois | |
|---|---|---|---|
| **piso do regime** (113k) | 2,136 ms | **0,301 ms** | **7,1×** |
| piso do regime (98k) | 2,193 ms | **0,298 ms** | 7,4× |
| **1º dab da região** | 68,2 ms | **34,7 ms** | **2,0×** |
| 1º dab (24k) | 17,05 ms | **9,16 ms** | 1,86× |

⚠️ **A ORDEM de visita do flip muda, e com ela o conjunto de flips** — `spent` deixa uma face entrar
numa troca só por rodada, então percorrer por *(face,canto)* não escolhe exatamente as mesmas trocas
que percorrer por id de aresta. O que é gateado não é a rota e sim a **propriedade**: o pior ângulo
mínimo depois de um traço (`a_moving_dab_does_not_shred_the_triangles`, barra 10°, medido 21,6°). Um
gate de identidade aqui pinaria uma heurística; este pina o que ela existe para entregar.

⚠️ **Mudança de comportamento nomeada:** numa malha IMPORTADA cujo lado oposto não é flip-estável, o
primeiro dab deixa de relaxá-lo. É o comportamento certo — um dab no nariz não re-triangula a nuca —,
e é a mesma lei que a W9.1 escreveu ao rejeitar o fecho vermelho (*a pegada não pode ser global*).

**Gates:** `the_refusal_does_not_build_the_edge_graph` (oráculo de **ALOCAÇÃO** via dhat, sem relógio
logo sem flake; a barra é uma RAZÃO contra o grafo medido na mesma corrida — 147,8 KB contra 1,345 MB
= **9,1×**, barra em 4×) · `the_flip_asks_only_about_the_faces_the_cut_touched` (fixture que
CONSTRÓI o fenômeno: um par de faces virado para a pior diagonal longe do pincel, mais o **controle
positivo** de que o operador quer repará-lo). **4 mutações, 4 sangram** — reconstruir o grafo antes da
negativa (1,49 MB contra 1,35) · o flip global (a 1ª metade do gate de escopo) · o corte não reportar
a região (qualidade 21,6° → **2,33°**) · a região não atravessar os passes (a mesma).

### W9.2b — ★ OS ANÉIS ACEITAM UMA EDIÇÃO ✅ (2026-08-05, **pendente de smoke**)

O primitivo que a wave inteira precisava, e o primeiro consumidor dele.

**O CSR deixou de ser uma soma de prefixos.** No canônico, o começo da linha `i + 1` é o fim da
linha `i` — então crescer uma linha desloca todas as seguintes, `O(malha)` para acrescentar UM
vizinho. Agora cada linha carrega o próprio começo e o próprio comprimento, e **uma linha que cresce
se muda para o fim** (a cópia é de meia dúzia de índices, porque uma valência é meia dúzia); o
rastro é contado e recolhido quando passa de metade. ⚠️ **Custo de memória: +4 B por vértice por
CSR** — `offsets` de `n+1` virou `starts` + `lens` de `n`.

⚠️ **As linhas ficam em ORDEM CRESCENTE, e isso não é estética.** Uma linha editada tem de ser
*exatamente* a que uma reconstrução produziria: a soma das normais de vértice percorre esta lista, e
somar os mesmos números noutra ordem dá outro último bit. Um incremental que "só" reordena o anel é
um incremental que diverge do `rebuild` no dígito que ninguém confere.

**O primeiro consumidor é o FLIP** (`Mesh::relink_faces`): uma troca de diagonal não cria, não apaga
e não move vértice nenhum — ela reescreve duas faces. O caminho antigo era `Mesh::from_parts` +
`rebuild`: validar toda face, refazer os dois anéis, o octree e todas as normais.

| medida (98k) | antes | depois | |
|---|---|---|---|
| **a troca do flip** | 10,4 ms | **3,27 ms** | **3,2×** |
| **1º dab da região** | 34,7 ms | **29,0 ms** | 1,20× |
| 1º dab (24k) | 9,16 ms | **7,27 ms** | ✅ **cabe nos 8 ms** |

⚠️ **A troca é 3,2× mais barata e o dab só 1,20×, e a razão é honesta:** o incremental **não é de
graça** — na primeira passada por uma região o flip troca **12 288** diagonais, o que toca 24 576
faces e ~18 800 vértices (adjacência 2,33 ms · passe de região 0,94 ms). O trabalho é proporcional
ao que MUDOU, que ali é um quinto da malha. Em regime, onde o traço vive, ele é ruído.

⚠️ **A ORDEM dos três passos do `relink_faces` é o contrato**, e trocá-la não falha: dá a resposta
errada em silêncio. O anel de faces é remendado contra as faces ANTIGAS; as faces são escritas; e só
então o anel de vértices é re-derivado contra as NOVAS. ⚠️ **E o anel de vértices é RE-DERIVADO, não
remendado** — quem está no anel de `v` é função de TODAS as faces de `v`, e um remendo por aresta
teria de saber que `a—b` só morre se NENHUMA outra face a compartilha.

**Gates:** `an_edited_ring_is_exactly_what_a_rebuild_would_have_produced` (o oráculo é o
`Adjacency::build`, anel por anel, os dois CSRs) · `the_freshly_built_csr_is_packed` (o invariante do
BUILD, que deixa de valer depois de uma edição) · `rows_are_disjoint` (o que sobrevive à edição, e é
ele que protege a leitura). **4 mutações, 4 sangram** — não re-derivar o anel de vértices · apendar
em vez de inserir ordenado · não tirar a face do anel que ela largou · violar a ordem dos três passos
(esta derruba **dois** gates, inclusive o da ausência de rachadura).

**Higiene:** `rebuild_with_faces` ficou sem chamador e foi **removida** — um `pub` órfão não é código
morto silencioso, é uma segunda resposta esperando alguém.

### W9.2c — ★ O CORTE ESCREVE NO LUGAR ✅ (2026-08-05, **pendente de smoke**)

O corte deixou de trocar a malha. Uma face que muda **fica no próprio slot**, as filhas são
apendadas, os vértices novos entram no fim — e a malha absorve tudo isso por uma porta
(`Mesh::splice_topology`) em vez de nascer de novo.

⚠️ **A TABELA QUE DECIDIU A WAVE não é a que este plano trazia.** A sonda irmã media `radius = 0.25`
numa esfera unitária, ou seja **um pincel que cobre um quinto do modelo** — e a nota da W1 já
chamava esse regime de extremo (*"o K1 só dispara com um pincel que cobre 19% da malha inteira"*).
Uma estrutura incremental ganha quando a região é 1% e perde quando é 20%, então o número que decide
é o custo por TAMANHO DE PINCEL (`measure_what_a_dab_costs_by_brush_size`, a 98k):

| raio | faces tocadas | 1º dab ANTES | `edges` | `ends` | `emit` | `SWAP` |
|---|---|---|---|---|---|---|
| 0,05 | **4,0%** | **16,61 ms** | 2,10 | 0,73 | 0,98 | **10,88** |
| 0,10 | 8,3% | 21,25 | 1,79 | 0,97 | 1,02 | 11,91 |
| 0,15 | 12,4% | 23,42 | 1,62 | 1,20 | 1,02 | 11,38 |
| 0,25 | 20,8% | 30,17 | 1,69 | 1,22 | 1,08 | 12,18 |

⚠️ **O dab custava 16,6 ms tocando 4% da malha, e a região encolher 5× cortava só 1,8× do custo** —
a assinatura de `O(MALHA)`, não de pegada. As quatro colunas da direita são varreduras da malha
inteira e **nenhuma responde ao pincel**: juntas, **14,7 de 16,6 ms = 89% do dab**. A leitura certa
não era *"o incremental ganha pouco porque a região é grande"*; era *"a região é 4% e o dab paga a
malha inteira quatro vezes"*.

**O que a wave fez** (o `SWAP`, 65% do dab):

1. **O emit escreve no lugar.** A 1ª filha vai para o slot da mãe, as demais são apendadas, e a face
   que não muda **não é sequer reescrita**. Corolário que simplificou o resto: a lista de faces
   tocadas passou a **ACUMULAR** entre passes em vez de ser reexpressa — a renumeração era a única
   razão de ela precisar disso.
2. **`Csr::grow_rows`** dá linhas vazias aos vértices novos, e o `insert_sorted` ganhou o caminho da
   **linha que já está no fim** (uma face nova tem índice maior que todas ⇒ ela entra no fim da
   linha; sem isso, dar seis faces novas a um vértice custaria seis realocações).
3. **`Adjacency::splice`** generaliza o `relink`: faces que trocaram de cantos, faces que nasceram,
   vértices que nasceram. O `relink` passou a delegar — uma porta, não duas.
4. **`Octree::insert_alongside`**: a filha nasce na folha da MÃE (ela vive dentro da extensão dela),
   a faixa da folha **se muda para o fim** de `face_indices` (o idioma do CSR editável), e as
   inserções são **agrupadas por folha** — um dab parte várias faces da mesma, e realocar uma vez
   por face seria `O(len²)` exatamente onde o trabalho se concentra.
5. **A folha que passa do teto SE DIVIDE**, e é para isso que o `Node` passou a guardar a **caixa de
   partição**. A frouxa não serve: ela encolhe até a geometria e sobrepõe as irmãs, então cortá-la
   ao meio não particiona espaço.

**Medido pela porta do produto, com a rota antiga (`rebuild` da malha de saída) cronometrada
COSTAS-COM-COSTAS na mesma corrida** (máquina em `load 1,92`):

| raio | tocado | **splice** | **rebuild** (a rota que ele substituiu) | ganho | 1º dab |
|---|---|---|---|---|---|
| 0,05 | 4,0% | **1,79 ms** | 8,49 | **4,75×** | **5,89 ms — cabe nos 8 ms do K1** |
| 0,10 | 8,3% | 3,86 | 9,12 | 2,36× | 10,61 |
| 0,15 | 12,4% | 5,03 | 9,16 | 1,82× | 15,20 |
| 0,25 | 20,8% | 7,67 | 10,46 | 1,36× | 24,30 |

⚠️ **O A/B é dentro da corrida de propósito.** A primeira medição desta wave comparou contra números
tirados sob `load 25` e teria atribuído ao código o que era a carga — o preço que a `line/Painter`
já pagou (`28_otimizacoes §5.46`). E o `rebuild` é a comparação HONESTA porque o `splice_topology`
substituiu exatamente ele.

⚠️ **O CRUZAMENTO ESTÁ NOMEADO:** a 20,8% o incremental ganha só 1,36×, e a curva diz que perto de
~30% ele empata. Essa é a lei geral do incremental (ele troca trabalho linear por trabalho
proporcional à região, com um fator de constante pior), e agora ela está medida em vez de suposta.

⚠️ **E o `SWAP` passou a RESPONDER ao pincel** (1,79 → 3,86 → 5,03 → 7,67), onde antes era plano
(10,88 → 11,91 → 11,38 → 12,18). Essa mudança de FORMA é a prova que nenhuma carga de máquina
falsifica: antes, quadruplicar a região não movia o número.

**A árvore não degrada, e o número prova.** Doze dabs cruzando a esfera
(`measure_whether_the_octree_degrades_along_a_stroke`): faces 48 768 → 61 526 (+26%), nós 2 633 →
3 313 (+26% — a árvore cresce junto), **folha mais cheia 97-100** (o teto é 100), consulta 0,5-2,0 µs.
Com a divisão local desarmada a folha mais cheia vai a **756** em oito dabs.

**Gates** (todos em `dyntopo_splice_tests.rs`, filho do `dyntopo_tests.rs` para as fixtures
continuarem sendo uma porta):

- `a_spliced_mesh_is_exactly_what_a_rebuild_would_have_produced` — o oráculo central, e ele compara
  **o último bit das normais**: a normal de um vértice é a soma sobre o anel dele, e somar os mesmos
  números noutra ordem dá outro `f32`. Um incremental que "só" reordena um anel passa em todo gate
  de conteúdo e diverge aqui.
- `the_spliced_octree_still_finds_every_face_it_should` — ⚠️ o oráculo é a **RESPOSTA**, não a
  árvore: duas partições diferentes são as duas corretas (a caixa frouxa decide a resposta, a
  partição decide só a forma da árvore), então comparar nós contra um build pinaria uma heurística.
- `the_leaf_ranges_stay_disjoint_after_absorbing_new_faces` — o invariante que SOBREVIVE à edição
  (irmão exato do `rows_are_disjoint` do CSR): não é *"as faixas ladrilham o vetor"*, que a
  realocação quebra de propósito, é *"nenhuma pisa na outra"* — mais a soma dos comprimentos, porque
  faixas disjuntas com um buraco entre elas também são disjuntas.
- `a_leaf_that_fattens_splits_instead_of_growing_forever`.

**6 mutações, 6 sangram:** a folha gorda não se divide (73 → **756**) · o octree não absorve as faces
novas (6 gates) · os anéis não aprendem as faces novas · o caminho rápido do `insert_sorted` esquece
de conferir que a linha está no FIM · a mãe não recebe a 1ª filha (as rachaduras voltam) · a faixa da
folha cresce no lugar (`(49,56)` e `(49,98)` se sobrepõem).

**Higiene:** `swap_topology` ficou sem chamador e **saiu** — um `pub(crate)` sem chamador é uma
segunda resposta esperando alguém chamá-la. E o `RegionScratch::reset` passou de `vec![]` a
`resize`: a malha muda de tamanho a cada dab, e re-alocar dois vetores do tamanho dela por dab é
`O(malha)` entrando pela porta dos fundos justamente na wave que existe para tirá-lo.

**LOC:** dois cortes por responsabilidade — `mesh_splice.rs` (*o que uma malha É* × *como a
topologia dela é EDITADA*) e `dyntopo_splice_tests.rs`. ⚠️ Os dois são **FILHOS** e não irmãos: as
portas escrevem campos privados da malha (privacidade em Rust alcança descendentes), e um módulo
irmão obrigaria a abri-los ao crate inteiro. Contagem de gates conferida antes e depois: **213**.

⚠️ **O QUE SOBRA, medido a `radius = 0.05` (5,89 ms):**

| fase | ms | responde ao pincel? |
|---|---|---|
| **`Edges::build`** | **1,86** | ❌ `O(malha)` |
| **`swap` (já incremental)** | **1,79** | ✅ |
| flip (varredura + relink) | ~1,7 | ✅ |
| `emit` (a varredura que decide quem muda) | 0,25 | ❌ mas barata |
| `ends` | 0,28 | ❌ mas barata |
| `lepp` | 0,20 | ❌ mas barata |

⇒ **a próxima alavanca é o `Edges::build`** (1,86 ms, 32% do dab): o corte constrói o grafo de
arestas da MALHA INTEIRA para marcar as poucas que vai partir. As quatro varreduras que sobram
(`edges` · `ends` · `lepp` · `emit`) são a mesma pergunta — *quais faces tocam uma aresta marcada?* —
e uma lista de candidatas do octree já a responde para a região; o que prende as três últimas ao
`O(malha)` é o fecho de aresta-mais-longa poder marcar fora da esfera, o que se resolve com uma
FRENTE de trabalho em vez de uma varredura por iteração.

### W9.2d — ★ O CORTE NÃO CONSTRÓI MAIS O GRAFO DA MALHA ✅ (2026-08-05, **pendente de smoke**)

A W9.2c deixou o `Edges::build` como o maior item que ainda não respondia ao pincel (**1,86 de
5,89 ms, 32% do dab**): o corte construía o grafo de arestas da malha INTEIRA para marcar as poucas
que ia partir. Junto com ele iam mais três varreduras (`ends` · `lepp` · `emit`) que são a mesma
pergunta — *quais faces tocam uma aresta marcada?*

⚠️ **A medição que decidiu o desenho separou as duas metades do grafo** e elas têm preços muito
diferentes (`measure_what_the_edge_graph_is_made_of`):

| vértices | prefixo `O(V)` | mapa `(face,canto) → id` `O(F)` | total |
|---|---|---|---|
| 6 050 | 0,011 | 0,087 | 0,098 |
| 24 386 | 0,044 | 0,363 | 0,406 |
| 97 922 | **0,184** | **1,498** | 1,682 |

⇒ **89% do custo é o mapa por-face**, e é justamente ele que o corte só precisa para a REGIÃO. A
numeração — o prefixo que torna o id denso — custa 11% e continua valendo a pena, porque é ela que
mantém `pending`/`split_at` como ARRAYS e a ordem de nascimento como função da varredura (um mapa
por par exigiria hash, e a ordem de iteração de um `HashMap` decidiria os índices da malha).

**O que a wave fez:**

1. **`EdgeIds`** — a numeração sozinha, extraída do `Edges`, que passou a CONTÊ-la e a delegar. Uma
   pergunta (*qual é o número desta aresta?*), uma resposta: nada que guarde um id entre as duas
   rotas pode divergir.
2. **O id sai sob demanda** (`id_of`, `O(valência)`) em vez de por tabela.
3. **O fecho de Rivara virou uma FRENTE.** A versão anterior repetia `for TODA face da malha` até o
   ponto fixo; agora uma marca empurra as ≤ 2 faces que dividem aquela aresta (`faces_of_edge`, que
   sai da adjacência que a malha já carrega) e só elas são reexaminadas.
4. **`ends` desapareceu**: a marca carrega os EXTREMOS junto, então não há o que redescobrir. Ela era
   uma varredura de toda a malha.
5. **O emit itera a FRENTE**, que é exatamente o conjunto de faces com um canto partido.

⚠️ **E a frente honra MELHOR o argumento que mantinha a varredura viva.** O comentário antigo dizia
que a varredura tornava a ausência de rachadura *independente da regra de marcação* — verdade, e a
frente faz o mesmo sem `O(malha)`: quem a preenche é o `faces_of_edge` a partir de *toda aresta
marcada*, seja qual for o critério que a marcou.

⚠️ **A fila é limitada por construção:** cada aresta é marcada uma vez e empurra ≤ 2 faces, então a
frente recebe no máximo `2 × |marcadas|` entradas. Re-entrar é inofensivo (a mais longa daquela face
já está marcada), e é por isso que **não há carimbo de já-visitada** — ele seria um vetor do tamanho
da malha dentro do passe que existe para não ter um.

**O gate central é o ponto FIXO** (`the_front_closes_the_same_set_the_sweep_did`): o oráculo é a
varredura **CONGELADA sob `cfg(test)`** — o código que shipava, verbatim —, e a fixture semeia as
mesmas marcas iniciais nas duas rotas. ⚠️ **Uma frente que esquecesse de empurrar uma vizinha
falharia em SILÊNCIO:** o padrão de corte fecha a rachadura sobre qualquer subconjunto de arestas
partidas, então a malha continuaria fechada e a única evidência seria um triângulo mais fino do que
precisava. O gate tem duas metades — o conjunto fechado é o mesmo, **e** a frente contém exatamente
as faces que tocam uma marca (é ela que o emit itera).

**3 mutações, 3 sangram:** a frente não empurra as faces de uma marca do fecho (54 contra 55 arestas,
mais rachaduras e ângulos) · a marca da ESCOLHA não entra na frente (o emit não vê a face) · o id da
aresta esquece o RANK (6 gates, incluindo os três do `edges_tests`).

**Medido pela porta do produto a 98k, máquina calma (load 5,3):**

| raio | tocado | 1º dab **antes** (W9.2c) | 1º dab **depois** | `rebuild` (o controle) | `dab/rebuild` |
|---|---|---|---|---|---|
| **0,05** | **4,0%** | 5,890 ms | **4,074 ms** | 8,83 | 0,694 → **0,461** |
| 0,10 | 8,3% | 10,609 | 9,144 | 8,72 | 1,163 → 1,048 |
| 0,15 | 12,4% | 15,199 | 14,573 | 9,44 | 1,660 → 1,543 |
| 0,25 | 20,8% | 24,297 | 22,521 | 9,67 | 2,323 → 2,329 |

⚠️ **O número é uma FAIXA e não um ponto**, porque três corridas independentes em máquina calma
deram **3,871 · 4,074 · 4,272 ms** (razão 0,432 · 0,461 · 0,448) — a linha da tabela é a do meio, e
a dispersão de ±5% é o que uma medição desta máquina vale mesmo quando ela está quieta.

⚠️ **O ganho é MAIOR no pincel pequeno, e isso é a assinatura do que a wave removeu:** um custo
CONSTANTE (o grafo da malha inteira) pesa tanto mais quanto MENOR a região. A r = 0,05 — o regime
real, o pincel de detalhe — o dab cai **1,45×** e fica em **metade do K1**; a r = 0,25 o custo é
dominado pelo flip e pelo splice, que escalam com a região, e a wave quase não aparece.

⚠️ **O `Edges` NÃO ficou órfão:** `holes` · `reversion` · `multires_reverse` · `subdivide` seguem
construindo o grafo completo, e para eles ele é a resposta certa (rodam quando o artista aperta um
botão, e precisam do mapa por-face sobre a malha toda). O que mudou é que o refino — que roda por
MOVIMENTO DO MOUSE — deixou de pagar por ele.

⚠️ **E a SONDA ganhou uma coluna, porque a máquina é compartilhada.** Ao longo desta wave o mesmo
binário mediu `rebuild` em **8,5 ms** (load 1,9) e **31,0 ms** (load 62) — um número absoluto de um
momento não se compara com o de outro. A coluna `dab/rebuild` é a razão contra um trecho que a wave
não toca, e é ela que se lê entre corridas. ⚠️ **Com o load acima de ~10 nem a razão sobrevive**
(as quatro linhas saíram 0,59 · 0,52 · 1,79 · 1,79, incoerentes), porque as duas medições veem
estados diferentes da máquina dentro da mesma corrida — o corolário operacional que a `line/Painter`
já registrou.

### W9.3 — ⏳ O COLAPSO: A TOPOLOGIA APRENDE A ENCOLHER (**pendente de smoke**)

> ⚠️ **Sem rodada registrada.** Perguntado ao Enio em 2026-08-06: não há memória de um smoke
> próprio desta wave. Fica **pendente** — a direção segura, porque o custo de re-smokar uma wave
> que já estava boa é um minuto, e o de shipar uma sem verificação é um bug que ninguém procurou.

A outra metade do dyntopo. ⚠️ **A justificativa que este plano trazia estava meia-errada:** *"mantém
a contagem estável num traço longo"* supõe que o refino cresce sem parar, e ele não cresce — quando
toda aresta da região está sob o alvo ele PARA. O que faltava é o **caminho de volta**, e ele aparece
em três gestos reais: **comprimir** a superfície (um Grab aproxima vértices, e triângulos do tamanho
certo viram lascas), **alisar** (o detalhe que justificava a densidade some, a densidade fica) e
**engrossar o pincel** (o alvo é uma fração do raio, então o mesmo lugar passa a pedir menos).

**O PRIMITIVO NOVO é uma RENUMERAÇÃO EXPLÍCITA** (`ph2d_mesh::Remap`). O refino só APENDA, e é por
isso que ele nunca precisou de canal nenhum: um índice guardado antes continua apontando para a mesma
coisa. Um colapso **apaga**, e apagar de um `Vec` sem deixar buraco é trocar com o último.
⚠️ **A alternativa foi recusada com mecanismo:** um slot morto envenena o `is_border` (o par *nº de
faces × nº de vizinhos* deixa de bater), faz `vert_count`/`face_count` **mentirem** para o artista, e
obriga todo laço `for v in 0..n` do crate a aprender que alguns slots não são nada.
⚠️ E o remap é uma **SEQUÊNCIA, não uma tabela**: com os mortos `[3, 8]` numa lista de 10 a face 9 vai
para 8 e depois de 8 para 3.

**DUAS FASES SEQUENCIAIS** na porta que encolhe (`Mesh::shrink_topology`), e a separação é o desenho:
um colapso renumera faces **e** vértices, e as duas renumerações entrelaçadas são a forma de errar —
o modo de falha é uma face citando um índice **VÁLIDO** que é de outro vértice, e nada explode.

**QUATRO RECUSAS, todas topologia:** a aresta sem exatamente duas faces · a beira · a condição de elo
· e a **valência-3 do oposto** (sem ela um tetraedro passa pelo elo e colapsa em duas faces com os
mesmos três cantos).

**A HISTERESE FOI MEDIDA, e o gate que a afirmava reprovou por afirmar DEMAIS.** A primeira versão
exigia que o colapso removesse ZERO logo depois de um refino: ele não remove, porque o corte 1→2 cria
uma **mediana** e a mediana de um triângulo fino é curta. A pergunta certa é se o par tem **PONTO
FIXO**, e a varredura diz que o joelho está entre 1,8 e 2,0:

| razão | contagem de vértices em 12 ciclos de refino+colapso no mesmo ponto | assenta? |
|---|---|---|
| 1,80 | 710 712 700 698 696 700 698 699 695 … | **NÃO** |
| 2,00 | 716 721 714 714 714 714 … | sim |
| **2,05** | 716 720 716 716 716 716 … | sim |
| 2,50 | 721 722 722 722 … | sim |

O `2,05` da referência é **margem, não arredondamento**.

⚠️ **E o oráculo de bit achou uma afirmação MINHA que era falsa:** eu escrevi num doc que renumerar
uma face não afeta ninguém *"porque o anel guarda VÉRTICES"*. O CONJUNTO de vizinhos não muda — a
**ORDEM** muda, porque o anel é derivado percorrendo as faces em ordem crescente e a primeira
aparição decide o lugar; e a normal de um vértice é a **SOMA** das normais do anel, então outra ordem
é outro `f32`.

**O TRAÇO EM VOO** aplica a renumeração por `SculptStroke::shrink_with`, e ele tem **dois eixos** que
a compactação move: um vetor por VÉRTICE (`slot`/`stamp`) e um por SLOT (`touched` + os cinco planos
do `pre`), ligados nos dois sentidos. Compactar um só deixa o `slot` a apontar para o `pre` de outro
vértice. ⚠️ Sem ele o produto **PANICA** (`slot.len() == mesh.vert_count()`), e é a forma boa de
falhar.

**O gesto, medido pela porta do produto** (10 dabs por passada, esfera grossa de 128 vértices):
**128 → 621 no FINO → 255 no GROSSO → 698 de volta ao fino**. O caminho de volta existe.

**O CUSTO, e as duas leituras erradas que eu quase publiquei.** Com UMA amostra o dab a 101k media
2,0 ms e eu o chamei de `O(malha)`; a ablação que abri para atribuir isso **não atribuiu nada**,
porque a dispersão entre corridas do MESMO código (1,78 a 2,12 ms) era maior que o que ela media. Com
a **mediana de nove** o número reproduz a três decimais entre corridas independentes, e há **dois
regimes** — o primeiro dab tem muito a retirar, os seguintes não:

| vértices | raio | 1º colapso | colapso p50 | dab p50 (col+refino) |
|---|---|---|---|---|
| 48 332 | 0,25 | 0,889 ms | 0,296 | **0,328** |
| 101 012 | 0,10 | 1,113 | 0,197 | **0,246** |
| 101 012 | 0,25 | 2,025 | 0,600 | **0,649** |

Contra o **K1 de 8 ms**. ⚠️ *Uma amostra só é o redutor certo quando toda amostra faz o mesmo
trabalho, e aqui a primeira é estruturalmente diferente.*

**Um dab remove metade das arestas curtas da região e quatro dabs a limpam** (60 → 30 → 8 → 0,
contagem parando em 227 vértices) — a trava que torna cada rodada um lote independente troca a
cascata da referência por repetição, e o artista dá sessenta dabs por segundo.

**21 gates, 16 mutações, 16 sangram.** ⚠️ Quatro sobreviveram à primeira rodada e as quatro eram
buraco de **fixture ou de oráculo**, não de produto: o `open_tube3` não tem miolo (todo vértice é
beira ou valência 3, e o colapso recusava por OUTRAS razões) · nenhuma fixture violava a condição de
elo (num toro 4×4 ela nunca dispara; num 3×6 o túnel é estreito o bastante para o anel encostar em si
mesmo — 18 arestas, valência 6) · o oráculo da ligação de mão dupla perguntava pelo lado do SLOT, o
que é **circular** · e os vértices que a compactação MOVE são os do fim do vetor, que numa esfera UV
ficam no polo OPOSTO ao dab.

**Smoke:** `PH2D_SCULPT3D_SMOKE=14`, passos (6) e (7) — o caminho de volta e a prova de que o par não
mói.

**Aberto:** o colapso não roda o `dyntopo_flip` (a valência sobe quando dois anéis se fundem, e o
pouso tangencial já melhora a forma — o número da degradação **não foi medido**, e é o gatilho) · o
`locked` e o `mark` são vetores de bytes do tamanho da malha por passe, a mesma ordem que o `pending`
do grafo de arestas já paga · a máscara do artista **não resiste ao colapso** (a referência escala o
limiar por ela; o nosso refino também não a lê, e mudar um sem o outro é a coroa).

## W10 — O SHADER

**W10** — PBR · SSS · AO · Cavity · IBL, mais **alphas/stencils**, **mask extract** e o **transform afim
ponderado pela máscara** (a metade do `Transform.js` que não é o gizmo — o gizmo é do shell, ADR-0110/0111).

### W10.1 — ✅ A CAVIDADE: O CANAL QUE FAZ A ESCULTURA SER LIDA (**smoke OK**)

> Aprovada pelo Enio — *"Smoke OK. Siga"*, registrado no handoff de continuação de 2026-08-06.
> ⚠️ O carimbo estava faltando aqui: *uma aprovação que não está no registro não aconteceu para o
> próximo agente.*

O [[05.1-Shader-de-runtime]] §4 nomeia a cavidade como *"o canal que mais melhora a leitura de uma
escultura por unidade de custo"*, e o §2a diz que o SSS pré-integrado consulta **a mesma curvatura**
(*"um dado, dois usos"*). Ela veio primeiro por isso: é o item mais barato da wave e é a fundação do
mais caro.

**A grandeza, e a divisão que decide tudo.** `k = dot(centroide(anel) − p, n) / raio_médio(anel)` — o
laplaciano uniforme projetado na normal, dividido pelo raio médio do anel. ⚠️ **Sem o divisor a wave
inteira estaria errada de um jeito difícil de nomear:** curvatura média tem unidade `1/comprimento`,
então a MESMA forma esculpida no dobro do tamanho devolveria metade do número, e a cavidade clarearia
quando o artista escalasse a peça — que ele leria como *"o sombreamento mudou sozinho"*. Com o
divisor o número é **metade do ângulo de virada através de uma aresta**, que é exatamente o que um
realce de forma quer: uma ruga de uma aresta de largura lê igual numa peça grande e numa pequena.

**Por que o laplaciano UNIFORME basta, apesar da má fama.** Ele confunde curvatura com *distribuição
de vértices*, e é por isso que suavizar com ele encolhe a forma. ⚠️ **A objeção não alcança este uso,
e o motivo é verificável:** a parte dependente da distribuição é **TANGENCIAL**, e a projeção na
normal a joga fora — num plano, irregular como for, todo vizinho está no plano, logo o produto
interno com a normal é **exatamente zero**. O cotangente de Meyer et al. 2003 custaria dois ângulos
por aresta e uma área mista por vértice para consertar o caso que a projeção resolve de graça.

**Ela é DERIVADA como a normal, não autorada como a máscara** — e é essa classificação que decide o
resto: não é preguiçosa, não entra no undo, não é serializada (`persist` a reconstrói pelo
`rebuild`). O que ela **tem** de fazer é viajar pelas quatro portas (`rebuild` · `refresh_region` ·
`splice_topology` · `shrink_topology`), e o comentário do splice já avisava, desde a W6, que *"o dia
em que entrar um quinto plano por-vértice quem esquecer dele é esta função"*.

**MEDIDO** (`measure_curvature`):

| malha | `k` mediano | \|k\| p99 | \|k\| máximo |
|---|---|---|---|
| esfera CRUA 48×72 | −0,0372 | 0,045 | 0,045 |
| esculpida 48×72 | −0,0372 | **0,305** | 0,685 |
| esculpida 96×144 | −0,0189 | **0,140** | 0,704 |

⚠️ **A tabela traz um fato que eu não tinha previsto, e ele é a razão de o ganho poder ser fixo:** a
curvatura de FUNDO cai pela metade quando a tesselação dobra (é o `−h/2R`), mas a de um VINCO fica
onde está. Um vinco é um vinco em qualquer densidade — então o **CONTRASTE**, que é o que o canal
desenha, não é função de quantos triângulos a peça tem. `CAVITY_GAIN = 4,0` satura em `|k| ≥ 0,25`,
entre os dois p99.

**Preço: 10,7 a 13,4% do `refresh_region`** (0,168 ms de 1,256 a 1 M vértices, raio 0,15) — ela
cavalga a descoberta de vizinhança que já está paga, que era a hipótese e agora é medição.

**O termo do shader é UMA linha e SIMÉTRICA**, porque a curvatura é *um* número com sinal:
`cav = 1 − amount × clamp(k × GAIN, −1, 1)`. ⚠️ **Um knob e não dois:** o §4 fala em *Cavity* e *Edge
Wear*, e eles são a UI de um MATERIAL (sujeira na fresta × tinta gasta na quina são histórias físicas
diferentes). Para LER FORMA um número simétrico é o modelo honesto; inventar o segundo agora seria um
knob que nenhum gesto alcança — **se o smoke disser que os dois lados querem quantidades diferentes,
é ELE que parte este número em dois**, e o oráculo disso é o olho.

⚠️ **Ela multiplica o DIFUSO e não o realce** (uma fresta oclui a luz de AMBIENTE, não o caminho
especular de uma lâmpada — que ou alcança aquele ponto ou não, e o `N·H` já responde isso) · **o
G-buffer a IGNORA**, como já ignora a máscara (o canal doado é uma NORMAL, e o alvo `vec4` não tem
canal livre: levar oclusão à tinta é um segundo plano, portanto uma wave) · **a curvatura não cruza
`obj.model`** e a normal cruza (ela é adimensional, e a escala da `Pose` já se cancelou na CPU).

**`DEFAULT_CAVITY = 0`** — o barro liso da W3, ao byte. Um canal de sombreamento que se arma sozinho
muda a arte de todo mundo que já esculpiu. **`Shift+C`** cicla `0 → 0,35 → 0,70 → 1,00 → 0`.

**14 gates novos** (10 no kernel + 4 de GPU) + 2 arch-gates de shell; GPU na RTX **26/26** (era 22).
**11 mutações: 9 sangram, 2 sobrevivem com a medição ao lado** — (a) somar posições absolutas em vez
de deslocamentos vale **2 a 3×**, não ordens de grandeza, porque o erro que domina já está na
coordenada ARMAZENADA (o comentário que eu escrevi exagerava, e a medição o corrigiu); (b) a linha
que carrega a curvatura na compactação é **redundante hoje**, porque `shrink_verts` marca todo
destino — e o **CONTROLE** fecha o argumento: a mesma mutação na linha das NORMAIS, que shipa desde a
W9.3 com oráculo de bit ao lado, **também sobrevive**.

⚠️ **E uma mutação ACHOU UM BURACO de verdade:** apagar o `write_buffer` da curvatura do caminho
**incremental** deixava os 25 gates de GPU verdes, porque o irmão que compara região com upload cheio
roda com a cavidade em zero. O gate novo pega o defeito pior que há aqui — um relevo novo com a
leitura de cavidade de ANTES do traço, a fresta desenhada onde ela estava, no lugar exato em que o
artista está olhando.

**Smoke: `PH2D_SCULPT3D_SMOKE=15`** — sete sulcos paralelos de profundidades decrescentes, e a escada
é o oráculo (com uma profundidade só, ligar o canal daria *uma imagem diferente*, e diferente não é a
pergunta). ⚠️ As sete forças saem da MEDIÇÃO: a primeira escada que escrevi punha os três de baixo em
0,026 / 0,008 / 0,0005 — invisíveis mesmo no teto, ou seja três degraus que não ensinam nada. A cena
**imprime a escada medida**; se o primeiro sulco não passar de ~0,15, PARE.

**Aberto:** a cavidade **não alcança a doação** (nomeado acima — é um segundo plano) · o **AO assado**
do §3 (cone tracing contra o campo SDF, que a `ph2d-sdf` já tem) usa o mesmo canal de vértice · e o
**SSS pré-integrado** (§2a) consome esta mesma curvatura, que era a razão de ela vir primeiro.

⚠️ **E o "irmão desta wave" do AO foi RE-PRECIFICADO em 2026-08-06, antes de uma linha dele ser
escrita** (`crates/ph2d-sdf/tests/measure_ao.rs`): o campo que ele exige custa **231-386 ms na malha
de 425 k** que a cena `=16` abre — 14× a 23× um quadro de 60 fps, **antes** do primeiro cone ⇒ ele é
irmão no **CANAL de vértice, não no ciclo de vida**: o AO assado é um **BOTÃO explícito**, como o
REMESH da W7, e a obsolescência é inerente ao desenho e tem de ser **dita**. ⚠️ **E a decomposição
contradiz o palpite:** voxelizar é limitado por **TRIÂNGULO** (11,3× para 31× a malha; só **1,52×**
para **23×** as células) e o flood fill por **CÉLULA** (quase linear) — *um campo FINO é barato; uma
malha densa é cara*. O **cone tracing em si ainda não foi medido**: meça-o pela porta do produto antes
de desenhar o resto.

### W10.2 — ✅ O ALPHA: O PADRÃO É DA SUPERFÍCIE, NÃO DO DAB (**smoke OK na 2ª rodada**)

> A 1ª rodada REPROVOU (*"os poros são gigantescos"*); depois do conserto da escala, *"smoke OK …
> siga"* — registrado no handoff de continuação de 2026-08-06.

O item **alphas/stencils** do cabeçalho do W10, e o que multiplica os dezesseis verbos: um número em
`[0,1]` que multiplica o **falloff** e decide onde, dentro da pegada, o verbo age. Seis padrões
analíticos, transcendental-free, sem asset nenhum — a mesma decisão dos matcaps da W12, pelo mesmo
motivo (não há alpha autorado para carregar, e sintetizar uma textura para depois a amostrar é a
mesma fórmula avaliada duas vezes).

⚠️ **O mapeamento é 3D, e é o OPOSTO do que o [[04.1-Pinceis]] previa** — a nota lá foi reescrita com
o porquê. Resumo: a lei do traço é um ENVELOPE sobre a lista de dabs, e num mapeamento projetado o
`max` sobre dezenas de amostras independentes **lava** o padrão até a envoltória superior dele; o
pincel ficaria mais forte em vez de texturizado. O padrão direcional — que é o caso que *quer* o frame
— entra como variante nova do enum, não como reescrita.

⚠️ **A LEI DAS DEZ ARESTAS**, medida pela correlação entre vértices vizinhos: razão 1,2 → 0,06
(chuvisco) · 5,1 → 0,45 · **10,2 → 0,82** · 20,4 → 0,95.

⚠️ **Mas ela sozinha NÃO decide o default, e o 1º smoke reprovou por isso** (*"os poros são
gigantescos"*): a primeira versão fixou `0,25` como *a escala mais fina que a malha RESOLVE*, e
resolver não é parecer — `0,25` põe **oito** features atravessando uma esfera unitária. **Uma escala
absoluta é a unidade certa e um literal absoluto não é.** Escolher um padrão passou a **semear** pelo
modelo (`recommended_scale`, o `max` do LOOK `maior lado ÷ 33` com o piso das dez arestas), e a
constante virou sentinela do *"o artista ainda não escolheu"*. Três defeitos meus saíram da medição no
caminho: a régua do LOOK era a **diagonal** e a tabela contava o **diâmetro** (fator √3) · o estimador
de aresta **ressoava com as linhas do sphere UV** (mesmo valor para malhas de 153 k e 734 k, viés
2,34×) · e ele roda **a cada quadro** no retrato (0,11 ms com 2048 amostras, **0,017** com 384, mesmo
viés).

⚠️ **A cobertura de cada padrão foi MEDIDA e a primeira calibração estava errada por quase 4×** — as
sementes são pontos no VOLUME e a superfície corta esse volume, então a fração visível cai com o
**cubo** do raio, e um raio que pareceria generoso num desenho 2D é diminuto em 3D.

**14 gates de motor + 2 de painel; 7 mutações de motor e 2 de UI, todas sangram.** ⚠️ **Duas nasceram
de mutações SOBREVIVENTES**, e a segunda vale mais que a feature: varrer só um octante no Worley
devolve a semente ERRADA e passava em tudo (o gate novo mede a janela de 27 contra uma busca em 125,
o que **afirma** o número em vez de o repetir na prosa) · e tirar uma fileira inteira do `populate`
deixava os **vinte** gates de costura verdes, porque a metade *"pintado → clicável"* **enumerava os
grupos à mão e a enumeração já tinha apodrecido** — os chips de matcap, o Accumulate e o Wireframe
nunca foram varridos. Ela perdeu a lista: o conjunto é agora o que o PAINT registrou, e a exclusão é
por GESTO (arrasto), então esquecer um controle novo o deixa **coberto** em vez de fora.

**Smoke: `PH2D_SCULPT3D_SMOKE=16`** — a cena abre com **425 k vértices** (35 ms, 0,555 ms/dab) porque só
a partir de ~800 segmentos o LOOK passa a mandar; ela **imprime a escala e as features**, e abaixo de
~20 o padrão sai como cratera.

**Aberto:** o alpha **por IMAGEM** (a variante direcional, que traz o frame de volta e é o caminho para
um carimbo autorado) · o padrão está colado ao ESPAÇO DO OBJETO, então deformação grande faz a
superfície deslizar por baixo dele — o comportamento do mapeamento 3D do Blender, e é ele que também
garante que girar o objeto não faz a pele nadar.

---

### W10.3 — ✅ O AO ASSADO: o KERNEL e o que ele custa (2026-08-06, **smoke OK pelo passo 5 da `=18`**)

> ⚠️ **O que foi smokado, exatamente:** o Enio rodou a cena **`=18`** (a do AO de tela) e aprovou
> — *"Smoke OK!"*. O **passo 5 dela é o desta wave**: aperte `Bake AO`, suba `Ambient Occlusion`, e
> as duas fontes têm de compor pela MAIS ESCURA sem a peça ficar preta na fresta. A cena
> **`=17`** (o TORO, onde o assado mostra o que só ele vê) **ainda não foi rodada** — ela é a
> demonstração dedicada, não a validação do canal.

O irmão da cavidade, e a wave que o handoff de continuação §4.1 abriu com uma ordem: *"o cone tracing
em si ainda NÃO foi medido — meça-o **antes** de desenhar o resto"*. Esta fatia é exatamente isso: o
kernel entra **para que a medição exista**, e o canal no `Mesh` **não** foi tocado (ele custa as quatro
portas do §W6 e só se paga depois de o número justificar a feature).

**O kernel** (`ph2d-sdf::bake_ao`): cones distribuídos por **cosseno** no hemisfério da normal, cada um
marchando contra o campo por **marcha de esfera** — o desenho que o [`05.1`](../05-Shading/05.1-Shader-de-runtime.md)
§3 fixa. Três referências publicadas, **portadas em vez de reinventadas** (DIRETIVA §1): o teste de cone
do *soft shadow* de Quilez, a base ortonormal de **Duff et al. 2017** (o `copysign` é o que faz o polo
`(0,0,−1)` não explodir) e a rede de **Fibonacci** com distribuição por cosseno — que é o que torna a
**média simples** dos cones o integral de AO, sem peso na soma.

⚠️ **Nenhuma das três constantes é escolhida:** a **abertura** sai da CONTAGEM de cones (eles ladrilham
o hemisfério), e o **viés** e o **piso da marcha** saem da resolução do campo.

**O que a medição decidiu, e duas coisas contradizem o palpite:**

| | |
|---|---|
| o traço, serial, 425 k vértices | **786 ms** — **2,6× o campo** (301 ms) ⇒ a metade que ninguém tinha medido é a **GRANDE** |
| os **passos** | **SATURAM em 24**: de 24 a 96 a resposta não move um dígito ⇒ o teto **não é a restrição que morde** (a marcha termina por `t > radius`) |
| o **alcance** | ⚠️ **É DE GRAÇA** — custo plano (5,6 a 6,3 ms) enquanto o raio cresce 6×, porque a marcha acelera em espaço aberto ⇒ o default `maior lado ÷ 8` é **tímido sem economizar nada** |

⚠️ **E o `INFINITY` do campo era uma armadilha real:** `0.0 * inf = NaN`, e um peso da trilinear cai em
zero **exatamente sobre as amostras da grade** — que é onde uma marcha de esfera mais pousa.
`VoxelField::sample` doma cada canto por `far()` antes da soma; gate red-first varre a grade inteira.

**O oráculo dos gates é a FORMA, nunca a fórmula:** um convexo isolado enxerga o céu (0,96 — o *quase* é
o viés rasante, que **encolhe quando os cones sobem**) e o aro interno de um toro enxerga a parede
oposta (**0,9091** contra **0,9954** no externo). ⚠️ A primeira fixture media a banda larga do tubo e
**diluía o fenômeno** — afiada para os dois equadores.

**E o traço PARALELIZOU** ([ADR-0156](../../architecture/decisions/0156-sculpt3d-ao-trace-is-a-per-vertex-gather-rayon-exception.md)):
é um *gather* por-vértice contra um campo imutável, com a soma **privada do vértice e de ordem fixa** (o
precedente exato do ADR-0147) ⇒ **764,8 → 39,3 ms, 19,44×, ZERO vértices divergentes** contra a rota
serial congelada. A cerca do ADR-0109 foi honrada em vez de contornada, e a exceção é **estreita**: o
voxelizador e o flood continuam seriais, pelo mecanismo que o `Cargo.toml` escreve.

⚠️ **A FRONTEIRA MUDOU DE LUGAR:** o bake completo cai de **~1,09 s para ~338 ms**, e o **campo passa a
ser 89% dele**. Quem for atrás do próximo ganho vai à **voxelização** — e lá a pergunta não é de
agendamento, é de **representação**.

**Aberto — e é o resto da wave, não dívida:** o **canal no `Mesh`** (o 6º plano por-vértice e as quatro
portas `rebuild` · `refresh_region` · `splice_topology` · `shrink_topology`; ⚠️ o comentário do splice
avisa desde a W6 que *quem esquecer do quinto plano é esta função*) · o **BOTÃO** e a **obsolescência
DITA** (a recusa do remesh sob multires é o precedente de como se diz) · o **shader** consumindo o canal
· e o **alcance default**, que é decisão de LOOK e a medição provou **gratuita** ⇒ o smoke decide onde
ele pousa.

⚠️ **O "resto da wave" FECHOU no mesmo dia** — o canal por-vértice, o BOTÃO com a obsolescência dita e o
termo do shader estão construídos. O que a W10.4 mudou é **quem é o default**.

---

### W10.4 — ✅ O AO DE TELA: o canal que o artista vê ENQUANTO trabalha (2026-08-07, **smoke OK**)

O Enio, olhando o botão de bake: ***"Todo app moderno tem AO em tempo real! pq um bake???"*** — e ele
tem razão no que decide. Num app de **ESCULTURA** o artista muda a forma o tempo todo, e essa é
exatamente a operação que invalida um bake; o canal que ele **VÊ** tem de ser o medido a cada frame.
O `05.1` §3 já pedia as duas metades e chamava esta de *"opcional"*: **a palavra estava errada**, e
foi corrigida lá.

**O kernel é GTAO** (Jimenez, Wu, Pesce, Jarabo — SIGGRAPH 2016 Courses), **portado, não inventado**
(DIRETIVA §1): horizontes marchados na profundidade, integral analítica do arco (Eq. 7), pesada por
cosseno. Passe de tela cheia sobre a profundidade **e o G-buffer de normais que a DOAÇÃO já
produzia** — a W3 pagou aquele alvo por outro motivo e ele serve aqui sem uma linha de mudança.

**Os defaults saíram da TABELA** (`measure_the_screen_ao`, 1920×1080, contra um quadro de 60 fps):

| fatias × passos | o AO custa | % de um quadro | a fresta escurece |
|---|---|---|---|
| 2 × 4 | 0,178 ms | 1,1% | 18,2% |
| **4 × 12** ← default | **0,408 ms** | **2,4%** | **46,0%** |
| 8 × 12 | 0,582 ms | 3,5% | 45,5% |

⚠️ **Meu primeiro corte usava `steps: 4`, que SUBAMOSTRA** (17,6% contra 46,0% no mesmo raio) — a
marcha passa por cima do horizonte sem o ver. E a **primeira versão da sonda media 128×128**, onde
os nove pontos cabiam entre 0,015 e 0,030 ms: escolher ali seria escolher pelo **RUÍDO**.

**As duas fontes compõem pela MAIS ESCURA (`min`), nunca pelo produto** — elas descrevem a mesma
sombra por dois caminhos, e numa fresta funda as duas acertam; multiplicar escureceria em dobro no
instante em que o artista assasse. `DEFAULT_SSAO_STRENGTH = 1.0` contra `DEFAULT_AO_STRENGTH = 0.0`,
e a assimetria é a wave inteira: **o assado não existe até alguém apertar um botão**.

⚠️ **DOIS erros de sinal, e eles se CANCELAVAM num dos eixos:** a marcha corre em coordenadas de
**framebuffer** (y para baixo) e o vetor da fatia era lido em espaço de **vista** (y para cima); e o
sinal do ângulo da normal não casava com o lado que recebe o horizonte negativo. Compostos, davam
**~1% em pitch e 45% em yaw** — e a minha primeira sonda, que só inclinava em **pitch**, **INOCENTOU
o eixo errado**. O gate do plano chato varre os dois eixos **separados** por isso; hoje o plano mede
0,00%–2,09% em todo ângulo, e a fresta **46,6%** contra **0,5%** no flanco aberto.

**Cena `=18`** (duas esferas encostadas — a fresta entre elas é o que **só** este passe mede, porque
o bake marcha contra o campo de UM corpo e cada esfera é convexa).

**Aberto:** o **borrão** (o GTAO da referência filtra com um bilateral guiado pela profundidade; sem
ele o ruído por-pixel some na média mas aparece como granulado em `slices` baixo — é por isso que o
default é 4 e não 2, e o oráculo de um borrão é o OLHO) · a **força e o contraste** são decisão de
LOOK, não de engenharia.

---

### W10.5 — ✅ O SSS PRÉ-INTEGRADO: a luz atravessa a borda da sombra (2026-08-07, **smoke OK 2026-08-08**)

O terceiro dos três canais que o Enio nomeou (*"SSS, AO, Cavity"*), e o último item de sombreamento
do `05.1` §2a. **Penner & Borshukov 2011** sobre o perfil de seis gaussianas de **d'Eon & Luebke** —
**portado, não inventado** (DIRETIVA §1): uma tabela `D(N·L, t)` assada na CPU e consultada por UMA
amostra de textura.

⚠️ **A wave inteira nasceu de uma frase do plano que a medição derrubou.** O `05.1` §2a dizia *"a
curvatura já a temos … um dado, dois usos"*, e é o mesmo **GATHER**, não o mesmo **NÚMERO** — o canal
do Cavity é **adimensional por construção** e a LUT precisa de `1/comprimento`. Detalhe, tabela e as
duas constantes que a medição corrigiu estão na §2a daquele doc.

**O que entrou:**

| | |
|---|---|
| `ph2d_mesh::world_curvature_at` | o `κ` em `1/comprimento`, **no mesmo gather** (`curvature_pair_at`), pelas **quatro portas** — a lei do §W6, e o comentário do splice avisava desde então |
| `ph2d_mesh_render::sss` | a integral exata, a tabela 128², os dois parâmetros, e a porta de empacotamento |
| o termo no `mesh.wgsl` | grupo 3 (a frequência mais baixa de todas: a tabela é assada uma vez na vida do processo) |
| `Shift+S` + **duas pistas no painel** | `Subsurface` (a força) e `Scatter` (o alcance) |

⚠️ **`sss.strength = 0` é BYTE-IDÊNTICO** ao barro anterior a este canal (gate de GPU), e é o
default: os dois AOs são MEDIÇÕES da forma e mostrá-las é honesto, mas isto é um **MATERIAL** — barro
não é pele.

**Medido na tela** (esfera de raio 1, `scatter` 2,0): o terminador vai de **62,63 a 82,55 (+31,8%)** e
o miolo aceso **cai 3,3%** — ⚠️ e essa queda é **conservação de energia**, não defeito: a luz que
vaza para o lado escuro sai do lado aceso.

**7 gates de tabela + 3 de GPU + 2 de canal; 6 mutações, 5 sangram.** ⚠️ A sobrevivente é a cópia do
canal no `shrink_topology`, e é a **mesma** que o `mesh_shrink.rs` já documenta para a irmã (todo
caminho que encolhe termina num `refresh_region`): defesa em camada, registrada no produto em vez de
gateada com uma fixture artificial. ⚠️ E **uma mutação achou um oráculo espelho**: os gates de
topologia comparam produto contra rebuild, e o rebuild usa a mesma fórmula — o viés de Jensen de 4%
passava por todos eles. O gate novo `the_world_curvature_of_a_sphere_is_one_over_its_radius` é o
oráculo EXTERNO (o raio é da fixture, não da função) e sangra com o número exato.

**Cena `=19`** — três esferas de raios 1,0 / 0,45 / 0,20 sob o MESMO material. ⚠️ A **escada é o
oráculo**: com uma peça só, *"a luz atravessa formas finas"* e *"a peça clareou"* são indistinguíveis.

⚠️ **O SLIDER veio de uma pergunta do Enio — *"não seria melhor um slider para testar?"* — e ele
apontou um buraco maior que o pedido.** Eu tinha shipado só a tecla `Shift+S`, que cicla quatro
degraus da FORÇA, e havia escrito no doc dela que *"não há um segundo controle a oferecer"*, porque o
alcance é semeado pela peça. **Isso deixava o número que decide o LOOK inalcançável** — eu o escolhi
por uma medição de *visibilidade* (0,25), e onde ele deve pousar é veredito de aparência, que nenhuma
medição responde. Os dois irmãos (`Screen Occlusion`, `Ambient Occlusion`) já tinham pista; o SSS
ficou de fora sem motivo.

**A tensão era real e a FRAÇÃO a resolve:** guardar um comprimento absoluto seria uma segunda verdade
sobre o tamanho da escultura — a que fica velha exactamente quando o artista a faz crescer. O que a
cena guarda é a **fração do maior lado**, então o alcance continua sendo função da peça *e* o slider
é o veredito de LOOK. A porta é `SssParams::for_bounds_with`, e o `for_bounds` passou a delegar a ela
com a constante: *quanto da peça a luz percorre* é uma pergunta só, e o default é um argumento, não um
segundo caminho.

⚠️ **E a pergunta do slider achou DOIS defeitos que nenhum gate via.** (1) O seam do painel faz
`for row in rows()` — certo para *"cada row registrada é clicável"* e **cego** para *"esta row
existe"*: apagar a linha do `Scatter` encolhe a lista que o gate percorre e os 21 testes ficam
VERDES sobre um slider que sumiu da tela (a forma que a `line/Painter` documentou como **oráculo
auto-referente**). O gate novo pergunta pelo **ID**, que a tabela não pode encolher. (2) E o corte de
LOC — `sculpt3d_scenes.rs` cruzou 600 com o roteiro da `=19` — expôs que os **doc-comments das duas
cenas estavam TROCADOS**: ao inserir a `sss_scene` eu a pus entre o doc do `=18` e a função dele, e o
doc órfão adotou a minha. Os números continuavam certos e a prosa descrevia a outra cena; só uma
leitura pega isso.

⚠️ **E um TERCEIRO defeito, no scanner que TODOS os arch-gates do cluster usam:** o
`function_body(src, "render")` busca `fn render` e casa **por PREFIXO** com `fn render_live`, que mora
num arquivo que vem antes na ordenação — então o gate da cavidade lia o corpo do bake de luz e
afirmava coisas sobre ele. Foi a wave anterior (o AO de tela) que criou a condição, ao mover o
`render` para `sculpt3d_view.rs`, o último do cluster alfabeticamente. **O nome de uma função termina
no `(` dos parâmetros**, e é isso que separa `render` de `render_live`; o helper passou a exigir o
delimitador (aceitando também a forma `"radius_px(&self)"`, que alguns chamadores já usavam para
desempatar). ⚠️ *Um gate cuja âncora casa por prefixo não afirma nada sobre a função que ele nomeia* —
e o modo de falha é verde, não vermelho.

⚠️ **A pista de `Scatter` só existe com `Subsurface > 0`** — com a força em zero a tabela nem é
consultada, e um slider que não move um pixel é o controle morto que esta casa varre a cada wave (a
mesma lei do `Plane Offset`, que só aparece nos verbos que leem um plano). E a tecla escreve o MESMO
campo que o painel publica, então apertar `Shift+S` **move a pista na tela**.

**Aberto:** ~~a transmitância~~ (**FECHADA na W10.6, abaixo**) · o SSS **não entra no matcap** (ele é
a luz do OLHO, com um `rim` que já finge translucidez; os dois convivem, e juntá-los é decisão de
LOOK).

### W10.6 — ✅ A TRANSMITÂNCIA: a luz ATRAVESSA a peça (2026-08-07, **smoke OK 2026-08-08**)

> Enio, sobre o smoke da W10.5: *"Todos os seus efeitos (cavity, SSS e AO) são muito discretos mas
> funcionam. Contudo, não chega perto de cera com SSS no máximo. Muda muito pouco"*.

⚠️ **A medição REFUTOU a cura óbvia.** O teto do slider (`T_MAX = 4`) entrega **49,1%** da assíntota
do modelo pré-integrado — mas subir o teto vai para o lado errado: a separação de cor no terminador
tem **pico em `t = 1,5` (0,0375)** e cai a **0,0001 em `t = 24`**, ou seja a peça vira um **disco
cinza uniforme**. O canal do §2a *redistribui* a luz da frente, e o teto dele é a média dela
(`1/π ≈ 0,318` do lambert cheio): **nenhuma quantidade dele acende o que o lambert deixou em zero.**

**Cera é um termo que SOMA**, e o [[05.1-Shader-de-runtime]] §2b já o prescrevia — com a palavra
*cera* dentro. `exp(−espessura · coeficiente)`, com a espessura **assada por vértice**.

**Como a espessura é medida — três candidatos, um oráculo EXTERNO** (numa esfera a resposta é `2r`,
ao dígito; `ph2d-sdf/tests/measure_thickness.rs`):

| método | esfera | toro | chapa | 13 682 vértices |
|---|---|---|---|---|
| proxy `2/\|κ\|` (grátis) | **0%** | **420%** | **511%** | 0 ms |
| raio contra o octree | 0,02% | exato | exato | **541 ms** |
| **marcha no campo** (escolhido) | 0,33% | — | — | **30 ms** |

⚠️ **O proxy grátis é exato na esfera e erra 511% na CHAPA** — que é justamente a forma pela qual a
luz atravessa. Ele teria passado numa fixture de esferas (**a cena `=19` era uma!**) e mentido no
caso de uso. ⚠️ **E o raio perde por ESCALA, não por precisão:** 4× os vértices multiplicam ele por
**14** e o campo por **3,6** — segundos contra minutos numa escultura de verdade. A minha primeira
nota afirmava o contrário (*"~30× mais barato que o AO"*) e a medição a derrubou: ele é **244× mais
caro**.

**A COR sai do mesmo `PROFILE`**, nunca de um literal: o raio RMS por canal dá `G 4,505×` e
`B 7,472×` de atenuação relativa ao vermelho, e é essa assimetria que faz uma mão contra a lanterna
ficar **vermelha** em vez de cinza.

**O default é OPACO** (malha sem bake é byte-idêntica). ⚠️ Na CPU isso é `f32::INFINITY`; **no
device, não** — o interpolador computa `w × valor`, e um `w` exatamente zero numa quina dá
`0 × inf = NaN`, um pixel preto no meio da peça sem erro nenhum a caminho. A conversão para um
coeficiente **grande e finito** acontece na fronteira de upload.

**Medido na tela** (ablação **pela espessura**, a mesma peça com e sem o plano): a esfera fina ganha
**+7,88 níveis** no lado escuro contra **+0,43** da grossa — **18,5×**.

**⚠️ TRÊS defeitos meus, e os três achados por mutação:** (a) o gate da escada media o canal
**pré-integrado** e ficava verde com `d = 0` cravado no shader (3,81×) — o oráculo virou a ablação
pela espessura; (b) o passe de preenchimento de furos era **código morto** herdado do motor de raio,
que o motor de campo não tem; (c) `trans_scale = scatter` **sobreviveu a três gates**, e apertar a
barra seria calibrar contra o mutante — o gate novo afirma a **direção do knob**.

**8 mutações: 7 sangram, 1 achou o código morto.** `ph2d-mesh` ganhou o 6º plano por-vértice (as
quatro portas do §W6) e `ao_stale` virou `baked_stale` — *uma validade para os dois canais assados,
porque o gesto que os produz é UM*.

**Aberto:** o teto `T_MAX = 4` fica onde está **de propósito** (a medição mostra que ir além destrói
a cor) · a transmitância **não entra no matcap**, pelo motivo do §2a.

### W10.7 — ✅ A DOAÇÃO CARREGA A OCLUSÃO (2026-08-07, **smoke OK 2026-08-08**)

O item **4.2** do handoff de continuação, e ele *"pesa mais que as outras porque o objetivo 2 do
módulo é exatamente esse"*: as quatro waves de sombreamento acima (cavidade, os dois AOs, o SSS, a
transmitância) viviam **só no viewport**. O G-buffer escrevia `vec4(normal, 1.0)` e mais nada.

**O que a tinta 2D passa a receber:** a **oclusão de forma** — `cavidade × min(AO assado, AO de
tela)` —, um escalar por texel, que multiplica o **DIFUSO** do passe de luz do Painter.

⚠️ **Os três canais NÃO são iluminação, e é isso que os torna doáveis:** o `mesh.wgsl` já o dizia
duas vezes (*"não é iluminação, é leitura de FORMA"*). O SSS e a transmitância **não** viajam — eles
dependem da direção da luz, e o lado 2D tem o próprio rig.

| peça | o quê |
|---|---|
| `mesh.wgsl::form_occlusion` | a **porta única**: o barro pergunta para se acender, o G-buffer para DOAR |
| `fs_gbuffer` → 2 alvos | `@location(1)`, `R16Float` — **2 B/texel** contra os 8 do primeiro |
| `FormPlanes` | a doação é um STRUCT: *meia doação* vira erro de tipo |
| `ReliefFields::form_occlusion_at` + `ImpastoLightInput::form_occlusion` | as DUAS rotas do Painter |
| `BakedFormDocument.form_occ` | **`PROJECT_SCHEMA` 55→56** (PROVISÓRIO — o valor se CONTA) |

**Os números que decidiram a wave já estavam medidos neste repo:** uma fresta escurece **46,0%** (AO
de tela, a tabela do §3 do `05.1`) e a cavidade abre **±35%** no primeiro degrau. Não é sutil.

⚠️ **E os DEFAULTS mudaram o desenho:** `DEFAULT_CAVITY = 0` e `DEFAULT_AO_STRENGTH = 0`, então a de
**TELA** é a única oclusão acesa por padrão — doar só cavidade + assado entregaria **nada** no estado
em que o artista de fato está. Por isso a doação **mede o AO de tela na PRÓPRIA resolução**: herdá-lo
do viewport desenharia a sombra de um enquadramento sobre a forma de outro.

⚠️ **Uma MUTAÇÃO achou um defeito de verdade:** só o `render` escrevia o uniform de sombreamento — o
G-buffer devolvia normal e cobertura, que não dependem de knob nenhum, e isso era **inofensivo até
esta wave**. Com a oclusão no segundo alvo virou defeito: num frame em **modo LUZ** o `render` não
roda, e no caminho do **BAKE** ele pode nunca ter rodado ⇒ a doação carregaria a cavidade de outro
instante, ou os ZEROS de um renderizador virgem. Hoje o `render_gbuffer` recebe e escreve o `Shade`.

⚠️ **E a oclusão VIAJA no documento em vez de ser assada no `base`**, que seria de graça em disco:
um re-bake **REUSA** o `base` (`bake_one`), então pré-multiplicá-la ali a comporia a cada gesto e o
objeto escureceria sozinho — *o defeito exato que o `base` existe para impedir*, um nível acima. Ela
viaja em **`R8`** (um byte por texel), o mesmo argumento da W8.7, e o erro é analítico e apertado:
`|Δ(albedo·mul·occ)| ≤ Δocc`, **um nível**.

**7 mutações, 7 sangram.** ⚠️ **Dois defeitos nos meus próprios gates:** o da porta única contava
`shade.ao` no **texto** do shader e achava duas ocorrências — uma delas a PROSA que explica o termo
(*um gate que varre fonte conta o token na documentação também*); e a fixture do gate de GPU esculpia
com um `Brush` e media a faixa **`1,108 .. 1,155`** — uma esfera **LISA** (`k ≈ −0,037` × ganho 4, a
aritmética do W10.1 ao dígito). *A fixture não continha o fenômeno*, e as barras teriam sido escritas
por raciocínio; com o sulco ela abre para **`0,000 .. 1,781`**, e é desse fosso que os números saem.

### ⚠️ E O SMOKE ACHOU UM DEFEITO QUE ATINGIA AS TRÊS CENAS DE SOMBREAMENTO

> Enio, na `=20`: *"com a luz ligada a esfera some"*.

**A metade reportada é o DESENHO**, e o `render_view` a declara: em `LUZ` o barro **não é desenhado**
(`if !self.shows_clay() { return; }`) — o que se olha ali é a TINTA acesa pela forma, e a esfera
taparia justamente o que há para julgar. O defeito era do **roteiro**, que mandava o artista comparar
a fresta no barro e na tinta *ao mesmo tempo* — um passo que a própria cena torna impossível. Hoje o
roteiro diz que o barro sai de propósito, e os passos são sequenciais: o controle em `BARRO`, o
veredito em `LUZ`.

**A metade que ninguém tinha visto é pior, e a aritmética a achou:** `smoke_mesh()` devolve a peça
**PRIMÁRIA** e `scene_objects()` só acrescenta **EXTRAS** — então uma cena que declarava o elenco
inteiro em `scene_objects` abria com a esfera lisa de 96×144 **por cima, não convidada**. As três
cenas de sombreamento faziam isso:

| cena | o que a primária acidental fazia |
|---|---|
| `=18` (AO de tela) | uma **terceira** esfera de raio 1 na ORIGEM — dentro da fresta de `0,04` que a cena existe para mostrar |
| `=19` (SSS/cera) | a bola de `0,45` em `x = 0,35` ficava **enterrada** (`0,35 + 0,45 = 0,80 < 1,0`); a de `1,0` em `x = −1,7` fundia-se com ela |
| `=20` (esta) | **duas esferas coincidentes** — a lisa tapando a sulcada |

⚠️ **Nada reclama:** a cena abre, desenha, e o artista julga um canal sobre uma composição que
ninguém desenhou. *Um roteiro que chama a escada de "o oráculo" com um degrau invisível é pior que
roteiro nenhum.*

A cura é estrutural — **uma cena de sombreamento é dona do elenco inteiro** (`primary_mesh`) — e vem
com **dois** gates, porque o primeiro sozinho não bastou: `a_shading_scene_owns_its_whole_cast` prova
que as duas portas do módulo concordam, e **a mutação que faz o `smoke_mesh` parar de perguntar
sobrevive a ele** (capacidade sem porta). O irmão
`the_primary_mesh_door_is_actually_asked_before_the_fallback` fecha isso, e a ORDEM é load-bearing.

**Cena `=20`** — uma esfera com **três sulcos** e a tela branca. ⚠️ A peça nasce sulcada porque numa
esfera lisa a oclusão fica em `1,108 .. 1,155` e *"a oclusão chegou"* seria indistinguível de *"a peça
clareou"*; com os sulcos ela abre para `0,000 .. 1,781`. O passo 5 é o que a mutação pediu: desligar a
doação, mexer no knob, religar — em modo LUZ o barro nem é desenhado.

⚠️ **E o gate de colisão de número de cena era do lado do VETOR**, não deste: lá o roteador é uma lista
de `if level == N` num arquivo só, aqui cada cena compara o **próprio literal** contra
`PH2D_SCULPT3D_SMOKE`, em dois arquivos. O irmão passaria VERDE sobre uma colisão aqui, e o modo de
falha é o mesmo — `scene_objects` pergunta em ordem, a primeira que diz sim vence, e a segunda fica
inalcançável em silêncio. Gate novo (`no_two_sculpt3d_scenes_claim_the_same_level`), com controle
positivo nas duas pontas; **a mutação que rouba o `=19` sangra**.

**Aberto:** o SSS e a transmitância seguem fora da doação (dependem da direção da luz) · a rota de
GPU do Painter ainda recebe o plano pela CPU (a ida e volta que o `form.rs` já nomeia) · ⚠️ **os
roteiros impressos de OUTRAS cenas saem com a indentação do FONTE** (falta o `\n\` de continuação em
`sculpt3d_scripts.rs` e no `=9` do `sculpt3d_scenes.rs`) — é cosmético, os da `=19` e da `=20` foram
corrigidos, e o conserto do resto é mecânico mas toca 180 linhas de arquivos que esta wave não tem
motivo para reescrever. **Um gate para isso foi escrito e DESCARTADO**: ele exige um detector de
*"onde o literal fecha"* que distinga `",` de continuação, e as duas primeiras versões dele
reprovaram código correto — a segunda denunciando comentários como roteiro. Fica nomeado em vez de
meio-feito.

---

# Riscos, cada um com a solução

| Risco | Solução (não mitigação vaga) |
|---|---|
| ⚠️ **Portar por LEITURA erra 55% das vezes** (medido: 67 alegações, 37 refutadas) | [[03.7-Oraculo-de-fidelidade]]: fixture que contém o fenômeno · oráculo de **propriedade** · transcrição congelada. E o livro-razão, para divergência sem motivo escrito ser **dívida**, não decisão |
| **O kernel na CPU pode não bastar** | é a pergunta explícita de [[03.5-Onde-roda-o-motor]], com **K1 escrito antes do build** (8 ms/dab a 5 M triângulos) e a porta única já construída. ⚠️ **A W1 já mediu:** o K1 dispara só com pincel cobrindo 19% da malha; o custo real é **descobrir a vizinhança** (88% do refresh), não recomputar normais — o K2 apontava para o lugar errado |
| **Dyntopo é a parte difícil** | o SculptGL o resolve em código MIT legível ⇒ tradução, não pesquisa. E é **W9**, não W2: o Nomad prova que *remesh sob comando* (W7) cobre a maior parte do uso |
| **Escopo: isto é um DCC inteiro** | a tabela acima + os **não-objetivos** de [[01.1-Missao-objetivos-e-nao-objetivos]] são recusas escritas |
| **O módulo não vingar** | procedimento de remoção literal em [[02.3-Modulo-removivel-e-mapa-de-crates]] — ⚠️ com **uma** exceção já paga: `ph2d-light` virou dona do rig de luz do Painter e é não-removível |
| **Qualidade abaixo do Nomad** | o smoke é do Enio, com o Nomad ao lado nos mesmos gestos. Sem "está quase" |
| **Memória de malha grande** | o teto sai da sonda `measure_memory` (dhat), por tier (ADR-0104) |
| **Undo de malha grande estoura** | delta por **janela**, teto em **BYTES** (ADR-0117 · U1 do Painter). ⚠️ **E a nota antiga estava errada:** o `states/` dele **já é delta por janela** — o defeito é o cap por CONTAGEM, e o nosso é pior |
| **A licença** | MIT permite adaptar; a condição é o aviso viajar ([[03.4-Referencia-SculptGL]]) |

### W10.8 — ✅ O PANIC DO `Shift+B`, E O BOTÃO QUE O GESTO NUNCA TEVE (2026-08-08, **smoke OK**)

> Enio: *"Panic com Shift+B"* e *"não temos um botão para o bake na escultura na Sprite"*.

**A causa não era do bake**, e o repro headless a nomeou em uma linha: `bound with size 224 where the
shader expects 240`. A W10.7 deu ao `Globals` do `impasto_light.wgsl` um bit novo ocupando a vaga que
era `pad1`; o Rust trocou o **nome** da vaga e o WGSL **acrescentou o campo deixando o `pad1` para
trás** — com o alinhamento de 16 bytes do `Lamp`, dezesseis bytes a mais. O wgpu recusa isso no
dispatch **como panic**, não como erro devolvido ⇒ **todo documento com relevo na rota de GPU do
Painter**, o bake da escultura e a re-acendida de um objeto assado morriam no primeiro quadro. O
`Shift+B` era só o caminho mais curto até ela.

⚠️ **E os seis gates que a teriam pego estavam VERMELHOS no tip da própria wave**
(`ph2d-render/tests/impasto_light_gpu.rs`, todos `#[ignore]`). A W10.7 fechou relatando *"GPU 44/44"*
— 44 gates do módulo 3D, e ela tinha editado um shader da `ph2d-render`. *Uma varredura por crate
ainda é uma ENUMERAÇÃO; a lista tem de sair do DIFF*
([[feedback_an_ignored_sweep_is_not_the_gpu_gate_sweep]]).

**Consertar o panic expôs um segundo defeito, e o segundo um terceiro:**

| # | o defeito | medido |
|---|---|---|
| 2 | o lado de **GPU** do gate de paridade passava `form_occlusion: None` **cravado** — a fixture montava o degradê, a metade de CPU o consumia, e este o descartava | 173 em 11373 bytes |
| 3 | os dois **early-outs** divergiam: a CPU sempre exigiu `occ >= 1.0` para não tocar o pixel, o shader não | 150 em 3504 bytes |

⚠️ **O comentário do shader AFIRMAVA que a oclusão não precisava entrar no early-out** — *"onde a
oclusão é < 1 há forma, logo `body > 0`"* —, e isso é verdade sobre o que a rasterização **produz**,
não sobre o domínio da função: as duas rotas concordavam **por coincidência do produtor**. Paridade
final: **0 de 16384 bytes**, nos seis gates.

**O gate que fecha a classe não precisa de device**, e é essa a metade que shipa no `ship.sh`:
`the_wgsl_globals_measures_exactly_the_rust_globals` lê o WGSL com **naga** (já dev-dep da crate),
compara o `span` do struct com `size_of::<Globals>()` e a **ordem** dos campos com a do Rust. ⚠️ A
lista de nomes é a **terceira cópia** da ordem de propósito: ela não é redundância, é o que torna a
terceira edição **obrigatória** em vez de silenciosa.

**E O BOTÃO.** O gesto tinha **uma porta só e ela era um atalho** que nada na tela menciona — na
prática ele existia para quem o escreveu. Seção própria e por ÚLTIMO (**`Bake to Sprite`**), pela lei
de ordenação que o painel já declara: as cinco de cima descrevem *como a escultura é*, esta descreve
*o que sai dela*. Não é uma linha na cauda do sombreamento porque o `Bake Occlusion` mora lá e os dois
carregam a palavra *bake* significando coisas diferentes — um mede um canal e o escreve na **MALHA**,
o outro escreve a forma inteira num **SPRITE**; os rótulos nomeiam o **ALVO**.

⚠️ **O botão é SEMPRE pintado, e isso vai contra o reflexo desta casa.** A regra local é *oferecer só
quando o gesto leva a algum lugar* (o "Join Selected Bodies" da física, o Filter Layer do Painter), e
aqui ela seria exatamente errada: um botão que só aparece para quem já preparou a cena é invisível
para quem ainda não sabe que precisa preparar — que é a queixa que ele veio resolver. Quem responde é
a **DICA**, no molde do `ao_stale`: a condição é dita, e a linha só existe quando há o que avisar.

⚠️ **O intent SOBE em vez de ser traduzido** — o bake precisa do mundo, do renderizador e do mapa de
atlas, e os três só existem dentro do frame —, e ele arma o **MESMO campo** que o `Shift+B`: *uma
porta, dois pedintes*, que é o que impede o botão e o atalho de virarem duas ferramentas com um nome
só. O `match` de tradução continua exaustivo, que é o que o retorno compra. O **alvo** vem da seleção
da cena **2D**, injetado pelo bridge: a escultura não sabe — nem deve saber — quem está selecionado.

**E o repro virou gate:** `the_bake_gesture_lights_the_selected_sprite` dirige o gesto inteiro num
device (a tela branca pela MESMA porta da cena de smoke, a malha, o `drain` do produto, o sprite lido
de volta). O oráculo é a **APARÊNCIA** — sem ele um `drain` que devolvesse `Ok` e reescrevesse a fonte
passaria por toda asserção de estado e a tela continuaria uma chapa. Medido: **6811 de 65536** texels
saíram do branco. ⚠️ O sufixo `_tests` do arquivo é **load-bearing**: o `texture_edit_chokepoint`
proíbe `readback_individual` fora da porta de alpha-mode e isenta os `*_tests.rs` — *um módulo que não
shipa não pode largar o `premultiplied` no documento de ninguém* —, e ele pegou o gate na hora.

**8 mutações, 8 sangram.** `PROJECT_SCHEMA` **56 intocado**; contrato congelado intacto; ids novos
`SCULPT3D_SEC_BAKE` e `SCULPT3D_BAKE_SPRITE` (hash de string ⇒ nenhum contador se move).

---

### W11 — ✅ O PADRÃO GANHA UM EIXO (2026-08-08, **smoke OK 2026-08-08**, cena `=21`)

> O item **4.4** do handoff de continuação, e a **medição reformulou a nota que o pedia**.

A nota dizia: *"o alpha por IMAGEM (a variante direcional) **traz o frame do dab de volta**"*. A sonda
[`measure_directional_wash.rs`](../../../crates/ph2d-sculpt3d/tests/measure_directional_wash.rs) mediu a
**lei 2 do módulo** antes de uma linha ser escrita — ela sempre tinha tido mecanismo e **nunca um
número** — e o número diz o contrário:

| gesto · âncora | dabs | média (a verdade é 0,500) | contraste |
|---|---|---|---|
| CARIMBO (1 dab) — o controle | 1 | 0,492 | **0,295** |
| traço 1,00 · **absoluta** | 27 | 0,490 | **0,285** |
| traço 1,00 · por-dab (o `04.1`) | 27 | 0,592 | **0,128** |

⚠️ **Quem lava não é a DIRECIONALIDADE, é o RE-ANCORAMENTO.** Uma coordenada medida a partir do CENTRO
DO DAB muda de fase a cada dab, e o `max` sobre dezenas dela come **57% do contraste** e deixa o pincel
**20% mais forte** (0,492 → 0,592), saturando já em ~8 dabs. Uma coordenada **absoluta** sobrevive ao
envelope **intacta**: 0,285 contra os 0,295 de um carimbo único. ⇒ **o frame do dab é exatamente o que
não se pode usar**, e um padrão direcional é apenas um padrão cujo eixo o artista aponta — não um método
de traço novo, não um `Grip::Anchor`, não uma wave grande.

**Três variants, apendados** — `Strata` · `Scratches` · `Weave`. ⚠️ **A ordem é load-bearing na UI:** o
painel desloca a seleção de um (a primeira opção dele é *nenhum*), então um variant no MEIO moveria o
chip que o artista já aprendeu. `Alpha::is_directional()` é a **porta única**: o painel pergunta para
OFERECER as pistas, o motor para decidir se projeta, e o gate para provar que os seis isotrópicos ficam
**byte-idênticos**.

**O EIXO, e a representação que apaga o caso especial.** `AlphaFrame::from_degrees(az, elev)` — a
tangente sai do PRÓPRIO azimute (`t = [−sen, cos, 0]`), unitária e perpendicular ao eixo **por
identidade** em qualquer elevação. Sem ramo, sem limiar, **sem polo** onde o frame saltaria com o slider
na mão; e no zênite o azimute passa a ROLAR o padrão, que é o controle que faltaria ali.

⚠️ **O FRAME SAI UMA VEZ POR DAB, e a ASSINATURA é o que garante.** O rotor deste app é a rotação de um
grau **acumulada** (`rotate_by_degrees`), ou seja **`O(graus)`** — até 359 iterações. `alpha_weight`
**recebe** o frame; `AlphaFrame` não tem `Default` e o `from_degrees` é `pub(crate)`, então a única porta
é `Brush::alpha_frame`.

**A UI veio junto** (as três chips caem de `Alpha::ALL`; as duas pistas novas — *Pattern Angle* /
*Pattern Tilt* — só existem com um padrão **direcional** armado), e a cena **`=21`** **não arma o
padrão**: a costura É o gesto, e uma cena que arma estado por baixo do pano pula justamente o que ela
devia provar.

**O que os gates acharam, e cada um mudou código:**

* ⚠️ `the_pattern_does_not_repeat_with_the_lattice` **mordeu com 0,956 no Strata — e estava CERTO sobre
  um padrão correto.** A premissa dele é sobre **RUÍDO**, e um estrato repete por construção: a
  regularidade dele é **AUTORADA**, que é a palavra que separa as duas coisas. Estreitado à família
  isotrópica **com controle positivo** (`iso > 0 && dir > 0`, senão ele vira vácuo), mais o irmão que
  afirma o que os três de fato compartilham — `turning_the_axis_turns_the_pattern`.
* ⚠️ **Uma leitura de uma linha minha é FALSA e ficou escrita como tal:** *"o eixo é a direção em que o
  padrão VARIA"* vale para o Strata e o Scratches e **quebra no Weave**, que é uma trama e varia nas
  DUAS direções do plano dela.
* ⚠️ **O Weave nascia como LISTRAS no eixo de fábrica.** Com a trama no plano perpendicular ao eixo, o
  default (+Y) a punha no plano do CHÃO, visto de perfil por uma câmera em +Z. O plano virou `t`–`n`:
  uma das famílias corre **ao longo** do eixo, a trama fica de frente, e a leitura *"um fio corre na
  direção que você apontou"* passa a valer para os três.
* ⚠️ **`Scratches` saiu em cobertura 0,034 com 1,1% acima de 0,9** — os números **exatos** que este
  módulo já registrou como *"um pincel que o artista lê como quebrado"* (a 1ª calibração dos Pores, 0,039
  e 1,1%). **Mas a CAUSA é outra**, e confundi-las levaria à correção errada: lá o erro era **volumétrico**
  (a fração visível cai com o CUBO); aqui o padrão é 2-D extrudado e o produto `densidade × perfil
  atravessado × perfil ao longo` **prevê 0,0337 contra os 0,034 medidos**. Re-calibrado: **0,103**.
* `each_row_owns_exactly_one_field` pegou o **truncamento** na conversão pista → `u16`: com zero casas o
  readout mostra **135** e eu guardava **134** — *seed ≠ sample*, a doença de quatro módulos.
* `arch_safe_clamp_only`: `f32::from(u16::MAX)` **não é literal** ⇒ `safe_clamp`.

**7 mutações, 7 sangram.** ⚠️ **E a primeira versão do gate de custo era uma RAZÃO ENTRE DOIS DOENTES** —
a doença que o campo de smear do Painter já pagou. O argumento `&brush.alpha_frame()` é avaliado **no
sítio da chamada**, antes de o padrão decidir se o usa, então a mutação encarecia os **dois** braços por
igual (0,155 → 0,602 no isotrópico e 0,168 → 0,637 no direcional) e a razão não se movia. O oráculo que
**discrimina** é o próprio rotor ser `O(graus)`: **o custo de um dab não pode depender do ÂNGULO** —
produto **0,84×**, mutação **5,41×**.

**Splits por RESPONSABILIDADE** (os dois tetos de LOC): `alpha.rs` **804 → 570** + `alpha_frame.rs`
(*em que direção* × *o que o padrão é*) + `alpha_scale.rs` (*que tamanho esta malha comporta* — ela lê
adjacência e não conhece uma fórmula sequer), e ⚠️ **os gates da escala viajaram junto com o assunto
deles**, porque um gate que citasse os privados dela de fora **não compila**, que é a forma certa de
descobrir que um teste ficou órfão do que ele mede. `sculpt3d_scenes.rs` **609 → 469**.

**Dep nova: `ph2d-painter-brush` na `ph2d-sculpt3d` — O ROTOR, e só ele**, pela MESMA razão que a
`ph2d-light` já escreveu no `Cargo.toml` dela: um `sin`/`cos` aqui daria outro número nos últimos bits
que a luz e o Jitter Rotate. Folha → folha, e ele **não traz a libm** (é transcendental-free por
construção) ⇒ esta crate continua `libm`-free.

`PROJECT_SCHEMA` **56 INTOCADO** · `SCULPT_DOC_VERSION` **1** · registro do `ph2d-ecs` **intocado** ·
contrato congelado **4/4 + 3/3** (por gate) · **nenhum ADR** (roda sob o ADR-0150).

**Aberto, com o preço medido:** o alpha por **IMAGEM** de verdade (uma imagem autorada, do sprite da
cena, pela porta `read_sprite_source` que o bake já usa). O eixo desta wave é o pré-requisito dele — uma
imagem precisa de um frame para ser projetada —, e o que sobra é a fronteira do `Copy`: **`Brush` é
`Copy` e é construído em ~20 arquivos**, então os pixels não cabem nele. As três saídas (um id + registro
na cena · `Arc` com o `Brush` virando `Clone` · a imagem como parâmetro do dab) têm preços diferentes e
nenhuma é mecânica.

---

### W12 — ✅ O PREVIEW DO PADRÃO (2026-08-08, **smoke OK 2026-08-08**, cena `=21`)

> Pergunta do Enio no smoke da W11: *"não seria interessante um preview ao mover os sliders das
> alphas? Desse modo o usuário veria como está antes de esculpir"*.

O swatch mora **logo abaixo das três pistas que o mudam** — a mesma decisão que já moveu a pista de
escala para junto dos chips: um controle e o que ele governa têm de estar no campo de visão um do
outro.

⚠️ **ELE MEDE UNIDADES DE OBJETO, e essa é a wave inteira.** Um swatch em unidades arbitrárias
abrangeria `N × escala`, teria sempre `N` features e sairia **idêntico em toda escala**: ele
responderia *"que padrão é este?"* — que os nove nomes já respondem — e ficaria mudo sobre a única
pergunta que o artista não consegue responder sozinho, que é ***este tamanho está certo para o MEU
modelo?***. É literalmente a pergunta que já reprovou um smoke deste módulo (*"os poros são
gigantescos"*). Ele abrange **1/8 do maior lado da peça**, e a densidade que ele desenha é a que o
pincel vai depositar.

**O CACHE não é otimização, é requisito — medido ANTES de escolher o tamanho:**

| grade | Noise | Strata | Scales | Scratches | Weave |
|---|---|---|---|---|---|
| 48² | 0,201 ms | 0,208 | **0,358** | 0,020 | 0,026 |
| 96² | 0,814 | 0,834 | **1,438** | 0,081 | 0,101 |

⚠️ **A 96² o pior padrão custa 1,44 ms — 8,6% de um quadro de 60 fps.** E a hora em que o cache
MENOS ajuda é a que importa: arrastando a pista, a chave muda a cada quadro e ele **nunca acerta**.
Daí a grade ser **64²** (pior caso ~0,64 ms; a família direcional, que é a razão de o preview
existir, custa 0,02-0,10). O Scales é o pior **por mecanismo**: o Worley visita 27 células por
amostra.

⚠️ **A chave carrega TODA entrada** — padrão, escala, azimute, elevação, tamanho do modelo e
**tema**. O modo de falha de esquecer uma não é um erro: é um preview **velho que ninguém vê que é
velho**, a frase que o cache dos planos da luz do impasto já pagou. O gate varre **campo a campo, um
por vez** — uma varredura que mudasse dois de cada vez não distinguiria *"a chave tem este"* de *"a
chave tem o outro"*.

⚠️ **E o AVISO é o que impede o preview de MENTIR:** o swatch desenha o campo **contínuo** e o barro
o amostra nos **vértices**. Abaixo da escala que a malha resolve, o artista veria um padrão lindo e
depositaria chuvisco — a lei das dez arestas. A condição é **DITA** numa linha, no molde do
`ao_stale`, e só existe quando há o que avisar.

**O snapshot ganhou `model_span`** — um FATO do modelo, como o `alpha_seed`. ⚠️ E ele **não é
derivável** daquele: o seed é `max(vão ÷ 33, 10 × aresta)`, e numa malha grossa quem vence é a lei
das dez arestas — o tamanho do modelo some da conta.

**Duas coisas minhas que o próprio código derrubou enquanto eu escrevia:**

* a 1ª versão dizia que a rampa saía em CINZA e *"quem a tinge é o `paint`"* — e o `draw_image_rgba`
  só **BLITA**: não existe estágio de tint depois dele. Ou a cor final sai do render, ou o swatch é
  cinza sobre um painel claro. ⇒ o **tema entrou na chave**;
* o doc do `#[path]` do `sculpt3d_scenes_tests.rs` afirmava que os testes leem `smoke_mesh`;
  **nenhum lê**. Eles leem `hooked_sphere` e `ridged_sphere`, que são privadas — a razão é outra, e
  agora está escrita certa.

⚠️ **A CÂMERA VIVA foi descartada de propósito:** a fatia é o plano **XY do objeto** (o que a vista
de repouso mostra), porque pôr a base da vista na chave faria **toda órbita re-renderizar** um
swatch que ninguém está olhando enquanto orbita. O limite — com o eixo inclinado para fora de XY o
swatch mostra a FATIA, não a face — fica **dito** no cabeçalho em vez de descoberto.

**5 mutações, 5 sangram.** ⚠️ A primeira M1 **nem compilava** e por isso não imprimiu nada — foi
re-armada de forma que compile antes de eu chamar qualquer coisa de sobrevivente.

**Dep nova:** `ph2d-vector` na `ph2d-panel-sculpt3d` — o `ImageQuality` e a porta de imagem da cena.
Desenhar 4096 retângulos é o que o espectrograma do áudio já recusou em letra (*"175k células não
são retângulos"*), e é a mesma aresta que o `panel-painter-layers` e o `panel-motion-graph` já
pagam. `cargo machete` limpo.

`PROJECT_SCHEMA` **56 intocado** · **nenhum ADR** · 7 gates novos + 26 de costura + 2402 no shell.

**Smoke: a MESMA cena `=21`** — passo **1b** (arraste a *Alpha Scale*: o quadro tem de mudar NA
HORA) e passo **3** (o preview e o barro têm de girar **juntos** — se só um girar, há duas respostas
para a mesma pergunta e a que mente é a que você está olhando).

---

### W13 — ✅ O PADRÃO É VISTO NO BARRO (2026-08-08, **smoke OK 2026-08-08**, cena `=21`)

> Correção do Enio, depois do smoke da W12: *"o preview no painel ficou OK, mas eu
> estava falando do preview como uma MÁSCARA NO OBJETO 3D"*.

O swatch da W12 desenha uma **fatia plana** e responde *"que densidade?"*. A
pergunta que sobra é a que só a peça pode responder — ***como este padrão se
deita na MINHA forma?*** —, e a resposta tem de ser desenhada onde a forma está.
Agora escolher um alpha **tinge o barro de violeta** com o padrão que o próximo
traço vai depositar; a caixa **Preview on Model** (nasce LIGADA) o desliga.

⚠️ **É o MESMO campo que o dab escreve, nunca um segundo.** O valor é o
`Brush::alpha_weight` na posição do vértice, freado pela máscara **pelo mesmo
predicado** que freia o dab (`Verb::paints_mask`) — um preview que pintasse
padrão sobre barro protegido prometeria um depósito que o traço recusa, e o
artista descobriria a mentira só depois de esculpir. O **falloff e a força ficam
de fora**: são do gesto, e aqui não há gesto; multiplicar pela `strength` faria
um pincel legitimamente fraco desenhar um preview quase invisível, que se lê como
*"o preview quebrou"*.

**A FASE 0 decidiu a forma da wave, e o número é grande** (`measure_alpha_over_a_whole_mesh`, pela porta do produto):

| malha | Noise | **Scales/Pores/Cracks** | Strata | Scratches |
|---|---|---|---|---|
| 96×144 (13,7k) | 1,23 ms | **2,33** | 1,25 | 0,12 |
| 533×800 (426k) — a cena `=21` | 37,9 | **63,7** | 36,4 | 3,8 |

⚠️ **64 ms são QUATRO quadros.** Um preview que recalculasse a malha inteira a
cada movimento do mouse faria o traço engasgar quanto maior a peça — literalmente
o defeito que o `upload.rs` da `ph2d-mesh-render` existe para não ter, um canal ao
lado. A lei é a mesma: ***o custo de um gesto é função da PEGADA, nunca do
documento***. Daí as **duas rotas**, que não são duas respostas à mesma pergunta:
a **CHAVE** mudou (outro padrão, escala, eixo, topologia) ⇒ o campo inteiro é
outro e nenhuma janela o descreve · um **dab MOVEU** vértices ⇒ o padrão é lido na
POSIÇÃO, então os que se moveram são exatamente os que ele deixou de descrever, e
a janela é a MESMA que a posição já sobe.

**O canal no device** é o gêmeo exato do `masks_of`, com uma diferença que decide
o desenho: a ausência **não é `Option` na malha, é um slice VAZIO vindo de fora**
— um plano na `Mesh` atravessaria a subdivisão, o remesh, o fechamento de buraco,
a fusão e o documento, cinco lugares que teriam de decidir o que fazer com ele.
⚠️ **Comprimento errado conta como AUSENTE:** um preview de outra topologia poria
valores válidos nos vértices errados, o modo de falha que nenhum olho pega. E
`DEFAULT_PREVIEW` mora na `ph2d-mesh` porque **dois crates que não se conhecem
precisam do mesmo número** (o kernel escreve isto para *"nada aqui"*, o
renderizador sobe isto para *"ninguém armou"*).

⚠️ **O tinto é VIOLETA e a escolha não é gosto:** o barro é claro e quente, a
máscara é AZUL-FRIA, o cursor é ÂMBAR — um preview em qualquer um dos três leria
como *"isto está protegido"* ou como *"aqui está a mira"*. E ele é mais **fraco**
que a máscara (0,45 contra 0,75), porque a pergunta dele se responde vendo a
forma por baixo. No shader ele entra **ANTES** da máscara: a proteção é a palavra
final, e o único caso em que os dois se encontram é o verbo de **Máscara**, que
não é freado por ela.

⚠️ **UM DEFEITO MUDO QUE EU ESCREVI E PEGUEI RELENDO O LAÇO:** a subida do canal
inteiro nasceu **dentro** do braço `Region` do `match line.job`. Com a chave
mudada e nenhum vértice movido — *girar o eixo não move um vértice* — o `dirty`
fica vazio, o plano cai em **`Skip`**, e a linha nunca rodaria no caso EXATO que
ela existe para cobrir: o barro com o padrão anterior enquanto o quadro do painel
já mostra o novo. **Dois previews discordando é pior que preview nenhum.** Subiu
para fora do `match`, com arch-gate próprio que afirma a **RELAÇÃO** (a chamada
vem depois do `}` que fecha o match) e nunca uma distância em bytes.

**Duas lições de gate, as duas minhas:**

* a 1ª versão do gate da correlação mediu **0,481** e eu quase a chamei de produto
  errado. O confundidor era a **fixture**: um dab deposita `alpha × falloff`, e num
  falloff graduado o segundo fator varia por toda a pegada. Com o disco duro
  (`Falloff::Constant`) o depósito é `alpha` vezes uma constante, e o que sobra na
  medição é exatamente a afirmação em teste;
* a mutação do **DEFAULT sobreviveu**, e o buraco era meu: todos os gates declaram
  o `alpha_preview` que querem, então nenhum testava o valor de fábrica. ***Um
  default só é testado por um teste que não o menciona*** — o gate novo tira o
  valor do estado de fábrica e afirma a CONSEQUÊNCIA (há o que desenhar), não o
  literal. E ele **DIZ o que não prova**: a cena carrega o próprio literal e o
  construtor dela exige um `wgpu::Device`.

⚠️ **E um vermelho LATENTE da W12 fechou de carona:** o `no_magic_numeric` acusou
dois literais no swatch da wave anterior — o gate mora em
`ph2d-editor-core/tests/` e **só corre na varredura impactada**, a mesma causa
estrutural que physics, motion-value e Vector já documentaram três vezes.

**8 mutações, 8 sangram** · **44/44 gates de GPU verdes na RTX** (o pipeline com o
8º buffer de vértice valida) · `PROJECT_SCHEMA` **intocado** (nada disto é
serializado — o preview é derivado do pincel vivo) · contrato congelado intacto ·
**zero dep nova, zero crate nova, nenhum ADR**.

**Smoke: a MESMA cena `=21`.** Passo **1b** — escolha *Strata* e o barro tem de
ficar TINGIDO na hora. Passo **3** — arraste *Pattern Angle* até 0 e as camadas
têm de ficar de pé nos **três** ao mesmo tempo (o quadro, o tinto, o relevo); se
um girar sozinho, **PARE**. Passo **3b** — esculpa uma cova funda e olhe o padrão
dentro dela: ele é lido na POSIÇÃO do vértice, então acompanha a forma nova.

---

### W14 — ✅ O EXTRACT: A MÁSCARA VIRA UMA PEÇA (2026-08-09, **smoke OK 2026-08-09**, cena `=22`)

> Aprovada pelo Enio — *"smoke OK. Siga"*.
>
> O item **`mask extract`** que o cabeçalho desta wave declara desde o começo, e que o censo de
> `03.4` marca como W9 (*cria malha*). Era o buraco: `Add` faz primitivas e `Duplicate` copia
> inteiro — **não havia gesto que fizesse uma peça NOVA a partir de uma que já existe**.

Pinte a máscara, aperte **Extract Mask**, e a região sai como um objeto. É o *Subtool > Extract* do
ZBrush e o *Mesh > Extract Mask* do Blender: a armadura, a roupa, a casca.

**Dois knobs, e os dois têm o número MEDIDO ao lado:**

* **Extract Thickness** (`−0,5 … 0,5`, default 0,05) — a espessura da casca no espaço local.
  **Zero é uma FOLHA** (uma camada só, aberta), positivo cresce para fora, negativo para dentro.
* **Extract Smooth** (`0 … 8`, default 3) — as passadas de relaxamento da costura.

#### O ERGUIMENTO não pode ser um comprimento, e é a Fase 0 que decide

A casca é erguida um pouco acima da superfície de onde saiu, para não brigar por profundidade com
ela. A referência ergue **0,01 absoluto** — e a sonda `measure_extract` mediu a aresta mediana das
fixtures desta casa:

| malha | verts | aresta mediana | o que 0,01 valeria |
|---|---|---|---|
| `uv_sphere(24,32)` | 738 | **0,13081** | 7,6% de uma aresta |
| `uv_sphere(64,96)` | 6.050 | 0,04908 | 20% |
| `uv_sphere(128,192)` | 24.386 | **0,02454** | **41%** |

⚠️ **5,3× de variação entre as nossas próprias fixtures**: o mesmo número significando duas coisas,
e na fina a casca **visivelmente FLUTUA**. O erguimento é **fração da aresta** (`5%`), e o gate
`the_lift_is_a_fraction_of_the_edge_not_a_length` afirma a RAZÃO entre duas resoluções (medido
**5,33**, exatamente a razão das arestas) — um comprimento absoluto dá 1,0 e sangra.

#### A COSTURA vem da TOPOLOGIA, e é aqui que isto deixa de ser um port

A referência marca fronteira por **VÉRTICE** (*as duas pontas fora da máscara, ou na beira*). A
pergunta exata é sobre **ARESTAS**: *quantas faces recortadas te contêm?* Uma é fronteira, duas é
miolo — a definição, não uma aproximação dela.

⚠️ **E a diferença está medida, não afirmada.** Numa calota com um **risco** de um vértice de
largura atravessando a máscara — o que uma mão pintada deixa —, a regra por-vértice costuraria
**96** arestas e a por-topologia costura **32**: as 64 extras são uma ponte atravessando a peça
**pelo meio**. O gate se RECUSA a passar se a fixture não contiver o fenômeno (a primeira versão
dele reprovou por isso: a faixa que eu escolhi fazia as duas regras concordarem, `64 = 64`).

De graça: uma aresta de **beira da malha original** dentro do recorte tem uma face e portanto **é**
costurada — o caso que a referência trata à parte.

#### A porta única do laplaciano, e a prova de que ela é uma só

O relaxamento da costura é o **mesmo** laplaciano do verbo Smooth, com as **mesmas duas regras de
borda**. Ele saiu do `ph2d-sculpt3d::stroke_target` para **`ph2d-mesh::ring_average`**, e a posição
chega por **closure** — o traço lê o `pre` CONGELADO (é o que o torna idempotente), o extract lê a
posição viva. A lei é a mesma; a fonte da posição não é.

⚠️ **A mutação que prova a porta:** neutralizar a regra de borda derruba **o gate do extract E o
gate pré-existente do verbo Smooth** (`smoothing_the_lip_of_an_open_mesh_does_not_suck_it_inward`).
Duas cópias divergiriam exatamente onde já custou uma medição.

⚠️ **E ela dá duas condutas certas com uma lei só:** numa casca FECHADA a costura não é borda, então
o anel inteiro entra e o lábio **arredonda**; numa FOLHA a costura **é** a borda, então ela desliza
ao longo de si mesma e o serrilhado se acalma **sem** o trecho ser sugado para dentro.

#### O que a medição derrubou de mim

⚠️ **O primeiro gate afirmava *"relaxar não ENCOLHE"* e ele reprovou** — alcance caindo **3,4%** em
seis passes. O encolhimento é **real e é geometria**: a borda de uma calota é um círculo, e a média
de um ponto com os dois vizinhos dele num círculo cai na **CORDA**. É encurtamento de curva, e todo
relaxamento de borda o tem. O que a regra de borda de fato compra é outra coisa, e é ela que o gate
mede hoje: a beira **não SOBE para dentro da folha** (y médio `−0,0000 → −0,0000`).

⚠️ **E o relaxamento SATURA** — medido: `−1,92%` na 1ª passada, `−3,39%` na 2ª, e **nunca mais se
move** (idêntico até 24 passes). Ele equilibra contra o anel fixo em volta; não é curve-shortening
correndo até um ponto. Numa costura **serrilhada**, que é o caso do artista, a rugosidade da beira
vai de **0,09369 a 0,05117 em oito passadas (−45%)** e da oitava em diante cada uma compra **0,4%**
— é esse número, e não um gosto, que põe o teto do slider em **8**.

#### A UI, as quatro condições

O botão **Extract Mask** e os dois knobs moram **no fim da seção Brush, logo abaixo das quatro
operações de máscara** — a mesma frase que pôs as outras quatro ali: *quem acabou de pintar uma
máscara procura o que fazer com ela onde a pintou*. A **consequência** aparece na seção da cena, na
contagem de peças.

⚠️ **`Row::in_tail` (um `bool`) virou `Row::place` (três valores).** O campo já respondia *onde esta
row é desenhada*; enquanto havia só *bloco* × *cauda*, dois estados bastavam. Os dois números do
extract são **argumentos de um botão** que mora no fim da seção, e pintá-los junto às pistas do
alpha os separaria do gesto que os lê. Um `bool` com um `if` por id ao lado seria a enumeração que
apodrece na quarta row.

⚠️ **Nenhuma tecla nova, de propósito.** O comentário da CRASE já enumera o teclado inteiro tomado
(`C`/`I`/`B`/`N` são máscara, `Q`/`E`/`R`/`F` a luz, …); inventar um acorde para um botão que o
painel já tem seria uma segunda porta para o mesmo gesto.

⚠️ **O painel ganhou `ph2d-mesh` como dep, e é a porta única que a exige:** o `Sculpt3dUi.extract` é
o **tipo do KERNEL**, então o `Extract::default()` é a única fonte dos dois defaults. Dois `f32`
soltos seriam um segundo lugar para eles morarem, e o painel mostraria o antigo no dia em que o
kernel mudasse o dele.

#### O que ele NÃO recusa, e por quê

⚠️ **O extract NÃO recusa com a pilha de multires montada**, ao contrário do remesh, do tapar buraco
e da fusão. A diferença é o mecanismo: aqueles trocam a **BASE**, e todo nível acima dela é uma
subdivisão que passaria a descrever uma malha que não existe mais. Extrair **não toca a origem** —
ele lê o nível vivo e escreve noutro objeto. Nem a máscara é consumida: extrair de novo com outra
espessura é o gesto normal, e é o que o ZBrush faz.

**9 gates · 6 mutações, 6 sangram** (a ponte com o enrolamento trocado · a fronteira por-vértice · o
erguimento absoluto · a casca para fora sem inverter · a regra de borda na porta compartilhada · o
relaxamento em Gauss-Seidel). ⚠️ **O oráculo central não confere a costura que eu derivei** — ele
pergunta se a peça é uma superfície **fechada, coerentemente enrolada e de volume positivo**, e são
três metades: *fechada* não vê o enrolamento trocado, e *coerente* não vê a peça inteira invertida.

**Nenhum schema** (`PROJECT_SCHEMA` intocado — a peça nova é um objeto da cena, que o documento da
W8.3 já sabe guardar) · registro do `ph2d-ecs` **intocado** · **nenhum ADR** (roda sob o ADR-0150) ·
contrato congelado intacto · **nenhuma dep externa nova** (`ph2d-mesh` já estava no grafo, via
`ph2d-sculpt3d`).

**Smoke: `PH2D_SCULPT3D_SMOKE=22`** — ⚠️ **duas esferas, e a assimetria é o smoke inteiro:** a da
DIREITA já vem mascarada (aperte e julgue a **geometria**, sem o confundidor de ter acabado de
pintar), a da ESQUERDA vem **nua** (pinte a sua e extraia — é a metade que prova a costura *pincel →
botão*). A cena **imprime quantos vértices ela mascarou**; se a linha não aparecer, **PARE**.

### W15 — ✅ O TRANSFORM: A MÁSCARA MOVE (2026-08-09, **smoke OK com correção**, cena `=23`)

> Smoke do Enio: *"Smoke OK. Mas a direção da rotação do mouse está invertida em relação à rot do
> objeto e é imprecisa (não consistente)"* — as duas queixas viraram a **W15b** logo abaixo, e o
> resto da wave passou.

> O item **`transform afim ponderado pela máscara`** que o cabeçalho desta wave declara desde o
> começo — *a metade do `Transform.js` que não é o gizmo* —, e que o censo de `03.4` marca como W9.
> Com ele, o cabeçalho da W10 fica com **um** item aberto (IBL) e o alpha por IMAGEM.

Pinte a máscara, arme **Move / Rotate / Scale**, arraste com o esquerdo: a parte **LIVRE** se move,
a protegida fica, e a banda entre as duas acompanha suavemente.

#### A LEI não é a da referência, e a Fase 0 mediu a diferença

O `Transform.js` aplica a matriz e **lerpa a POSIÇÃO** pelo peso
(`vAr[j] = x*(1-m) + transformado*m`). Para uma **translação** isso é exato. Para uma **ROTAÇÃO**
não é: o lerp corta pela **CORDA**, e o vértice cai *dentro* do círculo.

| θ | w | r antes | **fracionária** | lerp | previsto |
|---|---|---|---|---|---|
| 90° | 0,500 | 1,00000 | **1,00000** | 0,70711 | 0,70711 |
| 180° | 0,257 | 0,97003 | **0,97003** | 0,47140 | 0,47140 |
| 180° | 0,500 | 1,00000 | **1,00000** | **0,00000** | 0,00000 |

A meia volta com meio peso o lerp **colapsa o vértice sobre o eixo**. ⚠️ E a banda de peso
intermediário é *exatamente* o que um pincel de máscara macio pinta — o defeito mora onde o artista
trabalha, não numa borda exótica. É a mesma doença que a `line/FLIP` nomeou no tween v2 (*"um lerp
corta pela CORDA, então todo giro encolhia o traço"*), e a cura é a mesma: **a fração cai no
MOVIMENTO, nunca na posição** — `p = pivô + R(eixo, w·θ)·(pre − pivô)`, `p = pivô + (pre − pivô)·s^w`.
Com `w = 1` os três reduzem ao transform inteiro e com `w = 0` à identidade: **a representação apaga
o caso especial**, e não há lerp a corrigir porque nunca se interpola uma posição.

⚠️ **A previsão fechada da sonda nasceu ERRADA e a medição a corrigiu:** eu derivei `r·cos(w·θ/2)`
por analogia com a corda do meio-ângulo (0,92388 previsto contra **0,70711** medido). A verdadeira é
`r·√((1−w)² + w² + 2w(1−w)·cos θ)`, e com ela a coluna bate nas **doze** linhas — é isso que a torna
um oráculo em vez de uma segunda medição.

#### As decisões que o resto herda

* **O peso é `1 − máscara`, por PORTA ÚNICA** (`mask_ops::free_weight`). A pergunta *"que fração
  deste vértice o gesto pode mover?"* tem dois perguntadores — todo dab e este gesto —, e a
  convenção é **INVERTIDA em relação ao SculptGL** (*a armadilha nº 1 de todo port desta área*).
  ⚠️ A prova de que é uma porta só é executável: inverter a função derruba **58 gates**, os 3 do
  transform e os 55 do traço.
* **O pivô é o centroide PONDERADO** do que se move — onde o ZBrush põe o gizmo. Um centroide simples
  giraria a parte livre em torno do meio da peça INTEIRA, incluindo o que está pregado.
* **O `pre` congelado + o gesto TOTAL**, nunca um incremento: um transform incremental faria o
  resultado depender da **taxa de polling**, e girar 90° num tranco daria outra forma que girar 90°
  em vinte passos (gate próprio).
* **O vértice totalmente protegido não entra na lista** — não é lido, não é escrito, não aparece na
  janela do undo. É a escolha da referência (`getUnmaskedVertices` filtra `mask > 0`) e aqui ela
  também **impede deriva numérica** em quem o artista mandou não se mexer.
* **ZERO variant de undo novo:** a janela é `moving()` e o *antes* é `base_positions()` — exatamente
  a forma que o `StrokeUndo::Stroke` já guarda.

#### Sem gizmo, e a porta de armar é o PAINEL

O gizmo 3D de treze alças da referência não porta (o gizmo deste repo é o de **SPRITE**,
ADR-0110/0111). O substituto não é compromisso: é a **outra** metade do desenho do Blender — o
*modal transform*, armado e executado por arrasto, que encaixa num `Drag` a mais.

⚠️ **A porta é o painel por ELIMINAÇÃO, não por gosto:** o `sculpt3d_keys` declara, com a lista ao
lado, que *"o que sobra livre no app inteiro é a crase"*; e `Shift+G` também não serve, porque o
`match` dos verbos não olha o `shift` e já significa *pegar o Move*. Os três chips ficam ao lado do
Extract — quem acabou de pintar uma máscara procura o que fazer com ela onde a pintou. Clicar o aceso
**desarma**, e quem faz essa conta é a CENA (`arm_transform`), uma vez.

**Custo MEDIDO** (`measure_transform`, 98k vértices): aplicar **0,277 ms**, refresh **2,094** — o
refresh é **88%** do evento, e é linear. ⚠️ Ele mora dentro do `apply`, como no `SculptStroke::dab`:
quem escreve posições é quem sabe quais escreveu, e um chamador que esquecesse de o pedir ficaria com
normais velhas — que não falham, **escurecem**.

**12 gates de kernel + 3 de painel + 2 arch-gates de shell · 8 mutações, 8 sangram.** ⚠️ E a rodada
de mutação achou **três defeitos meus**: um gate que era **ESPELHO** (computava o esperado chamando a
própria porta sob teste, e ficou verde no meio dos 58 que sangravam) · uma fixture de pivô com
máscara **DURA**, onde ponderado e simples são o mesmo ponto (a mutação sobreviveu à suíte inteira) ·
e um oráculo que pedia **composição entre sessões**, que nenhuma escala ponderada tem (o pivô
re-derivado anda, porque `s^w` correlaciona com `w`) — trocado pela **simetria**, que discrimina as
duas leis e é o que o artista sente.

**Nenhum schema** (`PROJECT_SCHEMA` intocado — nada disto é serializado) · registro do `ph2d-ecs`
**intocado** · **nenhum ADR** (roda sob o ADR-0150) · contrato congelado intacto · **nenhuma dep
externa nova**.

**Smoke: `PH2D_SCULPT3D_SMOKE=23`** — ⚠️ **duas esferas, e a da direita tem máscara MACIA de
propósito:** uma máscara dura faria o transform mover meia esfera rigidamente, o que é correto e
**cego à lei** (as duas interpolações concordam em todo vértice de peso 0 ou 1). A cena imprime
quantos vértices estão na BANDA; se a linha não aparecer, **PARE**. E a pergunta da wave é de olho:
**gire meia volta e a cintura tem de continuar redonda** — se ela pinçar como uma ampulheta, é a lei
antiga de volta.

---

### W15b — ✅ O GIRO ACOMPANHA A MÃO (2026-08-09, **smoke OK**, mesma cena `=23`)

> Aprovada pelo Enio — *"Perfeito! Smoke OK!"*. Report que a abriu, no smoke da W15: *"a direção da rotação do mouse está invertida em relação à rot do
> objeto e é imprecisa (não consistente)"*.

**As duas queixas eram TRÊS defeitos, e uma medição os separou.** A sonda dirige o gesto REAL
(`arm_transform` → `begin_transform` → `transform_at`) e julga **na tela**: o ângulo que um vértice
percorre em torno do pivô projetado, contra o que o DEDO percorreu em volta do mesmo ponto.

| dedo (anti-horário) | peça | razão |
|---|---|---|
| 30° | −9,24° | **−0,308×** |
| 90° | −37,08° | **−0,412×** |
| 180° | −80,85° | **−0,449×** |

O **sinal** (o barro vai para o outro lado), a **magnitude** (menos de metade do pedido) e a
**inconsistência** (a razão muda com a amplitude, então não há número que o artista aprenda). Depois:
**1,000× em 30°, 90°, 180° e 360°**.

#### A causa: as duas metades do giro saíam do PEN-DOWN, e nenhuma do pivô

* **O EIXO** era o raio do pixel de pen-down, que aponta **para DENTRO da tela** — e Rodrigues gira
  no anti-horário visto da PONTA do eixo, então um eixo virado para dentro inverte a manipulação.
  ⚠️ **O mecanismo já estava escrito neste repo**, no kernel do Twist: *"O eixo é MENOS o olho, e o
  sinal é a ferramenta inteira"* (`stroke_target.rs`, que cita o `vec3.negate` do `Twist.js`). Lá a
  negação mora no consumidor; o `MaskTransform` aplica Rodrigues cru, e a W15 entregou-lhe o eixo
  sem virar. *A lição estava num doc-comment a dois arquivos de distância.*
* **E o eixo não passava pelo OLHO.** Uma reta que passa pelo olho projeta num **PONTO**, então girar
  em torno dela deixa o pivô parado e a silhueta rodando em volta dele; o raio do pen-down é outra
  reta — medido, ela inclina **3,2° a 50 px** do pivô e **19,2° a 340 px**, e o que se vê nessa
  inclinação é a peça **cambalhotando**. O eixo passou a ser `normalize(olho − pivô)`
  (`Sculpt3dScene::view_axis_local`), que resolve o sinal e a inclinação **na mesma expressão**.
* **O CENTRO da varredura** era o pixel de pen-down e passou a ser o **pivô projetado**. Com o centro
  no dedo, uma volta inteira em torno do pivô gira a peça **meia** (teorema do ângulo inscrito) — e a
  razão varia com o percurso, que é literalmente o *"não consistente"*.
* **E a varredura nasce ARMADA no pen-down** (`arm_sweep_at`). O acumulador só soma quando já existe
  uma referência, então o trecho entre o pen-down e o primeiro `move` sumia — um déficit **constante**
  que nunca volta. Ele apareceu sozinho na medição: 25° para 30°, 85° para 90°, 175° para 180° —
  **exatamente um passo de amostragem, sempre**. ⚠️ **O Twist continua a perdê-lo, de propósito:** lá
  o centro é a âncora, cujo pixel *é* o pen-down, e a direção de um ponto até ele mesmo não existe.

⚠️ **`swept_angle` virou `swept_angle_about(center, …)`** — uma função, dois chamadores, cada um
nomeando o seu centro (o Twist a âncora, o transform o pivô). Enquanto o Twist era o único, *centro*
e *pen-down* eram a mesma coisa, e foi essa coincidência que o transform herdou sem reparar. A zona
morta virou a porta `sweep_dir`, porque agora quem ARMA e quem ACUMULA têm de concordar sobre
*quando não há direção*.

**2 gates de comportamento + 1 arch-gate · 5 mutações, 5 sangram.** ⚠️ **A fixture nasceu SEM o
fenômeno:** enquadrada, a câmera põe o pivô no centro da imagem, onde a reta olho→pivô **é** o eixo
óptico — o espalhamento entre quarenta vértices media `0,000°`, que parece esplêndido e é o gate a
medir o caso em que toda escolha de eixo paralela à vista acerta. Com a vista deslocada, o espalhamento
honesto é **3,94°** (a projeção de uma rotação é uma homografia, não uma rotação) contra **61,8°** do
eixo do pen-down — e a barra do gate ficou **entre os dois números medidos**, não num redondo.
⚠️ E o oráculo teve de ACUMULAR: um ângulo lido de uma foto única vive em `(−180°, 180°]`, e a
primeira versão mediu 180° de dedo e imprimiu `−180°` — verde e vermelho indistinguíveis exatamente
na amplitude mais interessante.

⚠️ **Um arch-gate existente falhou ALTO e isso é o desenho dele:** a zona morta mudou de casa, o
`function_body` não achou `fn swept_angle` e **panicou** em vez de varrer o vácuo. Reancorado na
propriedade, na casa nova. E o arch-gate novo (`the_rotation_takes_its_axis_and_its_centre_from_the_pivot`)
existe porque os dois gates de comportamento são **`#[ignore]`** — a cena 3D exige um device, o
`ship` não os corre, e uma regressão que devolvesse o eixo ao pen-down passaria pela CI inteira.

**Nenhum schema · nenhum ADR · nenhuma dep · contrato congelado intacto.**

**Smoke: a mesma `PH2D_SCULPT3D_SMOKE=23`, passo (1b)** — arraste em CÍRCULO em volta da esfera: o
barro segue a mão, **mesmo sentido e volta por volta**. Faça com o pen-down LONGE do centro e depois
PERTO; tem de dar o mesmo.

---

### W16 — ✅ O AMBIENTE TEM DIREÇÃO (2026-08-09, **pendente de smoke**, cena `=24`)

> O item **IBL** do cabeçalho da W10 (`docs/3D/05.1` §5). Com ele o cabeçalho fica com **um** item
> aberto: o alpha por IMAGEM, cujo preço já está medido na W12.

O piso da difusa era **um número para toda direção** (`ph2d_light::AMBIENT`, 0,35): duas faces na
sombra, uma olhando para cima e outra para baixo, saíam **idênticas** — na região que a lâmpada não
alcança, a escultura não tinha leitura de forma nenhuma. Agora ele é o MESMO número **redistribuído**:
céu em cima, ricochete do chão embaixo.

#### A Fase 0 escolheu o modelo, e derrubou o candidato mais sofisticado

| ambiente | cima | baixo | contraste | zonal de 3 termos vs a integral |
|---|---|---|---|---|
| **hemisfério (linear)** | 0,6106 | 0,2775 | **2,20×** | **0,000000** |
| três zonas (céu · horizonte claro · chão) | 0,6764 | 0,3699 | 1,83× | 0,004195 |

⚠️ **O ambiente mais rico mede PIOR na coisa para a qual o termo existe** — a forma de um HDRI de
estúdio tem o horizonte mais claro que o zênite, o que *reduz* o contraste cima/baixo. E o linear não
é o barato: um ambiente linear na altura **não tem termo de grau 2**, então `E(n) = c + (2/3)·k·(n·cima)`
é a resposta EXATA (o `2/3` é o `Â₁` da convolução zonal com o lóbulo cosseno — Ramamoorthi &
Hanrahan 2001), medida em **3e-6** contra a integral numérica.

#### As decisões

* **A média é PRESERVADA em luminância** (`ENV_BASE` tem luminância exatamente 1, com gate): ligar o
  termo não clareia nem escurece a peça, ele tira luz de baixo e põe em cima. É o que separa *"o
  ambiente tem direção"* de *"a cena ficou mais clara"*. ⚠️ A **chroma** não é preservada, de
  propósito: a sombra de um estúdio é FRIA, e isso é metade do efeito.
* **Ancorado na TELA, não no mundo**, porque as lâmpadas deste rig são autoradas em azimute/elevação
  de tela: um estúdio cujo céu gira enquanto as luzes ficam paradas não é um estúdio.
* **O céu é `−y`**, porque o frame do rig tem `y` para BAIXO. ⚠️ É a mesma negação que o
  `canvas_normal` já paga, e o oráculo dela é um RENDER: com o sinal trocado o barro parece iluminado
  de um porão, e **as duas versões computam um gradiente perfeitamente bem-formado**.
* **O piso virou vetor e a dobra não mudou:** em `ratio = 1` o resultado é `1` para QUALQUER piso, então
  o termo redistribui a SOMBRA e não toca no que está aceso.
* **Sob um MATCAP o slider some**, porque um matcap **já É um ambiente** (uma esfera de iluminação
  capturada) — somar o nosso seriam dois, e o knob seria um controle que não faz nada.

#### ⚠️ O default nasceu ERRADO e DOIS gates de GPU o corrigiram

Eu o pus em **1** pela régua *o canal existe?* (o precedente do `ssao`). A régua certa é a que a
**cavidade** já escreve uma constante acima — *este canal muda a leitura de toda escultura que já foi
feita?* — e quem cobrou não foi um argumento: foi o
`the_two_lights_agree_where_the_form_turns_away`, que afirma que **a luz do BARRO e a da TINTA
concordam onde a forma vira**. É a carta da `ph2d-light` em forma executável, e um piso direcional só
no barro as separa. **`DEFAULT_ENV = 0.0`.**

⚠️ **Levantar o slider continua divergindo, e isso é ACEITO:** é a mesma classe de escolher um
matcap — um modo de VISTA do viewport, que ninguém chama de defeito. O que não se pode é entregar a
divergência como o estado inicial.

⚠️ **A adoção pela TINTA é o follow-up, com o preço medido:** o `channel` dos dois passes do impasto
(GPU e CPU) **não recebe a normal** — ela morre upstream —, então levar o ambiente para lá é enfiar um
parâmetro pela via quente do Painter, com paridade CPU/GPU pinada byte a byte. É wave do dono daquele
módulo. **Gatilho:** o Enio aprovar o look no barro.

**3 gates de lei + 1 mirror de WGSL + 1 render + 1 de seam · 5 mutações, 5 sangram.** ⚠️ **A fixture do
render nasceu errada TRÊS vezes, e as três correções são a mesma lição:** a `H/3` os pontos caíam no
FUNDO (`0.0` nos quatro) · na coluna central eles mediam **178** porque o modelo é RELATIVO e uma
lâmpada rasa deixa tudo o que olha para o observador *aceso* · e a primeira asserção esperava que o
topo CLAREASSE, quando ele escureceu de **254,0 para 248,8** — ali `ratio > 1`, e um piso maior
COMPRIME, que é o que um ambiente faz numa imagem de verdade. A fixture final põe a lâmpada **de
lado** e mede do lado oposto: **desligado 62,6 / 62,6 — idênticos**, que é o defeito; ligado
**75,2 / 50,4**.

**Nenhum schema** (`project.rs` com diff vazio) · registro do `ph2d-ecs` intocado · contrato congelado
**3/3 + 4/4** · **nenhuma dep nova** · **nenhum ADR** (roda sob o ADR-0150).

**Smoke: `PH2D_SCULPT3D_SMOKE=24`** — uma esfera com rugas EM ESCADA (a mesma malha da `=15`, e as
duas não se confundem: lá a cavidade nasce em zero e o smoke a LIGA, aqui o ambiente nasce em zero e o
smoke o LEVANTA). ⚠️ A cena imprime o contraste que ela promete; se a linha não aparecer, **PARE**. E a
pergunta é de olho: **o claro tem de ficar EM CIMA** — se a sombra clarear por baixo, o céu está no
chão.

---

# Performance — o mecanismo, e o que ele NÃO é

> Enio: *"Quero performance acima do ZBRUSH e BLENDER."*

| | ZBrush | Blender | PH2D Sculpt |
|---|---|---|---|
| Linguagem do hot path | C++ | C | **Rust nativo** |
| Paralelismo do pincel | limitado | parcial (PBVH) | **`rayon`, 32 threads** |
| Escopo por dab | pegada | pegada (PBVH) | **pegada, com octree** |
| Render | próprio | EEVEE/OpenGL | **wgpu, com o compositor que já existe** |

**E isto NÃO é vitória declarada.** O gate continua sendo:

> **A mesma malha, o mesmo gesto, os três apps, cronometrados.** Enquanto essa tabela não existir, a frase
> fica entre aspas no pedido, não no nosso relatório.

## Alvos a MEDIR (não são caps)

⚠️ **Nenhum número abaixo entra no código.** O `CLAUDE.md` §0 é explícito: *antes de escrever qualquer
limite, MEÇA — e escreva o número que a medição deu, com a tabela ao lado.*

| Sonda | O que mede | Estado |
|---|---|---|
| `measure_brush_kernel` | ms por dab × vértices afetados | ✅ W1 — **K1 dispara só a 19% de cobertura** |
| `measure_normals` | ms para recomputar a vizinhança tocada | ✅ W1 — **1,66 de 13,1 ms**; o custo é a descoberta |
| `measure_remesh` | ms de voxel remesh por resolução | W7 |
| `measure_upload` | MB/s e ms de sincronização por traço | W1 parcial |
| `measure_memory` | MB residentes por contagem de triângulos (**dhat**) | W6 (a subdivisão é quem a estressa) |
| `measure_gbuffer` | ms por objeto na rota ao vivo | ✅ W3 — **5,94 ms a 1024²** |
| `measure_stroke_e2e` | ms do evento de ponteiro ao pixel | W4 (com o espaçamento, ela muda de forma) |
