[package]
name = "ph2d-node-vector-llm-shape"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
license.workspace = true
authors.workspace = true

[lib]

[dependencies]
ph2d-nodegraph = { path = "../ph2d-nodegraph" }
ph2d-node-registry = { path = "../ph2d-node-registry" }
ph2d-vector-doc = { path = "../ph2d-vector-doc" }
ph2d-vector-graph = { path = "../ph2d-vector-graph" }
# The pure, deterministic LLM4SVG → VectorNetwork pipeline this node wraps
# (parse → sanitize → lower). The node is Effect::Pure over a host-cached
# response; the async LLM call lives in ph2d-vector-llm-client, not here.
ph2d-vector-llm = { path = "../ph2d-vector-llm" }
