# Timeline — estado do §5 em 2026-08-18

> **Arquivo histórico.** Este é o texto do `CLAUDE.md` §5 **verbatim**, como estava em
> 2026-08-18, antes de o §5 voltar a ser um roteador. O **estado vivo** (o que está aberto,
> o handoff corrente) fica no [`CLAUDE.md`](../../../CLAUDE.md) §5; o **mecanismo** fica nos
> handoffs que este texto cita. Nada aqui foi editado — só recortado.

---

- **Timeline** (`ph2d-panel-timeline` + `ph2d-timeline` + `ph2d-anim`, landou 2026-07-09): dope-sheet + graph editor + transporte sobre `ph2d-core::Playhead`. **W2** (transporte, régua, faixas/lanes, zoom/pan ancorado, box-select, `F`=fit, copy/cut/paste, `Ctrl+D`) + **W3** (curva por-faixa, handles bézier, âncora arrastável em (tempo, valor), menu de presets no botão direito, easing em massa) + canal **Summary** (linha-mestra que agrega keys por tempo). Chrome handler `timeline_segment` (`z=290`, dispatch GERADO por `ph2d-chrome-sync`); `IconId::SkipBack`/`SkipForward`. **Gotcha:** a captura do dope-sheet em `pointer_down.rs` **não pode engolir o Secondary** (senão o menu de presets nunca abre) e o menu já está FECHADO quando o Click chega (leia `context_menu().or_else(last_context_menu())`). **Gotcha (auto-key):** o passe `autokey_pass` fica **inerte durante o play** (`playhead.is_playing()` em `apply_samples`) — durante o play a pose é dirigida pela animação, não pelo usuário; o apply escreve `world = curve(t raw)` a cada frame e o diff off-curve compara contra `curve(t snap)`, então sem esse gate cada frame gravava uma key espúria (Play+AutoKey = key por quadro). Performing (gravar no play) é W5. **Pin de pose deslocada (2026-07-11):** DESARMADO + pausado, pose bound fora da curva = usuário posando pro K manual → entra em `AutokeyState.displaced` e o apply **pula** a entidade (senão o snap-back tornava impossível criar o 2º key sem AutoKey); o pin morre quando o playhead move (Blender: scrub/play descarta pose não-keyada) ou a pose volta à curva (K/undo). **Bindings sobrevivem ao objeto (2026-07-11):** binding missing (objeto deletado) **some do painel** (snapshot pula) mas fica dormente no doc; `timeline_persist::upkeep` (por-frame, zero-alloc se nada missing) re-stampa wire_ids por `Name` e **reconecta** quando um objeto de mesmo nome volta — o undo global RESPAWNA entidades (bits novos, mesmo Name), então delete+undo cura a animação. **Defaults de UX (Enio 2026-07-11):** `column_lock` **aberto** por default (clique em key seleciona SÓ a key; padlock fecha p/ colunas alinhadas) · R-click num summary de coluna **já selecionada** retuna a **seleção inteira** (espelho do scope Key), não colapsa pra coluna clicada. **W4.T5 (landou):** os scaffolds de debug `timeline_smoke.rs` (`PH2D_TIMELINE_SMOKE=1`) + hook **KeyB** (bind demo `SpriteAnimation`) foram **aposentados** — a autoria real (o painel) os tornou obsoletos; **tecla B livre** (cai no `_ => {}`). `apply_sprite_animations` fica (caminho por-componente programático, no-op vazio). **Speed graph (W5, landou 2026-07-11):** 2ª vista do graph editor plotando **velocidade** (`d(value)/dt`, estilo AE/Cavalry); toggle **Speed** panel-local na barra de transporte (`state.speed_view`, não-doc). Math: `ph2d_anim::Interp::slope(u)` = derivada **ANALÍTICA** do `remap` (chain rule paramétrica `y'(s)/x'(s)` no MESMO solver Newton; **port do Chromium `cubic_bezier.cc`** — `SlopeWithEpsilon` + cascata `InitGradients` p/ control-point coincidente; tangente vertical = ±∞, display pula non-finite; Eased = diferença central de `eval`). `ph2d-timeline::speed`: `sample_speed` per-segmento (Hold = 0 limpo), `segment_endpoint_speed` (dots ancoram NAS keys, convenção AE — via slope, NUNCA via `tangent_handles`: o chord do Hold mentia 3·rate), `speed_extent` inclui linha-zero + dots de segmento selecionado, `out/in_handle_y_for_speed` = inverso exato mantendo a influência x. Semântica AE conferida: speed graph 1D é **com sinal** (negativo = valor caindo). `graph_paint`/`graph::resolve_drag` ramificam em `speed_view` reusando `CurveHandle`/`HandleDrag`. `TIMELINE_SPEED` id + `panel.timeline.speed` i18n. **Weighted tangents (W5, landou 2026-07-11):** variant novo **`Interp::BezierW{x1,dy1,x2,dy2}`** (apendado por ÚLTIMO — índices postcard estáveis, saves v1 seguem legíveis) — x = influência (fração CSS, MESMO solver Newton), **dy = offset ABSOLUTO em valor** (AE keyframe-velocity/Blender F-curve) → **segmento flat pode curvar** (o gap documentado do `handle_coords`). Motor em `curve_weighted.rs` (módulo irmão); funil escalar = `Interp::value/value_slope` (o `interpolate` ramifica SÓ no W — o caminho lerp legado é byte-idêntico, goldens intactos). **Todo drag de handle (valor E speed) produz BezierW** via `ph2d_timeline::{segment_handle_points, weighted_with_handle, weighted_with_endpoint_speed}` — conversão lossless (o lado não-arrastado mantém a posição desenhada); speed-edit em flat também funciona. `tangent_handles()/handles()/with_*_handle` viram caminhos legados (sem forma normalizada p/ W). **Speed handles AE-style (mesmo dia):** cada endpoint no speed graph tem **braço de influência** (âncora no key + linha horizontal + ponta arrastável em `speed_handle_tip`); a ponta edita em **2D** — vertical = velocidade, horizontal = influência (`weighted_with_speed_handle`, clamp AE 0.1% via `MIN_INFLUENCE`). **Time remap (W5, landou 2026-07-11):** `PropKind::TimeRemap = 6` (apendado; **FORA do `ALL`** — o `ALL` é a POSE do autokey, `PoseSample=[;6]` intacto) — o "relógio por objeto" do AE: uma track "Time" keyável (playhead→tempo-fonte; slope<1 slow-mo, flat **freeze**, descendo **reverse**) que retima TODAS as outras tracks da entidade (`remapped_time` em apply.rs, scan linear zero-alloc, per-entity). A track é UI normal (dope/graph/weighted de graça); **K semeia identidade** em track vazia e **na-curva** com keys (`key_value_for` no bridge — `sample_prop_value` de TimeRemap = None, nunca auto-keya). +Track ganhou "Time" (`TIMELINE_ADDPROP_TIME` + `panel.timeline.prop.time`). NÃO confundir com `motion.time_remap` dos Motion Nodes (escopos de cook, outro sistema). **Fix definitivo do "Time anula a animação" (2026-07-11, `72803d18`, smoke OK 2026-07-19):** o seed do K (`key_value_for`) flat-clampava via `tr.sample` enquanto a amostragem (`remapped_time`) extrapola slope-1 — K duplo criava remap PLANO = freeze. Seed e amostragem agora são a MESMA função (`remapped_time`: identidade/na-curva/slope-1/Hold-freeze). Repro vermelho→verde no caminho real do K + varredura (graph editor nunca emite AddKey; auto-key duplamente guardado; duplicate/paste verbatim) em [HANDOFF_line_anim_integracao §13](../../Timeline/handoffs/HANDOFF_line_anim_integracao_2026-07-11.md). Fixes anteriores (`9a6ba0ee`/`56217b44`) foram unit-green mas product-red — a lição: [[feedback_derived_coordinate_seed_must_match_sample]]. **Fix "Time bugado" (2026-07-11, `56217b44`, INSUFICIENTE):** (a) fora dos keys a curva Time **extrapola em slope 1** (identidade deslocada pelo key de borda), NÃO por hold — senão UM key semeado pelo K congelava o relógio pra sempre (0 keys = identidade, 1 key = freeze era descontinuidade); exceção: **último key `Hold` = freeze-frame** deliberado (AE). (b) toda autoria de pose usa o **relógio da entidade**: auto-key (diff+insert), pin deslocado e K keyam no tempo FONTE (`remapped_time` agora pub + `key_insert_time` no bridge) — key em tempo cru de playhead ficava invisível pro apply e a pose saltava de volta; a track Time em si segue keyando no tempo do playhead. Sob remap ≠ identidade o K desenha o key da cena na régua no tempo-fonte (tracks são autoradas em tempo-fonte, modelo precomp do AE). **Simplificação de keyframes do record (W5, landou 2026-07-11, `b09f7003`, smoke OK 2026-07-19):** o record grava 1 key/frame; no release o autokey simplifica cada track gravada numa curva Bézier limpa de pouquíssimos keys, precisa a 0.5% do range — **Schneider** (Graphics Gems 1990: least-squares cubic + Newton reparam + split adaptativo). Núcleo `ph2d-anim::fit_fcurve`/`curve_fit.rs` (f64), adaptado p/ F-curve: erro em VALOR no tempo correto (inverte x(u)=t), eixos normalizados (tol = fração do range), handle-x clamp (função-do-tempo), saída `Interp::BezierW` (conversão exata). `Track::simplify_range` troca os densos pelos fitted (endpoints pinados, roving pulado); shell simplifica no release, 1 undo step; record grava em tempo REAL sub-frame (snap descasaria valor×tempo). **§17.1 (`e2df085d`): pré-filtro passa-baixa** (`smooth_values` binomial [1,2,1] ×8 passes) ANTES do fit — o tremor do mouse causava over-subdivide ("reduziu um pouco"). **§17.2 (`7ee4c1fa`): fit KEY-POR-EXTREMO** — o Schneider recursivo espalhava ~41 keys pelas rampas duma senoide de 6 extremos; agora detecta picos/vales (`anchor_indices` por prominência) + 1 cúbica por trecho entre extremos (split limitado a prof. 2). Senoide 6-ext 41→9 keys; fidelidade vira APROXIMADA (~1-3%, o trade que o Enio pediu). **§17.3 (`267ae2a2`): COLUNAS ALINHADAS** — todas as tracks da sessão keyam nos MESMOS tempos (por entidade): cada track propõe seus extremos → união + merge (`COLUMN_MERGE_S`) → re-fit em `fit_fcurve_at` (sem split, senão sairia da grade). Deferido: corner broken-tangents · overshoot clamp · rotation unwrap. Handoff §17. **Performing / Record (W5, landou 2026-07-11, `26a77af4`):** toggle **Record** na barra (ao lado do AutoKey) — armado + tocando, arrastar o objeto grava a pose ao vivo ao longo do playhead (mocap na mão; 1 key/frame de display, 1 undo step por sessão). **O aviso do Enio (autoplay+play gravava) honrado por construção:** guard `capturing = if playing { performing && drag_now } else { armed }` — no play SÓ grava com gesto de gizmo ativo; a pose passiva da animação nunca minta key (Play puro, mesmo com AutoKey, grava zero). 2ª barreira: diff no relógio cru do apply. `TimelineFlags.performing`+`SetPerforming`+snapshot; `TIMELINE_RECORD`+i18n; guard no `autokey_pass`. DOC_VERSION intacto (flags não serializadas). Handoff §16. **Fixes do smoke do rove (2026-07-11):** menu sobre o dope-sheet: Down não-Secondary numa superfície graph/timeline com menu aberto = dismissal puro (fecha e consome — antes a captura roubava o clique e o menu nunca fechava). E o diff/pin do autokey compara no **t_src CRU** (relógio do apply), não no snapado — pause fora de fronteira de frame mintava key (armado) ou pinava a entidade e "congelava" o undo (desarmado). Handoff §15. **Delete Track por R-click (landou 2026-07-11, `10cb7771`):** R-click na LABEL da row → menu `TIMELINE_TRACK_MENU` ("Delete Track") → `TimelineIntent::Unbind` (binding+track+keys, 1 undo step). `TimelineHitKind::Row` + `ContextMenuKind::TimelineTrack`; painel resolve target→(entity,prop) via snapshot e usa o canal `drain_intents` — zero shell. Gate anti-item-morto executável no seam (row nova na tabela sem arm = vermelho). **Roving keys (W5, landou 2026-07-11, `5b8b6e7f`):** key roving = só o VALOR autorado; `Track::resolve_roving` (`rove.rs`, módulo FILHO de track.rs) deriva o tempo p/ velocidade constante entre vizinhos pinados (∝ |Δv| acumulado; zero-travel = uniforme; bordas nunca rovam). Flags = vec paralelo PERSISTENTE no Track (`TrackData.roving` apendado, **`DOC_VERSION` 1→2**, v1 rejeitado). Choke points: `edit()` re-resolve após todo intent (mesmo undo step) + `doc.upsert_key` (auto-key). UI: 7ª linha "Rove Across Time" no menu R-click (`CTX_MENU_TL_ROVE`, toggle: all-roving→off, misto→ON; escopo espelha presets) + circulinho no dope / âncora menor no graph (`KeyView.roving`). Un-rove PINA no tempo derivado. Intents `SetRove`/`SetSelectedRove`. `preset_tests` extraído p/ `timeline_presets_menu_tests.rs` (LOC cap). **Relógio único (W4.T7, landou 2026-07-12):** o `MotionTransport` MORREU — o Motion cozinha no tick do `Playhead` (`motion_bridge::ticks_owed`: play = TODO tick p/ frente, a sim é sequencial; scrub/jump = UMA chamada, sem replay). **Seletor de clip (landou 2026-07-12):** ver/trocar/criar/renomear/apagar clips (`MAX_CLIPS=16`, `DOC_VERSION 2→3` — o **loop é POR CLIP**, `NamedClip.loop_range`/`loop_ping_pong`). Bindings são do **DOCUMENTO**, não do clip: todo clip anima os mesmos objetos e só as KEYS mudam (o precomp do AE) — 2º clip custa um nome. Barra de transporte = **flow layout** (quebra em N linhas), PingPong ao lado do Loop. **Composição de clips (o ex-"NLA") — DESENHADA, não construída ([ADR-0115](../../architecture/decisions/0115-clip-composition-sequencer-overlap-crossfade-sparse-lanes.md) + [plano](../../Timeline/02_plano_composicao_clips.md), 2026-07-12):** portar o strip-stack do Blender foi **DESCARTADO pela pesquisa** — o próprio Blender está movendo blend/influence do strip pra **CAMADA** (Baklava: Slotted Actions no 4.4, Layered Animation em WIP, strips-na-Action 2027+), cortou 5 modos pra 2 e quer matar o tweak mode (*"not a pleasure to work with"*); os sequenciadores (Unity/Unreal/Maya) convergiram no gesto **sobrepôs-cruzou** que falta ao Blender; e no **2D** "empilhar e blendar" NÃO é o idioma (Animate/Harmony/AE não têm blend de animação; a Cavalry escolheu camadas por-atributo; o Moho resolve overlap por canais disjuntos) — o idioma 2D é **nesting**, e nós temos zero (ADR seguinte). Desenho novo: faixas de instâncias de clip, **crossfade por sobreposição**, canais **esparsos** (o keyado É a máscara), blend/peso na FAIXA, sem modo/tweak (o dropdown de clip já resolve). **3 armadilhas que a pesquisa expôs no nosso código:** `remapped_time` lê `doc.active_clip()` (sob pilha, *qual clip dá o relógio?* é indefinido — o strip mapeia timeline→clip, o TimeRemap do clip mapeia clip→fonte) · o apply **era O(bindings²)** (hoist é pré-requisito, senão empilhar vira cúbico — **PAGO** pelo `ClockIndex`/`clock.rs`, 2026-07-12: hoje é linearítmico, crescimento por-binding 1,5× para 4× os dados) · `TranslationX` é posição **absoluta**, então "blend-to-default" jogaria o sprite na origem do pai (daí `rest` capturado por binding = Capture Base State do Rive / Base Pose do Unreal). **Composição de clips LANDOU e INTEGROU** (2026-07-12, ADR-0115 + `stack.rs`/`stack_eval.rs`/`stack_edit.rs`): faixas de instâncias de clip, crossfade por sobreposição, canais esparsos. **Fatia B FECHADA (B4 landou 2026-07-13):** a **alça de fade na quina do strip** (padrão Unreal — grip direto, não campo de Inspector) fica na **ponta da cunha**, no topo; arrastar pra dentro cresce o fade (in→direita, out→**esquerda**). Descansa uma largura-de-grip pra dentro da quina, então **nunca divide pixel com o grip de trim** (e numa strip estreita demais pra abrigar os dois, a alça simplesmente não é oferecida — perder o *fade* neste zoom é honesto, perder o *trim* é bug). **READ-ONLY onde um vizinho define a janela** (padrão Unity: a sobreposição É o fade — ease e blend são a MESMA curva, não dois números que precisam concordar): pintada cinza, e **sem hit nenhum** (dimmed que despacha mente). A pergunta *"de quem é esta janela?"* é feita **UMA vez** (`ClipLane::neighbour_reach_in/out`) — o painel decide se oferece a alça e o avaliador decide que número honra a partir da MESMA função. Intent `SetStripEase` (clamp `[0, span]`); a recusa do grip travado mora no `hit_plan` (função **pura e testada**), não no laço de pintura. **A ANIMAÇÃO AGORA É SALVA (W4.T6/B5, 2026-07-13):** o `TimelineDoc` viaja DENTRO do arquivo de projeto (`ProjectFile.timeline`, `PROJECT_SCHEMA` 7→8) — antes fechar o app perdia a animação inteira, e o "sidecar" que dizia salvá-la era **código morto** (o Ctrl+S global de projeto já retornava antes dele). A binding viaja pelo **`wire_id` (hash do `Name`)**, nunca pelos bits de entidade: o load instala o doc com toda binding **DESTACADA** (`entity=0`, `missing`) e o `upkeep` do frame — a MESMA função que cura delete+undo — a recola pelo nome. Destacar não é higiene: bits são reciclados, então os bits salvos de um objeto morto podem ser os de um objeto **vivo e diferente**, e a track dirigiria a pose errada em silêncio. **O load de projeto ESQUECE o documento anterior (2026-07-13):** relógio (rebobina **e pausa** — `rewind` preserva o play state, então o projeto abria correndo), fila de undo, **baseline do undo** (re-armado do MUNDO no fim do load, senão o `restore_painted_docs` — que troca o `texture_id` de cada sprite pintado depois do baseline — fazia o `post_frame_undo` do MESMO frame registrar um passo espúrio, e o 1º Ctrl+Z do artista devolvia uma textura morta), a timeline (senão as bindings do projeto A adotavam os objetos homônimos do projeto B), o alvo vivo do Flip, os pins do autokey. Gates: `project_tests.rs` (o `App` é dirigível **sem janela** — `App::new()` nasce com `window`/`gfx` em `None`, o winit só cria a janela no `resumed`). **Aberto:** W4.T4 (dock no `motion_timeline_slot` — **DESBLOQUEADO**, a linha Motion integrou) · nesting (o idioma 2D de verdade) — **[ADR-0133](../../architecture/decisions/0133-timeline-nesting-a-container-instance-is-a-strip-and-the-parent-owns-the-clock.md) ACEITO e CONSTRUÍDO** (2026-07-19, linha `line/anim-fixes`, **smokado**, aguardando integração — handoff [`docs/Timeline/handoffs/HANDOFF_INTEGRACAO_line_anim_nesting_2026-07-19.md`](../../Timeline/handoffs/HANDOFF_INTEGRACAO_line_anim_nesting_2026-07-19.md); pesquisa em [`docs/Timeline/03_pesquisa_nesting.md`](../../Timeline/03_pesquisa_nesting.md)): o relógio é do **PAI** (4 dos 5 produtos; relógio próprio = filho invisível em autoria) e uma instância de container é um **`ClipStrip`** cuja fonte vira `StripSource{Clip,Container}` — o strip já tem o mínimo universal de override. Container = **asset referenciado no documento**, não entidade: a árvore ECS já é a metade ORGANIZACIONAL (o *grupo* do Harmony), o container é a TEMPORAL (o *símbolo*). Ciclo: DFS na criação + rejeição no load, **sem auto-reparo silencioso**. **`DOC_VERSION` 7→8** (quebra dura; `PROJECT_SCHEMA` fica em 18 de propósito — a forma do `ProjectFile` não mudou, e o blob de dentro carrega a própria versão) e `ClipStrip.clip: u16` virou `source: StripSource`. **O z-order já estava respondido**: `SortingGroup` (Unity) faz a subárvore ordenar como bloco — a Fatia 0 mediu em vez de escolher. **A régua troca de relógio ao entrar** e o **mapa da instância** (`container_map`) serve as DUAS direções — ler num relógio e escrever noutro foi bug real desta linha, e onde a instância não é bijeção (toca 2×, ou dá a volta) a régua **não arrasta**, em duas camadas com gate próprio. ⚠️ **As duas réguas empilhadas do AE foram DESCARTADAS com medição** (emenda no ADR §5): a premissa era *"o tempo da timeline some lá dentro"* e o `transport.rs:281` mostra `time_seconds` o tempo todo — o que falta é a RELAÇÃO, e ela é um readout (`plays 4.00 - 12.00` / `not playing here`). A trilha é um **CAMINHO** (entrar em B por dentro de A precisa do degrau do meio) e elide por FORA. Smoke: `PH2D_NEST_SMOKE=1`. Plano em [`docs/Timeline/04_plano_nesting.md`](../../Timeline/04_plano_nesting.md) · markers→signals · export · **sem gate: o lado do SAVE** (`project_save` exige `gfx` = janela + GPU — o mundo mora dentro do `AppGfx`, e é isso que trava o harness headless do shell). (W2.E9 já landou: tokens `Timeline*` + chaves `panel.timeline.*` existem — as strings do painel NÃO estão hardcoded.) **⬛ Continuação da `line/anim-fixes` INTEGRADA (2026-07-21):** `lead_out` (o fade-out **FORA** da strip — o clipe toca inteiro e depois fadeia; cruza o GAP para a próxima strip e, sob loop, a **costura** de volta ao começo; **`DOC_VERSION` 8→9**, campo apendado, v8 recusado no load) · o atalho de **clipboard/Delete segue a ÁREA SOB O MOUSE** (regra Blender): mouse na timeline = keyframes, no canvas = formas — antes seguia a FERRAMENTA, e copiar keyframes com o vetor ativo copiava o desenho · e **containers são ASSETS**: três abas `[Keys | Containers | Arrange]` (**Arrange é SEMPRE a cena**, por mais funda que a trilha esteja) onde **Containers é uma LISTA** — uma strip em branco por container, do tamanho DELE, e um container vazio nasce medindo **2 s** (porta única `container_bar_seconds`, com QUATRO consumidores; ela consertou um bug latente: colocar um container vazio congelava `src_out` em 0 e preenchê-lo depois deixava a instância tocando NADA para sempre). **Três verbos e só eles:** lápis renomeia · lixeira apaga em **CASCATA** (as instâncias morrem na cena E dentro de outros containers, e toda referência acima do buraco desce um slot, porque `StripSource::Container` é um ÍNDICE) · **duplo-clique ENTRA**. `MAX_CONTAINERS = 16` (os ids do lápis/lixo/barra são arrays fixos — o cap que a nota antiga prometia *"para quando o widget chegar"*). O dropdown de FONTE lista clips E containers com **glifos de desenho distinto** (folha × caixa — ⚠️ o par `Layer`/`Layers` foi reprovado no smoke: são a MESMA figura, e o gate compara GEOMETRIA, nunca o identificador); escolher fonte **nunca navega**. ⚠️ **O duplo-clique era ENGOLIDO** pelo `pointer_up`, cujo upgrade Click→DoubleClick estava **enumerado por kind** (*"only a MARKER tap upgrades"*) ⇒ o segundo consumidor nasceu morto sob o mouse; a pergunta virou porta do TIPO (**`TimelineHitKind::wants_double_click()`**) e o gate dirige o par de cliques REAL pelo `dispatch_pointer` — o `click_at` do testkit espaça 1 s **de propósito**, e é por isso que nenhum seam pegava. Smoke: **`PH2D_NEST_SMOKE=2`** (o container "Jump": 3 clips, 2 lanes, 3 instâncias — uma esticada). ⚠️ **Flake conhecida, PRÉ-EXISTENTE:** `the_cost_of_depth_is_linear_not_explosive` (`ph2d-timeline/tests/nesting_clock.rs`) é gate de RAZÃO sensível a carga — passa isolado; re-rode sozinho antes de suspeitar de um merge. **⬛ A DURAÇÃO EXPLÍCITA — `line/anim` INTEGROU (2026-07-23, 28 commits, todos os smokes aprovados: *"Tudo ficou maravilhoso. Todos os bugs corrigidos e as animações complexas com múltiplos containers funcionam perfeitamente"*):** o modelo **composition-duration do AE** — cada clip, cada container e o Arranje têm um tamanho **AUTORADO** que define "o fim" (go-to-end, o loop recém-armado, a fatia de uma instância nova) e **CORTA o excedente SEM destruí-lo** (keys/strips além do fim continuam autorados; o avaliador só clampa o relógio no corte — `clip_cut`/`container_cut`/`cut_scene`). ⚠️ **Uma duração autorada é uma duração VISÍVEL:** o **véu** (a máscara escura pós-fim) e o clamp do playhead aparecem desde o 1º frame — um fim que não se vê é um fim que o artista descobre por acidente. Editável pela caixa **Dur(s)** no transporte (stepper ±0,2 s) **e** pela **alça ↔ na régua** (grab-relative, snap igual ao do playhead). **Toda composição nasce com 4 s** (decisão do Enio). **Containers viraram ABA:** entrar num container adiciona uma aba nomeada no grupo `[Keys | Containers | <container> | Arrange]`, nascida marcada; sai-se pelo mesmo grupo, e o **breadcrumb solto MORREU**. Dentro de um container o transporte é o **relógio DELE**, com loop independente por modo. **`DOC_VERSION` 9→11** (dois campos apendados: o relógio próprio do container 9→10, os `length_override` de clip/container/cena 10→11) — postcard é posicional ⇒ **quebra dura**, v9/v10 recusados no load, a política que todo bump deste documento segue desde o ADR-0133; ⚠️ **`PROJECT_SCHEMA` NÃO mudou** (o `TimelineDoc` viaja como blob DENTRO do `ProjectFile` e carrega a própria versão — a forma do `ProjectFile` não mudou), e é **isso** que o manteve fora da disputa de número com physics/vector/painter na mesma janela. **8 bugs de smoke fechados** (detalhe em [`docs/Timeline/handoffs/HANDOFF_line_anim_duration_autokey_bugs_2026-07-23.md`](../../Timeline/handoffs/HANDOFF_line_anim_duration_autokey_bugs_2026-07-23.md)): autokey/K mintando keys além do corte (seed==sample, a lição [[feedback_derived_coordinate_seed_must_match_sample]] outra vez) · Dur(s) autorando a VISTA em vez do documento · lane solta após o último strip · pingpong sem gap cíclico · additive de container inerte · scrub pausado saltando na saída de strip com fade · foco em chip numérico agora **seleciona tudo** (`init_number_buffer` → `selection_anchor=Some(0)`; `NumberInput` pula o `place_text_caret`) · e o default de 4 s. Smokes: `PH2D_NEST_SMOKE=1` (nesting básico) · `=2` (o container "Jump") · **`=3`** (a BIBLIOTECA de containers — 3 assets, um aninhado, um vazio; é a cena das *"animações complexas"* aprovadas). **Aberto (não-bloqueante):** a metade *"botões loop/pingpong pareciam mexidos ao entrar em Arrange"* do Bug B — seria o acoplamento tab↔stack (`keys_mode`), **não** tocado sem um smoke que o reproduza; o Enio não reportou recorrência no smoke final. **⬛ O MOTION PATH + O ONION DA TIMELINE — `line/anim` INTEGROU (2026-07-25, 31 commits, todos os smokes aprovados; ADR-0141/0142, handoff [`docs/Timeline/handoffs/HANDOFF_INTEGRACAO_line_anim_onion_2026-07-25.md`](../../Timeline/handoffs/HANDOFF_INTEGRACAO_line_anim_onion_2026-07-25.md)):** três coisas sobre autorar movimento e **VER** o que se autora. **(1) MOTION PATH (ADR-0141):** a posição de um objeto pode ser um **CAMINHO** — a track escalar passa a ser a *distância percorrida*, não dois números soltos. **(2) A FITA DE VELOCIDADE:** a trajetória é colorida pelo **RITMO** (comprimento de segmento por dt constante ∝ velocidade), via `marks() -> Vec<OverlayMark>`. **(3) O ONION (ADR-0142):** poses-fantasma dos vizinhos do objeto selecionado, desenhadas como silhuetas **recoloridas** (passado verde / futuro azul) — W1 os fantasmas · W2 o modo **Keys** (pose-a-pose, o **default**) · W3 os toggles na barra · W3b o **card de settings ARRASTÁVEL** (`chrome/onion_modal.rs`, `z=180`, máquina de drag espelho do `fill_drag`). ⚠️ **`DOC_VERSION` 11→12** — e o bump é do **motion path**, não do onion: o onion é **VISTA** (`TimelineState.onion` NÃO serializado), então ele não move schema nenhum. ⚠️ **`ITEMS` do transporte 19→20** — o array que a `line/physics` e a `anim-fixes` já mexeram antes: o tamanho se **CONTA**, não se escolhe. ⚠️ **E o PLAYHEAD ficou LIVRE (pós-smoke):** o clamp da duração autorada — que a wave da duração explícita instalou — **capava a simulação de FÍSICA** no fim da timeline; removido. Smokes: **`PH2D_MOTION_PATH_SMOKE=1`** · **`PH2D_ONION_SMOKE=1`**. **⬛ AS JOIAS DA COROA — `line/anim` INTEGROU (2026-07-27, 50 commits, smokes aprovados; ADR-0143/0144, handoff [`docs/Timeline/handoffs/HANDOFF_INTEGRACAO_line_anim_crown_jewels_2026-07-26.md`](../../Timeline/handoffs/HANDOFF_INTEGRACAO_line_anim_crown_jewels_2026-07-26.md) + [plano](../../Timeline/07_plano_joias_da_coroa.md)):** o essencial de estado-da-arte que faltava, em três waves. **(A) RETIMING de seleção:** Time-Reverse · Stagger/Quick-Offset + Distribute Evenly · **Time-Scale** (a caixa de retiming) · **Buffer Curves** (A/B da curva, Store/Swap + fantasma) — motor em `ph2d-anim` (`track_reverse.rs`/`track_buffer.rs`/`track_fit.rs`). **(B) EXTRAPOLAÇÃO por-track:** loopOut/cycle/pingpong/continue (o *out-of-range* do AE), com o dope-sheet **marcando** (tracejado + badge que MEDE o label); `Track.extrap` (**`DOC_VERSION` 13→14**), default `Hold` ⇒ hoje byte-idêntico. **(C) EXPRESSÕES de propriedade (ADR-0144):** uma fórmula (`time*100`, `wiggle(3,20)`, `value + Sprite.x`) dirige uma prop num passe pós-composição **SEPARADO** (`expr_pass.rs`) que roda DEPOIS das keys e **NUNCA** toca `stack_eval`/o blend — é isso que mantém o fade intocado (guardião: `fade_fingerprint.rs`, byte-idêntico ao motor pré-linha). Parser VEX-lite no **crate leaf novo `ph2d-expr-parse`**; ⚠️ **`ph2d-expr` é FROZEN (ADR-0039) e NÃO foi tocado** — o parser saiu PARA FORA dele, e a `motion.expression` agora **DELEGA** (um parser, dois consumidores, gate `the_motion_node_delegates_to_the_one_parser`). Mais os **SINAIS** (ADR-0143: um marker EMITE um evento desacoplado, não uma chamada; `Marker.signal`, **`DOC_VERSION` 12→13**) e **`Interp::Nearest`** (variant apendado, sem bump). **`DOC_VERSION` 12→15** (três campos apendados, postcard posicional) · **`PROJECT_SCHEMA` INTOCADO** (o `TimelineDoc` viaja como blob e carrega a própria versão). **A LEI que as 3 correções de smoke do wiggle deixaram** (a mesma doença, [[feedback_derived_coordinate_seed_must_match_sample]]): o passe de expressão **honra o `skip`** que o passe de keys honra (senão pose *displaced* realimenta e deriva ao PAUSAR) · roda no **relógio CORTADO** (`clip_cut`/`container_cut`/`cut_scene`) · e **segue a COBERTURA da composição** (`composed`): prop **com keys** fica QUIETA onde a composição não cobre, e `value` é o valor COMPOSTO, nunca o mundo. **⚠️ ABERTO, por ordem do Enio:** **expressão PURA (sem keys) extrapola a strip** — ela não tem track, logo nenhuma strip a referencia, logo não há janela a obedecer; ligar uma exige **vínculo explícito** (escopo autorado × prop que passa a viver dentro de um clip), decisão de produto + provavelmente `DOC_VERSION`. Smokes: **`PH2D_TIMESCALE_SMOKE=1`** · **`PH2D_STAGGER_SMOKE=1`** (⚠️ **Ctrl**+drag, o KDE rouba o Alt) · **`PH2D_BUFFER_SMOKE=1`** · **`PH2D_EXTRAP_SMOKE=1`** · ~~`PH2D_EXPR_SMOKE=1`~~ (**a cena MORREU com o card em 2026-07-30** — o smoke do motor é o `PH2D_EXPR_BLEND_SMOKE=1`) · **`PH2D_SIGNAL_SMOKE=1`**. ⚠️ **E A INTEGRAÇÃO EXPÔS UMA CONTRADIÇÃO DE DESIGN ENTRE DUAS LINHAS, no W4.T4** (o dock da timeline dentro do Motion) — **não é acidente de merge, e o veredito é do Enio**: a `line/anim` **REJEITOU** a feature depois de um smoke (2026-07-25: *"roubar uma faixa do grafo o espremeu, e arrastar o divisor colapsava o grafo a nada"*) e gravou a cerca de Chesterton no `layout.rs`; a `line/motion-value` **CONSTRUIU** (`89fbfe73e`, integrada 2026-07-26, [doc 64](../../Motion%20Nodes/64_dock_da_timeline_nota_adr.md)) com **`MOTION_TIMELINE_MAX_FRAC = 0.45`** — o cap que ataca **exatamente** o colapso que a reprovou — e 4 gates (`the_dock_never_eats_the_graph`). ⚠️ **O doc 64 não cita a rejeição: as duas linhas não se viram.** Na integração ficou o CÓDIGO do main (shipado e gateado) e a nota de (a) saiu do `layout.rs` — *um comentário que contradiz o código shipado é pior que comentário nenhum* —, com as duas metades registradas na linha W4.T4 do [plano 01](../../Timeline/01_plano_timeline_ui.md). **Re-smoke decide se o cap resolve o que o Enio reprovou.** **⬛ A AUTORIA DE EXPRESSÕES FOI RETIRADA; O MOTOR FICOU (2026-07-30, ordem do Enio: *"faça como vc sugere … sem resíduos"*; registro em [`docs/Timeline/14_a_autoria_de_expressoes_foi_retirada.md`](../../Timeline/14_a_autoria_de_expressoes_foi_retirada.md)):** o corte é entre **o motor** (fechado, gateado por fingerprint) e **a autoria** (inacabada) — **11.125 linhas, 42 arquivos**: a crate `ph2d-expr-recipes` inteira · o card (`expr_modal*.rs` ×5) · a superfície foundational dele (6 ids `EXPR_MODAL_*` + 7 helpers `expr_*_id` · a row "Expression…" das TRÊS tabelas de menu de track · os 2 `TimelineHitKind` · 7 chaves i18n) · o **preview vivo** · e os 2 smokes de card. ⛔ **O que decidiu foi o smoke de 30/07** (*"todos aparecem como custom"*): a **folha é write-once** — o card não reconstrói linhas a partir do texto (decisão declarada: *"um reconhecedor de fragmentos canônicos começa a MENTIR no dia em que alguém edita um caractere"*), então toda fórmula reaberta volta como `Custom Formula` cru, e o `Custom Formula` acabava sendo a receita mais usada do catálogo sem ninguém a ter escolhido. ⚠️ **Remover a FEATURE não é remover o SCHEMA:** `TargetBinding.expr` (v15) e `NamedClip.expr` (v16) **continuam serializados** — postcard é posicional, apagá-los recusaria **todo projeto já salvo**; `DOC_VERSION` fica em **16** *naquele commit* (a cauda da linha o levou a **17**) e o `PROJECT_SCHEMA` **não é tocado** — ele estava em 37 quando isto foi escrito e o `main` de hoje diz **46**, porque o `TimelineDoc` viaja como blob e carrega a própria versão. ⚠️ **O motor inteiro FICOU** e a porta de autoria (`TimelineIntent::SetBindingExpr` → `set_clip_expr`) segue viva: a expressão per-clip continua **fonte de lane que fadeia com o strip** (ADR-0152), o transform global continua (ADR-0144/0151), e **`ph2d-expr`/`ph2d-expr-parse` não são da timeline** (a `motion.expression` delega ao parser único). ⚠️ **`expr_live` → `expr_owed`:** o módulo tinha DUAS metades e só o preview era do card — o **livro-razão das poses devidas** é a metade que a auditoria de 29/07 (§4 D-I) provou ser a MAIOR (sem ela, `value + 250` fica em 250 para sempre depois de DELETE), tem gate próprio e ficou; o arquivo mudou de nome porque *um `expr_live` sem preview é um nome que mente*. ⚠️ **A prova de que a timeline não sofreu não é opinião:** `fade_fingerprint`/`_channels` pinam o fade num hash literal exato e saíram **verdes com o MESMO hash** antes e depois. ⚠️ **E a varredura achou TRÊS resíduos write-only** que o card mantinha vivos: `TimelineViewSnapshot::selected_entity` (o shell preenchia todo frame, ninguém lia) · `TimelineWheel::anchor_y` (existia para a guarda de moldura do card) · e quatro doc-comments que passaram a MENTIR (reescritos, nunca apagados sem substituto). Smoke do motor que sobrevive: **`PH2D_EXPR_BLEND_SMOKE=1`**. Os docs 09-13 da pasta Timeline viram **históricos** (o que medem sobre o catálogo segue válido). **⬛ A EXPRESSÃO FADEIA E A TRAJETÓRIA É DO CLIP — `line/anim` INTEGROU (2026-07-31, 89 commits, todos os smokes aprovados; ADR-0151/0152, handoff [`docs/Timeline/handoffs/HANDOFF_INTEGRACAO_line_anim_2026-07-31.md`](../../Timeline/handoffs/HANDOFF_INTEGRACAO_line_anim_2026-07-31.md)):** quatro frentes. **(A) ADR-0151:** a expressão é **por-CLIP**, então um strip a **janela** (`DOC_VERSION` 15→16). **(B) ADR-0152:** ela vira **fonte de lane que FADEIA** — antes era um passe pós-composição cego ao fade —, com `LinkFrame` (prop-links lendo a fonte já fadeada, em ordem topológica), keying coerente **através** da fórmula (o K inverte a afim e **recusa** a não-inversível) e o `expr_owed`, o livro-razão sem o qual `value + 250` fica em 250 para sempre depois de um DELETE. ⚠️ Os `fade_fingerprint` seguem **verdes no mesmo hash**: é a prova executável de que o blend não regrediu. **(C) A AUTORIA de expressões foi construída e RETIRADA** (11.125 linhas, a crate `ph2d-expr-recipes` inteira, o card, o preview e 2 smokes) — o smoke reprovou (*"todos aparecem como custom"*: a folha é write-once) e o **MOTOR ficou intacto**; ⚠️ **remover a feature não é remover o SCHEMA** (`TargetBinding.expr`/`NamedClip.expr` continuam serializados — apagá-los recusaria todo projeto salvo), e é por isso que a linha inteira tem **ZERO `Cargo.toml` tocado**. **(D) O MOTION PATH**, seis defeitos que são **uma pergunta só** (*a que clip esta geometria pertence, e quem está olhando para ele?*): a trajetória é do **CLIP** (clip novo nasce em branco — morre a alça fantasma) · no Arrange quem manda é o **STRIP que dirige** · o fade **compõe PONTOS** (misturar duas réguas de distância não significa nada) · o autokey **não planta âncora sobre a pose que o apply escreveu** (32 âncoras num único fade, medidas) · a trajetória só é **OFERECIDA** na aba Keys (as CINCO portas pela porta única `active_path`) · e o K/AutoKey **não ANCORAM** fora dela, com motivo (`KeyRefusal::PathNeedsKeysTab`) — recusar em silêncio transformaria o gesto numa ferramenta quebrada. ⚠️ **Um gate de unidade é CEGO à fiação do shell**, e isto está medido: com o `draw` em `true` literal os **20 testes do overlay ficam VERDES** e só o arch-gate sangra. Mais: duração 0 = **infinito**, projeto legado abre com 4 s + véu, **Arrange é escopo INDEPENDENTE**, o canal de **Morph** é autorável e o `lead_out` do ÚLTIMO strip de uma lane (era inerte). **`DOC_VERSION` 15→17** (dois campos apendados, quebra dura: v15/v16 têm a timeline recusada no load) · **`PROJECT_SCHEMA` INTOCADO em 46** · contrato congelado **intacto** · **nenhuma dep nova**. ⚠️ **Os ADRs nasceram 0145/0146 e RENUMERARAM na integração** (6ª e 7ª vez): Painter levou 0145·0146·0147, Vector o 0148, physics o 0149 e sculpt3d o 0150 na mesma janela — e como os NOMES de arquivo diferem o git **nunca conflitou**. ⚠️ **E o rewrite do token mediu o próprio preço:** **23 arquivos ALHEIOS** citam `ADR-0145/0146` (todos do wet paint), então o escopo foi a interseção *linha ∩ citações* — e nem isso bastou, porque o **`CLAUDE.md` é MISTO** (11 citações do wet paint, 1 da linha) e exigiu edição cirúrgica [[feedback_a_token_rewrite_scopes_to_the_changed_files_not_the_whole_tree]]. ⚠️ **A integração expôs um conflito que o handoff não previu:** duas linhas criaram `ph2d-editor-core/src/paint_shapes.rs` independentemente e **pelo mesmo motivo** (`paint.rs` no teto congelado de 884), uma com `fill_diamond` e outra com `fill_polygon` — add/add resolvido mantendo as duas, e depois **modify/delete** quando a própria linha apagou o arquivo ao retirar o preview. Apagar teria levado o `fill_diamond` que já shipa; e o commit de remoção tirou **também** o `pub mod paint_shapes;` do `lib.rs`, então restaurar só o ARQUIVO deixaria o módulo **órfão** — árvore que funde limpa e **não compila** [[feedback_clean_text_merge_can_be_semantically_broken]]. Smokes: **`PH2D_EXPR_BLEND_SMOKE=1`** · **`PH2D_MORPH_FADE_SMOKE=1`** · **`PH2D_MOTION_PATH_SMOKE=1`**. ⚠️ **`PH2D_EXPR_SMOKE` MORREU** com o card. Bugs com mecanismo e mutação: [`docs/Timeline/BUGS_timeline.md`](../../Timeline/BUGS_timeline.md) §1, §2a–§2g. **Aberto:** a expressão **PURA** (sem keys) extrapola a strip — ela não tem track, logo nenhuma strip a referencia; ligar a global exige vínculo explícito (produto + provavelmente `DOC_VERSION`) · o catálogo de receitas está morto, **a pesquisa não** · W4.T4 segue aguardando re-smoke. **⬛ A WAVE DOS FADES — `line/anim` INTEGROU (2026-08-01, 11 commits, 46 arquivos, todos os smokes aprovados; handoff [`docs/Timeline/handoffs/HANDOFF_INTEGRACAO_line_anim_fades_2026-08-01.md`](../../Timeline/handoffs/HANDOFF_INTEGRACAO_line_anim_fades_2026-08-01.md)):** o fade de um strip ganhou o **mesmo vocabulário de easing** que um keyframe sempre teve. ⚠️ **Mas ele pinta uma tabela PRÓPRIA** (`TIMELINE_FADE_MENU`, escopo novo `TimelineInterpScope::StripFade`) e não a do keyframe: `Hold`, `Nearest`, `Custom (Bézier)` e `Rove` nomeiam **KINDS de interpolação**, e um fade não tem nenhum deles — oferecê-los ali seriam **quatro rows que não fazem nada**, que é o item-de-menu-morto que este codebase mantém *uma tabela por menu* para prevenir. A escolha *"qual tabela este escopo pinta?"* é feita **UMA vez** (`TimelineInterpScope::menu_table()`), perguntada pelo overlay (para PINTAR) e pelo chrome handler (para decidir que rows são folhas onde ele pode parquear um pick) — duas cópias shipariam um menu cujas rows desenham e não respondem, **que é a cicatriz que o handler do segmento já carrega** (ele enumerou os ids de folha à mão uma vez, o `Nearest` entrou na tabela, e a row nasceu morta). ⚠️ **O ponto de merge sensível não era o schema, era uma REMOÇÃO:** o `pre_populate.rs` tinha uma lista de 10 tabelas escrita à mão e a linha a trocou por `ALL_TIMELINE_MENUS.iter()` — um merge textual limpo poderia **perder** uma tabela que outra linha tivesse acrescentado à lista antiga, e quem pega isso é o gate `every_timeline_menu_row_is_registered_and_hittable` (**literalmente o defeito que esta wave consertou**). Conferido verde na árvore combinada. **`PROJECT_SCHEMA` 46 e `DOC_VERSION` 17 INTOCADOS** (por `git diff`, não por auto-relato) · **zero `Cargo.toml`** · **nenhum ADR** ⇒ a linha ficou **fora** de toda disputa de número desta janela · contrato congelado intacto.
