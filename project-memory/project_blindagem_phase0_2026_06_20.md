---
name: project-blindagem-phase0-2026-06-20
description: Blindagem da implementação — diagnóstico (estrutural vs comportamental) + Fase 0 (testkit de seam + 3 gates); plano das 4 alavancas
metadata: 
  node_type: memory
  type: project
  originSessionId: 1e417319-a0f2-439b-a01c-f6805bdf28ee
---

Enio (2026-06-20): "ou corrigimos profundamente ou o projeto fracassa" — ciclos enormes de erro, UI fora do padrão, botão que precisa de 3 ciclos, god-files, auditoria que só compila. Diagnóstico forense (5 frentes paralelas) achou **uma** causa-raiz:

**Todo o aparato de qualidade mede correção ESTRUTURAL (compila? contrato congelado? arquivo<N LOC? deps acíclicas?) e quase nada COMPORTAMENTAL (o botão dispara? a ferramenta faz o que promete?). E "compile-green" foi treinado como sinal de pronto (inner loop = só `cargo check`).** Os gates `*_contract_surface` contam `.matches("fn ")`/`.matches("pub ")` = ABI, nunca comportamento. ~2 harnesses comportamentais em 3.354 testes; o do painter foi deletado.

Plano de 4 alavancas (ordem inegociável 0→1→(2∥3)→4) em [`docs/plans/2026-06-20-blindagem-implementacao.md`]. **Fase 3 (decompor god-files) NÃO começa antes da Fase 1** — splits às cegas de paint sem cobertura causam regressão visual (a própria gate de LOC admite isso).

**Fase 0 ENTREGUE (commit local `8befcdac`, sem push):**
- `crates/ph2d-ui-testkit/` — `MockPanelHost` (impl `PanelHostInternal` real sobre WidgetStore+ActionBus). Dirige o seam Panel↔Tool headless (populate→event→bus→handle_panel_event→assert). É a peça-mãe da Fase 1 (testes de seam baratos). Dev-dep only (a gate `architecture_cycle_prevention` lê só `[dependencies]`).
- `architecture_panel_wiring_parity` — hit↔register parity (generaliza `architecture_topbar_registration_parity`): pega vector-pills (pintado+hit mas não-focável=clique morto).
- `architecture_docs_reference_live_gates` — meta-gate: doc instrucional não pode citar gate `architecture_*` inexistente. Caçou 4 refs mortas reais.
- `architecture_workspace_file_loc_cap` — cap 600 LOC em `crates/*/src` (gap fora de painel/widget/runtime); 53 offenders congelados no baseline.

**Fase 1 ENTREGUE (commit local `bbc69974`, sem push):** comporta-verde vira lei. Gate `architecture_interactive_crate_has_behavioral_test` (painel com event.rs precisa de seam test via `ph2d-ui-testkit`; **12/12 cobertos — `BEHAVIORAL_TEST_DEBT` VAZIO, dívida zerada commit `f2c9a711`**). 23 seam tests no total. Backfill: bgremoval/color-equalization/upscale/equalize-sizes/grid-snap = 11 seam tests (evento real→efeito observável, guard anti-vacuidade). DIRETIVA §3 = rubrica executável de auditoria (template LENTE/CLAIM/TRAÇO/ASSERÇÃO-VERMELHA/LOC; "compila=OK" REJEITADO); §5 DoD = seam verde+smoke. **5 agentes paralelos escreveram os tests; 1 errou o path do State (`::state::` vs raiz) — pego ao COMPILAR, não no report. Verifique sempre.** **Fase 2 ENTREGUE (commit `880479a4`):** `ph2d_editor_core::panel_seam!` (macro em `panel/seam_macro.rs`) gera `populate`+`apply_event` do **lado do painel** p/ forwarders (slider+chip→SetValue, botão→Click/Cancel) de UMA declaração — clique-morto impossível por construção. Padding migrado como referência (`src/seam.rs`; event.rs+populate.rs deletados; seam test = rede, comportamento idêntico). **Kill-criterion APLICADO:** o "1 declaração → 13 sites" foi abandonado (sites em 3 crates; tool-side é bespoke); macro cobre só a forma forwarder uniforme. Spike revelou que `seam.rs` quebrava as gates 0.2/1.2 (indexavam por nome de arquivo) → co-evoluí ambas. Só padding migrado; CEQ/bgremoval/upscale/equalize-sizes têm dropdown/toggle/swatch fora do vocab → ficam explícitos. **Estado: 5 gates + 23 seam tests + 1 macro; 4 commits locais (8befcdac/bbc69974/f2c9a711/880479a4), sem push.** **Fase 3 (parcial, commit `d9b6e11d`):** 3.3 (freeze acoplamento shell) já estava gateado (`no_downcast`/`no_per_tool_branch`). 3.1 (parser comment-aware + cap de FUNÇÃO workspace) **deferido** — re-baseia toda allowance de panel + desmascara violações (foundational deliberado), e cap de fn confiável cross-crate precisa de AST/syn (brace-walk frágil em raw-string/WGSL); cap de ARQUIVO (0.4) já trava regrowth. 3.2: decompôs o god-file `ph2d-painter-effects/src/adjustments/compute.rs` (1636 LOC) → diretório `compute/` com 10 módulos por família (max 486 LOC), superfície pública idêntica, **117 testes idênticos ao baseline** (move puro); entrada removida do allowlist 0.4 (drive-to-zero). **4 god-files decompostos** (commits `d9b6e11d` + `bf4d5e2a`): compute.rs 1636→10mods (117 testes) · ids/chrome.rs 1027→9mods por domínio (export-set 282→282 por diff, collisions verdes) · render/atlas.rs 1159→5mods (146 testes, Metal headless) · tool-painter/layers.rs 1148→7mods (54 testes). Todos <600, superfície pública idêntica, entradas removidas do allowlist 0.4. **Restam (alto risco, caps-guarded):** pointer.rs (1261, 1 fn gigante de dispatch) + compositor.rs (2396, GPU) — passe dedicado. **SHIPPED 2026-06-20: 8 commits pushed (`ebe2c23b..997450f6`), CI VERDE** (run 27881117678, matrix linux/macOS/windows + replay-hash + bench). ship.sh pegou fmt+clippy (corrigidos: unit-struct `::default()`, collapsible_if→let-chain) e um HANG do nextest no asset-cooker ISPC (pré-existente, não meu) — consertado com `slow-timeout` no `.config/nextest.toml` (mata em 120s→retry; cobre o modo hang que o serial+retry-só-de-crash não pegava). nextest final = 3409 passed. **Fase 3.2 god-files de alto risco FECHADOS (2 commits locais, sem push):** (1) **pointer.rs** 1261→5 módulos-irmão por arm do match (`pointer.rs` 66 + `pointer_move` 310 + `pointer_down` 504 + `pointer_down_menus` 203 + `pointer_up` 244); pure-move via sed (byte-fiel), `events` virou `&mut BumpVec` param (3 `&mut events`→`events`), `return events.into_bump_slice()`→`return;`/`return true`; rede comportamental forte provou no-op (75 dispatch + 626 lib + 16 mapped-link + HR-3 `interaction_no_alloc`); commit `01e993f5`. (2) **compositor.rs** 2396→subdir `compositor/` (mod 519 + api 322 + buffers 329 + dispatch 434 + pass 365 + effects 475); pure impl-split — campos privados visíveis a descendentes (submódulos), 22 helpers→`pub(super)`, `ShTonal` privado em `compositor/mod.rs` (dispatch constrói, effects lê, ambos descendentes); **subdir (não flat) p/ `layer_compositor/mod.rs` ficar byte-idêntico → não cresce o 2º god-file (934)**; submódulos usam `use super::super::*` (itens do layer_compositor) + `use super::ShTonal`; 146 lib + **4 GPU device headless Metal** + clippy verdes; commit `a2f03028`. **Lição método:** GPU pure-impl-split é quase tudo compile-checked (visibilidade/campos); o risco real é body alterado no move → sed extrai byte-fiel + Metal `--ignored` confirma. `tail` MASCARA exit do cargo (vi exit-0 falso; smoke W2-W5 são stubs `unimplemented!()` pré-existentes, `#[ignore]`, CI nunca roda). **Fase 4 FECHADA (2 commits locais `d327fb5d`+`6d6cd881`, sem push):** purgou a canon read-every-step de refs a features deletadas (ADR-0099 pintura/brush · ADR-0096 watercolor). DIRETIVA §2 path brush 8-sites (`set_brush_param`/`lifecycle.rs`=0-file mortos) → seam genérico painel↔tool 7 sites; cycler → grid-snap vivo (`kind_option_ids_in_order`); §4 golden de pintura → paridade de efeito (`shader_adjustment_coefficients_bit_identical` + blend.rs + Metal); DIRETRIZ+examples template morto `ph2d-tool-brush`→`-move` (o `is_default` real é MoveTool), `Tool≤10`→`≤11`. **SESSION_ACTIVE.md reset** de 411 linhas de estado de mundo-deletado → idle (→ CLAUDE.md §5 + git log; era router-linked = alto dano). **Arquivamento agressivo investigado e NÃO feito (medir-antes salvou):** `Painter_projeto/02_layers.md` é design CANÔNICO do layer-stack/compositor/blend SOBREVIVENTES (citado por código vivo em 3 crates) → mover 404aria links; `Novo Painter/` morto mas referenciado por 3 comentários stale no shell + 2 ADRs. Árvores não-router-linked = agente não alcança em task → risco>valor. **Lição docs:** o "maze 418 .md" é majoritariamente ADRs + design citado por código + handoffs já arquivados; o dano real é via ROTEADOR (reconciliado), não volume. **Follow-up Enio:** 3 comentários stale `docs/Novo Painter` em `shells/desktop/src/{main,image_import,input_handlers}.rs` (brush deletado). **TODOS os 6 god-files >1000 LOC decompostos.** **Lição ship:** ship.sh exit code é mascarado por wrapper `echo` — sempre ler o bloco "ship.sh summary" (✓/✗) + `SHIP EXIT`, nunca confiar no exit da notificação.

**Lição durável (não-óbvia) — dispatch de widget de painel tem MÚLTIPLOS caminhos legítimos**, então gate textual estático tem de modelar todos senão dá falso-positivo: (1) focusable-widget (InteractiveState→Click via populate+event.rs); (2) special hit-zone (drag/resize/swatch despachado direto por hit-id em `interaction/dispatch/pointer.rs`, NÃO precisa store.register); (3) chrome global (`screens/hero/pre_populate.rs` registra INSP_*/HIER_* drag/resize/close p/ TODOS os painéis); (4) mapping indireto (grid-snap resolve opções via `kind_option_ids_in_order()` em paint_helpers, id literal nunca aparece no event.rs); (5) multi-arquivo (inspector = event.rs + event_ordering.rs); (6) registro dinâmico (`register_picker_swatch`). Por isso `register↔event` é não-confiável (kill-criterion disparou >30% FP) e a versão certa é `hit↔register` + união do registro global; o dispatch fica para o teste comportamental, que segue indireção. Ver [[feedback-tool-unit-green-integration-dead]].
