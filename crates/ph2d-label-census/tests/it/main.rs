//! Um binário de teste por crate (auditoria de velocidade de 2026-09-10, onda W1).
//! ⚠️ Teste novo = ficheiro novo AQUI + uma linha `mod` abaixo — nunca um `tests/*.rs` solto.

mod a_cegueira_da_regua_tem_nome_e_lista;
mod a_test_module_is_asked_to_its_parent_even_as_a_mod_rs;
mod the_reader_knows_every_form_that_already_cost_a_wrong_census;
