[package]
name = "ph2d-imageio-exr"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
license.workspace = true
authors.workspace = true

[lib]

# `ph2d-imageio-exr` — OpenEXR magic recognition + (deferred) decode/
# encode (W3.T2). Pure-Rust path via `exr = "1"` lands when first
# real client (Blender HDRi import) materialises. Magic-only stub
# now per ADR-0054 §3.8 fan-out drop-crate.

[dependencies]
ph2d-imageio = { path = "../ph2d-imageio" }
