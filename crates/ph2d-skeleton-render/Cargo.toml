[package]
name = "ph2d-skeleton-render"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
license.workspace = true
authors.workspace = true

[lib]

[dependencies]
# ⭐ O desenho do osso **já era** independente da mídia — ele nunca importou a cena vectorial, só
# a stack de desenho e os tokens. Mudou de casa em 2026-09-06 com o resto do módulo: um esqueleto
# sobre uma imagem ou sobre uma malha desenha-se exactamente igual.
# ⚠️ Vello pelas re-exports da `ph2d-vector` (nunca `vello` direto) — gate-proof, a mesma regra da
# `ph2d-vec-render` de onde este ficheiro veio.
ph2d-vector = { path = "../ph2d-vector" }
ph2d-tokens = { path = "../ph2d-tokens" }

[lints]
workspace = true
