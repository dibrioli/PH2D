[package]
name = "ph2d-node-motion-color-array"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
license.workspace = true
authors.workspace = true

[lib]

[dependencies]
# The palette TEXT FORM lives here, beside the gradient's — one crate owns how a
# colour is written down (the precedent is ph2d-node-motion-color-ramp).
ph2d-color = { path = "../ph2d-color" }
ph2d-nodegraph = { path = "../ph2d-nodegraph" }
ph2d-node-registry = { path = "../ph2d-node-registry" }
