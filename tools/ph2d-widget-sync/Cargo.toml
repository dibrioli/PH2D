[package]
name = "ph2d-widget-sync"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
license.workspace = true
authors.workspace = true

[lib]

[[bin]]
name = "ph2d-widget-sync"
path = "src/main.rs"

# Wave 10 / Etapa 4.3 — widget-family codegen.
# Scans `crates/ph2d-editor-core/src/widget/*.rs` (single-file widgets,
# not subdirs) and rewrites the `mod foo;` declaration block in
# `widget/mod.rs`. The `pub use foo::{...}` re-exports stay hand-written
# (per-widget export shapes vary).

[dependencies]
