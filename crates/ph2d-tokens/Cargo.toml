[package]
name = "ph2d-tokens"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
license.workspace = true
authors.workspace = true

[lib]

# ph2d-tokens é design-data puro — zero deps. Cores são structs simples,
# type/spacing são enums com const tables. Lookup é function call.
# Validation WCAG AA roda em tests/ sem precisar de libs externas.

[dependencies]

[dev-dependencies]
