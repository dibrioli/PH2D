[package]
name = "ph2d-pan-diag"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
license.workspace = true
authors.workspace = true

[lib]

[dependencies]
ph2d-host = { path = "../ph2d-host" }
ph2d-render = { path = "../ph2d-render" }
# ⚠️ **É por ESTA linha que a sonda não pode viver dentro de uma família:** ela já lê o estado do
# Flip. Pô-la em `ph2d-app-motion` faria a família `motion` depender da família `flip` — o que o
# HOWTO §1.2 proíbe por escrito. Uma folha pode depender das duas; uma família não.
ph2d-app-flip = { path = "../ph2d-app-flip" }

[lints]
workspace = true
