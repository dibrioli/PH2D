[package]
name = "ph2d-asset-index"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
license.workspace = true
authors.workspace = true

[lib]

[dependencies]
# ⭐ A álgebra dos caminhos de catálogo mora numa folha PARTILHADA com as tags
# (`docs/Components/08_plano_tags.md` §2.1) — zero dependências, então esta crate continua folha.
ph2d-label-path = { path = "../ph2d-label-path" }

[lints]
workspace = true
