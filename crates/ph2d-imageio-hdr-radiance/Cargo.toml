[package]
name = "ph2d-imageio-hdr-radiance"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
license.workspace = true
authors.workspace = true

[lib]

# `ph2d-imageio-hdr-radiance` — Radiance HDR magic recognition +
# (deferred) decode/encode (W3.T3). Pure-Rust via `image` 0.25 hdr
# feature when first real client appears. Magic-only stub now per
# ADR-0054 §3.8 fan-out drop-crate.

[dependencies]
ph2d-imageio = { path = "../ph2d-imageio" }
