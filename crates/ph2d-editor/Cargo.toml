[package]
name = "ph2d-editor"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
license.workspace = true
authors.workspace = true

[lib]

[dependencies]
ph2d-tokens = { path = "../ph2d-tokens" }
ph2d-a11y = { path = "../ph2d-a11y" }
# Vello rendering removed in the egui-migration pivot — egui owns
# widget paint now. Editor crate is back to data structures + a11y
# only; the shell renders panels via egui-wgpu directly.
