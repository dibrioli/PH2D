[package]
name = "ph2d-style"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
license.workspace = true
authors.workspace = true

[lib]

# ⛔ ZERO dependências de produção, e é o ponto — a mesma razão da `ph2d-view-transform`: esta é a
# lei que o traçado de CPU e o pintor do DISPOSITIVO têm de responder IGUAL. Uma lei que puxasse o
# `wgpu` ou a cena não serviria aos dois, e o gémeo em WGSL nasce com gate de paridade contra ESTA.
[dependencies]

[dev-dependencies]
# ⭐ O ORÁCULO dos pesos da luminância: eles são declarados aqui (a crate é folha) e o gate prova
# que concordam com a porta canónica da casa. *Uma lei escrita em dois sítios ainda não é uma lei.*
ph2d-color = { path = "../ph2d-color" }

[lints]
workspace = true
