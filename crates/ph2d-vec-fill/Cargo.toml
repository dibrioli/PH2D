[package]
name = "ph2d-vec-fill"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
license.workspace = true
authors.workspace = true

[lib]

[dependencies]
# ⚠️ **Crate-FOLHA sobre o documento**: ela conhece `VecVertex`, o corte por arco e a solda — e
# **nada** da cena, da pose, do painel ou do ponteiro. O balde é uma lei geométrica; quem sabe o
# que está visível e onde o dedo caiu é a shell.
ph2d-vec-scene = { path = "../ph2d-vec-scene" }
