# Física global — o motor rígido — estado do §5 em 2026-08-18

> **Arquivo histórico.** Este é o texto do `CLAUDE.md` §5 **verbatim**, como estava em
> 2026-08-18, antes de o §5 voltar a ser um roteador. O **estado vivo** (o que está aberto,
> o handoff corrente) fica no [`CLAUDE.md`](../../../CLAUDE.md) §5; o **mecanismo** fica nos
> handoffs que este texto cita. Nada aqui foi editado — só recortado.

---

- **Física global — o motor rígido** ([`docs/Physics/`](../../Physics) · [ADR-0131](../../architecture/decisions/0131-physics-global-runtime-truth-rapier-ecs-bridge.md) · plano [`00_plano_waves.md`](../../Physics/00_plano_waves.md) · tracker [`HANDOFF_line_physics.md`](../../Physics/handoffs/HANDOFF_line_physics.md); **W0+W1+W1.5+W2a integraram 2026-07-18; a linha está FECHADA com TODAS as waves e TODOS os smokes aprovados, aguardando ordem de integração**): um sprite ganha corpo pelo Inspector e **cai**; Play/Pause/Reset dirigem a sim; arrastar a régua pra trás re-simula **bit-exato**. **Norte (não re-litigar):** *runtime-truth* + bake opcional; **rígido primeiro**; o solver é o `rapier2d 0.28` que já existia (M10) — esta linha escreve **integração e autoria, não solver**. **A crate-ponte `ph2d-physics-ecs`** carrega os components `RigidBody{kind}` + `Collider{shape,density,restitution,friction}` e o `PhysicsBridge` (`AppGfx.physics`, ao lado de `sim`/`motion`; `render_loop/physics_bridge::dispatch` **antes do `sim_extract`**). ⚠️ **Component de física é CONFIG, nunca estado vivo de solver** — o `canonicalize` do undo ordena por BYTES do componente, então guardar velocidade/sleep ali faria **cada frame virar um passo de undo**. O mundo rapier **não é persistido** (D2): ele é *derivado* dos components a cada frame, como o Motion re-cozinha do grafo. **`BTreeMap`, não `HashMap`, é a espinha do determinismo** (itera por `Entity`, estável cross-OS; a lint disallowed-`HashMap` é o guarda estrutural), e o hash `physics-ecs-c9` roda na matriz 3-OS do `spike.yml` provando o **nosso** código no caminho determinístico, não só o wrapper. **Sem porta de escala (D4 corrigido MEDINDO):** o `Transform` já é METROS (Y-up, radianos CCW) = rapier metros ⇒ fronteira **1:1**; a única px→m é `ProjectSettings.pixels_per_meter` no import, **do projeto** — um 2º `PIXELS_PER_METER` seria a segunda porta que diverge. **O `dispatch` é função do TICK** (`target < last` = replay · `>` = step · `==` = hold): rapier não rebobina (**nenhum** motor rebobina — resolução de contato não é invertível), então cada corpo carrega o `BodyDesc` do spawn e `rewind_to` reconstrói o mundo e re-simula. ⚠️ **A pose de repouso é a pose AUTORADA no tick 0** — *lida* todo frame, não lembrada (senão mover um objeto e apertar Reset jogava fora o posicionamento do artista); e há gate irmão provando que a regra **não** dispara com o relógio andando, senão o `Transform` — que ali é a SAÍDA da sim — seria realimentado e o corpo renasceria a cada frame. ⚠️ **O `settle` só teleporta quando o `Transform` autorado de fato DIFERE do corpo**: `set_body_pose` zera a velocidade, então teleportar todo frame pausado fazia Pause→Play recomeçar a queda parada. **W1.5 — o ring de checkpoints:** GGPO save/load/advance sobre `Clone` dos 8 tipos cross-frame do rapier (**sem `serde-serialize`, sem bincode**; o `PhysicsPipeline` não é `Clone` porque é *workspace*, não estado). **`STRIDE = 10` é MEDIDO**: um checkpoint custa **~um step** (11,2 µs vs 7,3 a 50 corpos) ⇒ denso **dobraria o custo do play** e comeria 17,4 dos 20 MB — o oposto do Motion, cujo estado é pequeno. **Cap em BYTES (8 MB), nunca em contagem** (contagem é multiplicador — ADR-0117): cena pesada ganha janela mais CURTA, não conta maior. **O fallback É o produto:** miss → `rebuild_from_rest`, o caminho que já shipou no W1 — apague o ring e o produto ainda scrubba, só mais devagar. ⚠️ **O oráculo do scrub é a TRAJETÓRIA, não o endpoint** — uma pilha assentando é um sistema **amortecido**, ele *esquece* a perturbação e re-converge, então o gate ficou VERDE sob uma mutação real (`restore` sem o `narrow_phase`); o que o artista assiste é o CAMINHO. Corolário medido: tirar o `broad_phase` do restore **sobreviveu** a 2 fixtures ⇒ o BVH é **derivado**, não autoritativo (fica no checkpoint, mas ninguém precisa re-litigar por prosa). **W2a — a autoria (a seção "Physics Body" no Inspector):** antes disto **não existia gesto nenhum no editor que tornasse um sprite físico**. ⚠️ **A seção tem DUAS faces e a VAZIA é a importante** (`build_physics_info` devolve `Some` p/ qualquer entidade com `Transform`, `has_body: false`) — senão a física é alcançável só onde já há física, ou seja em lugar nenhum. ⚠️ **O collider nasce da CAIXA DO SPRITE**, a única forma inicial que não pode discordar do que está desenhado (uma bola default sob um sprite 2×1 desenharia retângulo e rolaria como círculo desde o primeiro clique — Unity e Godot ajustam a box ao renderer pelo mesmo motivo); e trocar de forma **preserva a pegada**. **O contorno do collider (tecla `B`, default ON):** *"os colliders parecem redondos mas os desenhos são box"* (Enio) **é o caso NORMAL** — um collider é invisível e um sprite é um quad, então a resposta é a que Unity/Godot/Box2D dão: wireframe sobre a arte (`render_loop/physics_overlay.rs`), **verde = estático / ciano = dinâmico** (*"quem aqui se move?"* é a 1ª pergunta que se faz a uma cena de física), bola com **raio-guia** (sem ele um círculo rolando é idêntico a um parado). ⚠️ **Geometria em px de TELA sob `Affine::IDENTITY`** — no Vello o transform do `stroke` **multiplica a largura**, e foi isso que virou o realce do Flip num borrão. **A INTERPENETRAÇÃO foi MEDIDA antes de mexer** (*"observa-se alguma interpenetração dos objetos dinâmicos com o chão"*): repouso **1,3 mm** (o `normalized_allowed_linear_error`, 0,13 px — não é o que ninguém viu) vs impacto **83 mm por 9 frames**. ⚠️ **A profundidade NÃO é falha do solver:** damping de contato, teto de velocidade corretiva, iterações extras e **CCD** deixaram o número em *exatamente* 83,2 mm — é **`v × dt`** (a 9,4 m/s o corpo anda 157 mm num tick, então no tick em que encosta **já está dentro**, e nenhum solver desfaz isso depois). Duas alavancas **ortogonais**: **`DEFAULT_SUBSTEPS = 4`** ataca a PROFUNDIDADE (1→83 mm · 2→73 · 4→31 · 8→8,8; é o joelho, e o **Box2D v3 ships o mesmo default pelo mesmo motivo**) e **`DEFAULT_CONTACT_HZ = 120`** ataca a DURAÇÃO (30 Hz→9 frames · 120→1) ⇒ **83 mm/9 frames → 23 mm/1 frame**, 264 µs p/ 500 corpos. Subir o **damping** — o conselho usual do rapier — vai pro lado **errado** (20 esticou a recuperação de 9 pra 30 frames). ⚠️ **`dt()` é o TICK, `substep_dt()` é o do integrador** — um nome, um significado (um `dt()` que virasse o sub-passo em silêncio discordaria do Playhead). Os dois hashes C9 **mudaram** com isso e **nenhum é pinado em literal** (o CI compara os 3 OSes entre si). **`PROJECT_SCHEMA` = 18**, tripla-pin `(18, 8, 8)` — ⚠️ o valor **se CONTA, não se escolhe** (o 17 desta linha + o bump da `line/FLIP` na mesma janela), e **nenhum gate consegue ver um campo APENDADO a um componente** porque nenhuma constante muda: postcard é posicional e devolve lixo bem-formado. ⚠️ **Um número de ADR escolhido numa linha paralela é PROVISÓRIO** — o 0130 desta linha virou **0131** no merge (dois donos, 2ª vez no repo). **Smokes:** `PH2D_PHYSICS_SMOKE=1` (queda) · `=2` (pilha + scrub — a cena é uma PILHA de propósito: é onde um scrub errado é *visível*) · `=3` (autoria no Inspector, relógio PAUSADO, 3 proporções diferentes). **W2b — O PAINEL GLOBAL DE MUNDO LANDOU (2026-07-18, smoke APROVADO):** crate `ph2d-panel-physics` docada, categoria MUNDO, aberta pela tecla **`W`** (espelho do `L` da timeline — painel de mundo não é tool-gated, então sem abridor próprio é feature que ninguém alcança). Gravidade · sub-passos · iterações · frequência de contato · arrasto · sono · Show Colliders · Reset. **UMA TABELA, QUATRO CONSUMIDORES** (`rows.rs::SECTIONS`): `paint`/`populate`/`event`/seam iteram a MESMA lista, então um knob nasce pintado, registrado, vivo e varrido — é a resposta estrutural ao *"o card mais cheio apodrece"*. **Não há tool ⇒ não há `ToolPanelEvent`**: o painel emite INTENTS que a ponte drena (padrão motion-graph/timeline). **O ARTISTA é dono da visibilidade** (sem edge-trigger, sem tomada do slot do Inspector). ⚠️ **Duas coisas são EXIBIDAS, nunca possuídas:** a escala é `ProjectSettings.pixels_per_meter` (D4, já tem dono no menu Settings) e o contorno é o `App.show_colliders`, o MESMO flag da tecla `B`. ⚠️ **damping e sono globais NÃO existem no rapier** — os dois são POR CORPO (medido); expô-los como setting de mundo é o idioma do Godot/Unity, e por isso nasceu o `BodyDefaults` com **uma porta só** por número (um override por-corpo, se vier, TEM de trazer modo de combinação — o `damp_mode` do Godot). **TODO TETO FOI MEDIDO** (`measure_settings.rs`): sub-passos **12** (500 corpos = 101,9% do HR-4) · iterações **16** (85,7%; 24 estoura) · contact Hz **480** (deriva 0,0000 mm até 960, aparece a 1920) · arrasto **10** (terminal ≈ g/d ⇒ 0,98 m/s). ⚠️ **A hipótese de Nyquist (120 Hz) foi REFUTADA pela medição** — as soft constraints do rapier são estáveis muito além; e a 1ª rodada do harness mediu NADA porque **a pilha DORMIA** (corpo dormindo não é integrado ⇒ os zeros eram garantidos, não observados). **`PROJECT_SCHEMA` 18→19** + tripla-pin `(19,8,8)`: as settings viajam no arquivo (senão seriam knobs que ESQUECEM), FORA do `ProjectState` (Ctrl+Z do canvas não rebobina a gravidade), e ⚠️ **o load instala DEPOIS do `rebuild()`** — antes seria escrever num mundo que ele joga fora, e a cena abriria com a gravidade do documento anterior, em silêncio (pinado por arch-gate sobre o fonte). **30 gates, 26 mutações, 25 sangram (1 sobrevive por projeto) — e TRÊS nasceram VERDES sobre o bug que existiam pra pegar:** o oráculo dos defaults comparava `BodyDefaults::rapier()` **contra ele mesmo** · o "cada row muda só o campo dela" computava a expectativa **com `row.set`** · o do scrub acertava o **RING** (que carrega o damping dentro do body set) em vez do `rebuild_from_rest`. **O padrão: um oráculo que usa a função sob teste para computar o que espera é sempre verde** — procure essa forma antes de confiar num gate que passou de primeira. **⚠️ O SMOKE REPROVOU DOIS, e os dois fecharam (2026-07-18):** (a) *"não vejo o painel, não abre com w"* — **ele não existia no build**: o shell põe `default-features = false` no `ph2d-panel-registry-init` e re-enumera os painéis na lista `default` DELE, então ligar a feature na lista `default` da crate de registry **não alcança ninguém**. Tudo a jusante funcionou sobre um painel inexistente (a tecla vira `panel_visibility`, o z-order pergunta o id, recebe `None`, não pinta) — sem erro, sem warning, e com o `EXPECTED_TYPED` **verde** porque ele roda na crate de registry com as features DELA. Gate novo mora **onde o shell é compilado** (`every_panel_the_shell_drives_is_in_its_registry`). (b) *"Air Drag… todos caem na mesma velocidade"* — **verdade, e o erro era o RÓTULO**: o `linear_damping` do rapier é decaimento **uniforme** (medido: 25× de massa, todas a **4,8925 m/s**, idênticas até a 4ª decimal) e isso é o correto daquele knob (Godot/Unity shipam o mesmo). Só não é ar. Portei a equação publicada (`F = ½ρCdA|v|v` ⇒ `a ∝ v²/s`) em `world/drag.rs`, e os **dois modelos coexistem separados por SEÇÃO**: **Air Drag/Density** (escala com a secção, resistido pela massa ⇒ o grande cai mais rápido) vs **Damping/Linear+Angular** (uniforme). ⚠️ **`add_force` do rapier é CONSTANTE até `reset_forces` e o pipeline nunca limpa** — por substep acumulou ~720× e os terminais saíram não-monotônicos (0,05/0,51/0,52/0,01 m/s); o primitivo certo é o **impulso** (`F·dt`). Teto `MAX_AIR_DRAG = 10` **medido**, e o recurso é o **limiar de sono**: a k=20 o corpo de 0,28 m fica abaixo dele e **dorme no ar**. **`PROJECT_SCHEMA` 19→20** (campo apendado). ⚠️ O gate *"o maior cai mais rápido"* **sozinho não basta** — com `length=1.0` o terminal ainda cresce com o tamanho; quem pega é a **forma fechada**. Smoke: **`PH2D_PHYSICS_SMOKE=4`** (painel já aberto; 12 corpos de 3 tamanhos — a cena é escolhida POR KNOB). **W2c — CAMADAS DE COLISÃO LANDOU (2026-07-18, smoke aprovado):** modelo da **Unity** (matriz global + camada por-corpo), não o do Godot/Box2D (`layer`+`mask` por corpo) — a regra é autorada UMA vez no mundo em vez de re-digitada em cada bala. rapier é nativamente o segundo, então `world/layers.rs` é o primeiro em cima dele: `memberships` = o bit da camada, `filter` = a LINHA da matriz. ⚠️ **A matriz TEM de ser simétrica e aqui o assimétrico é INEXPRIMÍVEL**: a regra do rapier é `(A.mem ∩ B.filter) ≠ ∅ **AND** (B.mem ∩ A.filter) ≠ ∅`, então meio-escrita não significa "i vê j mas não o contrário" — significa **colisão nenhuma**, uma regra que ninguém escreveu. `set` escreve as DUAS metades, `from_rows` **simetriza** na leitura de arquivo, e o painel desenha só o **triângulo inferior** (a célula espelho seria um 2º controle pro mesmo checkbox). `groups_for` é a porta única, e o collider já carrega a própria camada (ela É o `memberships`) ⇒ re-filtrar os vivos não guarda nada duas vezes. **8 camadas com o limite NOMEADO:** a representação permite 32 (`Group` é u32) e o que aperta é o painel — triangular de N tem N(N+1)/2 células: 8→**36**, 16→136, 32→**528** (a Unity shipa 32 e é o exemplo padrão de tela ilegível). **`PROJECT_SCHEMA` 20→21.** ⚠️ **Dois gates nasceram VERDES sobre o bug que existiam pra pegar** (mesma família das três do W2b): o da simetria envolvia o valor guardado em `from_rows` — a função sob teste — e o das 36 células mandava `WidgetEvent` **sintético**, que pula a checagem de FOCABILIDADE no store: tirar as células do `populate` deixava 36 widgets pintados, hit-registrados, com arm, e **mortos sob o mouse**. Agora dirige `click_at`. ⚠️ **As células são registradas num LAÇO, que o `architecture_panel_wiring_parity` não enxerga** — os ids são array **const** (pro `node_id_collisions` cobri-los) e o seam que clica as 36 é a única coisa cobrindo aquele widget. Smoke: **`PH2D_PHYSICS_SMOKE=5`**. **W3 — JOINTS LANDOU (2026-07-18, smoke aprovado):** um joint é uma **ENTIDADE** (o norte do [ADR-0110](../../architecture/decisions/0110-vector-nodes-are-ecs-entities-one-hierarchy.md): no vetor um path *é* uma entidade, árvore única) ⇒ herda Hierarquia, seleção, nome, delete, undo e save de graça — apagar um joint é apagar um objeto, sem "remover joint" pra inventar; e tira o TETO, porque um joint guardado NO corpo só pode ser um por corpo (bevy tem um componente de cada tipo por entidade), o que proíbe laço fechado e impede a pelve de um ragdoll de receber três. ⚠️ **A âncora sai de graça e não foi sorte:** o `snapshots.rs` já dizia, no próprio código, que o Position do Inspector *"lands on every entity that has a `Transform`, not just sprites"* — a entidade-joint carrega um, então o pivô é autorável em números no dia um, com **zero widget novo** (um gizmo de PONTO no canvas é outra coisa: os 3 publicadores de `GizmoView` são CAIXAS com alças de escala, e ficou fora). ⚠️ **Os dois corpos são NOMEADOS, nunca apontados** (`stable_name_id`, hash FNV-1a do `Name`): bits são id de ALOCAÇÃO e o undo respawna tudo com bits novos — e, **pior que a referência solta**, bits dentro dos *bytes de um componente* envenenam o próprio undo, porque o `canonicalize` ordena pelas bytes e remapeia só o `parent`, então dois estados logicamente iguais comparariam diferente (o passo espúrio que ele existe pra matar). A timeline já tinha essa resposta desde o W4.T6 ⇒ a função **subiu pro `ph2d-ecs`** (ao lado do `Name` de onde deriva) e o `timeline_persist` **delega** — duas cópias seriam duas respostas a *"qual é o id durável deste objeto?"*; ⚠️ o hash é **FORMATO DE ARQUIVO** (todo projeto em disco já o carrega) e o gate o pina contra valores computados **FORA** do codebase, porque chamar a própria função é o oráculo sempre-verde. Preço pinado num gate: **renomear um corpo DESACOPLA os joints dele** (e reacopla se o nome voltar) — a mesma exposição de toda binding da timeline. **A política de âncora numa frase:** o `Transform` do joint é a âncora em A; em B é o MESMO ponto pro pino (dois corpos num lugar só **é** o que um pino é) e o **centro de B** pra mola/corda, cujas pontas são pra estar separadas — colapsar o par foi MEDIDO e pendurava a bola de uma corda de 2 m a **2,5 m** (Box2D e Unity tomam o par pelo mesmo motivo). Joints reconciliam **DEPOIS** dos corpos e são re-descritos só em repouso (as âncoras locais derivam de onde os corpos estão, então têm de derivar de onde o artista os PÔS), e voltam no **MESMO chamado** do `rebuild_from_rest` — o rewind replaya na hora, e um replay sem os joints é outra simulação. **TRÊS números medidos, TRÊS palpites refutados:** ⚠️ **corpos unidos por um joint NÃO colidem entre si** e o default do rapier é o **OPOSTO** (`contacts_enabled: true`; Box2D `collideConnected` e Unity `enableCollision` são false) — o caso canônico é um elo de corrente, que sobrepõe o vizinho **por construção**, e ligado o motor mandado girar a 4 rad/s foi medido em **−80** · `MOTOR_TRACKING = 100` (a 10 não ergue o próprio braço; em TODO valor um motor capado em 0,1 N·m ainda trava, então o teto continua mordendo) · mola default **30/0,5** (era 100/5: a 100 uma bola de 0,2 kg afundava 1,9 cm num descanso de 1 m = 2%, que **lê como vara**). ⚠️ **Dois fixtures foram trocados porque MEDIAM MAL, não porque o código errava:** o pêndulo é **PERIÓDICO** (o gate pedia "termina embaixo" e falhava sobre um pêndulo perfeito, pego no alto do balanço ⇒ tudo virou **TRAJETÓRIA**, e a rigidez, sendo invariante, é checada a cada passo — a lição que o W1.5 já pagou) e uma prancha presa pela PONTA é um pêndulo **FORÇADO, caótico** (a mesma entrada caindo em −8,5/+9,1/+2,0 rad/s) ⇒ o motor se mede numa **RODA**, presa pelo centro, onde a gravidade não faz torque. ⚠️ **`PROJECT_SCHEMA` NÃO bumpou, e o plano pedia 21→22** — *o valor se CONTA, não se escolhe*: o blob de um componente é chaveado por `stable_type_id = blake3(NOME)[..8]`, então registrar `ph2d::physics::PhysicsJoint` cunha id novo e **não move nada** (o oposto do W2c, que apendou campo DENTRO do `Collider`, onde postcard é posicional); e bumpar não é neutro, porque schema divergente **recusa o arquivo inteiro** e jogaria fora todo projeto já salvo pra melhorar a mensagem de erro na única direção que não funciona de qualquer jeito. **O gesto de CRIAR mora na §11**, não na §12 — um joint não existe ainda quando você quer fazer um, então o botão fica onde você já está: "Join Selected Bodies" aparece com exatamente DOIS corpos selecionados, fato que só a shell enxerga, perguntado UMA vez (o pintor decide se oferece, o arm se honra). ⚠️ **Join NÃO pode fan-out** (todo outro edit da §11 é por-entidade e a `render_loop` o espalha; espalhado, criaria **dois** joints entre os mesmos dois objetos no clique que deveria criar um) — interceptado antes, com **arch-gate sobre o fonte**, porque nenhum unit test alcança aquela função. Corpos sem `Name` são **nomeados** na criação (um corpo sem nome é um que um joint não aponta). **A §12** pinta só os parâmetros do tipo escolhido, e quem responde *"este tipo tem motor?"* é `JointKind::is_hinge` — que a **PONTE pergunta também** antes de entregar ao solver ⇒ knob pintado que o solver ignora não existe; graus na fronteira, radianos no componente. **O joint na TELA:** um collider é invisível e um joint é MENOS que isso (é uma relação, sem geometria) ⇒ segmento entre as âncoras + anel em cada ponta, em **âmbar**; ⚠️ as âncoras vêm do **SOLVER**, não do `Transform` (que é a âncora *autorada* e nada reescreve no play — desenhar dele pregaria o marcador onde o artista o largou enquanto a corrente balança embora), e é por **coincidirem** que existem os anéis: um pino em repouso tem as duas âncoras no MESMO ponto e o segmento de comprimento zero **não pinta nada**. **56 gates, 39 mutações, 39 sangram** — ⚠️ a 1ª rodada teve UMA sobrevivente (a ordem determinista de inserção, que com **um** joint não pode importar; o gate que faltava monta a MESMA corrente com os arquétipos caindo diferente — um joint com `Name`, outro sem — e exige o mesmo hash). ⚠️ **A auditoria de 2 lentes achou SEIS coisas, e as duas graves eram minhas:** (1) **qualquer joint dormente destruía o cache de scrub, todo frame** — `joints_to_remove` era populado para todo joint que não dava pra construir INDEPENDENTE de ele ter SIDO construído, então a lista nunca esvaziava, `ring.clear()` disparava a cada frame e o W1.5 morria calado (scrub pro tick 150 replayava **150** passos em vez de 0); a metade dos CORPOS nunca teve esse bug — ela só enfileira quem está em `self.bodies`, e a dos joints tinha divergido da própria irmã. (2) **A âncora ANDAVA pelo corpo**: `rest` guardava MUNDO e o spawn re-derivava contra a pose VIVA ⇒ o spawn ao vivo e o `rebuild_from_rest` respondiam *"onde no corpo isto está preso?"* diferente — **1,611 m ao vivo, 0,642 m depois de um Reset**, 0,969 m de caminhada sem ninguém tocar em nada; e o doc afirmava uma regra de "só em repouso" que **não existia**. O fix não foi gatear o gesto: a âncora virou **função do estado AUTORADO** (converte UMA vez, contra a pose de **REPOUSO**, e guarda o par **LOCAL**) — ⚠️ o gate nasceu vermelho com 1,771 m e **sobreviveu ao primeiro fix** (guardar locais mas ainda converter contra a pose viva). Mais quatro: `clamped()` na porta de carga (NaN em `stiffness` levava a pose pra `(NaN,NaN)` e o readback escrevia isso no `Transform` **e no hash**) · **limites invertidos SOLDAVAM a dobradiça** · `create_joint` dizia Some pra dois corpos de mesmo NOME · e um comentário sobre o motor que nunca foi verdade. E a lente de seams: as **duas** seções desta linha (§11 e §12) pintavam dot de cor e chevron de collapse que não estavam em NENHUMA das três listas compartilhadas — *painted, hit-registered e mortos sob o mouse*; ⚠️ o braço do picker **ENUMERA seus leitores e já apodreceu** (NAME/VISIBILITY armam e não abrem nada), então as minhas duas entraram e o resto está **nomeado no handoff em vez de contrabandeado** (são waves de outros donos). Smoke: **`PH2D_PHYSICS_SMOKE=6`** (pêndulo · corrente · ragdoll: cada um responde uma pergunta diferente — a âncora na PONTA, os elos sobrepostos, os limites do joelho). **Aberto no W3:** ~~gizmo de âncora no canvas~~ (**FECHADO — W-JointAnchor, 2026-07-24, cena `=38`; ver abaixo**) · ~~re-escolher os corpos de um joint~~ (**FECHADO — W-JointAuthoring, 2026-07-25, cena `=40`; ver abaixo**) · Weld/`FixedJoint` (~4 linhas, fora de propósito: nada no smoke o exercita) · motor em mola/corda. **W4 — BAKE-TO-TIMELINE LANDOU (2026-07-18, smoke aprovado):** assar **não é simular de novo, é ANOTAR** — a sim já é função pura de `(tick, repouso autorado)` e o W1.5 provou todo tick alcançável e bit-exato, então o bake é a MESMA simulação com a pose anotada em **cada** tick. `ph2d-physics-ecs::bake` lê a trajetória (headless, sem saber o que é uma curva); o shell escreve as chaves — a divisão é deliberada, porque *"o que é uma chave"* já tem resposta e uma segunda morando dentro da física é como uma curva assada passaria a diferir de uma gravada. **O bake DEVOLVE o relógio** (gate red-first: clicar Bake olhando o tick 40 teleportava a cena pro fim do alcance). **O ajuste é o do RECORD, EXTRAÍDO e não reescrito** (`render_loop/record_fit.rs`): duas coisas produzem um valor por frame e viram keyframes — o record da timeline e o bake — e agora é a mesma resposta. ⚠️ **Mas o passa-baixa é um número da ENTRADA, nunca do ajuste** (MEDIDO): as 8 passadas existem porque uma mão carrega **tremor**; um solver não carrega nenhum, e uma trajetória de física é feita de **IMPACTOS** — um quique é uma **cúspide** e o kernel binomial arredonda o ápice, que *é* o quique. Pior erro contra a pose simulada: **0 passadas 2,13% · 1→2,02% · 2→2,48% · 4→3,31% · 8 (a do record) 5,70%** — monotônico, *o número que a suavização existe pra melhorar era o que ela estava piorando*. O bake passa **0**. ⚠️ **O bake ENTREGA a pose (`BodyKind::Kinematic`), e isso É o bake** — a **ordem do frame** decide: o apply da timeline escreve o `Transform` e o readback da física escreve **DEPOIS**, então um corpo dinâmico recém-assado é sobrescrito pelo solver todo frame e o artista clica, não vê nada mudar, e conclui que o botão está quebrado (**dois autores de um fato, o de trás vence calado**). O flip vai pela MESMA porta do chip da §11, e o playhead **rebobina** — que é também o que o faz chegar ao rapier (`reconcile_structure` re-descreve corpo **em repouso**). ⚠️ **Duas filas de undo:** as chaves são da timeline (UM bracket, gateado), o kind é edição de objeto e cai na fila global ⇒ um Ctrl+Z devolve o corpo a Dynamic com as curvas de pé; o toast e o chip tornam o estado visível em vez de inferido. **`BodyKind::Kinematic` chegou** (o componente já o prometia desde o W2/W3) e o gate da carona achou um bug **cuja lei já estava escrita uma linha acima**: `step()` roda `substeps=4` e `set_next_kinematic_position` é consumido pelo **PRIMEIRO** deles ⇒ a plataforma atravessava o tick inteiro em ¼ de tick a 4× a velocidade e ficava parada nos outros três; o atrito não tinha o que integrar e **a carga andava 0,009 m dos 1,000 m** — e o comentário do `drag::apply`, imediatamente acima do laço, diz *"Per SUBSTEP, not per tick"*. Fix: a mira é pro fim do **TICK** e o `step` a **fatia** entre os sub-steps (**0,009 → 0,975 m**; sono foi medido e **não** é fator). A **última fatia é o TARGET**, não um caminho aritmético até ele (`a0+(a1-a0)` não é sempre `a1` em f32, e a pose é lida de volta pro `Transform` ⇒ um ulp por tick, sempre na mesma direção). `BodyKind::tag`/`from_tag`: o mapeamento tag↔variant estava escrito **duas vezes** e o consumidor dobrava todo tag desconhecido em `Dynamic` — com 2 variants era redundante, com o 3º seria um chip que seleciona outra coisa. **Canal que nunca se move NÃO vira track**, e isso protege TRABALHO: escrever uma track é tomar posse do canal, e assar um X plano por cima de quem o artista já animou de lado apagaria a animação dele. Constância **bit-exata** (limiar onde o domínio é vazio). **O alcance é mostrado NO BOTÃO** (`Bake 5.0s to Timeline`): loop armado → extensão do documento → `DEFAULT_BAKE_SECONDS` (medido). ⚠️ **Bake NÃO faz fan-out e errar custa mais que no Join** — uma corrida da sim serve TODOS os selecionados; espalhado re-simularia a cena inteira uma vez por corpo (**mesmos números**, N× o trabalho) e deixaria N passos de undo, então desfazer *"o bake"* custaria tantos Ctrl+Z quantos objetos, e **nada pareceria errado**. **36 gates, 30 mutações, 28 sangram**; ⚠️ **o sobrevivente GRAVE da 1ª rodada:** apagar o ajuste deixa **uma chave por tick**, o que reproduz a sim EXATAMENTE (o gate de fidelidade fica mais verde que nunca) e custa um passo de undo — **a entrega da wave não estava gateada por nada**. Dois sobreviventes FICAM, documentados: o early-out do bake vazio é sobre **custo** (o `commit_if_changed` é a garantia real) e o `COLUMN_MERGE_S` é **INERTE** num bake — medido, `[0.0, 0.583, 1.5]` com e sem a fusão: uma mão cruza cada eixo milissegundos separada, um bake amostra tudo na **MESMA grade de ticks**. **`PROJECT_SCHEMA` e `DOC_VERSION` INTOCADOS** (variant apendado não move layout; o bake só acrescenta chaves). Smoke **`PH2D_PHYSICS_SMOKE=7`** (rampa + bola que ROLA — rotação é o 3º canal e o mais fácil de errar calado — + duas caixas, porque assar DOIS corpos num clique é onde um fan-out apareceria). **W4b — O TOGGLE `Physics` DO TRANSPORTE (2026-07-18, smoke aprovado — *"Funciona muito bem"*):** *"os controles de simulação e de animação parecer ser os mesmos e na timeline o play ativa a simulação física … a simulação roda junto com a animação"* (Enio) — o conflito era real: o transporte é **UM relógio com DOIS consumidores** (as curvas e o mundo rapier), então revisar uma animação com scrub/play também **derrubava mais um pouco** todo corpo dinâmico, e a cena que o artista julga nunca era a que ele autorou. Agora há um toggle **Physics** na barra, ao lado de Loop/PingPong (o cluster do *que o relógio FAZ*, não o de autoria AutoKey/Record), **desmarcado por padrão** — a resposta a *"o que o Play faz?"* tem de ser a mesma no frame seguinte a um load que era antes do save, e simulação que se arma sozinha é cena que já mudou quando você olha. `TimelineFlags::simulate_physics`, **não serializado** (classe do Record; `DOC_VERSION` e `PROJECT_SCHEMA` intactos). ⚠️ **ISTO CORRIGE UMA NOTA MINHA DO W4** que dizia *"o desligamento manual seria o desenho errado de qualquer jeito"*: ela respondia *"o Bake desliga a física no corpo assado?"* (não — ele **entrega a pose** via `Kinematic`, porque o readback escreve DEPOIS do apply) e enunciou isso como verdade sobre **qualquer** interruptor. São perguntas diferentes e não se tocam: **o toggle diz se o solver RODA, o `Kinematic` diz quem ESCREVE A POSE quando ele roda.** ⚠️ **"Off" NÃO é "pule o dispatch" — é `PhysicsBridge::hold`**, e as 4 coisas que ele ainda faz são 4 bugs distintos: **reconcilia** (corpo autorado desarmado existe e desenha contorno) · **assenta** (o corpo rapier segue o `Transform` autorado ⇒ **armar retoma do que está na TELA**, a única retomada previsível) · **`last_stepped` segue o alvo** (a armadilha: 10 s desarmado e a ponte deveria **600 ticks**, simulados num frame) · **descarta o ring** (todo checkpoint descreve corrida que acabou; semear um scrub com ele responderia de antes de o artista mexer na cena — e só nos ticks em cache, então o scrub discordaria de si mesmo). **Nada em `hold` escreve `Transform`** (é o que o off promete; `settle` lê a cena e escreve o rapier, o `readback` só existe nos caminhos que dão passo). Preço documentado: scrubbar pra trás sobre trecho nunca simulado o replaya — a trajetória daqueles ticks não existe. ⚠️ **O default quase quebrou todos os smokes:** as 7 cenas abririam PARADAS e leriam como "a física quebrou" ⇒ o prólogo do `physics_smoke.rs` arma o flag, e a **cena 7 pede que o artista o DESARME** — é a demonstração inteira do Bake (assar converte simulação em **animação**, e animação é o que toca com o solver off). 12 gates, **13 mutações, 13 sangram**. ⚠️ **Dois achados do processo de mutação:** um "sobrevivente" era o **meu harness** (`--bins timeline_bridge_tests` casa com **zero** testes — o módulo é `render_loop::timeline_bridge::tests` — então o verde era *nada rodou*; [[feedback_a_negative_search_needs_a_positive_control]]) e o outro era **real**: apagar o fill do `rebuild` ficava verde porque o gate do painel **constrói o `TimelineViewSnapshot` à mão** e nunca chama o `rebuild` — o fixture não continha o fenômeno. Smoke: a **mesma `PH2D_PHYSICS_SMOKE=7`** (assa, desmarca Physics, dá Play: o movimento assado continua, a caixa não-assada para de cair). ⚠️ **A AUDITORIA DE 2 LENTES ACHOU DEZ COISAS, e as três piores eram UMA:** `Kinematic` faz a sim depender de um **fluxo de entrada por-tick**, e três lugares supunham que ela depende só de `(tick, repouso)` — o laço de ticks devidos, o **replay** do rewind e o **bake**. Fix: **`SceneAtTick`** — a ponte PERGUNTA onde a cena põe seus corpos dirigidos num tick dado, e quem responde é a metade que possui as curvas (o shell, via `apply_from_doc`); a física não aprende o que é uma timeline, e o invariante volta a valer (*o mundo é função do tick, dado o repouso **e as curvas***, ambos reproduzíveis). `dispatch` mantém a assinatura (99 chamadores intactos) delegando a `dispatch_with_scene`. Medidos: **PLAY** — eu criei este consertando os sub-steps (o `step()` limpa a mira todo passo e eu mirava FORA do laço) ⇒ arrastar a régua ao tick 60 **ARREMESSAVA** a carga (1,049 → **−0,520**); e mirar o MESMO alvo N vezes não resolve, a mira é **absoluta** ⇒ o vão é **fatiado** entre os ticks devidos · **SCRUB** — o replay rodava `step()` puro ⇒ corpo kinematic congelado, e **a resposta dependia do CACHE** (3,4 cm num replay parcial; *"a caixa nunca viajou"* num miss do ring) · **BAKE** — nada avançava a timeline ⇒ todo corpo já assado ficava parado e o bake reportava **X constante** para um objeto que anda um metro. **Mais quatro do produto:** o bake dava `RigidBody{Kinematic}` a **toda** entidade selecionada (sprite comum ganhava órfão sem `Collider`, invisível na §11 e **salvo no projeto**; achado pelas DUAS lentes) e o braço `Kind` **anexava** o componente sem conferir corpo · assar **TOCANDO** nunca entregava a pose (`rewind` preserva o play state e `advance_ticks` roda ANTES do dispatch ⇒ `at_rest` nunca mais é verdade) ⇒ o bake **pausa** · as chaves iam em **segundos crus de playhead** e não no relógio da ENTIDADE (**1,618 m** de erro sob Time Remap de meia velocidade — [[feedback_derived_coordinate_seed_must_match_sample]]) ⇒ agora por `key_time`, e instante sem resposta única **recusa o bake inteiro** · bake dentro de bracket alheio + toast que mentia. **E DOIS defeitos de GATE, os dois meus:** `a_kinematic_body_follows_the_transform_it_is_given` era **VAZIO** (afirmava um valor que ele mesmo escrevera e que o `readback` não pode tocar ⇒ **verde com o drive inteiro deletado**) · e o **`architecture_panel_wiring_parity` só lia arquivos cujo NOME continha "paint"** — o Inspector pinta quase tudo de `src/sections/*.rs`, então **todo id registrado lá era invisível ao gate**, no painel com mais widgets do app (buraco que **precede a wave**; alargar surfou **ZERO** ofensor, e agora tirar botões do `populate` fica VERMELHO). **TRÊS fixtures nasceram fracas, todas por não conter o fenômeno:** o scrub media o **endpoint** depois de um replay que lavava o erro (sistema amortecido re-converge — a lição do W1.5 dentro de um gate que a cita) · o alvo caía **exatamente na grade do `STRIDE`** ⇒ o ring sedia ali e replayava **zero** passos · e a plataforma andava devagar demais. **W5 — CORPOS FILHOS (2026-07-18, smoke OK 2026-07-19):** correção de bug, não capacidade. **O collider não estava onde o sprite estava** em todo corpo parenteado: `Transform` é **LOCAL** e compõe com o pai, o solver fala **MUNDO**, e a ponte lia/escrevia cru nos dois sentidos. Medido: bola em local `(0,4)` sob pai em `(5,0)` ⇒ **simula em x=0, desenha em x=5** — sem erro, sem warning; o collider colide com o que o artista não vê. ⚠️ **Para corpo-RAIZ os dois espaços coincidem**, e é só por isso que isto atravessou 4 waves e 190 gates verdes: *toda* fixture, cena de smoke e demo usava raiz — a premissa nunca foi escrita, era verdade por acidente do fixture. ⚠️ **O `readback` prometia a correção desde o W1** (*"child bodies land in W2"*); o W2 shipou em 3 pedaços e nenhum tocou nisto — **nota que promete wave futura apodrece pior que nota errada**, porque lida de passagem ela TRANQUILIZA. **São CINCO sítios, não um** (`body_desc` nasce/repousa · `settle` compara local com mundo ⇒ todo filho parece "movido à mão" **todo frame pausado** e é teleportado com velocidade zerada · `drive_kinematic` leva a carga por um caminho que ninguém autorou · a **âncora do joint** é ponto de MUNDO · `readback`): meia correção deixa o sistema **estável e errado**, derivando um offset-de-pai por frame. **A álgebra do repo é INVERTÍVEL e isso apagou o compromisso** que eu ia propor (*"só pai rígido"*) — `compose` soma rotações, multiplica escalas e cisalha com `[[1,tan sx],[tan sy,1]]` ⇒ `Transform::inverse_compose` existe **exato**, mora AO LADO da lei que inverte (módulo novo `ph2d-ecs::transform_inverse`), e o round-trip é a especificação inteira (**erro medido 6,08e-6** sobre rotação, escala não-uniforme, escala NEGATIVA e skew nos 2 eixos). ⚠️ **A guarda de degeneração não é limiar, e a v1 estava errada:** `det == 0.0` nasceu VERMELHA (em `f32` um shear construído singular dá `det ≈ 1e-8`) ⇒ virou **"todo campo do resultado é finito?"** — a pergunta que o chamador de fato tem (*posso guardar isto?*), sem número mágico, e que ainda recusa `NaN` vindo da ENTRADA; pai **mal-condicionado passa de propósito** (local enorme, mas `compose` o leva de volta à pose certa). Recusar importa porque `±inf`/`NaN` num `Transform` **envenena o `GlobalTransform` da SUBÁRVORE inteira** (o próprio `debug_assert` do `compose` diz isso). Duas portas em `bridge/space.rs`; buffer `chain` persistente ⇒ zero alloc. **12 gates, 10 mutações, 10 sangram** (uma por sítio); regressão pinada: **corpo-raiz é BYTE-IDÊNTICO**. ⚠️ O `hot_path_no_alloc` **não tinha hierarquia na fixture** ⇒ o zero-alloc do caminho novo era afirmado sobre código que ela nunca entrava; e `scratch_capacity` passou a **somar** os buffers. LOC: 2 tetos, os 2 por split — `transform.rs` estava EXATAMENTE na linha congelada (784) e a catraca **BAIXOU para 768**; `physics_smoke.rs` → irmão `physics_smoke_rigs.rs`. **Aberto:** ~~a escala não alcança o collider~~ (**FECHADO — W6, ver abaixo**) · `GlobalTransform` não é consultado (é `PresentComponent`, a física roda no `SimWorld`). Smoke: **`PH2D_PHYSICS_SMOKE=8`** (3 rigs, um deles ROTACIONADO, cada bola sobre um pedestal **estreito** — quem voltar a ler local como mundo cai pela linha x=0 e some de quadro). Registro: [`BUGS_physics.md`](../../Physics/BUGS_physics.md) #2. **Aberto:** ~~assar um **JOINT**~~ (**FECHADO — W-BakeJoint, 2026-07-25, cena `=39`; ver abaixo**) · escolher os canais · ~~alcance com INÍCIO~~ (**FECHADO — W-BakeRange, cena `=37`**) · um Ctrl+Z para as duas metades · `reconcile` de corpos obsoletos é O(N²) (trivial nos counts de hoje) · o `readback` só trata corpo **raiz**. **W-Area — O CAMPO DE FORÇA (2026-07-21, smoke `=24`):** uma área que **EMPURRA** o que está dentro dela (vento · corrente ascendente · esteira) — um collider **sensor** carrega um vetor em **newtons**, aplicado por sub-passo a todo corpo dinâmico que o sobrepõe. ⚠️ **FORÇA, nunca aceleração:** o impulso `F·dt` é **resistido pela massa** (a folha voa, o caixote não), e uma zona de *aceleração* seria a 2ª resposta ao que o `GravityScale` já diz por-corpo. ⚠️ **A zona e o One-Way ficaram MUTUAMENTE EXCLUSIVOS, e é física:** one-way modifica **CONTATOS** (que sensor não gera) e a zona lê o grafo de **INTERSEÇÃO** (que só existe com sensor) ⇒ cada controle é morto no modo do outro; sólido pergunta *de que lado*, sensor pergunta *com que força* — os primeiros controles da §11 gateados em **outro CONTROLE**, não no kind. ⚠️ **O impulso ACORDA o corpo** (o `drag` não): a esteira sob o caixote adormecido é o caso de uso. ⚠️ **TRÊS lições de fixture, a mesma doença:** os dois CONTROLES foram *atropelados pelo próprio experimento* (12,9 m de "não deve se mexer"; o de ECS foi para **contra o vento**, porque ali o caminho é uma direção) · a fixture do sono **não continha o fenômeno** e por isso 2 mutações passaram (a bola só dorme se a zona nascer DEPOIS) · o filtro `intersecting` sobreviveu a 5 gates porque **toda fixture era uma caixa**, onde forma e AABB coincidem — o gate que faltava usa uma zona **REDONDA**. ⚠️ **Duas defesas em camada medidas e NÃO load-bearing** (o `apply_impulse` do rapier já abre com `if !impulse.is_zero()`; o grafo de interseção já exige sensor) — documentadas como custo+porta-única, no molde do early-out do `drag`; e um guard de auto-empurrão foi **removido por inalcançável** (um corpo, um collider). Componente `AreaEffector`, registro **14→15**, `PROJECT_SCHEMA` **fica em 29**; c9 **67→69**; tabela `effectors` **ordenada por handle** (soma de `f32` em ordem fixa, HR-5). **Seta laranja no overlay** (*para que lado sopra?*), desenhada **mesmo no play** — força é propriedade da ÁREA — reusando a régua da seta de velocidade via 1 kg de referência. **LOC: TRÊS splits por responsabilidade** — os 5 braços de marker viraram `inspector_physics_markers.rs` (e ganharam o gate de shell que nunca tiveram), `components.rs` → `components/overrides.rs`, e os testes de overlay separam **geometria** de **cena**. 12 mutações, 11 sangram. Smoke `=24` com números **medidos** (a 1ª esteira usava 6 N e jogava o caixote **para fora do mundo** em 1 s). **Aberto:** falloff dentro da área · ~~torque de área~~ (**FECHADO — W-AreaTorque, cena `=32`**) · arrasto de área (é pergunta por-REGIÃO, o `DampingOverride` é por-CORPO) · a força é em eixos de MUNDO, então girar a zona não gira o vento. **W-Contacts — QUEM TOCA QUEM, ONDE, SOB QUE CARGA (2026-07-21, smoke `=25`):** o canal SÓLIDO, irmão do sensor do W7 — um sensor diz *quem está dentro*, este diz *em quem estou encostado*, e um contato tem **lugar** e **carga**. ⚠️ **Read-only é o contrato inteiro:** nada no `step` chama, o c9 **não ganhou corpo** e o hash saiu **byte-idêntico** (`c01d4c6a…`, 69) — com gate pedindo o hash antes/depois de uma leitura completa. **Um relatório por PAR** (uma caixa deitada tem 2 quinas; relatar cada uma responde *quantas quinas*, fato sobre tesselação, não sobre a cena) com o ponto mais PROFUNDO + o impulso SOMADO. ⚠️ **O que o impulso É, e o 1º gate perguntava errado:** cair de 6 m e estar parado dão o MESMO número (**0,010032237 vs 0,010032236**) — o `step` retorna depois de o solver já ter parado o corpo, então o pico do impacto vive ENTRE os sub-passos; o número é a **CARGA que o par carrega agora**, e numa pilha de 4 caixas sai **4:3:2:1** (fato sobre a CENA, o mesmo em qualquer timestep ⇒ o oráculo certo). ⚠️ **A banda do quase-toque foi medida:** 2 círculos a 0,566 são **1 par no grafo, 0 contatos ativos** (a 0,003 num eixo nem estão no grafo) — e **DUAS camadas** honram isso (a flag do rapier + o `?` do `find_deepest_contact`), cada uma bastando sozinha, então só mutar **as duas** deixa o gate vermelho; fixture **REDONDA** de propósito (numa caixa, forma e AABB são o mesmo retângulo — foi assim que o filtro do W-Area sobreviveu a 5 gates). **A metade visível é a CRUZ BRANCA** (nenhum collider/joint/campo usa branco; um toque é evento *entre* duas coisas), braços de 3→9 px com régua `LOAD_FULL_NS=0.05` **medida**, cruz e não ponto (contatos assentados ficam a milímetros; um disco vira mancha), por cima de tudo. **Lista PLANA, não mapa** — um contato não tem dono (o `triggers` é mapa porque um trigger é perguntado sobre UMA entidade). **Sem componente, sem `PROJECT_SCHEMA`** (contato é estado vivo do solver, o oposto de config). ⚠️ **Comentário mentiroso corrigido de passagem:** o `is_triggered` do W7 dizia que *o Inspector lê isto para o readout de N inside* — **nunca leu**, a §11 não tem row de readout. 9 mutações, 9 sangram. ⚠️ **O smoke `=25` teve o V de cabeça para baixo** (rotações trocadas montavam um **Λ**; a bola rolava do pico e a sonda headless mostrou 1 contato onde deviam ser 2) — agora 2 marcas, 0,00872 cada, metade do peso em cada rampa. **Aberto:** eventos de INÍCIO/FIM (outra estrutura, memória entre frames, consumidor é gameplay) · força de impacto real (acumular o pico DENTRO do laço de sub-passos custa em toda cena por uma leitura de debug) · readout na §11. ⚠️ **E o [`00_plano_waves.md`](../../Physics/00_plano_waves.md) estava MENTINDO:** o mapa dele parava no W5 e o módulo tem 18 waves — a tabela das posteriores entrou lá (uma linha cada, detalhe no tracker), com a regra de que wave fora do mapa entra na MESMA sessão. **W-AreaDrag — A ÁREA RESISTE: vento vs ÁGUA (2026-07-21, smoke `=26`):** a outra metade do campo de força, aberta desde o W-Area. Zona com força e sem resistência é um **vácuo que sopra** (a caixa é arremessada e nunca desacelera); com `drag` ela entra, **afunda devagar** e lê como líquido. **A MESMA lei em todo lugar** — `v /= 1 + d·dt`, o `apply_damping` do próprio rapier, igual ao default de mundo e ao `DampingOverride`; damping **linear E angular** com um knob (um meio resiste a giro; o Godot separa os dois, aqui a região é uma *substância* e o caso assimétrico já tem dono por-corpo). ⚠️ **Mesma LEI, aritmética diferente, e o gate pina o número:** o gate nasceu pedindo igualdade e ficou vermelho por **1,25%** (mundo 1,3512776 · zona 1,3681686) — o rapier aplica damping DENTRO do velocity solver e a zona no topo do sub-passo, alcançando só quem o sub-passo ANTERIOR reportou ⇒ **exatamente um decaimento a menos**, razão `1 + d·dt_substep` = **1,0125**, que é o que o gate afirma (mudar ONDE se aplica vira quantidade nomeada, não 1% misterioso). Escrever no `linear_damping` do rapier daria igualdade exata e foi **rejeitado**: seria o 3º escritor de um campo cujo clobber o W-Damping já pagou. ⚠️ **DOIS componentes, e a razão é o custo de um BUMP:** o wrapper junta em `AreaEffect{force,drag}` (não é serializado), mas do lado ECS ficam `AreaEffector` + **`AreaDrag`** separados — blob de componente é postcard **POSICIONAL**, então apendar campo seria bump de `PROJECT_SCHEMA`, e **um bump RECUSA todo projeto já salvo**; jogar fora trabalho real para evitar um 2º componente é o trade errado. Registro **15→16**, schema fica em **29**; c9 **69→71**. ⚠️ Trocar `Option<[f32;2]>` por `Option<AreaEffect>` custou **2 sítios de fixture, não 40** — todo `effector: None` segue compilando: trocar o TIPO dentro de um `Option` é barato onde apendar campo é caro. **Sem visual novo, e é decisão:** força precisa de seta (*para que lado sopra?* não é inferível), arrasto **se vê nos corpos desacelerando**. 9 mutações, 9 sangram. Smoke `=26` mede três meios com as MESMAS três caixas: no vento a média estava no fundo em **1 s**; na água ela ainda atravessa a poça em **5 s** (y=0,87), a pequena flutua (2,23), a pesada chega ao chão. **Aberto:** empuxo de verdade (hoje flutuar é força constante vencendo o peso; empuxo real depende da fração SUBMERSA, e a zona não sabe quanto do corpo está dentro) · a força é em eixos de MUNDO · falloff · torque de área. **W-Buoyancy — ARQUIMEDES: a área sabe QUANTO do corpo está dentro dela (2026-07-21, smoke `=27`):** fecha a lacuna que o W-AreaDrag nomeou. "Flutuar" era uma `Force Y` constante vencendo o peso, com **três** defeitos: **não se auto-nivela** (a força não sabe onde a superfície está ⇒ o corpo leve é *arremessado para fora da piscina*) · é por **MASSA, não densidade** (o número certo muda por objeto, quando a intuição é *madeira boia, pedra afunda* — propriedade do MATERIAL) · **não endireita nada**. `ρ·|g|·A_submersa`, para cima, no **centroide da parte submersa**, resolve os três com **um número só** — e o centroide se deslocando quando o corpo inclina **endireita o barco de graça**. ⚠️ **`apply_impulse_at_point`, não `apply_impulse`** (o único gate que distingue as duas). **Superfície ⊥ à GRAVIDADE, não ao eixo Y** (água é horizontal mesmo em poça torta; gravidade lateral ⇒ superfície vertical), e **sem gravidade não há empuxo** — degenerado que a física resolve sozinha. **Polígono do collider VIVO do rapier**, nunca do `BodyDesc` (é ele que a escala alcançou, W6) — Sutherland-Hodgman + shoelace; ⚠️ o ponto de interseção ENTRA na lista, senão a área salta de vértice em vértice e o corpo treme na linha d'água. ⚠️ **Viés de 0,64% medido, não acreditado:** bola/cápsula não têm vértices no rapier ⇒ tesseladas pelas mesmas portas do overlay; N-gono tem `(N/2π)·sin(2π/N)` = **99,36%** da área, e o gate afirma esse número — cuja **1ª medição deu 0,745 = exatamente 3/4**, denunciando o lag de um sub-passo em vez de escondê-lo. ⚠️ **QUATRO fixtures nasceram erradas:** *um retângulo não tem quilha* (o gate exigia ângulo pequeno e ficou vermelho sobre um barco nivelado que girou 180° ⇒ oráculo virou `|sin|`) · o gate de "corpo acima" **zerou a gravidade do MUNDO e desligou o empuxo que media** · o controle foi **atropelado pelo experimento pela 3ª vez** nesta linha (atingido a 21,8 m) · a linha d'água foi medida **cedo demais** (sistema amortecido). ⚠️ **E o smoke MENTIA:** dizia que a caixa de densidade igual à do fluido *fica a meia-água* — medido, ela **vai ao fundo** (empuxo neutro não empurra de volta, só deixa de puxar); virou densidade 3. Componente `AreaBuoyancy`, registro **16→17**, schema **fica em 29** (3ª vez pela mesma razão: bump recusa todo projeto salvo); c9 **71→73**. Gates: wrapper 7, ECS 1, seam +1 (as **4** rows da área), shell +2 — incluindo **a poça com empuxo autorável só com gestos da UI**, cujo oráculo é a cortiça **PARAR** perto da superfície. 6 mutações no kernel, 6 sangram. Smoke `=27` medido: cortiça 0,14 · madeira −0,11 · pedra −2,55 · barco sin 0,84→0,01. ⚠️ **A LINHA D'ÁGUA (mesma sessão, pergunta do Enio *"não temos UI para isso?"*):** a UI existia (row **Fluid Density**) mas o **overlay não desenhava NADA** — força tem seta, arrasto não tem direção, e o empuxo tem um **LUGAR** que era o único número do modelo invisível na tela. Agora a poça desenha sua superfície (**ciano claro**, o único traço que descreve um lugar e não um corpo), **antes** dos corpos, saindo da MESMA `surface_level` do empuxo — nunca de uma re-derivação, que divergiria em poça rotacionada. ⚠️ **Uma mutação SOBREVIVEU e nomeou o buraco:** trocar `up = -g/|g|` por `(0,1)` deixou os 9 gates verdes, porque **com gravidade padrão os dois são iguais** — a metade que faltava é gravidade LATERAL (superfície VERTICAL). LOC: `world.rs` 703/700 ⇒ split `world/queries.rs` (*o que MOVE* vs *o que se OLHA*). **W-FormDrag (mesma sessão):** o `Drag` da área é **viscosidade** (uniforme); **Shape Drag** empurra cada aresta ao longo da **própria normal**, dando **resistência por SECÇÃO** (4× de través vs de proa) e **freio de rotação pela FORMA**. `AreaFormDrag`, registro **17→18**, schema 29, c9 **73→75**. ⚠️ **TRÊS expectativas minhas que a medição negou:** o **cata-vento NÃO existe** num corpo simétrico (centro de pressão = centroide = centro de massa ⇒ `r×F` some; uma flecha só alinha porque as penas ficam ATRÁS do CM) · a força tem de seguir a **NORMAL** e não a velocidade (forças paralelas sobre polígono fechado cancelam **por identidade**; a não-paralelidade é o que produz **sustentação**) · **uma amostra por aresta mede freio de rotação ZERO** (no meio da aresta a velocidade de rotação é tangencial ⇒ `v·n=0`; o efeito é o GRADIENTE, `EDGE_SAMPLES=2`). ⚠️ 2 mutações sobreviveram à 1ª rodada, ambas nomeando fixture faltante (a sustentação, e o fold da ponte); com os gates novos **8 mutações, 8 sangram**. **Aberto:** a superfície é plana (ondas seriam outra coisa) · `Compound`/`TriMesh` devolvem `None` de propósito. **A JORNADA DE 21/07 INTEGROU (2026-07-22)** — 32 commits, 21 waves (W6 → W-FormDrag), todos os smokes aprovados. ⚠️ **Quem assume a linha começa por [`HANDOFF_REABERTURA_line_physics_2026-07-23.md`](../../Physics/handoffs/HANDOFF_REABERTURA_line_physics_2026-07-23.md)** (o de 07-22 é histórico: as frentes A/B dele estão FEITAS) — ele diz como reabrir a worktree (a branch e a worktree JÁ existem: `cd` + `git rebase main`, rota "linha reaberta" do [MODELO_ABERTURA_LINHA](../../IntegracaoMultiAgente/MODELO_ABERTURA_LINHA.md)), o que já existe para não reconstruir, e o plano em 6 frentes (eventos de contato · força de impacto · gizmo de âncora · assar joint · a família das zonas · dívidas menores). Estado no main: `PROJECT_SCHEMA` **29** · registro **18** · c9 **75** · 28 cenas de smoke. **⚬ W-ContactEvents (2026-07-22, `line/physics`, cena `=29`, smoke OK 2026-07-22):** o canal de **TRANSIÇÃO** — `PhysicsBridge::contact_events()` com `ContactPhase::{Began,Ended}`, lugar e carga (o consumidor de GAMEPLAY segue **não construído**: cross-line, decisão do Enio, a fronteira do W7). ⚠️ **A lei:** o conjunto permanente é recomputado do zero todo dispatch, então um diff ingênuo faz de TODO movimento descontínuo do relógio uma tempestade de colisões — um evento descreve transição que a sim ATRAVESSOU, e as duas descontinuidades (`rewind_to` e `hold`) re-baselizam em **SILÊNCIO** (`discard_contact_history`). A baseline nasce VAZIA e contínua ⇒ o 1º frame simulado reporta o que achar, inclusive pilha autorada já encostada (leitura da **Unity**; antes do 1º passo não existe verdade anterior). ⚠️ **BUG VIVO que a busca pela baseline achou:** o `hold` limpava `triggers` — com o comentário explicando o porquê — e **não limpava `contacts`**, então desmarcar o toggle **Physics** deixava as cruzes na tela descrevendo um mundo que o artista podia desmontar com a mão. ⚠️ **O flash é um `×`, não um `+` maior:** o braço da cruz **já significa CARGA**, e um flash no mesmo canal tornaria um toque novo e leve idêntico a um velho e pesado — 45°, 6 ticks, `age_ticks: Option` (o `None` é o que impede o scrub de acender a cena inteira); e um gate pegou o flash nascendo **menor** que a cruz de carga máxima ⇒ `FLASH_MIN_PX` **derivado** de `MARK_MAX_PX`. ⚠️ **LIMITE MEDIDO:** um impacto RÁPIDO não produz evento nenhum — o solver resolve e **separa dentro do mesmo passo** (bola de r=0,3 largada de 3,9 m desce a `y=−0,478` contra toque em `−0,5` e já sobe quando o tick acaba); varrido: de **1,2 m** todo quique é reportado, de **2,0 m** o primeiro não ⇒ a cena larga na faixa reportada **de propósito**. **É o mesmo mecanismo do pico de impulso que falta, e tem a mesma cura** (amostrar DENTRO do laço de sub-passos) ⇒ são **uma** wave, não duas. **Zero bump** (`PROJECT_SCHEMA` fica 29, registro fica 18 — transição é estado vivo do solver, o oposto de config) e o **c9 saiu byte-idêntico ao main** (`7d55a4ab…`, 75 corpos), rodado nas DUAS árvores porque a wave adiciona MEMÓRIA e essa é a adição que começa a escrever de volta. 13 gates, **8 mutações, 8 sangram**; LOC: `bridge.rs` 722→642 via `bridge/rewind.rs` (irmão do `hold.rs`). ⚠️ **E o plano ganhou política de UI:** toda wave futura só fecha com as **quatro** condições — o componente EXISTE (gate `every_physics_component_is_authorable`) · é **pintado e registrado** (`architecture_panel_wiring_parity`) · o **clique chega ao barramento** (varredura de seam) · e **a SEQUÊNCIA leva a algum lugar** (`inspector_physics_gesture_tests`). ⚠️ **A quarta é categoria NOVA e não é implicada pelas outras três:** todo edit pode ter gate e o gesto ainda não levar a lugar nenhum — foi ela que pegou o passo *"converta para Capsule"* que eu quase ensinei ao Enio (geometricamente correto, destrói o tronco). Mais: a **metade VISÍVEL conta como UI** (a resposta pode ser *"nada, de propósito"*, desde que escrita) e **toda wave ganha CENA com números MEDIDOS** — a sonda headless roda antes de a mensagem ser escrita, porque nesta jornada duas cenas afirmaram coisas que a medição desmentiu. **⚚ W-ImpactForce (2026-07-22, `line/physics`, cena `=30`, smoke OK 2026-07-22):** a frente **B** que a W-ContactEvents nomeou — o `impulse` do contato é a **CARGA** que o par segura (medido: uma bola cai de 0,6 m ou de 10 m e pesa o mesmo, ~0,014 N·s), não o **PICO** do impacto, que vive **entre** os sub-passos e some quando `step` retorna. `ContactReport`/`BodyContact`/`ContactEvent` ganharam `impact` = o `max` do impulso sobre os sub-passos do tick (cresce com a queda: **0,58 → 3,89** de 0,6 a 10 m). ⚠️ **Sempre-ligado, porque MEDIDO barato PRIMEIRO** (`tests/measure_impact.rs`): ≤ **2,4% do HR-4** a 500 pares — um flag que nunca se desliga seria flag morto. É um **READOUT** (o solver não o lê) ⇒ c9 **byte-idêntico** (`7d55a4ab…`, 75). O flash (`×`) do overlay agora **escala com o impacto** (piso `FLASH_MIN_PX` intacto ⇒ o gate do front A segue). ⚠️ **A armadilha que quase passou (fixture, não código):** no nível do mundo `impulse` (tick-end) É o pico quando o par ainda toca no fim do tick (o corpo é pego mais forte NA fronteira), então num tick de POUSO `impact == impulse` e um gate que lê um endpoint não distingue `max` de último-sub-passo; o gap só aparece num tick de **RASPÃO** (o corpo já saiu antes do último sub-passo: endpoint ~0, pico não) — os gates usam bola que QUICA + chão FINO, onde o raspão cai dentro do `contact_reports`. 3 gates (wrapper+ponte+overlay), **5 mutações, 5 sangram**. ⚠️ **Impacto RÁPIDO invisível — FECHADO pela wave seguinte (ver W-TickContacts).** `PROJECT_SCHEMA` **fica 29**, registro **fica 18** (impact é readout, não config). LOC: `world.rs` 706→661 via `world/convenience.rs` (os 2 construtores `add_dynamic_circle`/`add_static_cuboid`). Smoke: **`PH2D_PHYSICS_SMOKE=30`** (a **DEMOLIÇÃO** — bola pesada contra uma torre de caixas, lenta vs rápida: a torre EXPLODE e o `×` cresce com a força; a 1ª versão batia num chão imóvel e o Enio recusou — *"não mostra o efeito"* — então o impacto agora tem CONSEQUÊNCIA visível). **⚙ W-TickContacts (2026-07-22, `line/physics`, cena `=31`, smoke OK 2026-07-22):** o evento para o toque RÁPIDO — a próxima wave que o front A nomeou. O diff de contatos rodava por **DISPATCH** sobre `contact_reports` (estado de FIM de passo), então um toque que começa e termina dentro de um tick (ou entre os dois endpoints de um dispatch multi-tick) era invisível — MEDIDO: o 1º pouso de uma queda de 3 m não gerava evento (o 1º reportado era um quique lento no tick 91, não no 45), e uma queda de 8 m não gerava **nenhum**. Agora o diff roda por **TICK** sobre a **UNIÃO dos sub-passos** que o `contact_peaks` já acumulava para o pico de impacto (front B) — `PhysicsWorld::tick_contacts`; o `PeakSample` ganhou `point`+`impulse` (o evento de um toque que já saiu precisa de lugar e carga). O único toque que ainda escapa é o que começa **e** termina no MESMO sub-passo, que o solver discreto nem produz (seria túnel, trabalho do CCD). ⚠️ **O flash (`×`) do overlay virou canal PRÓPRIO** (`ContactFlash`, decaído em ticks pela ponte, `CONTACT_FLASH_TICKS` mora lá) — antes ele cavalgava `BodyContact.age_ticks` e só vivia enquanto o par TOCAVA, então um pouso curto **sub-piscava** e um rápido **nunca piscava**; agora um começo pisca sua vida inteira, encoste ou não. `age_ticks`/`began` **saíram** (existiam só para o flash antigo; a supressão de re-baseline que o `None` fazia foi subsumida — a ponte simplesmente não cria flash num re-baseline). ⚠️ **Custo:** em play normal (1 tick/dispatch) o diff roda **exatamente** as vezes que o front A rodava; só um catch-up/scrub multi-tick paga K diffs, e cada diff é BTreeMap-sobre-contatos (µs contra os ms do `step`, que roda por tick de qualquer jeito — medido 57 ms/tick a 500 contatos). **c9 byte-idêntico** (`7d55a4ab…`, readout puro). `PROJECT_SCHEMA` **fica 29**, registro **fica 18**. 12 gates ecs (2 novos fast-touch red-first: single-tick 8 m + multi-tick entre-endpoints; + flash-decay + live-flash-na-descontinuidade) + 10 overlay; **6 mutações, todas sangram** (união→fim-de-passo mata os 2 fast-touch + o gate de impacto do front B · once-per-dispatch mata só o multi-tick · light_flash no-op mata 3 de presença · nunca-descartar mata o decay · discard-não-limpa mata o live-flash · re-baseline-off mata só o re-arm). Cena `=31`: bola de 8 m cujos pousos eram ESCUROS agora acende `×` em cada um (o `×` maior que o da bola baixa, porque o impacto é maior). **⚙ W-AreaTorque (2026-07-22, `line/physics`, cena `=32` smoke OK 2026-07-22):** a **família das zonas** que a reabertura nomeou — o `AreaEffector` (W-Area) empurra pelo centro de massa e **não gira nada**; este é o análogo ROTACIONAL: uma área que aplica um **torque** a cada corpo dinâmico dentro dela (redemoinho, mesa giratória, esteira). Fecha o *torque de área* aberto desde o W-Area. **Kernel:** `AreaEffect.torque` (o bundle do `desc`, **não serializado**) + `effector::apply` chama `apply_torque_impulse(torque·dt)` no MESMO laço da força. ⚠️ **`apply_torque_impulse`, não `set_angular_velocity`** — resistido pelo **MOMENTO DE INÉRCIA**, o espelho EXATO de força=massa: barra 4×0,25 gira **8,03× mais devagar** que compacta 1×1 de mesma área (medido; é o pin *"torque não é aceleração angular"*). ⚠️ **O SINAL é o sentido** (`>0` anti-horário), então o neutro é `== 0.0` — diferente dos irmãos de arrasto (`<= 0.0`): um torque negativo é uma direção, não um valor inválido. **Componente novo `AreaTorque(f32)`** — 5º da mesma área, ZERO bump (blob-key própria; registro **18→19**, `PROJECT_SCHEMA` **fica 29**); a ponte dobra no MESMO `AreaEffect`, então rida o `BodyDesc` e um rewind re-arma. **A metade visível é o GLIFO de giro VIOLETA** (arco 270°+flecha, cor que nada mais usa), construído numa BASE de tela derivada da câmera (segue o giro VISÍVEL sob qualquer y-flip), desenhado **mesmo no play** (o giro é da ÁREA) — sem ele uma zona de giro seria indistinguível de um sensor comum. **Inspector:** row Torque (N·m, **sensor-only**, sinal preservado sem clamp), id `INSP_PHYS_AREA_TORQUE`. ⚠️ **A armadilha que a medição pegou:** `Transform.rotation` **WRAPA** em ±π, então um torque forte gira várias revoluções e o ângulo lido vira ruído (compacta 2,688 / barra −1,254 sem sentido); os gates de MUNDO leem `angvel` cru, os de ECS/gesto/smoke leem `rotation` e por isso a fixture mantém o giro **sub-revolução** ([[reference_topic_oracle_discipline]]: uma coordenada que wrapa é oráculo ruim quando a grandeza passa do período — meça a TAXA). **c9 77 corpos** (mesa+spinner), hash `27f3c1aa…` determinístico entre debug/release — ⚠️ **MUDA** vs main (`7d55a4ab…`), e isso é CORRETO (o torque muda a pose, ao contrário dos readouts A/B/C). 4 gates mundo + 2 ecs + count + seam(commit-negativo) + gesto + overlay-scene; **5 mutações, todas sangram**. Cena `=32`: compacta/barra/reverse/controle, medido 171°/21°(razão 8,03)/−171°. **⚠️ As rows de área MOSTRAM o valor agora (fix pós-pergunta do Enio *"não vi nada disso agindo na UI"*, `9ec4b43b`, cena `=33`, pendente de smoke):** a autoria sempre funcionou (Add → Static → **Sensor** faz a row Torque aparecer → digita), mas as **5 rows de área** eram **write-only** — `sync_physics_fields` não as sincronizava, então re-selecionar a zona lia `0` em vez do valor no collider (gap de TODA a família W-Area..W-FormDrag). Fix: as 6 entram no sync (roda só na troca de seleção, guardado por `entity_changed` ⇒ não briga com a digitação); gate `selecting_a_zone_shows_its_authored_area_values` (mutação M6 sangra). Cena `=33` demonstra a autoria pela UI (sprite pelado para autorar + mesa pronta que gira e mostra a row preenchida ao selecionar). **Aberto no W-AreaTorque:** falloff (o giro é uniforme; um redemoinho real cai com o raio). **⚙ W-AreaFrame (2026-07-23, `line/physics`, cena `=34`, pendente de smoke):** o último item do W-Area junto com o falloff — a força estava em eixos de **MUNDO**, então **girar o sensor não girava o sopro** (esteira diagonal inexprimível). Agora é autorada no frame da **ZONA**, com o toggle **`Force Axes: Zone | World`** (§11, sensor-only, logo abaixo das rows de força que ele qualifica) prendendo a direção ao mundo — o `useGlobalAngle` da Unity; **default Zone**, toggle como escape (decisão do Enio). ⚠️ **Só a FORÇA é dependente do frame, e isso é GEOMETRIA, não escopo escolhido:** o torque 2D é escalar sobre **Z** e uma rotação no plano é *em torno de* Z (`τ_local ≡ τ_mundo`), o drag é isotrópico, o empuxo mede a superfície pela **GRAVIDADE** e o shape drag empurra pela normal de cada aresta do **CORPO** — há gate pinando a invariância do torque para ninguém 'completar' a wave. ⚠️ **A porta única `zone_force_world` toma `(sin, cos)`, NUNCA um ângulo:** rapier guarda a rotação como `UnitComplex` (`im`/`re` **já são** seno e cosseno) e pedir `.angle()` chamaria **`atan2`** — do `std`, **não pinado cross-OS** — num resultado que alimenta o `physics_ecs_c9` (lei 6). Quem tem ângulo (o ECS) usa `zone_force_world_at`, que cruza por `libm::sincosf` **uma** vez e **não alcança o hash**. O solver E a **SETA do overlay** perguntam a MESMA porta (o motivo do `scaled_shape`: uma seta de segunda resposta desenha um vento que não sopra ali, e ninguém lê número em screenshot). ⚠️ **A pose é a VIVA**, lida por sub-passo — para a zona estática é o mesmo número, mas separa numa zona **KINEMATIC** que uma curva gira (um ventilador varrendo), e assar no spawn proibiria isso em silêncio. **Marcador `AreaForceWorldAxes`** (presença = booleano, idioma do `Ccd`), registro **19→20**, `PROJECT_SCHEMA` **fica 29** — 6ª vez pela mesma razão (campo novo = postcard posicional = bump, e **bump recusa todo projeto salvo**); o `AreaEffect` do wrapper ganhou o campo de graça por **não ser serializado**. **Zona não-rotacionada é BYTE-IDÊNTICA nos dois modos** (`sin 0 = 0`, `cos 0 = 1` exatos) e **nenhuma zona de força do repo tinha rotação ≠ 0** (varrido) ⇒ o default novo não move nada. c9 **77→79**, hash `747dff39…` (igual em debug e release; **muda** vs main, e é correto). **14 mutações, 12 sangram.** ⚠️ **Três defeitos de gate, os três meus:** o do torque comparava os dois **FLAGS** e a mutação que gira o torque passou (escala os dois ramos igual ⇒ **razão sadia sobre dois doentes**) — o oráculo certo é invariância sob a **ROTAÇÃO**; o da pose viva nascia em rotação 0, onde *não-roda* e *rodou-pelo-spawn* são indistinguíveis (agora nasce em π/2 e é dirigido até π, separando **três** kernels); e os dois chips estão no **ponto cego** do `architecture_panel_wiring_parity` (ele só coleta `.register(ids::LITERAL` direto, e `seg_row` registra em LAÇO — o buraco das 36 células do W2c) **e** do `click` sintético do seam ⇒ nasceu o helper **`click_real`** no `seam_physics`, sem o qual tirar os chips do `populate` deixa tudo verde. **LOC:** o `file_loc_caps` da shell pegou ⇒ o overlay separou **CONTORNO** de **ANOTAÇÃO** (`physics_overlay.rs` 605→416, `_scene_tests` 615→403, + `physics_overlay_annotations{,_tests}.rs`) — não é hack de tamanho, **o falloff é outra anotação e agora tem casa**. Cena **`=34`**: duas zonas idênticas a 40°, só a da direita marcada — medido headless, Zone `(2,81, 2,36)` = **40,0°** vs World `(3,67, 0,00)` = **0,0°**. **Aberto:** ⚠️ **espelhar a zona NÃO espelha o vento** (medido: `scale.x = -1` dá deslocamento idêntico ao da não-espelhada) — o frame honra a **rotação** e ignora a **reflexão**, o que **contradiz o precedente do W-Offset** (*'escala SINCADA, não abs'*); **decisão de produto, não construída sem pedido**. **⚙ W-AreaFalloff (2026-07-23, `line/physics`, cena `=35`, pendente de smoke):** o ÚLTIMO item aberto da família das zonas — a área empurrava **igual em toda a sua extensão** (encostado na parede ou no olho da rajada, o mesmo empurrão), e o corpo que atravessava a fronteira passava de força cheia a nada dentro de um sub-passo. Agora o `Falloff` (0..1, row sensor-only logo abaixo do Torque) faz a **força E o torque** desvanecerem, chegando a **zero exatamente na borda**. ⚠️ **A régua é a fração do caminho do centro até a BORDA** (`ShapeDesc::radial_fraction`), não um raio próprio: um "raio de falloff" seria um segundo número a manter de acordo com o tamanho da zona (a falha de duas-portas que esta linha já pagou) e discordaria dela ao primeiro redimensionamento. Três propriedades decidem tudo — não precisa de 2º número · chega a zero em TODA direção (é isso que tira o degrau da saída) · é **invariante sob mapas lineares**, logo a curva de nível `t=0.5` **é a silhueta encolhida à metade** (o anel do overlay sai exato pela `scaled_shape` que já existia) e o falloff acompanha a escala do W6 de graça. Três formas fechadas, zero iteração (Ball ≡ Ellipse de raios iguais · Capsule ≡ Stadium de calotas iguais · todo Stadium é uma cápsula unitária vista por `diag(rx,ry)`); ⚠️ **sem `hypot` e sem transcendental** (lei 6 — `hypot` é a libm da plataforma e este número alimenta o c9). ⚠️ **Descartado com motivo:** pedir a distância à borda ao parry (`cast_local_ray`) cai no **GJK** para convexo — iterativo, com `eps=0.001` — e meteria uma iteração numérica no caminho determinista para responder o que tem forma fechada. ⚠️ **O escopo é geometria, não gosto:** pesa os dois EMPURRÕES e nada mais — `drag`/`density`/`form_drag` descrevem um **MEIO**, e um meio não fica mais ralo perto da própria margem (gate `the_falloff_leaves_the_medium_alone`, irmão do gate de invariância do torque do W-AreaFrame). ⚠️ **O cap `t ≤ 1` é load-bearing:** a sobreposição é forma-contra-forma, então o CENTRO de um corpo grande pode estar fora enquanto ele encosta — sem o cap o fator fica negativo e a zona **puxa para trás** na borda onde deveria soltar, com o ledger fechando. **A metade visível é o ANEL laranja apagado** da curva de meio caminho, desenhado só quando há o que atenuar (sem ele o falloff seria o único número do modelo de área sem marca na tela: a seta desenha a força AUTORADA, que é a do centro). Componente **`AreaFalloff(f32)`** valuado, registro **20→21**, `PROJECT_SCHEMA` **fica 29** (7ª vez pela mesma razão); c9 **79→81**, hash `bfca28f7…` (debug≡release). 14 mutações, **13 sangram** — o sobrevivente é a irmã exata do do W-AreaFrame (o `any` da ponte), e ⚠️ **uma frase minha foi corrigida pela mutação**: eu escrevera que ficar fora do `any` *"não é meramente higiene"*, e a medição diz que é. ⚠️ **O controle foi atropelado pelo experimento pela 4ª vez nesta linha** (o corpo da margem SAÍA da zona; agora a fixture declara a premissa, mede VELOCIDADE e os corpos têm massa de verdade) e um `assert_ne!` teria falhado sobre produto correto (no EIXO uma caixa e um disco de mesmo tamanho medem o mesmo `t` — a fixture que os distingue é fora do eixo). **LOC: três splits pela MESMA linha de corte** — *o que este CORPO é* × *o que esta ÁREA faz a outros*: `components/overrides.rs` 703→452 (+`components/area.rs`) · `inspector_physics_apply.rs` 614→498 (+`inspector_physics_area.rs`) · `apply_physics_event` 201→165 (+`area_edit`); ⚠️ o `every_physics_component_is_authorable` **nasceu VERMELHO** com o split (ele ENUMERA os arquivos de escrita, e o corte moveu seis componentes para fora da lista — a falha alta que a lista existe para produzir), `WRITERS` 3→4. Cena `=35`: duas rajadas redondas iguais, quatro caixas cada — a fila **voa junta** na uniforme (10,01/9,95/9,70/8,96 m em 3 s) e **se abre em leque** na de falloff (7,64/6,43/4,35/**1,71**). **Aberto:** perfis de falloff (smoothstep/inverso-quadrado são outra CURVA sobre a mesma régua — knob de MODO, não número novo; só com pedido). **⚙ W-AreaMirror (2026-07-23, `line/physics`, cena `=36`, pendente de smoke):** a decisão de produto que o W-AreaFrame deixou aberta e o Enio mandou seguir — a zona honrava **METADE do próprio frame** (a rotação sim, a reflexão não), e é o MESMO `Transform` que carrega as duas: espelhar o sprite de uma esteira deixava a correia correndo para o mesmo lado, com a seta do overlay concordando e nada dizendo por quê (medido: `scale.x = -1` dava deslocamento idêntico). ⚠️ **O precedente já estava escrito na função que dobra isto** (`scale::body_desc`, W-Offset: *"escala SINCADA, não `abs` — o offset é POSIÇÃO ⇒ flip espelha"*) — uma força autorada no frame da zona é um **vetor** nesse frame, e as duas linhas agora moram lado a lado. ⚠️ **A metade que torna a wave correta em vez de meio-feita: força é VETOR, torque é PSEUDOVETOR, e uma reflexão distingue os dois** — sob `diag(-1,1)` a força vira `(−fx, fy)` e o torque **troca de sinal** (um redemoinho no espelho gira ao contrário). É por isso que o *"torque é invariante"* do W-AreaFrame valia para ROTAÇÃO e **não se estende**: uma rotação no plano é em torno de Z e deixa um escalar-z quieto; uma reflexão NO plano o nega. O fator é `det(S)` (porta `zone_spin_sign`), e a forma fechada apaga o caso especial — **espelhar os DOIS eixos não é reflexão, é rotação de 180°**, e o produto devolve `+1` sozinho (gate nesse par; a mutação que troca `det` por paridade-de-um-eixo passa em tudo menos nele). ⚠️ **Três ordens, cada uma gateada:** o espelho entra **ANTES** da rotação (a ordem do `Transform`, `R·S`; a errada só diverge numa zona espelhada **E** rotacionada, que é o caso de uso inteiro) · **`world_axes` desliga o espelho junto com a rotação** (o toggle diz *"esta força é um vetor de MUNDO"*) · **a silhueta e o falloff são CEGOS ao espelho** de propósito (um retângulo espelhado é o mesmo retângulo; a `radial_fraction` é simétrica — só o que tem DIREÇÃO responde a um espelho). A **seta** reflete e o **glifo** inverte, pelas MESMAS portas do solver. **Nenhum componente novo, nenhum id, nenhum bump** — a lateralidade é função da POSE que o artista já manipula, não de um controle a mais, e é por isso que a wave não tem row na §11; `AreaEffect.mirror` é plain data do wrapper (não serializado). Registro fica **21**, `PROJECT_SCHEMA` **29**; c9 **81→83**, hash `4e862761…` (debug≡release). **7 mutações, 7 sangram.** LOC: `physics_ecs_c9.rs` 736 ⇒ as 8 lanes de zona saíram para `physics_ecs_c9/zones.rs` — ⚠️ **um arquivo em `src/bin/` vira OUTRO binário**, a forma certa é o diretório (`src/bin/<nome>/main.rs`) com o `[[bin]].path` do `Cargo.toml` apontado ao `main.rs`; hash inalterado pelo split. Cena `=36`: quatro zonas de mesma autoria, só o sinal da escala difere — vento `(2,81, 2,36) = 40°` × `(−2,81, −2,36) = −140°`, giro `+117°` × `−117°`. **Aberto:** o **SKEW não entra no frame** (o espelho lê só os sinais da escala; mesma limitação honesta que o collider já tem, W6) · nada na §11 diz que a zona está espelhada (o overlay mostra, e o `Transform` é onde o gesto foi feito — uma row de leitura seria 2ª porta). **⚙ W-BakeRange (2026-07-24, `line/physics`, cena `=37`, pendente de smoke):** a metade do BAKE aberta desde o W4 — o `bake_seconds` lia o `end` do loop e **descartava o `start`** (`if let Some((_, end))`, o `_` era o começo), então um loop `[2s, 5s]` assava `[0, 5s]` ignorando a decisão do artista. É **correção de input descartado**, não feature. Agora o `bake_range` devolve `(start, end)` e a sim **ainda roda de 0** (é função do tick, e o front `[0, start)` tem de adiantar a cena de quem cavalga uma plataforma já assada) — o que muda é que os samples do front **não viram chave**. ⚠️ **A janela é aplicada ANTES da checagem de constância, e a ordem carrega o peso:** `BakedTrajectory::channel_in(ch, start, end)` recorta ao `[start, end]` e SÓ ENTÃO decide se o canal é constante (o `None` que protege um canal parado) — um corpo que caiu no front e DESCANSA na janela moveu-se na trajetória inteira mas é constante na janela, e escrever a "moção" dele seria track plana por cima do que o artista animou à mão. `channel()` de range cheio delega com janela infinita ⇒ o comum é byte-idêntico. O botão colapsa em `Bake 5.0s` com start 0 e mostra `Bake 2.0-5.0s` com início positivo (as chaves pousam nesses tempos ABSOLUTOS; toast espelha `Baked 0.5-2.5s`); `InspectorPhysicsInfo` ganhou `bake_start_seconds`, a fiação virou `(f32, f32)`. **Nenhum componente/id/bump** — comportamento do bake, não config: registro fica **21**, `PROJECT_SCHEMA` fica **29**, o `physics_ecs_c9` **não é tocado** (hash `4e862761…`, 83 corpos). 3 mutações, 3 sangram (descartar o start ⇒ `baked 0..2` RED · `channel_in→channel` ⇒ t=0 casa com o full RED · `bake_label` ignora start ⇒ RED). LOC: o gate de janela + helper foram para `physics_bake_curve_tests.rs` (o `_tests.rs` bateria 600 exatos, e sentar no cap é frágil); cena 37 em `physics_smoke_rigs.rs` (575). ⚠️ **Aberto — um Ctrl+Z para as duas metades do bake NÃO é mecânico e foi REPORTADO, não construído:** são duas pilhas de undo separadas (a GLOBAL captura a troca para `Kinematic`, a da TIMELINE as chaves) com roteamento próprio em `keyboard.rs`; uni-las é mudar o *roteador de undo* do editor e tocar a timeline (outro domínio), o que o doc-header do `physics_bake.rs` já avisa. **⚙ Follow-up de FIDELIDADE do bake (2026-07-24, DUAS rodadas de smoke do Enio):** 1ª rodada (*"funciona mas é imperfeito, provavelmente a simplificação da curva"*) apertou a tolerância do fit de 1% (herdada do record) para **0,3%** — havia PONTO ÓTIMO medido (abaixo de ~0,2% a Bézier de tangente suave OVERSHOOTA 6,6% sob Time Remap entre chaves densas). 2ª rodada REPROVOU (*"não fica bom. Melhor sem simplificação. Busque o padrão ouro, a perfeição"*) ⇒ **o fit MORREU no bake.** O padrão-ouro para reproduzir uma sim DISCRETA é não reamostrá-la: **uma chave por tick, `Interp::Linear`, sem fit.** Amostrada a 60 fps o playhead pousa nos tempos de tick (que SÃO as chaves) ⇒ **byte-exata à sim**; linear **não pode dar overshoot** (fica entre os endpoints por construção — some o 6,6% do remap); o quique vira **canto agudo** (chave onde a velocidade inverte), não tangente arredondada. Custo honesto: canal que se move = ~60 chaves/s — o trade que "sem simplificação" pede (fidelidade > editabilidade; canal parado segue sem track). `simplify_recorded`/`REC_*` FICAM: só o record os usa (hand gesture é ruidoso, solver não — os dois inputs querem tratamentos OPOSTOS). Gates reescritos: `the_bake_writes_one_key_per_tick` (denso; mut re-fit → 5 chaves RED) + `the_bake_reproduces_the_sim_exactly_with_no_overshoot` (exato **< 0,1% nos ticks** E overshoot **< 0,1% entre eles** — a mut re-fit sangra as DUAS metades: 0,673% nos ticks E 6,6% no remap, os dois números que o fit TROCAVA um pelo outro). Record byte-idêntico (31 gates). ⚠️ Hermite-da-velocidade seria "mais perfeito" sub-frame mas reintroduz risco de overshoot no CONTATO e é imperceptível a 60 fps — **deliberadamente não construído**. **⚙ W-JointAnchor — O GIZMO DE ÂNCORA DE JOINT NO CANVAS (2026-07-24, `line/physics`, cena `=38`, pendente de smoke):** a âncora de um joint só era autorável digitando nos campos Position; agora há um **dot âmbar agarrável** no pivô. Um joint é entidade com `Transform` (a âncora autorada) mas **sem geometria**, então não publica `GizmoView` (os 3 publicadores são **CAIXAS com alças de escala**) e não tinha handle no canvas. Primitivo novo em **editor-core**: `PointGizmoView` + `paint_point_gizmo` (`gizmo/point.rs`) — desenha o dot no anchor projetado e registra `GIZMO_JOINT_ANCHOR` (id **964**, **fora** do `gizmo_kind_for_id` de propósito: o shell o reconhece explicitamente, como os alvos Flip/MovePivot); `world_to_screen_px` foi para `camera.rs` (a projeção, compartilhada pela caixa e pelo ponto). O shell publica-o **só para um joint selecionado** (`render_loop/point_gizmo::build_point_view`, no `Transform.translation` — joint é RAIZ ⇒ local == mundo; extraído de `snapshots` que está no teto de LOC, para ser **gateado headless**), e um ramo `began_joint_anchor` no Down abre um **`Translate` da SELEÇÃO** — ⚠️ um joint **não tem sprite** para o `pick_sprites_at_world` do Translate genérico resolver, então a entidade vem da SELEÇÃO, não de um pick. O resto (mover `Transform.translation`, undo em **1 passo**) vem de graça do `advance_gizmo_drag` + `post_frame_undo`, e o `rebuild_from_rest` re-deriva as âncoras locais da nova posição. **Nenhum componente/id de física/bump** — é gizmo, não física; `PROJECT_SCHEMA` **29**, registro **21**, c9 intocado. Gates mutação-provados: o dot é hittable no anchor e segue pan/zoom (drop do `register` → RED) · `build_point_view` = Some p/ joint / None p/ sprite ou nada (drop do guard `PhysicsJoint` → o caso sprite RED) · arch-gate: o Down abre `Translate` da seleção e **não** faz `pick_sprites_at_world`, e o passe de paint desenha o `point_view` (glue window-gated). ⚠️ **A armadilha de LOC latente REINCIDIU:** `gizmo/paint.rs` nasceu em **706 > 700** e o `cargo test -p` filtrado a mascarou (a família do miss do `file_loc_caps`) — pega só no gate `architecture_workspace_file_loc_cap` isolado; corrigido movendo o helper p/ `camera.rs` (686). O handle de PONTO fica de graça p/ qualquer entidade-ponto futura. Smoke: **`PH2D_PHYSICS_SMOKE=38`** (pêndulo pausado: selecione 'Pivot' na Hierarquia → dot âmbar → arraste → o pivô move; Ctrl+Z desfaz em 1 passo; Play mostra o plank pendurado do NOVO pivô). **Aberto:** só-raiz (joint parenteado leria local como mundo — joints nascem raiz); o dot aparece sempre que um joint está selecionado. **⚙ W-BakeJoint — ASSAR UM JOINT PUXA O GRUPO ARTICULADO INTEIRO (2026-07-25, `line/physics`, cena `=39`, pendente de smoke):** o front B da reabertura, e a pergunta de design **dissolveu na arquitetura** — *"a articulação sobrevive DINAMICAMENTE"* é impossível: o bake vira o corpo **Kinematic** (o `readback` escreve o `Transform` DEPOIS do apply ⇒ um Dynamic recém-assado é sobrescrito) e um joint do rapier **não move um corpo kinematic** (massa infinita) ⇒ *curva-dirigido* e *joint-articulado* no mesmo corpo se excluem. A leitura coerente (a de todo DCC): o movimento **articulado** já é capturado (a sim roda uma vez do repouso, a trajetória de cada elo reflete o acoplamento) e o rig sobrevive **não-destrutivamente** (a entidade-joint não é apagada). ⚠️ **O footgun real era o bake PARCIAL** — o bake operava só na SELEÇÃO, sem fan-out; assar um elo deixava os vizinhos Dynamic, que **CONGELAM** com Physics off enquanto o elo assado toca. Fix: **`ph2d_physics_ecs::jointed_group(world, seed)`** — assar qualquer corpo puxa o **componente conexo** pelo grafo de joints; `bake_selection` expande a seleção antes de qualquer leitura, e o toast reflete o grupo (a metade VISÍVEL). ⚠️ **Função PURA sobre o ECS AUTORADO** (`PhysicsJoint` resolvido por `Name`, a chave do reconcile), não sobre o `self.joints` vivo — o grafo autorado está sempre atual (inclui um joint feito neste frame) e é **headless-testável** sem dispatch. ⚠️ **Só corpos DINÂMICOS conduzem, e é FÍSICA:** um Dynamic congela E transmite o acoplamento; um Kinematic já segue curva (não congela) e um joint não o move; um Static é fixo ⇒ o grupo é o componente conexo **Dynamic**, e Static/Kinematic são **FRONTEIRAS** (alcançadas, nunca cruzadas) — é isso que mantém dois pêndulos no MESMO gancho estático independentes (o gancho é uma parede, não um fio). **Sem componente/id/schema/registro novo** (`PROJECT_SCHEMA` **29**, registro **21**, c9 intocado). Gates mutação-provados: 5 no crate (`tests/joint_group.rs`, headless — corrente puxada por qualquer elo; dois pêndulos separados; vizinho Kinematic fronteira; solo; seed verbatim) + 1 comportamental na shell (`physics_bake_joint_tests.rs`, split do `physics_bake_tests.rs` que bateria 624>600 — assar UM elo vira ambos Kinematic/2 corpos, Hook fora; drop da expansão → assa 1, RED). Smoke: **`PH2D_PHYSICS_SMOKE=39`** (corrente de 3 elos de lado; selecione UM → toast "Baked 3 bodies", não 1; descarte Physics, Play → a corrente inteira reproduz; elos viajam 0,7/2,5/4,2 m medidos). **Aberto:** assar a RESTRIÇÃO viva é **impossível** pela arquitetura (contradição, não adiamento) · um Ctrl+Z para as duas metades segue aberto/não-mecânico. **⚙ W-JointAuthoring — RE-PICK DOS CORPOS DE UM JOINT + SMOKE DE AUTORIA (2026-07-25, `line/physics`, cena `=40`, pendente de smoke):** a pergunta do Enio *"quando teremos UI para criar joints?"* revelou que a criação **JÁ EXISTE desde o W3** (o botão **"Join Selected Bodies"** na §11 com 2 corpos selecionados → cria um Pin; §12 escolhe tipo/afina; a âncora é autorável pelo campo Position OU pelo dot âmbar do W-JointAnchor). O que faltava era **descobribilidade** e **corrigir um par mal-unido** sem deletar+refazer. **RE-PICK (§12) — redesenhado (2026-07-25, report do Enio com screenshot):** a §12 mostra **uma linha por corpo** — `Body A: <nome>  [eyedropper]` / `Body B: <nome>  [eyedropper]` — com o nome vigente **SEMPRE visível** (só a joint selecionada, nenhum outro objeto), e um **botão-picker de ícone** (`IconId::Eyedropper`, `IconButtonStyle::Compact`) por ponta. Nome que não resolve mais mostra **"(missing)"** apagado **POR PONTA** (substitui a linha combinada "X ↔ Y not connected", que dizia uma vez para as duas e não apontava QUAL quebrou). ⚠️ **O eyedropper ARMA um CANVAS-PICK** (não o design por-seleção da v1): clicar o ícone arma o slot, o **próximo clique num corpo** religa aquela ponta — o idiom de pick do app (arma, clica o alvo, como o eyedropper de cor), agora para uma entidade; o ícone do slot armado pinta **Pressed**. Wiring espelha o `vec_path_pick` do Vector: `App.joint_body_pick: Option<(u64, bool)>` (runtime-only) · `JointFieldEdit::PickBodyA/PickBodyB` **ARMAM** (sem operando), armados no **action loop** (onde `self` é mutável, como o Join) · `input_dispatch` intercepta um Down modal (precede picking/gizmo, independe da ferramenta) → resolve o corpo sob o cursor (`pick_sprites_at_world` + filtro `RigidBody`, ≠ a própria joint) → `set_joint_body`. ⚠️ **`set_joint_body` escreve IN PLACE** (o pick resolve mid-frame no handler de ponteiro; o undo global por-diff captura), pelo MESMO `clamped()`; **recusa um self-joint** (`body_a == body_b`) e devolve bool, então o pick segue armado em vez de deixar uma joint dormente. **Sem componente/id de física/schema novo** (`PROJECT_SCHEMA` **29**, registro **21**, c9 intocado); ids só de painel (`INSP_JOINT_PICK_A/B`), `InspectorJointInfo` troca `rebind_target_name` por `pick_armed: u8`. Gates mutação-provados: painel (`seam_joint.rs`, os 2 eyedroppers são SEMPRE vivos, armam seu slot e pintam em todo kind, `click_at` real) + shell (`inspector_joint_tests.rs`, `set_joint_body` religa o slot certo e o joint segue no solver — slot errado RED; recusa self-joint — guarda removida RED). ⚠️ **LOC:** `physics_smoke.rs` bateu 606 e a tabela de doc `| n | Wave | Scene |` estava estagnada (parava na cena 28) e DUPLICAVA o `00_plano_waves.md` ⇒ trocada por um ponteiro (581) — remoção de duplicata, não hack. Smoke: **`PH2D_PHYSICS_SMOKE=40`** (três corpos, NENHUM joint — autore do zero: Join → tipo/afina → arraste o dot → clique o eyedropper de Body A e depois 'Post' no canvas, sem pré-selecionar). É a cena que faltava para o gesto de CRIAR ser descobrível. **⚙ W-AnchorFollow — A ÂNCORA É BODY-LOCAL E SEGUE O CORPO (padrão-ouro W1, 2026-07-25, `line/physics`, `6f337986c`, cena `=41`, pendente de smoke; nome distinto do W-JointAnchor=38, o dot):** report do Enio + avaliação do padrão-ouro (*"estado da arte, sem pensar em custo"*): mover um corpo (Kinematic animado ou arrastado no canvas) fazia a âncora do joint **DESLIZAR** pelo corpo — medido, mover a prancha 2 m arrastava o pino 2 m (corpo de 0,2 m). A âncora era um ponto de MUNDO (o `Transform` do joint) re-derivado para local TODO reconcile contra a pose viva; a cura é a rep NATIVA do rapier — âncora **body-local por corpo**, que segue o corpo por construção. `PhysicsJoint` ganhou **`local_a`/`local_b`/`anchored`** (`PROJECT_SCHEMA` 29→30, tripla `(30,8,13)`); `reconcile_joints` LÊ os locais (o slide fix) e SEEDA uma vez do `Transform` contra a pose de REPOUSO quando `!anchored` (a MESMA conversão do modelo antigo, agora **uma vez** — zero churn de fixture); depois disso **um move de corpo nunca re-deriva**. `sync_joint_pivots` (rest-only) escreve `Transform = bodyA·local_a` pro dot/Position seguirem o corpo (o dot único segue **body A**; a 2ª alça é wave posterior). ⚠️ **Reposicionar o pivô é gesto EXPLÍCITO** (`anchored=false` → re-deriva), costurado nos 3 sítios de autoria (alça-dot no `advance_gizmo_drag` · commit de Position no `inspector_commits` · re-pick no `set_joint_body`); um move de corpo não passa por nenhum. **Runtime já estava certo** (o play congela os locais e o Kinematic animado arrasta o body B pelo constraint) — o bug era só de AUTORIA, o solver não foi tocado. Gates RED-first mutação-provados em `tests/joint_anchor_follows.rs` (o move não desliza · o dot segue body A · re-authoring re-cola). LOC: `bridge.rs` 718→679 (`readback` → `bridge/readback.rs`). **É a Wave 1 do padrão-ouro; waves 2-5 (grupo carrega o rig · 2 alças + snap · limite/motor visuais com o FRAME · break force) pendem do smoke da W1** — detalhe em [`HANDOFF_line_physics.md`](../../Physics/handoffs/HANDOFF_line_physics.md). **⚙ W-JointCreate — ESCOLHER O TIPO NA CRIAÇÃO (padrão-ouro, 2026-07-25, `line/physics`, `ec0c944ad`, cena `=40`, pendente de smoke):** report do Enio *"só dá pra criar Pin pela UI"* — medido: a criação sempre fazia Pin (`create_joint` = `PhysicsJoint::default()`), o tipo só mudava na §12, e a §12 só aparece com a JOINT selecionada — mas o "Join Selected Bodies" **não selecionava a joint nova** ⇒ o seletor Kind ficava escondido (a troca de tipo em si SEMPRE funcionou — era descobribilidade). Três correções: **(1)** seletor **"Join As" (Pin/Spring/Rope/Weld)** na §11 ao lado do botão (id `INSP_PHYS_JOIN_KIND`, `App.join_kind`, seam completo → `create_joint(.., kind)`), default Pin; **(2)** **auto-seleção** da joint nova (o `create_joint` já devolvia a entidade, era descartada) ⇒ a §12 abre na hora; **(3)** **re-seed da âncora na troca de tipo** (`apply_joint_edit(Kind)` marca `anchored=false`, o 4º sítio de autoria) pro Spring/Rope ancorar body B no centro. Gates mutação-provados (seam + `create_joint_makes_the_requested_kind` + `changing_the_kind_re_seeds_the_anchor`). LOC: `paint_join_gesture`→`physics_rows.rs`, §12 `paint_joint_section`→`paint_body_rows` (231 latente). **Fora de TODAS as waves (D9):** soft-body XPBD, fluidos FLIP/PIC, collider-gen vetorial + fratura (ADR-0063, aposentada com a 0108). **W6 — A ESCALA ALCANÇA O COLLIDER (2026-07-19, `line/physics`, smokada pelos gates, aguardando ordem de integração):** um sprite escalado 2× desenhava 2× e o collider **não** — `body_desc` lia `col.shape` verbatim, então o corpo físico era do tamanho AUTORADO e discordava do desenhado (a única CORREÇÃO aberta do cardápio, escolha do Enio). ⚠️ **Pré-existente e igual para corpo-raiz** — toda fixture/smoke usava raiz, então atravessou a linha inteira; o gate que pega a classe é ter um **pai** na fixture ([[feedback_a_condition_that_enumerates_its_readers_rots]]). Fix: **`ph2d_physics_ecs::scaled_shape(ColliderShape, scale) -> ShapeDesc`** é a **PORTA ÚNICA** — a ponte (→ rapier) E o overlay (→ o wireframe) resolvem por ela, senão o contorno descreveria um tamanho que o solver não usa. O `t` do `body_desc` é o WORLD transform (W5), então `t.scale` é a escala de MUNDO ⇒ um corpo sob pai escalado herda a escala do pai (Unity/Godot). **Cuboid** toma escala per-eixo nativamente. **Ball uniforme** (`|sx|==|sy|`, limiar EXATO ⇒ `(1,1)` é byte-idêntico a antes) fica **círculo** (mais barato/redondo que polígono); **Ball não-uniforme** vira **elipse** (decisão do Enio: o collider casa com o sprite, não colapsa pra círculo como a Unity) — variant novo **`ShapeDesc::Ellipse{rx,ry}`** (append-only, só na plain-data; o `ColliderShape` AUTORADO **não muda** ⇒ **zero bump de schema**, a escala já vive no `Transform` persistido) realizada como **polígono convexo** (`ellipse_vertices` → `convex_polyline`), tesselação por **`libm::sincosf`** (determinismo cross-OS — `physics_ecs_c9` ganhou uma bola escalada, 52 corpos, hash comparado nos 3 OSes). Overlay traça o MESMO polígono (`ellipse_vertices`) ⇒ o wireframe senta no casco que o solver vê. LOC: `world.rs` bateu 700 ⇒ split `world/shape.rs` (ShapeDesc + tesselação). 8 gates novos (4 pure `scaled_shape` · 2 behavioral via sim — a bola 2× repousa mais alto, e a **parenteada** repousa como raiz 2× · 1 AABB da elipse no sim · 1 determinismo da tesselação) + 2 no overlay (elipse desenhada · o contorno cresce com a escala do PAI), **7 mutações, todas sangram** (dropar a escala mata 4; não-uniforme→círculo mata a resolução; spawn ball≠elipse mata a AABB; overlay elipse→círculo e outlines-sem-escala matam cada um o seu). **Aberto (herdado, não desta wave):** escala não-uniforme + **rotação** compõe um cisalhamento que a decomposição do `world_transform` põe em `scale`+`skew`, e o skew é IGNORADO (rapier não cisalha collider — mesma limitação honesta do Cuboid). **Smoke:** `PH2D_PHYSICS_SMOKE=9` (4 bolas: círculo · 2× uniforme · elipse não-uniforme · herança de escala do pai; tecla `B` mostra o contorno resolvido). Handoff [`docs/Physics/handoffs/HANDOFF_INTEGRACAO_line_physics_scale_collider_2026-07-19.md`](../../Physics/handoffs/HANDOFF_INTEGRACAO_line_physics_scale_collider_2026-07-19.md). **W7 — SENSORES / TRIGGERS (2026-07-19, `line/physics`, smokada pelos gates + smoke `=10`):** o **primitivo de trigger** — um `Collider.is_sensor` (append, `PROJECT_SCHEMA 26→27`) que **atravessa** (sem forças de contato) mas o solver **reporta o que o sobrepõe**. O `is_sensor` era só um esboço no ADR-0131 (*"waits for a consumer of its own"*); o Enio pediu (item B do cardápio). A metade da DETECÇÃO é limpa e contida na física (rapier `intersection_pairs`); o **consumidor de GAMEPLAY** (colisão→sinal via script/timeline) é **outra camada, cross-line, e continua decisão do Enio** — este primitivo é a fundação necessária, então não é trabalho jogado fora. **O consumidor VISÍVEL desta wave** (pra não ser flag morto): o overlay **acende** o sensor (magenta apagado→brilhante quando algo está dentro) + o estado é consultável (`PhysicsBridge::bodies_inside`/`is_triggered`/`triggered_sensors`, `BTreeMap` determinístico) + toggle **"Solid | Sensor"** no Inspector §11 (reusa o `seg_row` do Kind/Layer — populate/event/seam de graça). ⚠️ **A detecção só corre no play** (o `hold` LIMPA o estado — sem sim não há overlap; um trigger aceso sem nada dentro seria mentira). ⚠️ **Um sensor respeita as CAMADAS** (só detecta o que está configurado pra colidir). `world/sensors.rs::intersecting_body_pairs` (ordenado+dedup — o hash C9 não inclui trigger, mas o readout tem de ser reproduzível) · `bridge/triggers.rs` mapeia handle→entity. LOC: `world.rs` split `world/desc.rs` (BodyDesc/BodySnapshot); `bridge.rs` split `bridge/triggers.rs` + `body_desc` mudou-se pra `scale.rs` (ao lado do `scaled_shape` que usa). Gates: 4 behavioral ecs (detecta+atravessa · sólido bloqueia+nunca dispara · sem-sensores-sem-triggers · hold-limpa) + 2 physics (`intersecting_body_pairs` reporta/não-reporta) + 2 overlay (magenta idle/active · sensor não usa cor de kind) + seam (toggle Solid/Sensor) + persistência (is_sensor sobrevive ao round-trip). 3 mutações provadas (spawn ignora `.sensor()`→mata detecção · `rebuild_triggers` no-op→mata trigger state · idle==active→mata overlay). **Smoke: `PH2D_PHYSICS_SMOKE=10`** (2 pistas: bola bloqueada por plataforma sólida × bola atravessa o sensor que acende). **Aberto (a próxima camada, decisão do Enio):** o **sinal de gameplay** (colisão→ação: um `Marker` da timeline / um callback do `ph2d-script`) — cross-line, precisa do desenho do consumidor. **Weld / `FixedJoint` (2026-07-19, smoke `=11`):** o 5º tipo de joint (o W3 o deixou fora — *"~4 linhas, nada no smoke o exercita"*) — trava dois corpos **rigidamente** no ponto de âncora (sem translação NEM rotação relativa; rapier `FixedJoint`). Variant `Weld` apendado nos DOIS `JointKind` (ecs + physics, discriminante 3, `PROJECT_SCHEMA 27→28` — apender variant não move os índices, o bump é pro caminho INVERSO, mesmo raciocínio do v24 do vetor). ⚠️ **A âncora deixou de key-ar no `is_hinge()`** — um Weld **compartilha um ponto** como o Pin mas **não é hinge**, então `has_length()` parou de ser `!is_hinge()` e nasceu o `shares_a_point()` (Pin|Weld); o anchor policy lê ELE. Sem params (nem motor, nem limite, nem comprimento) ⇒ o §12 recusa toda linha de param (o `else` de Rope virou `else if KIND_ROPE`, senão o Weld herdava "Max Length"). Chip "Weld" no seletor do §12 (seg_row de graça). Gates: behavioral (um Weld segura a prancha RÍGIDA onde um Pin balança — o Pin é o controle) + discriminante pinado + `has_length`/`shares_a_point` + `each_kind_paints_only_the_rows_it_uses` estendido pro Weld (0 linhas). Mutação: Weld→Revolute deixa a prancha balançar. **Smoke `=11`** (barra soldada fica horizontal × barra pinada balança, lado a lado). **Bake — seleção de canais (2026-07-19):** o §11 ganhou um seletor **"Bake: All | Position | Rotation"** (acima do botão Bake, só p/ corpo Dynamic) — assar um SUBCONJUNTO dos canais de pose (o caso de **layering**: manter a posição animada à mão e assar só o giro da física, ou vice-versa). ⚠️ Mais estreito que "canal que não se moveu é pulado" (que o bake já faz — protege um canal que a sim não tocou); isto **descarta** um canal que a sim MOVEU porque o artista quer possuí-lo. `BakeChannels` (transiente, não salvo — é como o botão se comporta, não o documento; app-state, roteado como o Bake/Join) → `bake_selection` itera `channels.channels()` em vez de `PoseChannel::ALL`. Gate: All assa os 3 (o piso tombado move todos), Rotation-only assa só rotação (nem X nem Y), Position-only o inverso; mutação (iterar `ALL`) sangra. ⚠️ **`inspector_model.rs` está a 699/700 LOC** — a próxima adição de tipo de inspector obriga o split por domínio (os tipos de physics/joint p/ um irmão). **W8 — GRAVITY SCALE por corpo (2026-07-19, `line/physics`, smoke `=12`, aguardando integração):** multiplicador de gravidade POR CORPO (`0` sem peso · `<0` balão flutua · `>1` pesado) — algo que a gravidade GLOBAL não expressa. ⚠️ **Componente opcional `GravityScale`, NÃO campo do `RigidBody`:** a nota do W1 previa apendar ao `RigidBody`, mas ele é literal `{ kind }` em **~80 sítios** de fixtures (apendar é churn grande e RECORRERIA p/ damping/ccd/can-sleep); o presence-override (o idioma de `ZIndexOverride`/`BlendMode`, ausente=default) custa **zero churn e ZERO bump de schema** (componente novo cunha blob-key próprio — precedente do `PhysicsJoint`/W3), e o braço `Kind` do apply **preserva a gravidade de graça** (`RigidBody` intocado). A nota do plano fica corrigida. **`BodyDesc.gravity_scale` (recipe de spawn) É preciso** — o `rewind_to` reconstrói do descriptor, então sem ele o scrub perderia a gravidade (~19 fixtures de `ph2d-physics/tests` ganharam o campo neutro). Row **Dynamic-only** (rapier só aplica gravidade a Dynamic; row em Static/Kinematic seria controle morto); **detach no 1.0** (arquivo sem no-op). **O split que a nota acima prometeu ACONTECEU:** `inspector_model.rs` → §11+§12 p/ o irmão `inspector_model_physics.rs` (re-exportado, paths intactos — e a churn de física passa a morar num arquivo desta linha). **Dívida de LOC latente consertada de carona:** `render_loop/physics_overlay.rs` estava em **776** (556 no fork +220 por W2a/W7, sem marcador) ⇒ o gate `file_loc_caps` (shell, 600) estava **vermelho-latente desde o W7** e os smokes filtrados nunca o rodaram — `mod tests` (521 linhas) saiu p/ `#[path = "physics_overlay_tests.rs"]` (mantém `tests` FILHO ⇒ `use super::*` alcança privados, zero mudança de visibilidade) → 258/519. Gates mutação-provados: trajetória (0/1/2/-1) · a ponte LÊ o componente · seam offer+honour Dynamic-only · persistência round-trip · `registers_every_physics_component` 3→4 · c9 53 corpos. `PROJECT_SCHEMA` fica **28**. **CAPSULE — o collider de personagem (2026-07-19, `line/physics`, smoke `=13`):** 3ª forma (`ColliderShape::Capsule{half_height,radius}`, Y-alinhada) — caixa engancha em emenda/quina, cápsula desliza (por isso personagem 2D é cápsula). Escala uniforme → `Capsule` EXATA (rapier nativa); não-uniforme → `Stadium` (polígono convexo via `capsule_vertices`, tampas elípticas) — a MESMA regra Ball→Ellipse; `capsule_vertices` é porta única (build + overlay). **Variante apendada = sem bump de schema.** ⚠️ `Radius` num capsule edita as TAMPAS (não força bola — apagaria a cápsula); `Shape(_)` desconhecido é descartado; id próprio pro `half_height` (é o SEGMENTO reto, não o half-extent da caixa). Gate que justifica: `a_capsule_climbs_a_step_that_stops_a_box` (mut = cápsula vira caixa → trava em x=−0,13). c9 54 (um Stadium). LOC: `paint_shape_dims` extraído; cenas 12+13 → `physics_smoke_props.rs`. **VELOCIDADE INICIAL por corpo (W9, 2026-07-19, smoke `=14`):** lançamento autorado (linear+angular) aplicado no SPAWN — até aqui todo corpo começava parado (o vão que fez as cenas inclinarem gravidade pra fingir empurrão). Componente opcional `InitialVelocity` (padrão do W8: presence-override, **sem bump de schema**, registro 4→5); `BodyDesc.linvel/angvel` (recipe) É preciso — aplicada no BUILD, do solver depois (nunca por-tick), mas rida no `BodyDesc` pro **rewind RE-ARMAR** (gate: scrub t=0 + replay reproduz). Eixos de MUNDO (convenção `Rigidbody2D`); angular deg/s na UI ↔ rad/s no componente (conversão na fronteira do painel). **SETA amarela no overlay** (espaço de tela; só com `last_stepped()==0` — depois que anda, a velocidade viva ≠ lançamento). Rows Dynamic-only (`paint_dynamics_rows` extraído). Gates: `initial_velocity_launches_and_spins` · `..._rewind_re_arms_it` (fold + re-arm) · seam+conversão · c9 **55** (um lançado). `PROJECT_SCHEMA` fica **28**. **W-CCD — detecção CONTÍNUA por corpo (2026-07-20, smoke `=15`):** o sequel do W9 — a detecção *discreta* (default do rapier) só testa a pose de FIM de cada sub-passo, então corpo pequeno e rápido passa **limpo através** de parede fina entre 2 passos (a bala que atravessa; distinta do POUSO profundo que `DEFAULT_SUBSTEPS=4` ataca). Toggle **Discrete/Continuous** no §11 (vocabulário do Unity), **Dynamic-only**. **Marcador `Ccd` — a PRESENÇA é o booleano** (idioma do `Locked`, não o valued do W8/W9: booleano não tem valor a carregar); registro 5→6, blob-key, **sem bump** (fica **28**). `BodyDesc.ccd` (`.ccd_enabled()` no build, rida no recipe pro **rewind re-armar**). ⚠️ **Tunelamento é ALINHAMENTO-sensível (medido):** a 80 m/s os 4 sub-passos amostram pose exata na parede e o discreto NÃO tunela; 100..600 tunelam com margem — gate usa 200 m/s, smoke 160 (varridos, não "bonitos"). `enhanced-determinism` cobre o solver de CCD; c9 ganhou 1 bola CCD contra parede fina (o sweep entra no hash) → **57 corpos**. Gates: `a_discrete_body_tunnels_..._and_a_continuous_one_does_not` (mut `.ccd_enabled`→`false` tunela, RED) · `the_bridge_makes_a_marked_body_continuous_and_a_rewind_re_arms_it` · seam Dynamic-only + sweep · persistência (marcador sobrevive). Ids: `INSP_LIVE_PHYSICS_CCD` + `INSP_PHYS_CCD[2]` · `ph2d::physics::Ccd`. Aberto (Chesterton): CCD em Kinematic (rapier suporta; caso canônico é o projétil Dynamic). **W-LockRot — FREEZE ROTATION por corpo (2026-07-20, smoke `=16`):** o bloco que todo personagem 2D precisa — caixa livre numa rampa **tomba**, travar o DOF angular a mantém em pé (ainda translada e colide, só não gira). Toggle **Rotation: Free/Locked** no §11, Dynamic-only (`lock_rotation` do Godot / "Freeze Rotation" do Unity). **Marcador `LockRotation`** (presença = travado, idioma do `Ccd`/`Locked`); registro 6→7, **sem bump** (28). `BodyDesc.lock_rotation` → `.locked_axes(LockedAxes::ROTATION_LOCKED)`, rida no recipe pro rewind re-armar. Corpo travado **ignora `angvel`** (sem DOF pra girar) — daí o gate afiado (spin inicial → livre gira 2,5 rad, travado 0). c9 ganhou 1 caixa travada girando → **58**. Gates: `a_free_body_spins_and_a_rotation_locked_one_does_not` (mut `.locked_axes`→`empty()` gira, RED) · `the_bridge_pins_a_marked_body_and_a_rewind_re_arms_it` · seam Dynamic-only + sweep · persistência. LOC: wrapper+ponte bateram 700 → split (`world/tests.rs`, `bridge/diagnostics.rs`); `body_desc` 8 args → `#[allow(too_many_arguments)]`. Ids: `INSP_LIVE_PHYSICS_LOCKROT` + `INSP_PHYS_LOCKROT[2]` · `ph2d::physics::LockRotation`. Aberto (Chesterton): Freeze Position X/Y (o resto do `RigidbodyConstraints2D`; controle raro de trilho, família própria — o Godot ship `lock_rotation` standalone). **W-Offset — OFFSET do collider por corpo (2026-07-20, smoke `=17`):** o collider quase nunca fica centrado no sprite (hitbox dos pés abaixo, ponta de projétil à frente) — **Offset X/Y** no §11 (o `Collider2D.offset` do Unity) o desloca. ⚠️ **Campo no `Collider`, NÃO componente opcional** (offset é intrínseco, como restituição/atrito/layer/sensor) → **BUMPA o schema 28→29** (postcard posicional; tripla-pin `(29,8,13)`) — oposto dos 4 marcadores anteriores. `BodyDesc.offset` → `ColliderBuilder::translation` (rapier rotaciona COM o corpo); ⚠️ **escala SINCADA, não `abs`** (offset é POSIÇÃO → flip **espelha**). **O overlay É a única forma de VER o offset** (lição da seta de velocidade) — o contorno desenha no offset com a MESMA escala-sincada+rotação que a ponte manda pro solver (`physics_overlay.rs`). Não-Dynamic-only (qualquer collider). Gates: `a_collider_offset_moves_where_the_body_rests` (mut `.translation`→zero, RED) · bridge fold+rewind · `scale::tests` scale+mirror (unit no `body_desc`) · `an_offset_collider_outline_sits_where_the_collider_is` (overlay desloca+rotaciona; bbox-center por causa do spoke) · persistência · c9 **59**. Ids: `INSP_PHYS_OFFSET_X/Y` · `Collider.offset`. `PROJECT_SCHEMA` **29**. Aberto: rotação do collider · múltiplos colliders (composite). **W-LockPos — FREEZE POSITION X/Y por corpo (2026-07-20, smoke `=18`):** o resto do trio de constraints do Unity/Godot (ao lado do Freeze Rotation) — travar X prende a um trilho (elevador/lane), travar Y faz flutuar (plataforma). Toggles **Freeze X / Freeze Y** no §11. **Dois marcadores `LockPositionX`/`LockPositionY`** (a PRESENÇA é o booleano, idioma do `LockRotation`) → registro **7→9**, blob-key, **sem bump** (fica **29** — marcador registrado é aditivo; o oposto do W-Offset). `BodyDesc.lock_x/lock_y` ORam no MESMO `LockedAxes` (`TRANSLATION_LOCKED_X`/`_Y`), rida no recipe pro rewind re-armar. ⚠️ **rapier NÃO zera a velocidade inicial de um eixo de translação travado** (o gate red-first pegou): `LockedAxes` zera a inversa-massa (força não acelera — Y-locked não cai), mas o integrador avança pela `linvel` CRUA sem projetar os eixos travados (medido: X-locked lançado a 3 m/s deslizou 1,5 m; rapier só trata rotação assim), então `spawn_body` zera a componente travada (um eixo congelado não carrega velocidade — é o que "Freeze Position" significa). Dynamic-only (2 `seg_row`). Gates red-first+mutação: bit de X (força lateral) · bit de Y (gravidade) · drop de velocidade + independência (via FORÇA, não velocidade — a velocidade crua atravessa o bit largo) — **duas camadas por eixo = um gate por camada**. `populate.rs` registra os 2 groups (senão toggles mortos sob o mouse — `wiring_parity` pega). c9 **60 corpos**. Smoke `=18` (3 bolas lançadas de lado: verde Free arca, ciano Freeze-X cai reto, laranja Freeze-Y plana à altura constante). **W-Mass — MASSA MANUAL por corpo (2026-07-20, smoke `=19`):** o artista pensa em MASSA, não densidade — toggle **Mass: Auto | Manual** no §11 (o `useAutoMass` do Unity): Auto mostra Density, Manual mostra Mass (kg). ⚠️ **Densidade e massa são a MESMA grandeza por dois caminhos** (`massa = densidade × área`), então só UMA row é viva por vez (mostrar as duas = "duas portas pra uma grandeza"); Static/Kinematic (massa infinita) mantêm Density sem toggle (Dynamic-only). Componente **VALUADO** `MassOverride(f32)` (idioma do `GravityScale`) — registro **9→10**, blob-key, **sem bump** (fica **29**). `BodyDesc.mass_override: Option<f32>` → `ColliderBuilder::mass(m)` (kg, ignora densidade) vs `.density(d)` (auto, byte-idêntico); rida no recipe pro rewind. **Auto→Manual semeia com a massa auto** (`shape_mass` = densidade×área, exato pra corpo sem escala — só o valor inicial). Gates red-first+mutação: wrapper lê `RigidBody::mass()` cru (auto≈0,785, manual==10; mut sempre `.density`→RED) + colisão de momento (massa muda COMPORTAMENTO — gravidade esconderia, todos caem a `g`); ECS a bola pesada `MassOverride(20)` ara a fila de leves e passa (mut ignora→vira leve, para=RED) + rewind. Seam Auto pinta Density/não-Mass, Manual o inverso (presença E ausência). c9 **61**. ⚠️ **LOC:** `physics.rs` do painel bateu 609>600 (cap do painel, que o `workspace_file_loc_cap` NÃO cobre) → helper das rows de massa foi pro irmão `sections/physics_rows.rs` (recebe `is_dynamic`/`mass_manual` resolvidos, zero const compartilhada). Smoke `=19` (bola pesada Manual 30 kg ara 5 pinos; a leve do mesmo tamanho para no 1º). **W-Dominance — DOMINANCE/prioridade de colisão por corpo (2026-07-20, smoke `=20`):** o contraponto da massa — Dominance é uma PRIORIDADE (rapier `dominance_group`): o corpo de dominância estritamente maior é tratado como massa infinita pelo menor (atropela, nunca é empurrado de volta) enquanto ainda cai e colide com pares. ⚠️ **Ortogonal à massa:** um corpo LEVE de dominância alta empurra um PESADO neutro — o que a massa sozinha NÃO faz; e é um meio-termo que nenhum KIND expressa (Static/Kinematic também empurram mas não caem/reagem). Row **Dominance** no §11, Dynamic-only. Componente VALUADO `Dominance(i8)` (idioma do `GravityScale`) — registro **10→11**, blob-key, **sem bump** (fica **29**), detach em 0. `BodyDesc.dominance` → `dominance_group`, rida no recipe; painel converte float→i8 (round+clamp). Gates red-first+mutação: wrapper mover leve Dominância 5 atravessa pesado 20 kg (o que a massa não faz; mut tira `.dominance_group`→RED) + readback `RigidBody::dominance_group()`; ECS bridge fold (mut ignora→quica x=0,02=RED) + rewind; seam presença/ausência + conversão. c9 **63** (mover+alvo — a dominância só percorre o solver se COLIDIR). ⚠️ **LOC — 2 splits + lição:** a shell tem gate PRÓPRIO `shells/desktop/tests/file_loc_caps.rs` (HR-18, 600) que o `architecture_workspace_file_loc_cap` (só `crates/`) NÃO cobre — a wave da Massa deixou `physics_smoke_props.rs` em 643>600 sem eu ver. Fix: cenas 19+20 → irmão `physics_smoke_collision.rs` (mover-numa-fila: peso vs prioridade); `shape_mass` → `Collider::auto_mass()` na `ph2d-physics-ecs`. **O `file_loc_caps` da shell agora entra no gate de fechamento.** Smoke `=20`. Aberto: damping por-corpo (única propriedade padrão que falta; precisa de modo de combinação). **W-Material — REGRAS DE COMBINE do material (Bounce/Friction Combine, 2026-07-20, smoke `=21`):** completa o `PhysicMaterial` — `Bounce`/`Friction` já eram autorados mas sempre combinavam por `Average` (default do rapier), então Bounce 1.0 num chão morto mal quicava (as duas médias dão 0.5) e não havia como fazer uma superball quicar em qualquer piso. Duas segmentadas novas **Bounce/Friction Combine** (Average/Min/Multiply/Max) logo abaixo dos valores. ⚠️ **NÃO Dynamic-only** (material de collider — o combine de um chão ESTÁTICO importa); rapier resolve com `rule1.max(rule2)` ⇒ a regra de MAIOR prioridade dos 2 vence ⇒ uma bola `Max` quica em QUALQUER piso (e o smoke fica determinístico: só a bola carrega a regra). Não confundir com o `damp_mode` do damping (combine de 2 SUPERFÍCIES ≠ override-vs-global). `BodyDesc.material: CombineRules{restitution,friction}` (bundle — 1 linha `Default::default()` por fixture, **sem** import) ← componente único `MaterialCombine{restitution,friction: CombineRule}` (enum serde; `scale::body_desc` mapeia p/ `CoefficientCombineRule`, porta única, padrão `BodyKind→RigidBodyType`); registro **11→12**, sem bump (fica **29**), detach quando ambos Average. Gates red-first+mutação: wrapper `Max` quica alto/`Min` morre (mut colapsa `Max` em `Average`) + readback; ECS fold+rewind; seam oferecido p/ TODO kind + cada opção despacha seu tag; shell read-modify-write preserva a OUTRA regra + detach no neutro. c9 **63→64**. LOC: `apply_physics_edit` → irmão `inspector_physics_apply.rs` (634>600, re-export mantém caller paths), `paint_physics_section` → `physics_rows::paint_material_rows` (230>200 fn). ⚠️ **`arch_safe_clamp_only` estava VERMELHO desde a Dominance** (o clamp dela usa bounds não-literais sem `safe_clamp`; a Dominance não rodou o gate — mesma classe do miss do `file_loc_caps` que ela mesma documentou) — corrigido, e **entra no gate de fechamento.** Smoke `=21` (2 superballs Bounce 1.0 no MESMO chão morto: a de `Max` quica alto repetidamente, a de `Average` morre em 2 saltos). Aberto: damping por-corpo (segue sendo a única propriedade padrão que falta). **W-Damping — DRAG por corpo (Linear/Angular + modo Combine/Replace, 2026-07-20, smoke `=22`):** fecha o conjunto padrão Unity/Godot — era a última propriedade que faltava, e a `defaults.rs` já a projetou exigindo um modo Combine/Replace (o `damp_mode` do Godot: *"segundo campo que ganha em silêncio do global = falha de duas-portas"*). §11 Dynamic-only: **Linear Damping** + **Angular Damping** (num) + **Damp Mode** (Combine|Replace). Combine SOMA ao drag global do mundo, Replace o IGNORA. ⚠️ com drag global default (0) os modos COINCIDEM — o mode só diverge sob um drag de mundo autorado. Não confundir com o combine do material (2 SUPERFÍCIES ≠ override-vs-global). ⚠️ **O clobber, e a porta única:** é o 1º override que colide com o global — `stamp_defaults`/`apply_to_all` escrevem drag global em TODO corpo, então mudar o drag global no Play clobbaria um override. Fix: `PhysicsWorld::apply_damping_override` (porta única, resolve Replace/Combine vs `body_defaults`, usada por spawn E bridge) aplicada no spawn **DEPOIS** do stamp + um **passe de re-stamp por dispatch** no bridge (`restamp_damping`) que ⚠️ lê o `rest.damping` (config), NÃO o componente vivo (edição mid-play é transiente como todo config; mudança de drag global é pega ao vivo pro Combine — o que `apply_to_all` pretende). `BodyDesc.damping: Option<DampingDesc{linear,angular,replace}>` ← componente único `DampingOverride{linear,angular,mode: DampMode}`; registro **12→13**, sem bump (fica **29**). ⚠️ **detach MODO-AWARE:** neutro = `0,0,Combine`; `Replace(0,0)` NÃO é neutro (força drag zero ignorando o mundo) e FICA. Gates: wrapper (freado desliza <60%/spin <50%; Replace vs Combine sob drag global; readback) + ECS fold+rewind + **`a_global_drag_change_mid_play_does_not_clobber_a_replace_override`** (pina o passe: mut tira `restamp_damping` → replace==plain==3.14 RED) + seam Dynamic-only + shell RMW/detach-modo-aware. c9 **64→65**. **LOC — 3 splits por RESPONSABILIDADE:** `world.rs` 721 → `apply_damping_override` p/ `world/damping.rs` (694); `bridge.rs` 728 → `restamp_damping` p/ `bridge/damping.rs` + o `settle` pré-existente p/ `bridge/hold.rs` (é a behavior de pausa, `hold` já o chamava — move honesto) (662). Um tofu `→` no eprintln do smoke 22 pego pelo `no_tofu_glyphs`, trocado por ASCII. Smoke `=22` (bola VERDE Linear Damping 4 desce como pena vs ORANGE que cai rápido; caixa VERDE Angular Damping 4 para de girar vs ORANGE que gira sempre, hover com GravityScale 0). **Aberto: o conjunto padrão por-corpo está COMPLETO.** **W-OneWay — PLATAFORMA JUMP-THROUGH (one-way, 2026-07-20, smoke `=23`):** o collider icônico do platformer 2D, e a 1ª wave desta linha que adiciona uma CAPACIDADE em vez de outro knob. Um collider one-way é sólido **só pelo lado +Y LOCAL dele**: quem chega por BAIXO atravessa limpo e POUSA em cima na volta (`one_way_collision` do Godot). ⚠️ **rapier já traz a primitiva** (`update_as_oneway_platform`) **e a histerese** que impede o corpo de pipocar ⇒ esta wave é INTEGRAÇÃO, não solver. A flag chega ao hook (stateless, `&self`) como **bit no `user_data` do collider** + `ActiveHooks::MODIFY_SOLVER_CONTACTS`, sem o qual o hook nunca é chamado; instalar o hook é **byte-neutro** sem plataforma. ⚠️ **A normal permitida vive no frame do COLLIDER1 e a plataforma pode ser o collider2** — o rapier não ordena o par, e um `±Y` constante (como o demo dele, cuja fixture é axis-aligned) assume que os 2 colliders compartilham orientação, o que plataforma ROTACIONADA quebra ⇒ direção DERIVADA: `allowed_local_n1 = R1⁻¹·(s·platform_world_up)`, `s=−1` se a plataforma é o collider2; quando é o collider1 reduz ao `+Y` local ⇒ UMA fórmula, sem caso especial. ⚠️ **A MUTAÇÃO QUE SOBREVIVEU:** apagar o flip de sinal passava em TODOS os gates porque a fixture spawnava a plataforma sempre PRIMEIRO (⇒ sempre collider1, ramo do collider2 nunca rodava) — a ordem de spawn virou PARÂMETRO e agora a mesma mutação mata os 3 testes em `platform_first=false` (bola cai a y=−41 onde deveria pousar). O sinal estava certo; faltava a PROVA. Marker `OneWayPlatform` (registro **13→14**, **sem bump**, fica **29**) e não campo do `Collider` (postcard posicional ⇒ bump, o que `layer`/`is_sensor`/`offset` custaram). ⚠️ **NÃO Dynamic-only** — plataforma é quase sempre **Static**, então gatear em Dynamic deletaria o controle do próprio caso de uso. Gates: os 3 wrapper nos DOIS spawn orders (incl. plataforma de CABEÇA PARA BAIXO atravessável por cima — o teste que a matemática ganha) + ECS fold/rewind + seam oferecido-para-todo-kind; 3 mutações. c9 **65→67**. **LOC — 2 splits por responsabilidade:** `paint_physics_section` 211>200 → Layer+Trigger+One-Way viraram `physics_rows::paint_collision_rows`; `world.rs` 711>700 → a metade-collider do `spawn_body` virou `world/collider_build.rs` (634). `ALLOWED_ANGLE = FRAC_PI_4` explicado (a distinção é CIMA vs BAIXO, 180° de distância; o 0.1 rad do demo do rapier é afinado p/ caixa chata). **Aberto:** contact events (precisa de consumidor; a precedência do W7 diz torná-lo VISÍVEL primeiro) · soft-body/fluidos/fratura seguem fora de escopo (D9) · per-axis `damp_mode`. **⬛ OS JOINTS GANHARAM MÃOS + A CENA GANHOU UMA — `line/physics` INTEGROU (2026-07-27, 22 commits, todos os smokes aprovados; handoff [`docs/Physics/handoffs/HANDOFF_INTEGRACAO_line_physics_joints_hand_2026-07-26.md`](../../Physics/handoffs/HANDOFF_INTEGRACAO_line_physics_joints_hand_2026-07-26.md)):** duas frentes. **(A) O plano 02 — a UI/UX dos joints, as nove linhas fechadas:** tunar param AO VIVO (`=42`; 2 causas — gate `at_rest` obsoleto + fila sem flush) · **o joint DESENHA o que ele é** (`=43`, glifo por TIPO, não um segmento genérico) · a âncora tem **DUAS alças + ímã** (`=44`, snap CTRL aos 9 pontos do collider) · **pose, não digite** (`=45`, limite e comprimento autorados no canvas) · **criar onde se olha** (`=46`, press em A → arrasta → solta em B) · **o TRILHO** (`=47`, Slider/prismatic, o 5º tipo) · **o SERVO e o GUINCHO** (`=48`, o motor ganha modo Position e chega ao Slider/Rope) · **o joint que PARTE sob carga** + readout (`=49`) · **a higiene do PAR** (`=50`: Active · Collide · Swap A↔B · o nome) · **o grupo carrega o rig** (`=51`, Alt+arrastar move o componente conexo INTEIRO). **(B) A FERRAMENTA DE INTERAÇÃO com a cena RODANDO:** **a MÃO** (`=52` — pegar um corpo no PLAY por **mola via solver**, nunca teleporte) e **a seção da ferramenta** (`=53` — 3 modos de segurar · explosão · campo de atração · **o bug do collider fantasma**). **`PROJECT_SCHEMA` 31→34** (v32 `motor_mode`+`motor_target` · v33 `break_*` · v34 `active`+`collide_connected` — três campos APENDADOS ao `PhysicsJoint`, postcard posicional ⇒ saves v31 **recusados**) · registro `ph2d-ecs` **fica 21** (nenhum componente novo) · **c9 `c9d4baee…`, 87 corpos** (debug ≡ release) · gizmo ids **965/966/967/968** (`GIZMO_JOINT_ANCHOR_B` · `_LIMIT_MIN` · `_LIMIT_MAX` · `_LENGTH`; **próximo livre 969**). **Nenhum ADR novo** (tudo sob o ADR-0131) e **nenhum contrato congelado tocado** (conferido por grep). **Três mudanças de comportamento, todas smokadas:** um corpo **ESTÁTICO** arrastado com o relógio ANDANDO agora move o collider junto (antes o desenho ia e o collider ficava — e a cena `=52` teve o passo 5 REESCRITO porque afirmava o oposto) · o default do motor nasce em `Velocity` e o motor passa a existir no Slider e na Rope · **Alt+arrastar** um corpo jointado em repouso arrasta o rig inteiro. **Aberto (não bloqueia):** a explosão **não impõe torque** (decisão medida) · o campo repelindo **arremessa para fora de quadro** (−20 N abre a nuvem para 9,23 m em 1 s: força sustentada sem freio fora do alcance) · **Rigid e Rope atravessam parede** (inerente à rigidez infinita, nomeado no doc, no enum e no smoke) · soltar a mão deixa **um passo de undo** (pré-existente de QUALQUER clique no play; a cura mora no roteador de undo, outro domínio) · o horizonte do plano 02 §8 (IK multibody · params keyframáveis · Wheel preset · Rod/soft weld · copiar-colar propriedades) segue **não escalonado**. ⚠️ **E a integração produziu a falha EMERGENTE que só a árvore combinada pode ver:** `shells/desktop/src/input_dispatch/keyboard.rs` cruzou o cap de 600 LOC (**604**) — a `line/anim` somou +9 e a `line/physics` +13 sobre um arquivo já em 582, e **nenhuma das duas cruzava sozinha**. Corte por RESPONSABILIDADE: as quatro travas com a MESMA guarda (`timeline_panel_open()`) saíram para o irmão **`keyboard_timeline.rs`** (496 + 144), com a ORDEM preservada (Vector/Motion → timeline → Escapes) e os 2 arch-gates do acorde reapontados para afirmar a **propriedade** em vez do endereço (3 mutações, 3 sangram — inclusive a metade que faltava: *a chamada existir com a trava vazia*). **⬛ A POLIA + O PINO DE MUNDO — `line/physics` INTEGROU (2026-07-30, 70 commits, smokes `=61`/`=63`/`=64`/`=65` aprovados; [ADR-0149](../../architecture/decisions/0149-physics-ik-is-a-transient-posing-tree-not-a-second-joint-representation.md), handoff [`docs/Physics/handoffs/HANDOFF_INTEGRACAO_line_physics_world_pin_2026-07-30.md`](../../Physics/handoffs/HANDOFF_INTEGRACAO_line_physics_world_pin_2026-07-30.md), plano [`03_plano_polia.md`](../../Physics/03_plano_polia.md)):** duas frentes. **(A) A POLIA (W0..W6 + a WESTON)** — o 7º tipo de joint, e **o primeiro cujo comprimento é um ORÇAMENTO, não uma distância**. ⚠️ **O `ratio` SAIU no W1 por ser física ERRADA:** numa corda única a tensão é uniforme ⇒ vantagem 1, e um knob que a contradiz é a ferramenta mentindo. No lugar, **uma roldana é uma ENTIDADE com RAIO** — rota de N nós tangenciando a SUPERFÍCIE, o arco entra no COMPRIMENTO e não no Jacobiano, lado decidido por ponto fixo, `ω = s/r`. Daí tudo o mais cai **sem um número novo**: a **TALHA** (W3 — a roldana montada num corpo que se move; 2 kg equilibram com **1,00 kg**, medido), o **TAMBOR DIFERENCIAL** (W4 — um eixo é UM nó, logo um tambor é UMA roldana com DOIS raios, vantagem `r_entra/r_sai`), a **COMPOSIÇÃO** (W5 — tambor e cadernal na MESMA corda: as vantagens **MULTIPLICAM**, 1 kg segura 16 kg **sem ninguém digitar um "16"**) e a **WESTON** (o MESMO eixo atravessado DUAS vezes, `R/(R−r)`). Mais o **MOTOR** (roldana dirigida = guincho) e a **RUPTURA** (UM limiar; o que difere é o EIXO de cada roldana) · as **ALÇAS** do W6 (re-colocar o eixo de uma roldana montada era gesto **morto e silencioso**) · o **ÍMÃ** do eixo · o **conta-gotas de corda**. ⚠️ **A Weston achou DOIS bugs PRÉ-EXISTENTES e os dois entram corrigidos:** o teto do guincho era **cego ao peso da corda** (taxa içada caía a **38%** no peso 8; latente desde o W4) e **um rewind replayava SEM AS CORDAS** — o `rebuild_from_rest` trocava o mundo sem reinstalar a tabela, e ficou **calado** porque `target == 0` replaya zero passos. **(B) W-JointWorld — O PINO DE MUNDO:** um joint cujo lado B é um **PONTO DO CENÁRIO**, não um corpo, removendo o **corpo estático inventado** que era obrigatório para pendurar qualquer coisa. ⚠️ **NÃO é `body_b == 0`** (aquele estado já significa *meio-autorado*): é o **marcador `JointWorldAnchor`** ⇒ **zero bump de schema**; e o ponto é o **`Transform` da própria entidade-joint**, que já era a âncora autorada, já tinha o dot arrastável e já viajava no save. ⚠️ **`JointRef.entities.1` e `JointView.body_b` viraram `Option<Entity>`** — esquecer o caso do mundo fica **impossível por TIPO**. Medido: dois pêndulos idênticos, **0,8383 m de percurso nos DOIS** (quatro decimais), e só o da esquerda precisa de um objeto inventado. **`PROJECT_SCHEMA` 38→46** (a escada da linha era 45; ⚠️ o valor se **CONTA** a partir do `main` do dia, não se escolhe — a `line/Vector` tinha acabado de levar o 38) · registro `ph2d-physics-ecs` **21→24** · gizmo ids **969/970/971** (**próximo livre 972**) · **c9 `7cb7728d…`, 96 corpos** (debug ≡ release). ⚠️ **E o ADR nasceu 0145 e RENUMEROU para 0149 NA INTEGRAÇÃO** — a `line/Painter` levou 0145·0146·0147 e a `line/Vector` o 0148 na mesma janela; *um número escolhido numa linha paralela é PROVISÓRIO*. Smokes: **`PH2D_PHYSICS_SMOKE=54..65`**. **⬛ O RIG SAI DA HIERARQUIA + A SOLDA QUE CEDE + UM CORPO COM VÁRIAS FORMAS — `line/physics` INTEGROU (2026-08-01, 7 commits, 33 arquivos novos, todos os smokes aprovados; handoff [`docs/Physics/handoffs/HANDOFF_INTEGRACAO_line_physics_2026-08-01.md`](../../Physics/handoffs/HANDOFF_INTEGRACAO_line_physics_2026-08-01.md)):** quatro waves. **W-JointCopy** (cena `=66`) — afine UM joint e **carimbe o rig inteiro**, em vez de repetir o mesmo par de números em cada elo. **W-Rig** (cena `=67`) — o rig sai da **HIERARQUIA**, mais as três correções do smoke dele (Reset · emenda · batentes). **A régua existe** — o prólogo passou a abrir a timeline para **toda** cena de smoke (antes cada cena decidia por conta, e as que esqueciam mostravam um transporte que o artista não alcançava). **W-SoftWeld** (cena `=68`) — **a solda que CEDE**: `PhysicsJoint.soft`, um bool apendado, e a dureza **reusa a `stiffness`/`damping` da mola** em vez de inventar dois números novos. **W-Compound** (cena `=69`) — **um corpo, VÁRIAS formas** (id `INSP_PHYS_ADD_SHAPE`), que fecha o *múltiplos colliders* aberto desde o W-Offset. ⚠️ **`PROJECT_SCHEMA` 46→47 nesta linha, e ele COLIDIU com a `line/FLIP` pela TERCEIRA vez** (depois do 30 em 25/07 e dos 32/33/34 em 27/07) — as duas escreveram **47** contra o `main` de 30/07. Quem chegou primeiro ficou com o 47; a FLIP **contou para 48**. Detalhe da armadilha na entrada da persistência abaixo. **`physics_ecs_c9` 99 corpos, `556cb652…`** (debug ≡ release) · registro `ph2d-physics-ecs` **fica em 24** · gizmo ids **nenhum novo** (o último segue 971, próximo livre **972**) · **nenhum ADR** (tudo sob o ADR-0131) · contrato congelado intacto (por grep) · **zero `Cargo.toml`**. Smokes: **`PH2D_PHYSICS_SMOKE=66..69`**. **⬛ A PEÇA VIRA EDITÁVEL, O SENSOR DE PÉ, A JANGADA, O SINAL, A CORDA E O LIMITADOR — `line/physics` INTEGROU (2026-08-02, 39 commits, todos os smokes aprovados; handoff MESTRE [`docs/Physics/handoffs/HANDOFF_INTEGRACAO_line_physics_MESTRE_2026-08-02.md`](../../Physics/handoffs/HANDOFF_INTEGRACAO_line_physics_MESTRE_2026-08-02.md)):** ⚠️ **a espinha da jornada é UMA PREMISSA QUE ENVELHECEU.** A W-Compound (01/08) deu a um corpo **várias formas**, e a frase *"um corpo tem exatamente um collider"* estava escrita — em código **e** em doc-comment — em **quatro** lugares que ninguém reconferiu; cada um virou um defeito de classe diferente: a **peça era inedidável** e a única porta oferecida a **apagava** (W-PartFace, `=70`) · o **sensor de pé** — o `isGrounded` de Box2D/Unity — **nascia morto** porque o canal de triggers era por CORPO (medido: tronco assenta 1,6990 sólido × 1,4990 sensor, e `triggered_sensors()` **vazio nos dois**; W-PartSensor, `=71`) · as **zonas** liam `rb.colliders().first()` em **cinco sítios** e a jangada composta **CAPOTAVA** (controle 0,000° × composta **−90,007°**; W-CompoundZone, `=72`) · e um corpo composto **tocava DUAS vezes** (2 entradas / impulso 0,030677+0,030636 × **1 / 0,061313**). ⚠️ **O caso das zonas era invisível por COMPENSAÇÃO:** a zona aplicava o empuxo uma vez por PAR de colliders, então uma jangada de duas metades iguais levava `2 × meia-força` = a força certa, **por acidente aritmético** — consertar só o empuxo a faz boiar com METADE da submersão, e *meia correção é pior que nenhuma*. ⚠️ **E o caso do contato é o único em que a frase estava escrita como LEI** (*"dois objetos se tocando é UM evento; relatar cada quina responde quantas QUINAS"*): ela valia para **pontos de contato** e quebrou para **FORMAS**. Mais o **SINAL** (uma colisão passa a *fazer alguma coisa acontecer* — `SignalOnHit`, `=73`) · o **LEAD-DRAG** (arrastar o corpo da âncora **leva o sistema** — rígido em FK, como uma **corda** em IK, `=74`) · e **O LIMITADOR** (`=75`), cuja 2ª rodada de smoke é o que vale ler. ⚠️ **O defeito do limitador era pior que *"colide"*:** medido antes de uma linha, a folga de tangente chega a **0,0000 aos 12,00 s**, a rota **degenera**, o passe recusa a corda inteira e **a carga deixa de ser segurada — ela é DEVOLVIDA** (1,18 m aos 10 s, 3,40 aos 15), sem erro e sem aviso. A grandeza **não foi escolhida**: `len = √(d² − r²)` zera **exatamente quando a amarração toca o ARO** (uma distância ao CENTRO diria meio metro de folga com a carga já encostada numa roldana de meio metro). E a 2ª rodada (*"uma força bizarra que empurra o objeto na direção x das polias"*) mediu a força **23,76° FORA da corda** — desvio que é pura geometria, `atan(r/s)`, de **9,5° a 76°**: uma corda puxa ao longo de si mesma, e qualquer componente perpendicular é **força sem matéria que a transmita**. A v1 fazia a CORDA falar o radial; hoje quem cede é a **TRAVA**, e só na metade em que ela pode — **empurra pela corda, SENTE pelo radial** (`End::k2`, a forma bilinear `gᵀM⁻¹u`; `End::k` vira `k2(dir, dir)`, uma porta só). ⚠️ **Nenhuma das metades basta, e as duas foram medidas uma contra a outra:** sentir-e-empurrar pela CORDA dá deriva lateral 0,0000 m e folga **0,0000** (não segura nada) · pelo RADIAL dá folga 0,4948 mas **1,0445 m de deriva** (é ela que punha a carga de lado) · **sentir radial e empurrar corda** dá **0,0000 m** de deriva e **0,3685 m** de folga. ⛔ **MEDIDO E REJEITADO, não refaça:** folgar o orçamento da corda pela violação da trava compra **0,0007 m** — ruído. ⚠️ **A barra do gate `the_stop_holds_on_a_big_wheel…` foi RE-MEDIDA (0,45 → 0,30), não afrouxada** — ela era calibrada para a lei que punha a carga de lado, e o número honesto da lei correta é 0,3685. **Zero `Cargo.toml`, zero crate nova, zero ADR** (tudo sob o ADR-0131), `ph2d-ecs` **intocado**, e ⚠️ **`PROJECT_SCHEMA` INTOCADO — esta jornada fica FORA da disputa de número** (o único toque no `project.rs` é o degrau **v48** que faltava na escada: a `Cap::Square` do Flip, que arrasta este schema porque o `FlipDoc` viaja DENTRO do `ProjectFile`; *uma escada com buraco é como o próximo bump nasce mal-numerado*). Registro do `ph2d-physics-ecs` **24→26** (`SignalOnHit` · `RopeStops`) · gizmo ids **972 · 973** (`GIZMO_ROPE_STOP_A`/`_B`; **próximo livre 974**) · `physics_ecs_c9` **`16ba80e8…`, 99 corpos, debug ≡ release** (movido pela W-CompoundZone, atribuído por ablação; a W-RopeStop **não o move** — nenhuma corda do c9 carrega limitador). ⚠️ **A cena `=75` não manda apertar `B`, de propósito:** o contorno já nasce ligado e `B` o DESLIGARIA, levando junto as alças que o passo seguinte manda arrastar. **Aberto:** a trava é restrição de **posição**, então um balanço violento a ultrapassa por um sub-passo (**0,3685 contra os 0,5 pedidos**, pinado no gate) · a corda de IK **não colide com nada** enquanto é arrastada — é pose, não simulação. **⬛ O PLAYER DE PLATAFORMA — o módulo inteiro, `line/physics` INTEGROU (2026-08-04, 32 commits, 182 arquivos; handoff MESTRE [`docs/Physics/handoffs/HANDOFF_INTEGRACAO_line_physics_MESTRE_2026-08-04.md`](../../Physics/handoffs/HANDOFF_INTEGRACAO_line_physics_MESTRE_2026-08-04.md)):** duas jornadas. **(A) A cauda de 03/08** — `W-SignalLeave` (a porta que FECHA) · `W-PartAdopt` · `W-RopeSays` · `W-RailRope` · `W-JointAnim` (um param de joint é uma entrada por TICK, com os canais da timeline) · **`W-JointCustom`** (o joint que o artista descreve por EIXO — cada grau de liberdade é *Free / Limited / Locked*, o modelo do Unreal, expondo o `GenericJoint` que sempre esteve lá). **(B) O PLAYER, 25 commits da pesquisa à W11** — crate nova **`ph2d-platformer`** (lei pura: sem rapier, sem ECS) + a ponte + a §14 do Inspector. **`PROJECT_SCHEMA` 50→55 — DOIS degraus, e o handoff contou UM.** ⚠️ Ele escreveu *"a linha escreve 52, o main diz 53 ⇒ o valor certo é 54"*, mas a escada da linha tem **v51 (`custom`) E v52 (`corner_reach`+`lift_momentum`)** ⇒ contra o `main` em 53 o par CONTADO é **54 e 55**. *O valor se CONTA a partir do `main` do dia, e nem o handoff da própria linha pode ser tomado como aritmética pronta* [[feedback_numbers_that_sum_across_lines_count_dont_pick]]. Registro do `ph2d-physics-ecs` **26→28** (`PlatformPlayer` · `SignalOnLeave`) · `ph2d-ecs` **intocado** · gizmo ids **nenhum novo** (próximo livre segue **974**) · **nenhum ADR** · contrato congelado intacto · **nenhuma dep externa nova** (o `libm` da `ph2d-platformer` é o MESMO pin `=0.2.16` de cinco crates que já o usam — aresta, não pacote) · `physics_ecs_c9` **`b3dbe792…`, 108 corpos**, debug ≡ release. Smokes **`PH2D_PHYSICS_SMOKE=76..90`** (⚠️ **`=84` não existe, de propósito** — ele foi pulado; ⚠️ **e a razão que esta linha registava era a do VECTOR, copiada**: o roteador da física é um `match` sobre a string do env, então um número duplicado é `unreachable pattern` no compilador, e não *"o primeiro vence"* em silêncio). ⚠️ **A W11 tem veredito PARCIAL do Enio** (*"o player sobe sozinho bem devagar; faremos os ajustes amanhã"*): o default que shipa deixa **0,164 m de resíduo por 10 s numa rampa de 30°** — metade do que era, **NOMEADO e gateado dos dois lados**, com a cura medida no §14 do handoff da W10/W11. *Integrar isto é integrar uma melhoria medida e um resíduo declarado.* ⚠️ **E o resíduo FECHOU — a nota acima descreve 04/08 e sobreviveu ao fato:** a **W11b** o cortou 5× (0,1644 → 0,0331) fatiando o cancelamento da gravidade entre os sub-passos, e a **W11c** pôs o amortecimento no TETO, onde ele é **0,0000 m EXATO** — e não num cruzamento calibrado a 30°: a varredura por inclinação dá zero de 20° a 45° e em todo número de sub-passos, com as outras colunas a colapsar na lei `deriva(10 s) = 0,153 · sen θ · (1 − d)`. O preço do teto é o que mudou de produto: a perna transmite **91%** do peso (era 53% antes da W11b). Re-medido pela sonda do produto em 2026-08-09. **⬛ A CAUDA DO PLAYER + O PLANO 07 + O SEGUNDO MODO — `line/physics` INTEGROU (2026-08-08, 47 commits, 148 arquivos, +23.072/−1.227; handoff MESTRE [`docs/Physics/handoffs/HANDOFF_INTEGRACAO_line_physics_MESTRE_2026-08-08.md`](../../Physics/handoffs/HANDOFF_INTEGRACAO_line_physics_MESTRE_2026-08-08.md), que **supersede** o de 05/08 — ⚠️ *o detalhe de mecanismo das waves até a W23 continua LÁ e não foi copiado*):** quatro blocos. **(A)** a cauda do player dinâmico (W11b·W11c·W12..W23). **(B)** os quatro últimos itens da §8 (**W24·W25·W26·W27**). **(C)** o plano 07, os três reports do dinâmico (**W-Water · W-Submerged · W-ClingPull · W-Landing**). **(D)** o **SEGUNDO MODO** (**W-KinMove**). ⚠️ **A ORDEM entre C e D é load-bearing e está escrita no plano 07:** a cura do pouso (W-Landing) é uma mola mais RÍGIDA, e uma mola mais rígida **amplifica** a puxada que a W-ClingPull conserta — consertar o pouso primeiro pioraria a jangada no mesmo commit; os commits já estão nessa ordem, e **um rebase que os reordene quebra a premissa**. **`PROJECT_SCHEMA` 55→60 na linha, CONTADO para 64→68** (cinco degraus: W13 `wall_slide_speed` · W14 `dash_speed` · W15 `crouch_height` · W17 o campo de ARQUIVO `player_tape` · W23 `wall_grab_stamina`) — a `line/Vector` (v56..v62) e a `line/sculpt3d` (v63) pousaram antes na mesma janela. ⚠️ **E o degrau v67 é o único de ARQUIVO da linha:** a fita do jogador (o que o dedo fez tique a tique), que fecha o último item aberto do §4 do plano 06 — o **bake da W16** é o que a torna útil (reabrir um projeto e apertar Bake devolve a corrida de ontem), e ela pesa **28,1 kB por 60 s** de corrida, medido. ⚠️ **O botão novo da W23 (`PlayerInput::grab`) NÃO move o formato da fita** — ela guarda os botões num **BITMASK** (`(f32, u8)`), e um bit livre não muda um byte do postcard; um campo novo na tupla teria custado o bump por si só e recusado todo arquivo já salvo. Registro `ph2d-physics-ecs` **28→29** (`PlayerMode`) · registro `ph2d-ecs` (as **três** casas) **INTOCADO** · gizmo ids **nenhum novo** (próximo livre segue **974**) · **nenhum ADR** ⇒ a linha ficou **fora de toda disputa de número** · contrato congelado **4/4** (rodado) · **ZERO `Cargo.toml`/`Cargo.lock`** — nenhuma crate nova, nenhuma dep nova · `physics_ecs_c9` **`dd5230d7…`, 108 corpos, debug ≡ release** (⚠️ o hash que esta §5 registrava como `b3dbe792…` era o da jornada de 04/08 e **sobreviveu ao fato** por uma janela; a fonte é o binário, não esta linha) · `ph2d-platformer` **133/133**. **⬛ O MODO CINEMÁTICO GANHOU O QUE LHE FALTAVA — e a última medição é a ÁGUA — `line/physics` INTEGROU (2026-08-10, 22 commits, 68 arquivos, +9.849/−414; handoff [`docs/Physics/handoffs/HANDOFF_INTEGRACAO_line_physics_kin_2026-08-09.md`](../../Physics/handoffs/HANDOFF_INTEGRACAO_line_physics_kin_2026-08-09.md)):** sete waves e cinco medições sobre o segundo modo do player, que a `W-KinMove` fez nascer em 08/08. **`W-KinWeight`** — a row de massa era oferecida por **KIND** (`Dynamic`) e passa a ser oferecida por **quem a LÊ**: um corpo cinemático não tem massa para o solver, mas a lei do player a lê ⇒ ⚠️ **é a única mudança de PREDICADO compartilhado da linha**, e é ali que um conflito futuro nasce, não no resto (que é apêndice). **`W-KinPush`** — o personagem cinemático **empurra o que está ao lado dele**, e o caixote **não rodopia** (75 rad → 0,00005 medido). **`W-KinPure`** — o terceiro modo, em que o mundo físico vira **cenário**. **`W-KinCarry`** — a plataforma móvel era contada **DUAS vezes**. **`W-FloatFloor`** — o piso da perna é geométrico: **medido, gateado, e NÃO há defeito** (o resultado honesto de uma sonda é às vezes *não existe bug aqui*). Mais o empurrão lateral que **entra no CENTRO**, a absorção de gravidade que ganhou **PISO** (e devolveu o snap à vida), e o `max_slope` que **volta a significar o que diz**. ⚠️ **E o resíduo de rampa da W11 — que a entrada de 04/08 desta §5 registrava como *"0,164 m por 10 s, NOMEADO e gateado"* — FECHOU e a nota sobreviveu ao fato por uma janela:** a W11b o cortou 5× e a W11c o pôs no TETO, onde ele é **0,0000 m EXATO** de 20° a 45° e em todo número de sub-passos, com as demais colunas a colapsar na lei `deriva(10 s) = 0,153 · sen θ · (1 − d)`; o preço é de produto e está nomeado — a perna transmite **91%** do peso, contra 53% antes. **`PROJECT_SCHEMA` 69→70 — CONTADO, não escolhido:** o degrau é `PlatformPlayer.reaction_push`, e o valor foi conferido contra o `main` do dia **nos DOIS arquivos** (`project.rs` **e** `project_schema_tests.rs`, tripla **`(70, 13, 14)`**) porque ⚠️ **esta colisão passa MUDA quando as duas linhas escrevem o MESMO literal** — o git não sabe o que o número significa. A `line/motion-value`, integrada horas antes na mesma janela, **não o move** ⇒ 70 está certo. ⚠️ **E a escada do `project.rs` já trazia o degrau v70 escrito** — a lição do v69, que nasceu AUSENTE numa integração anterior, foi honrada pela própria linha. **`physics_ecs_c9` `fb27f676…`, 117 corpos, debug ≡ release** (medido pelo integrador na árvore combinada; move porque a linha acrescentou lanes de player) · registro `ph2d-physics-ecs` **fica 29** · registro do `ph2d-ecs` e os **dois** espelhos **intocados** · gizmo ids **nenhum novo** (o último segue **973**, próximo livre **974**) · 4 ids novos, **todos por hash de string** · **nenhum ADR** ⇒ fora de toda disputa de número · **ZERO `Cargo.toml`/`Cargo.lock`**. **Quatro mudanças de comportamento, nomeadas:** ⚠️ **um SENSOR passa a ver corpos cinemáticos** (`ActiveCollisionTypes::all()`, só em sensor) — isto alcança **triggers e zonas**, não só a água, e um gatilho que um player Snap atravessa passa a **disparar** · a razão empuxo/peso **deixou de ser capada em 1** (o consumidor antigo pergunta `> 0` e não muda; quem ler a magnitude vê a razão real) · a absorção do modo Snap pede as **DUAS** respostas (rampas caminháveis ficam **byte-idênticas**; 60° e 80° passam a escorregar) · e a massa por quem LÊ. **Nada pendente de smoke** (as cinco cenas foram aprovadas pelo Enio). Smokes: **`PH2D_PHYSICS_SMOKE=101`** (o modo cinemático — ⚠️ os dois modos **repousam a alturas diferentes de propósito**, 1,400 × 1,057: um paira, o outro pousa) · **`=102`** (o empurrão) · **`=103`** (o modo puro) · **`=104`** (a **ÁGUA** — três cápsulas idênticas, e o **azul tem de acompanhar o âmbar**) · **`=100`** (o dinâmico na água, o **CONTROLE** de que a wave não o regrediu). **Próxima cena livre: `120`** (o `=84` não existe, de propósito) — ⚠️ **este número era `105` e descrevia o `main` de 10/08**, que é onde ele estava certo; as jornadas seguintes desta linha levaram o roteador a **119**, e uma nota que descreve outra árvore é como a próxima cena nasce colidida. **Aberto, com o preço ao lado:** `form_drag` não alcança a lei cinemática (é kernel por-aresta sobre o polígono, não um escalar do meio) · a **FORÇA** de uma zona não leva um personagem cinemático, **por desenho** (ela precisa do frame, do espelho e do falloff, e re-derivá-los numa consulta seria a **segunda resposta** a *"que empurrão esta zona dá neste ponto?"*) · **nadar** é produto, não correção · a paridade de arrasto com o dinâmico é **APROXIMADA** (`(1+d·h)⁻⁴` contra `(1+d·4h)⁻¹` — a mesma classe que a W-AreaDrag mediu em 1,25%; um corpo cinemático não tem sub-passo) · e o player **bobeia ~1,44 m numa poça nos DOIS modos** (1,4357 × 1,4394), que é **anterior** a esta jornada. **⬛ A FILA DA AUDITORIA, A AUDITORIA DA AUDITORIA, E A CAUDA QUE FECHOU O §3.A — `line/physics` INTEGROU (2026-08-15, 117 commits, 224 arquivos, +42.342/−2.336; handoff MESTRE [`docs/Physics/handoffs/HANDOFF_INTEGRACAO_line_physics_MESTRE_2026-08-15.md`](../../Physics/handoffs/HANDOFF_INTEGRACAO_line_physics_MESTRE_2026-08-15.md), que **supersede** o de 08-12 apenas como *o que integrar agora* — ⚠️ *o detalhe de mecanismo das waves anteriores continua LÁ e no de 08-11*):** três partes, e elas **se julgam de formas diferentes**. **(A) A FILA da [auditoria 09](../../Physics/09_auditoria_engines.md), executada na ordem que o [plano 10](../../Physics/10_plano_fila_da_auditoria.md) fixou** — sete waves, **todas smokadas pelo Enio** à medida que fecharam: **`W-PlayerOut`** (`=113`, o jogador **PUBLICA** estado e transições, e o evento vira **SINAL**) · **`W-Brake`** (`=114`, **frear ≠ acelerar**) · **`W-Surface`** (`=115`, a superfície **FALA** com a lei: gelo e esteira) · **`W-Fall`** (`=116`, a queda tem **TETO** — não havia velocidade terminal: **142,57 m/s aos 8 s e a crescer**) · **`W-Launch`** (`=117`, o mundo empurra o personagem, e a explosão passa a alcançar os três modos) · **`W-Leave`** (`=118`, ⚠️ a sonda do Snap fechou **VERDE** e o buraco era outro — a altura autorada era medida **contra a plataforma**) · **`W-Brink`** (`=119`, o `bCanWalkOffLedges`: ele **PARA na quina**, com o alcance **DERIVADO**). ⛔ **E DUAS foram RECUSADAS por medição, o que vale tanto quanto uma construída — é o que impede o item de voltar** (as duas sondas ficam no repo, `--ignored`, imprimem e não afirmam, cada uma com o seu **CONTROLE**): o **air control boost** morre porque *no ápice ele já corre à velocidade de CRUZEIRO* (**5,9999** contra `speed = 6,0`, em toda a varredura) e **8× o `air_acceleration` compra 8,5% de deriva e move a velocidade do ápice em ZERO** — o regime em que o sintoma EXISTE também está medido (abaixo de `air_accel ≈ 10`: 67% / 29% / 15% / 7% do cruzeiro) e **o knob que o cura já está no painel**; ⚠️ *o Unreal precisa dele e nós não porque lá o `AirControl` é uma **FRAÇÃO** da velocidade de caminhada (5% por default) e aqui é uma **aceleração própria** que alcança o cruzeiro em 18 dos 73 tiques de voo* — um `air_control_boost` seria a **segunda porta** para a mesma pergunta. E o **noclip** morre porque **a capacidade já existe pelos gestos que o editor tem**: com o toggle **Physics** desmarcado o artista põe o personagem **dentro** de uma parede sólida (`6,0000 · 4,0000`), **do outro lado** dela (`12,0000 · 0,9000`) e **20 m acima**, exato a quatro casas, e o Play **retoma dali** (deriva lateral **0,0000 m**) — com CONTROLE: empurrado contra a parede **com o relógio a andar** ele **pára em x = 3,8011** contra uma parede que começa em 4,0. ⚠️ **O que a recusa NÃO cobre está dito por inteiro:** voar **com as teclas durante o play** não existe (a MÃO agarra por mola *através do solver*, logo colide). ⚠️ **E a fixture do noclip nasceu ERRADA, com a primeira tabela a MENTIR:** o gesto do toggle desmarcado é **`PhysicsBridge::hold`**, e **não** `dispatch(playing = false, …)` — aquela porta, com o alvo a CRESCER, entra no braço `Greater` e **DÁ PASSO** (o doc dela chama-lhe *"um scrub para a FRENTE enquanto pausado"*, porque o estado da sim é função do TIQUE e não do botão de play). **(B) A AUDITORIA FINAL — três blocos, e o mecanismo é UM.** Lentes independentes convergiram na mesma causa estrutural: o veredito do **`bridge::pose_owner` não alcançava a shell**, que respondia às mesmas quatro perguntas por conta própria a partir do `PlayerMode` — a segunda cópia de uma resposta que aquele módulo existe para dar **uma** vez. ⚠️ **O caso que decide:** um player **ASSADO** (`Kinematic` + `PlatformPlayer`, **sem** `PlayerMode` ⇒ `default()` é `Dynamic`) resolve para `PoseOwner::Scene` — a pose vem de uma curva, o `drive_players` **nem entra no laço**, e **nenhum dos doze cards da §14 é lido por ninguém**; o painel pintava os doze como se corresse. ⚠️ **O `pose_owner_tests` já PINAVA esse fato desde a W-KinMove — ninguém tinha ligado os dois lados.** Agora `PlayerLiveness` nasce **ao lado da lei**, cada campo sendo a condição literal de um `if` do `drive_players`, e a shell **LÊ** em vez de re-derivar. ⚠️ **A `Cling Distance` FICA sob Snap, e é por isso que o card LEG não se esconde inteiro** — ali ela é o `snap_distance` **e** o `step_height` do controlador, o número mais vivo da seção, e a cura preguiçosa (esconder o card) o levaria junto. ⚠️ **O `Remove Platform Player` era um BECO SEM SAÍDA:** o gesto do modo escreve DUAS metades (o `PlayerMode` **e** o `RigidBody.kind`) e o Remove desfazia UMA — sobrava um corpo `Kinematic` sem player, o estado que a §14 **não oferece**, com o corpo a **deixar de cair em silêncio** e um `PlayerMode` órfão a viajar no arquivo. ⚠️ **E o teto da rigidez é o §0 a morder em casa:** a linha do registro dizia *"sem teto medido"* — **verdade no dia em que foi escrita** —, e a `W-Landing` (07/08) mediu o teto em `1/dt²` = 3600 e pôs o clamp na LEI **sem ninguém reconferir a nota**; a correção achou um **segundo** fato que ninguém procurava: *a tabela de faixas do painel é a segunda cópia dos defaults da lei*, e já divergira em dois campos (rigidez **400 contra 2000**, amortecimento **0,5 contra 1,0**), invisível porque o `sync_physics` sobrescreve o store ao selecionar. ⚠️ **E um número que a auditoria reportou NÃO reproduziu, e fica registado:** ela dizia *"seis controles da §14 sem tooltip"*; medido, a §14 pinta **70** ids próprios, a varredura cobria **57**, e os **três** sem dica são todos **chrome do painel** — *nenhum controle está descoberto*, o defeito era o **gate ser cego a 15 dos 70**. **(C) A CAUDA — cinco waves, e a auditoria 09 sai de NOVE ❌ para QUATRO:** `W-WallNormal` (a normal da parede entra no readout, e **o TETO virou uma recusa MEDIDA** em vez de um item aberto) · `W-Ceiling` (`is_on_ceiling`, com margem que **se auto-silencia**) · **`W-Bonked`** (o espelho do `Landed` na outra ponta — ⚠️ **ABSOLUTA e não relativa**, porque o bit `ceiling` só existe com o personagem **NO AR**, onde a velocidade de chão publicada é `[0,0]`, logo a forma relativa do `Landed` **já reduz** à absoluta ali) · `W-HitNormal` (o contato ganha **ORIENTAÇÃO**) · `W-WallMaterial` (**esta superfície não é parede**, por CORPO/PEÇA). ⚠️ **E três achados da cauda não são sobre o diff.** **(1) Uma defesa que nenhum gate consegue ver não é uma defesa:** o rapier **não ordena o par**, então a normal publicada leva um flip de sinal quando os handles vêm trocados — o ramo dispara **976 vezes** na suíte e **nenhuma cena o distingue** (ele nunca vence o teste de profundidade); a cura não foi caçar fixture, foi **extrair `published_normal` como função pura** com gate próprio. *Um ramo alcançável que nenhum oráculo separa é código sem cobertura, por mais verde que a suíte esteja.* **(2) O oráculo é a PROPRIEDADE, nunca o EIXO:** o primeiro gate da normal **reprovou produto correto** — ele exigia alinhamento com o eixo dominante da separação de centros, e uma cápsula alta contra a face de um caixote baixo tem centros separados em `y` (0,586) com normal **horizontal** (`[−0,997, 0,079]`); *direção de centro não é normal de superfície*, e o que a geometria garante é o **semiespaço** (`n · (b−a) > 0`). **(3) Duas metades do mesmo item tiveram vereditos OPOSTOS, e medir decidiu:** o `platform_floor_layers` (*o que me CARREGA*) **já era exprimível** — uma plataforma horizontal leva `0,99×` com tração cheia e **`0,00×` com `WalkSurface { grip: 0 }`**, nos dois modos, e a sonda **já estava no repo** —, só o `platform_wall_layers` (*o que me SEGURA*) era gap real. ⚠️ **E o filtro cai no SENSOR, não na lei:** uma superfície marcada some do array de acertos **como se o raio não tivesse batido**, então os três verbos (deslizar, agarrar-se, pular) somem juntos e o `cling` **não aprende o conceito** — a mesma divisão que a matriz de colisão já faz. ⚠️ **`NoWallCling` é MARCADOR** (presença = booleano) ⇒ **zero `PROJECT_SCHEMA`**: um campo apendado a um struct serializado é postcard **posicional** e teria bumpado. ⚠️ **E o CONTROLE reprovou a fixture DUAS vezes**, que é o padrão desta casa a funcionar: mandar o personagem **escalar** deu controle 1,389 m (ele não escala — um pulo de parede empurra para LONGE, e o gate da peça passava **por vácuo**); re-aimado para a **descida**, controle 1,499 contra marcado 0,151 — **ao contrário**, porque encostado à parede o atrito de Coulomb segura um corpo dinâmico quase parado nos dois casos. O oráculo honesto é **a amostra existir**: **119 de 120 tiques agarrado contra 0** — e ⚠️ **o gate afirma `> 30` contra `== 0`, não o 119**, porque a contagem exacta é afinação do solver e a PROPRIEDADE é *agarra* contra *não agarra*. **A superfície de colisão, MEDIDA pelo integrador na árvore combinada:** `PROJECT_SCHEMA` **70 → 82** (doze degraus — **v71** o nado · **v72–v73** os sensores editáveis · **v74** o pulo do ar · **v75** a beirada · **v76** o planeio · **v77–v78** o sensor da beirada, ⚠️ *o v78 existe porque `reach_y` é TAMANHO e não POSIÇÃO, e o v77 tinha mapeado os dois no mesmo número* · **v79** o freio · **v80** o teto de queda · **v81** o que a plataforma dá ao pulo · **v82** a trava de beirada; **todos nascem NEUTROS**, e o bump é pelo caminho **INVERSO**), tripla **`(82, 13, 14)`** · registro `ph2d-physics-ecs` **29 → 32** (`PlayerSignals` · `WalkSurface` · `NoWallCling`) · registro do `ph2d-ecs` e os **dois** espelhos **INTOCADOS** · gizmo ids **nenhum novo** (o último segue **973**, próximo livre **974**) · **41 ids novos, TODOS `hash_node_id`** ⇒ zero literais numéricos no diff daquele diretório, logo **nenhum valor para o integrador grepar contra outra linha** · **nenhum ADR** ⇒ a linha ficou **FORA de toda disputa de número** · **ZERO `Cargo.toml`/`Cargo.lock`** · `ph2d-i18n` **INTOCADO** · contrato congelado **3/3 + 4/4 + 11/11** · cenas de smoke **105 → 119** (⚠️ **próxima livre: 120**, e o `CLAUDE.md` dizia 105 — a própria linha corrigiu) · **`physics_ecs_c9` `2d7f9d51…`, 121 corpos, 120 passos, debug ≡ release** (⚠️ ele **MOVE** contra o `main` por **DUAS** causas distintas e as duas são medidas: a **CONTAGEM** sobe pela lane **PAREADA** da `W-WallMaterial` — uma parede marcada e uma não, porque uma lane só cobriria a rota do filtro e não a do sensor que segue a passar, e ela **DISCRIMINA**: tirar o filtro leva o hash a `9dbb8bc2…` —, e o **HASH dos 117 antigos** já movia antes dela pela **LEI do player**, cujos defaults mudaram, o mais visível sendo o **leque de pés**, que nasce com **três** raios contra o raio único do `main`). ⚠️ **E o ponto de merge mais sensível NÃO é um número — é um CORTE, e ele decide as três linhas que faltam:** esta linha **PARTIU o `project.rs`**. A escada inteira **e a constante** saíram para o irmão **NOVO** `shells/desktop/src/project_schema.rs` (498 linhas), e o `project.rs` perdeu 374 linhas de doc-header. *Uma linha que acrescente um degrau **dentro** do `project.rs` funde **LIMPA** contra um arquivo de onde a escada saiu* — o modo de falha exacto que o corte de 04/08 produziu na `line/Vector` [[feedback_clean_text_merge_can_be_semantically_broken]]. **São TRÊS sítios a conferir, nunca um:** o literal (`project_schema.rs`), **a escada ao lado dele**, e a **tripla** do `project_schema_tests.rs` — e ⚠️ **esta colisão passa MUDA quando duas linhas escrevem o MESMO literal**, porque o git não sabe o que o número significa. **Onze mudanças de comportamento, nomeadas** (as sete da fila + as quatro da cauda) — ⚠️ **a única de SUPERFÍCIE PÚBLICA é a `W-HitNormal`:** `ContactReport` · `PeakSample` · `BodyContact` · `ContactEvent` ganharam **`normal: [f32; 2]`**, então quem os constrói por literal precisa do campo; **o solver não o lê** ⇒ é READOUT, e é por isso que o c9 é indiferente a ele. **A INTEGRAÇÃO — o que ela mediu e o que ela achou.** ⚠️ **A §1 do handoff envelheceu entre o fechamento e a ordem**, e o próprio doc avisava que aquela caixa envelhece: ele mediu *"o `main` não se mexeu desde o fork"* e, quando a ordem chegou, a `line/Painter` já o tinha movido **85 commits** ⇒ **rebase obrigatório**, não o fast-forward trivial que ele descrevia. O rebase de **117 commits** saiu com **ZERO conflitos**, e a razão é medível: a interseção *arquivos da linha ∩ arquivos que o `main` moveu* é de **QUATRO** (`CLAUDE.md` · `app_state.rs` · `main.rs` · `render_loop/mod.rs`), todos **acréscimos puros dos dois lados**. Gate da árvore combinada: `check --workspace --all-targets` **EXIT 0** · **14.935/14.935 testes, 1.368 skipped** (a suíte INTEIRA, incluindo os arch-gates de `shells/desktop/tests/` que só correm na varredura impactada) · clippy `--workspace --all-targets` **EXIT 0, zero warnings** · codegen sync **no-op** · c9 **debug ≡ release**. ⚠️ **Crates de GPU: diff VAZIO** (`ph2d-render` · `ph2d-flip-render` · `ph2d-paint-gpu` · `ph2d-mesh-render` · `ph2d-gpu-cook`) ⇒ os gates de adapter foram verificados no `main` horas antes e esta linha não os alcança. ⚠️ **E a PRIMEIRA corrida da suíte teve UMA falha que NÃO era desta linha, exonerada por TRÊS testemunhas em vez de por opinião:** o `the_cost_of_a_gated_stroke_follows_the_footprint_not_the_canvas` (do **Painter**) mediu **1,49 ms @1024² contra 2,60 @2048²** — a crate `ph2d-tool-painter` tem **diff VAZIO** contra o `main`, o gate **passa isolado**, e a **segunda** corrida da suíte deu **14.935/14.935**. A máquina tinha `rustc` de outras linhas a **399% e 257%**, com load **20-31**: *nenhuma leitura de relógio desta workstation significa coisa nenhuma acima de `load ~5`*, e a linha `poca:`/o load são o detector. **Nada pendente de smoke** — as sete waves da fila foram aprovadas pelo Enio à medida que fecharam, as duas recusas **não têm o que smokar** (elas são medição), e a auditoria final mais a cauda têm o argumento escrito no §2d do handoff: das catorze correções, as que mudam o produto são **bugs a SUMIR** (três controles que desaparecem onde já eram inertes, um botão que desaparece onde a lei apagava o efeito dele, um gesto que passa a **devolver o corpo** em vez de o prender, dois clamps e uma faixa de slider), e o oráculo de cada um já é um gate com mutação provada — pergunta mais afiada que a que o olho faz ali. ⚠️ **O que a cauda NÃO cobre, e fica nomeado:** **nenhuma cena shipada demonstra o `player.bonked`** — o percurso do `physics_smoke_out` não tem teto, e acrescentar um teto a um percurso **já smokado** é decisão do Enio. Smokes: **`env PH2D_PHYSICS_SMOKE=113..119 cargo run -p ph2d-host-desktop --release`** (⚠️ **`=84` não existe, de propósito**; e ⚠️ **o número da próxima cena se CONTA lendo o `match`** em `shells/desktop/src/physics_smoke.rs`, **nunca uma nota** — a desta §5 já envelheceu em onze cenas uma vez). **Aberto, com o preço ao lado:** o campo de ATRAÇÃO ainda não alcança um player de pose própria (ele é **sustentado**, não um evento, e pede um canal **por-tique**, não a porta do `W-Launch`) · o `bXYOverride` do Unreal (substituir em vez de somar no empurrão) entra quando houver quem o peça · a trava de beirada **não tem gesto de canvas** (é autorada por chip na §14, como as irmãs; um marcador visual da quina seria overlay, e o overlay do player já tem dono) · o `player_liveness` cai no recuo do `RigidBody` **autorado** quando a ponte ainda não construiu o corpo (deliberado, e no doc-comment: sem ele a §14 **piscaria inerte por um quadro**, no gesto que a acabou de criar) · e ⚠️ **restam QUATRO ❌ na auditoria 09 e NENHUM é trabalho pendente** — `AirControlBoostMultiplier` e `MOVE_Flying` fecharam **por medição** (acima), `MovementMode`+`MOVE_Custom` é **ARQUITETURA** com a recomendação escrita de *não agora*, e Root motion está fora da fila sem pedido; *um ❌ que significa «recusado com motivo» e um ❌ que significa «ninguém fez» leem igual numa tabela, e é por isso que cada um tem a seção ao lado*. O único buraco REAL contra o referencial **não é da auditoria 09**: *obstacle actions: climbing*, que o plano 08 §4.8 já nomeia.
