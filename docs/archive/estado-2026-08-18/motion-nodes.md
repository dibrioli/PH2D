# Motion Nodes — estado do §5 em 2026-08-18

> **Arquivo histórico.** Este é o texto do `CLAUDE.md` §5 **verbatim**, como estava em
> 2026-08-18, antes de o §5 voltar a ser um roteador. O **estado vivo** (o que está aberto,
> o handoff corrente) fica no [`CLAUDE.md`](../../../CLAUDE.md) §5; o **mecanismo** fica nos
> handoffs que este texto cita. Nada aqui foi editado — só recortado.

---

- **Motion Nodes** ([`docs/Motion Nodes/`](docs/Motion%20Nodes/) + [SKILL §11.13](SKILL_Stack_PH2D_Definitiva.md), landou 2026-07-09): dinâmica declarativa sobre `ph2d-nodegraph`, avaliador `ph2d-eval-motion`, painéis `motion-graph`/`motion-params`. **Semântica Houdini:** as forças (`force.attractor`/`vortex`/`wind`/`drag`/`curl`) são nós **Pure** que acumulam na coluna transiente `accel`; **UM** integrador aplica (`motion.integrate`, Euler semi-implícito) — mais `motion.spring`. `motion.emitter` é **stateless** (o conjunto vivo é função pura do playhead; aleatoriedade por `hash(seed, id, lane)`), `force.curl` é divergence-free (Bridson). **Sem fio de retorno no grafo:** o laço de simulação é plumbing do editor e aparece como **badge de portal (⊙)** no socket; `Cook::cook_scoped` + `motion.time_remap` (`TimeMap`/`TimeMode` Scale/Loop/PingPong/Freeze/Reverse) dão escopos de tempo por sub-árvore. Também `motion.trail` (eco/rastro) e `WHITE_TILE_KEY` no átlas (sem tile branco reservado o gradiente sai tingido pela tile 0). **Contrato de nós NÃO foi tocado** — `cook.rs` ganhou métodos inerentes + o módulo irmão `time.rs`; `NodeOp`/`OpResolver`/`NodeManifest` intactos. **M2.N2 FECHOU** (`Cook::checkpoint`/`restore` + `CheckpointRing` no pump + `advance_or_scrub_scoped` → scrub para trás bit-exato, GGPO save/load/advance; doc 11). **Linha `line/motion-value` integrada (2026-07-12):** +33 crates-nó (domínio de **VALOR** `value.*`/`pulse.*` · M3 deformers/distribuições · **simulação** boids/verlet/soft-body/collide · cauda M1 cor/streams/adapters) → **67 nós**; e o **canal de TEXT PARAM** (`Graph::set_text_param` + `EvalCtx::text_param` + record `x` / header `v2` no formato textual) que deu a **`motion.expression`** (parser VEX-lite → `ph2d-expr`) **sem tocar o contrato congelado** (params vivem no `Graph`, não no `NodeManifest`) — é o **padrão canônico para param não-f32**, e supersede o plano M4.N1 (bumpar o `NodeManifest`); a fórmula é editável no painel via `ParamWidget::Text`. **Aberto** (handoff: [`docs/Motion%20Nodes/handoffs/HANDOFF_line_motion_value_continuacao_2026-07-12.md`](docs/Motion%20Nodes/handoffs/HANDOFF_line_motion_value_continuacao_2026-07-12.md)): **editor F2/F3** (backdrops/knife/probe/smart-connect ainda são TODO no `interact.rs` — o `[backdrop]` já existe no doc e no formato, falta a UI) · M4 rig+FX · Zona de Simulação (O4) · keyframes (deferidos até a timeline, [memória](project-memory/project_motion_keyframes_deferred_timeline_integration.md)) · **GPU** ([plano](docs/plans/2026-07-gpu-resident-node-pipeline.md) — **linha foundational dedicada**, NUNCA enxertada na linha de fan-out) · `ph2d-timeline::PropKind::Rotation` diz radianos e o app usa graus (enum fechado — decisão do dono da timeline). **⬛ A GPU RESIDENTE — `line/gpu-nodes` INTEGROU (2026-07-21, ADR-0135..0140):** a **grade espacial** na GPU (**ADR-0140**, scan reusável → grade → boids → collide, cenas `=7`/`=8`/`=9`) ⚠️ **o número dela nasceu 0134 e RENUMEROU na integração** — a `line/Painter` escolheu o mesmo 0134 para o Wet Paint na mesma janela, e como os NOMES de arquivo diferem o git **nunca conflitou**; quem chegou ao `main` primeiro fica com o número (gate `architecture_adr_numbers_are_unique`, 3ª vez no repo) · a família **`sim.zone`** (o contêiner de laço é um *passthrough condicional*; recuo parcial — ADR-0135, cena `=10`) · o **voronoi via JFA** (ADR-0139, cena `=11`) · e a fila da **GRANDE AUDITORIA** do Motion Nodes: broadcast misto passa a RECUSAR, starvation dos rings medida, **ADR-0136** (a família que muda CONTAGEM), **ADR-0137** (reforma dos rings), **ADR-0138** (colunas `Arc`). ⚠️ **Todo canal novo é side-metadata no REGISTRY, nunca contrato** — `KernelResolver` ganhou 4 canais com default `None` (`grid`/`state_select`/`stream_op`/`algorithm`), exatamente como o `register_ui`, então `NodeOp=2`/`OpResolver=1`/`NodeManifest=8` seguem intactos. ⚠️ **`Stream.attrs` virou `Arc<Column>`:** `get`/`columns` continuam devolvendo `&Column` (quem só LÊ não muda), mas `clone()` de um `Stream` agora é **refcount, não cópia** — e a soundness apoia-se num fato verificável: **não existe mutação in-place de `Column`** (não há `get_mut`); adicionar um quebra o ADR-0138 **em silêncio**. `CountLawCtx` ganhou `dt: f64` (toda lei de contagem o constrói). ⚠️ **O último item da fila DISSOLVEU na medição:** o `soft_body` **não é XPBD** (é shape matching — reduce→broadcast→map, sem cadeia sequencial) e o cap dele **não era de custo** (1600 partículas = 0,005 ms; 1 milhão = 7,0 ms) ⇒ cap corrigido pela MEDIÇÃO (40→512 por lado) e o port pra GPU **não se justifica**; quem é sequencial de verdade é o `verlet_rope` (Gauss-Seidel por aresta), que **não tem cap**. O censo de cobertura dá **frontier VAZIA**. Rodar: `env PH2D_GPU_COOK_DEMO=<7..11> cargo run -p ph2d-host-desktop --release` (GPU **ON por default**; `PH2D_GPU_COOK=0` volta pra CPU, útil pra bissecar). **Aberto:** cache por-params do cook do voronoi (`relax` animado re-cozinha a relaxação inteira todo frame, 3,02 ms a 20k — era o desenho). **⬛ OS DEFORMERS FORAM PARA A GPU — o 6º canal do resolver, `line/motion-nodes` INTEGROU (2026-07-23, 12 commits, smoke `=16` "O ORGANISMO" aprovado pelo Enio):** a família de deformers cozinha **100% no device**. O primitivo novo é o **REDUCE reusável** (`ph2d-gpu-cook::reduce.rs`, bit-exato, irmão do `scan`), e o padrão que ele destrava é **`reduce → broadcast → map`** — o deformer precisa de um número que só existe depois de olhar TODAS as linhas (um centro, um bbox), e era exatamente isso que o prendia na CPU. Cinco nós portados: `bend`/`twist` (a estreia do canal) · `spherize` (um `Sum` + DUAS reduções num nó só) · `four_point_warp` (4 reduções = o bbox; estreia do `Min`) · `kaleidoscope` (o 1º `StreamOp::SourceRows`, que LÊ a porta-template). ⚠️ **O canal novo é side-metadata no REGISTRY, nunca contrato** — `KernelResolver::reduces()` nasce com default `&[]`, exatamente como os 4 canais que a `line/gpu-nodes` acrescentou em 21/07 e como o `register_ui`; `NodeOp=2`/`OpResolver=1`/`NodeManifest=8` seguem **intactos** (§6 conferido por grep, não por auto-relato). Foundational tocado, todo aditivo: `ph2d-nodegraph` ganhou os módulos-irmãos **`reduce_meta.rs`** (`ReduceOp` `Max`/`Min`/`Sum` com `wgsl_combine`/`wgsl_identity`/`cpu`/`empty`, + `ReduceSpec`) e **`column.rs`** (`ColumnAccess`/`ColumnBinding` extraídos do `gpu.rs`, que estava em 713 > 700 LOC) — e o `ColumnAccess` ganhou o variant **`SourceRead`** (APPEND: leitura de porta-template comprimento-desacoplada, o `StreamOp::SourceRows` do ADR-0136). ⚠️ **As 5 crates-nó entraram em `[dev-dependencies]` da `ph2d-gpu-cook`, NÃO em `[dependencies]`** — são só para o gate de paridade CPU×GPU, o `src/` não as usa ⇒ **machete-safe** (o oposto teria feito o motor de cook depender dos nós que ele coze). Rodar: `env PH2D_GPU_COOK_DEMO=<12..16> cargo run -p ph2d-host-desktop --release` — `=12..15` são as cenas por-deformer (490k+ instâncias cada, fully GPU) e **`=16` é O ORGANISMO**, o grafo que exercita o canal inteiro de uma vez. E o censo de cobertura (`motion_gpu_coverage.rs`) passou a incluir os 4 docs de deformer — ele era **CEGO a eles**, então a frontier que ele reportava vazia não os contava. **⬛ A FAMÍLIA `field.*` + A CURVA COMO PARAM — `line/motion-nodes` INTEGROU (2026-07-25, 24 commits, smokes aprovados; handoff [`docs/Motion Nodes/handoffs/HANDOFF_INTEGRACAO_line_motion_nodes_field_A1_2026-07-25.md`](docs/Motion%20Nodes/handoffs/HANDOFF_INTEGRACAO_line_motion_nodes_field_A1_2026-07-25.md)):** duas frentes. **(A) `field.*` — o falloff que COMPÕE:** 5 nós-crate novos, todos GPU-resident bit-exato — `field.index_range` (banda ordinal por rank) · `field.box` (campo espacial + rotação) · `field.combine` (o composer) · `field.radial_sweep` (radar angular, herda o gizmo) · `field.remap` (o **remapper**, a peça-chave da fatoração) — mais o **gizmo de canvas dos campos espaciais** (`field_gizmo.rs`, espelho do FlipSelection) e o fix do **drift crônico** (o chrome projetava full-window sob a cena na banda do split). **(B) A CURVA como param:** crate nova **`ph2d-curve`** (leaf, dep-free) + `ParamWidget::Curve` + o 5º contour (Curve) do `field.remap` · **editor de curva ARRASTÁVEL** no painel `motion-params` — que foi **painel-only** porque reusou o primitivo pronto `InteractiveState::CurvePoint`, o editor de falloff do Painter · e o **canal de LUT**, que leva a curva ao **DEVICE** (o demo `PH2D_GPU_COOK_DEMO=22` saiu de HYBRID/CPU para **FULLY GPU, 5 stages**). ⚠️ **Todo canal novo é side-metadata no REGISTRY, nunca contrato** (a 4ª vez): `LutSpec` carrega a `fill` como **fn-pointer no crate do NÓ** — o substrato fica agnóstico de curva — e `KernelResolver::luts()` nasce com default `&[]`, irmão exato de `reduces` ⇒ **zero churn nos 32 kernels existentes** e `NodeOp=2`/`OpResolver=1`/`NodeManifest=8` **intactos** (conferido por grep). ⚠️ **O único `pub fn` cross-cutting da linha é `codegen::kernel_module` (6→7 args)**, e as sentinelas que provam que o sequenciador fundiu limpo são `generated_wgsl_validates` (naga sobre TODO kernel) + `motion_gpu_kernel_budgets`. **Zero schema, zero ADR** (a curva é text param, a LUT é derivada) ⇒ esta linha ficou **fora** da disputa de número da jornada. **⬛ O EDITOR DE NÓS GANHOU MÃOS — `line/motion-value` INTEGROU (2026-07-30, 43 commits, smokes aprovados; handoff [`docs/Motion%20Nodes/handoffs/HANDOFF_INTEGRACAO_line_motion_value_2026-07-30.md`](docs/Motion%20Nodes/handoffs/HANDOFF_INTEGRACAO_line_motion_value_2026-07-30.md)):** uma jornada inteira de USABILIDADE do grafo. **Adapter automático** (arrastar um fio entre portas incompatíveis INSERE o conversor, em vez de recusar) · **`value.attribute` vira PICKER DE CANAIS** (palavras em vez de jargão) e o column picker lê os canais VIVOS do stream, **GPU-cook-aware** (achava vazio quando o grafo cozinha na GPU) · o **editor de GRADIENTE multi-stop** no painel de params (barra + stops arrastáveis + picker OKLCH; [doc 85](docs/Motion%20Nodes/85_gradient_editor_nota_adr.md)) · **sockets com FORMA por dimensionalidade** (◇ coluna / ○ valor) e link ilegal em VERMELHO · **Arrange** + **Frame Selected** · o **SPLICE** (soltar um nó SOBRE um fio o INSERE na cadeia; R-click num fio insere pelo menu; **delete-and-reconnect**, o Ctrl+X do Blender) · **snap magnético** (~22 px) com o fio-fantasma PULANDO para o socket alvo · e o **BYPASS/MUTE (H)** — o *disable* do Blender/Nuke: o cook passa `input[0] → output[0]` e os demais outputs vão `Empty` (convenção Houdini). ⚠️ **O bypass é SEMÂNTICO** (muda o resultado cozido) ⇒ entra no **fingerprint** do cook e na seção semântica do formato textual (**record `y`, header v5**; `from_text` aceita v1..v5, e um grafo que ninguém mutou é **byte-idêntico a v1**); ele mora num **`BTreeSet` paralelo** no `Graph`, não num campo do `NodeInstance` — o padrão append-only do `node_text_params`, que é o que deixa uma linha nova estender o grafo sem colidir num sítio de construção. ⛔ **REVERTIDO na própria linha, não integra:** o **Post Stack** (grade HDR de tela inteira) foi construído e revertido inteiro — o smoke reprovou —, então o diff cumulativo tem **ZERO ADR novo** e zero mudança em `ph2d-render`; se o Enio o reviver, o número se re-conta contra o `main` do dia. **Nenhum bump de schema** (`PROJECT_SCHEMA`/`VEC_SCENE`/`DOC_VERSION` intactos — o grafo viaja como TEXTO e carrega a própria versão) e **contrato congelado INTACTO** (`NodeOp=2`/`OpResolver=1`/`NodeManifest=8`): todo canal novo é side-metadata ou text param, **nunca o manifest**. ⚠️ **E o `motion.color_ramp` ficou com `params: &[]`** — o gradiente inteiro migrou para o text param `ramp` —, o que na integração derrubou **três demos** que ainda o semeavam por `set_param("preset"/"interp")`: params que o manifesto não declara mais fazem o `validate` **recusar o grafo inteiro**, e quem pegou foi o `gpu_coverage_census` na árvore combinada (o controle rodava VERDE no `main`). Corrigidos preservando a intenção autorada (Rainbow+Ease nos dois demos GPU, Ice no voronoi). Smokes (`--release`): **`PH2D_SPLICE_SMOKE=1`** (o guarda-chuva: splice + snap + replace + Ctrl+A/L + box-subtract + **o BYPASS**) · **`PH2D_ADAPTER_SMOKE=1`** · **`PH2D_ATTR_SMOKE=1`** · **`PH2D_PICKER_SMOKE=1`** · **`PH2D_GRADIENT_SMOKE=1`**. **Aberto:** mutar um **grupo** (card de subgrafo) inteiro é decisão de design — hoje o card de grupo nasce não-mutável, de propósito. **⬛ O EDITOR DE NÓS GANHOU O CONJUNTO COMPLETO DE IDIOMAS — `line/motion-value` INTEGROU (2026-08-01, 10 commits, QoL puro; handoff [`docs/Motion Nodes/handoffs/HANDOFF_INTEGRACAO_line_motion_value_graph_qol_2026-08-01.md`](docs/Motion%20Nodes/handoffs/HANDOFF_INTEGRACAO_line_motion_value_graph_qol_2026-08-01.md)):** **clipboard** (Ctrl+C / Ctrl+V / **Ctrl+X**, e o **paste de um grupo cola COMO grupo** — o nesting sobrevive) · **Select Inverse** (Ctrl+I, que fecha a família de seleção ao lado de All/Linked/box) · os **menus de contexto de TODO sujeito de R-click** (nó · card de grupo, com Enter + Ungroup · backdrop, que ganhou Rename + Delete e não só as cores) · e **clicar num fio o SELECIONA, com Delete removendo**. ⚠️ **Cada verbo passa por UMA porta (`apply_key`)**, então o menu **nunca** faz algo diferente do atalho — é o que impede o par menu/atalho de divergir no dia em que um deles ganha um caso especial. ⚠️ **E um double-dispatch que era inócuo deixou de ser:** o Ctrl+V colava **DUAS vezes**, porque o dispatch duplicado nunca tinha importado enquanto todos os verbos eram idempotentes (`dedup_double_dispatch`). Toque foundational **aditivo**: `GraphKey` ganhou `Cut` e `SelectInvert` (variants apendados) + `KEY_KEY_I` no keymap — ⚠️ **`GraphKey` NÃO é contrato congelado** (a §6 congela `NodeOp`/`OpResolver`/`NodeManifest` e `Tool`/…; este é enum interno de interação e evolui livre), e **não há gate que pine a contagem dele**. **Nenhum schema, nenhum contrato congelado, nenhum ADR, nenhuma dep nova.** ⚠️ **A linha shipou uma dívida de LOC vermelho-latente que só a árvore combinada viu:** os dois commits de clipboard levaram `render_loop/motion_bridge_edit_tests.rs` de 353 para **615 > 600** — e **não foi colisão de integração** (615 medido nas DUAS árvores, antes e depois do rebase): os gates de `shells/desktop/tests/` **só correm na varredura impactada**, e um fechamento por `cargo test -p` por crate não os alcança, a mesma causa estrutural que a `line/Vector` documentou em 23/07. Cortado por **assunto** pelo integrador (o pai fica com os verbos que EDITAM o grafo no lugar; o filho novo `motion_bridge_clipboard_tests.rs` com o que sai do documento e volta), **FILHO e não irmão** para o `scene()` da fixture continuar sendo uma porta. **⬛ O GRAFO GANHOU UM BUSCADOR, E OS OBJETOS DA ENGINE ENTRARAM NELE — `line/motion-value` INTEGROU (2026-08-02, 22 commits; handoff [`docs/Motion%20Nodes/handoffs/HANDOFF_INTEGRACAO_line_motion_value_2026-08-02.md`](docs/Motion%20Nodes/handoffs/HANDOFF_INTEGRACAO_line_motion_value_2026-08-02.md)):** três clusters. **(1) O fio vira CONJUNTO** (Shift acumula, Delete solta o feixe, realce próprio distinto do hover) e **`H` num card de grupo BYPASSA o grupo como unidade** — o cook RE-LIGA a fronteira do grupo bypassado. ⚠️ Isso **derruba a cerca de Chesterton** *"card de grupo nasce não-mutável"* que esta §5 registrava como decisão de design, e o estado mora no record **`yg`** (`MotionDoc.bypassed_subgraphs`), **append-only** no formato TEXTUAL — o gêmeo exato do record `y` do bypass de nó, e a razão de **`PROJECT_SCHEMA` não bumpar**: o grafo viaja como texto e carrega a própria versão. **(2) O COMMAND-PALETTE de tela cheia** (tecla **A**) — o *node picker* que **substitui o dropdown de Add Node em TODOS os casos**: campo de busca, cada categoria vira um CARD, categorias grandes viram FAIXAS full-width, masonry balanceado. Superfície nova em `ph2d-editor-core`, toda **aditiva** (`widget/command_palette` + o chrome + a máquina de estado + `CMD_PALETTE_CLOSE`, id por **hash de string**, logo sem gate de contagem). **(3) DOC 86 — os objetos da engine no grafo** ([plano 86](docs/Motion%20Nodes/86_plano_objetos_engine_render_e_preview.md)): o preview sai de dentro do card para uma **MOLDURA própria** · a coluna **`texture_id`** (⚠️ **CONVENÇÃO de stream**, como `uv_rect`/`P`, lida pelo `lower_to_instances` com **fallback 0 ⇒ byte-idêntico** — *não* é campo do `NodeManifest`) · os nós **`source.object`** (media-agnóstico) e **`motion.duplicator`** (Shape × Points) · **bake-to-tile de VETOR e de FLIP** (cache por conteúdo, câmera fixa por DPI) · um **GRUPO de mídia mista vira N instâncias VIVAS** · o preview do Spawn **para de PISCAR** (a moldura existe pelo **TIPO** do output, não pelo conteúdo do frame) · e o preview vira o **thumbnail assado** do objeto. **2 crates-nó novas** (`ph2d-node-source-object` · `ph2d-node-motion-duplicator`), **glob members** ⇒ zero edição de `Cargo.toml` central. **Contrato congelado INTACTO** (`NodeOp=2`/`OpResolver=1`/`NodeManifest=8`, 3/3), **nenhum ADR**, **nenhum schema**, registro do `ph2d-ecs` não tocado. ⚠️ **E a integração desta linha achou QUATRO vermelho-latentes que nenhum fechamento por `cargo test -p` alcança** — os gates de `ph2d-editor-core/tests/` só correm quando *aquela* crate é impactada, e **o `nextest` do gate CANCELA na primeira falha**, então dois deles nunca chegaram a rodar: o `pub mod command_palette;` estava escrito **à mão dentro do bloco que o `ph2d-widget-sync` GERA** (o `pub` está certo — os chamadores alcançam sub-caminhos direto —, o que faltava era registrá-lo no `PUB_MODULE_OVERRIDE`, onde o bloco nasce; *editar a SAÍDA de um gerador é o que deixa o gate de staleness vermelho*) · `command_palette.rs` em **661 > 500** (split por responsabilidade: a metade que **MEDE** saiu para `command_palette/layout.rs`, o pai fica com o que **DESENHA**) · `command_palette_tests.rs` era o **PRIMEIRO `*_tests.rs` solto em `src/widget/`** e o gerador varre `*.rs` do topo ⇒ **ele virava um "widget"** sem showcase, sem a11y e sem opt-out (movido para `command_palette/tests.rs`, o precedente exato do `tool_rail/tests.rs`: o dir e o arquivo **dedupam** para um `mod` só) · e `no_magic_numeric` + `no_tofu_glyphs`. ⚠️ **O `rustfmt` reflowou a 1ª correção dos literais para FORA da linha** — o marcador `LITERAL-PX-OK` tem de estar **NA** linha, então a contagem de colunas virou `const`, que é a convenção do arquivo pai. **Aberto:** mutar um grupo pelo CARD já existe; o que segue em aberto é o resto da lista do handoff. **⬛ AS FORMAS VIVAS NA GPU E O GRAFO QUE SE DIAGNOSTICA — `line/motion-value` INTEGROU (2026-08-04, 23 commits; ADR-0154/0155, handoff MESTRE [`docs/Motion Nodes/handoffs/HANDOFF_INTEGRACAO_line_motion_value_MESTRE_2026-08-04.md`](docs/Motion%20Nodes/handoffs/HANDOFF_INTEGRACAO_line_motion_value_MESTRE_2026-08-04.md)):** três frentes. **(A)** o item 3 do doc 86 (filho vetor/flip de grupo SEM NOME carimba pelo drawing id). **(B) [ADR-0154](docs/architecture/decisions/0154-motion-shapes-are-live-gpu-vector-not-baked-tiles.md) — `source.shape`:** formas vetoriais **VIVAS**, crate nova `ph2d-node-motion-shape`; `geometry_id` é **CONVENÇÃO DE STREAM** (o gêmeo exato do `texture_id`), ausente ⇒ byte-idêntico. **(C) [ADR-0155](docs/architecture/decisions/0155-motion-graph-setup-is-diagnosed-and-healed-not-refused.md) — o setup é DIAGNOSTICADO e CURADO, não recusado:** o grafo tem uma classe de erro que **não produz erro** (uma `force.*` ligada à saída SEM integrador escreve `accel`, nada consome, e a cena fica estática — o `validate` só confere tipos de porta). Crate nova **`ph2d-motion-diagnose`**: `diagnose(graph, reg)` **DERIVA** os papéis produce/consume da **própria declaração do nó** (a binding de GPU + o side-channel `Coupling`), nunca de uma tabela paralela à mão — auto-cura no gesto, reorder reusando o integrador que já existe, ou **badge ⚠ + quick-fix** que **EXPLICA e seleciona** onde a escolha é criativa (*nunca adivinha*). ⚠️ **Mudança de comportamento, correctness > speed:** um documento com fonte de APARÊNCIA (Shape/Object) **RECUSA o cook GPU** — o lowering é sprite-only e hardcoda `texture_id = 0`, então as estrelas saíam como **quads brancos do atlas** enquanto o `vector_instances` da CPU ficava obsoleto (*dois produtores lendo dados diferentes*); o trade — grafos com objeto/forma perdem a aceleração — está NOMEADO. ⚠️ **E o `MissingSource("P")` tinha FALSO POSITIVO no Boids**, cujo doc afirmava *"zero false positives"*: uma **fonte COM ESTADO** lê o próprio `P` do frame anterior por aresta **DELAYED** e semeia a nuvem sozinha ⇒ isenção `seeds_own_state`, e *a afirmação era o próprio bug*. **Zero schema** (`PROJECT_SCHEMA` intocado — o `project.rs` não é tocado) · contrato congelado **3/3** (os três canais novos do `NodeRegistry` são **side-metadata**, o padrão de `param_gates`/`reduces`/`luts`) · **zero dep externa**. ⚠️ **Os ADRs 0154/0155 ficaram como escolhidos** — nenhuma outra linha da janela criou ADR. Smokes: **`PH2D_AUTOFIX_SMOKE=1..7`** · **`PH2D_SHAPE_SMOKE=1`/`=2`** (⚠️ *se vir retângulos brancos, PARE*) · **`PH2D_MOTION_OBJ_SMOKE`**. ⚠️ **E a integração achou TRÊS vermelho-latentes no painel `motion-graph`** que nenhum `cargo test -p` alcança (os gates de `ph2d-editor-core/tests/` só correm na varredura impactada, a 3ª vez desta linha): `paint` em **201/200** · dois arquivos novos fora do `PANEL_A11Y_DELEGATE_OK` · um `0.55` sem marcador. Curados por **corte de responsabilidade** (`paint_overlays.rs`, irmão dos cinco que já existem) — e ⚠️ **o 1º corte deixou o helper no próprio `paint.rs` e o levou de 596 a 613**: o gate de **fn** e o de **ARQUIVO** medem coisas diferentes, e um corte que só olha para um empurra o problema de lado. **⬛ OS PARÂMETROS DOS NÓS — a unidade, o piso, as seções, o slider dual e o painel que ROLA — `line/motion-value` INTEGROU (2026-08-08, 63 commits, 205 arquivos, +19.125/−2.877; plano [doc 88](docs/Motion%20Nodes/88_plano_parametros_nos_unidades_e_slider.md), handoff [`docs/Motion%20Nodes/handoffs/HANDOFF_INTEGRACAO_line_motion_value_params_2026-08-07.md`](docs/Motion%20Nodes/handoffs/HANDOFF_INTEGRACAO_line_motion_value_params_2026-08-07.md)):** quatro clusters independentes. **(A) A GPU do `source.object` + o VETOR VIVO** — o objeto **cozinha e renderiza no device**, e um `source.object` de VETOR renderiza *crisp* em vez de virar tile raster (LOD híbrido + cache de tesselação por-frame). ⚠️ Isto **reabre** a recusa que o ADR-0155 instalou em 04/08 (*"um documento com fonte de APARÊNCIA recusa o cook GPU"*): ela continua, mas **só onde ainda é necessária** — o gate `the_gpu_cook_recusal_placement` pina **onde** ela mora e o `gpu_texture_id` prova que o lowering do device escreve o id REAL. **(B) O doc 88** — o vocabulário de **UNIDADE** (`ParamUnit`, *o que o número É*, nunca como se mostra) · a **fronteira de DISPLAY** (o número sai UMA vez na face do artista e volta pela mesma porta) · o **piso duro** por param (`ParamHardMin`, e a assimetria morava em DOIS lugares) · a unidade chega a **43 nós** por varredura de opt-in **com censo que a tranca** · as **SEÇÕES** de params (a parede de treze sliders vira três perguntas, **10 nós**) · o **reset ao default** · o **teto de linhas** do painel (escondia params: 8 contra os 13 do `field.remap`) · e o **oscilador** com régua de tempo, com o WGSL do ruído deixando de existir em três cópias. **(B2) O SLIDER DUAL** — o `ParamUiHint.max` passa a ser **só a faixa confortável do arrasto** e o `ParamHardMax` (side-metadata que já existia no registry) diz **onde o disfuncional começa**: o número que a caixa de texto ainda aceita, com **onze nós de contagem** carregando um teto **MEDIDO** e a tabela ao lado no doc-comment do próprio nó. ⚠️ **A feature NÃO funcionava em lugar nenhum e três gates ficavam VERDES sobre isso:** o espelho chip↔slider re-escrevia o chip com a re-projeção do slider **saturado**, e depois — já com o chip certo — o slider **também** emitia, com o thumb parado no topo, e *o último vencia*; os gates de faixa **escrevem o valor com `set_number_value` e nunca PINTAM**, e a faixa registrada e o link nascem no `paint` ⇒ **a fixture deles não continha o espelho que diziam exercitar**. A cura: a faixa do CHIP é a autoridade, e o evento do slider passa por **porta única** (`push_mirrored_slider_event`) que se cala quando o thumb é um substituto saturado — *dois sítios emitem esse evento hoje, e um `if` copiado é a regra que o terceiro nasce sem*. Ferramenta nova que tornou o gate possível: **`MockPanelHost::type_into_number`** (o testkit só sabia ESCREVER no store, o que pula o caminho de commit inteiro). ⚠️ **E a varredura dos tetos achou mais defeito na SONDA que nos nós:** *um teto digitável não pode passar do que o kernel HONRA* (`lattice` 400, `kaleidoscope` 256 — uma caixa que aceita 5.000 sobre um clamp de 400 **aceita e mente**), e o `motion.boids` media 3,2 ns por agente porque **nunca dava um passo** (o estado dele chega por uma aresta DELAYED que o editor auto-liga e que `Graph::add_node` não faz; com o grafo certo o quadrático aparece — 500 → 0,475 ms · 2.000 → 10,392 · 8.000 → **186,388**). **(B3) O CORPO DO PAINEL ROLA** — `MAX_PARAM_ROWS` respondia a **duas** perguntas com um número (quantos ids o pool tem × quantas linhas cabem na altura); medido, uma linha escalar ocupa **34,0 px** e o dock comporta **24** delas contra um teto de 16 e um pior nó de 15. ⚠️ **O FATO FICA NOMEADO: a rolagem está INERTE hoje** (o teto mora DENTRO do `paint_rows`, então o corpo mede ~544 px contra um dock de 880 e nenhum nó transborda) — ela **remove o dock da lista de razões para o teto não subir**, que é o pré-requisito da varredura. ⚠️ **As QUATRO edições que um painel rolável exige** (o `scrollable_panels_intercept_the_wheel` as nomeia): id do thumb → arm no `scrollbar_panel_for_id` → o painter lê `panel_scroll` e publica `content_h`/`visible_h` → **e o id no `cursor_over_hero_panel`**, que é a que *compila, pinta, arrasta e deixa a roda dando ZOOM na câmera em silêncio*. ⚠️ **E a cerca cuja premissa isto matou foi ENCARADA em vez de deixada verde:** o gate que afirmava *"as linhas CABEM"* pediria um teto **MENOR** no dia em que uma não coubesse — o oposto da cura; virou `the_reported_height_is_the_true_height_of_the_rows`, a propriedade **load-bearing** (o scrollbar deriva `max_scroll` de `content_h − visible_h`, e uma altura saturada convenceria o painel de que tudo cabe, com as linhas desenhadas, o thumb ausente e **a cauda perdida em silêncio**). **(B4) A VARREDURA POR FAMÍLIA COMEÇA, e um CENSO a escolhe** — a §6 do plano dizia *"nó a nó, ou família a família"* e a §0 manda medir antes de decidir ⇒ sonda `param_census` na `ph2d-node-registry-init` (a crate que registra os 118 nós, e o build mais barato que os enxerga): **118 nós · 411 params · 395 com hint · 105 com unidade**; **um** nó tem param sem hint nenhum e **cinquenta** têm ≤ 2 controles. ⚠️ *Magro por natureza* e *magro por omissão* são coisas diferentes e **o censo não as distingue** — quem distingue é a referência, e é disso que a §9 nova do doc 88 é a tabela, com o veredito **recusado-com-motivo** para as famílias VALUE, ESTRUTURAIS e RIG (para ninguém as "completar" depois). **`PROJECT_SCHEMA` 55→56 na linha, CONTADO para 69** — o degrau é as **SETTINGS DO PROJETO** (`SavedSettings`: a escala do mundo, a unidade que o artista lê, os dois snaps do gizmo e o modo de filtragem), FORA do `ProjectState` pela razão dos irmãos `physics`/`motion`/`timeline` (*um Ctrl+Z do canvas não deve rebobinar a escala do mundo*), em **tipo próprio do arquivo** e não no `ProjectSettings` de runtime (a `ph2d-editor-core` não fala serde, e herdar o layout de um tipo de runtime torna um refactor interno numa quebra de save). ⚠️ **E a linha do degrau v69 nasceu AUSENTE da escada do `project.rs`** — a integração renumerou o literal de 56 para 69 e não escreveu a entrada, deixando a escada documentando até v68 sob uma const que dizia 69; **escrita na varredura da §5, no fim da mesma jornada**, porque *uma escada com buraco é como o próximo bump nasce mal-numerado: quem conta o próximo degrau lê a escada, não o literal*. Scrollbar id **841** (`MOTION_PARAMS`; próximo livre **842**) · contrato congelado **3/3** (todo canal novo é side-metadata no registry, nunca o manifest) · **nenhum ADR** · **nenhuma dep externa nova**. **Smokado pelo Enio.** **⬛ A CONFERÊNCIA DOS NÓS — os 118 são medidos contra a referência, e a medição REFUTA o gabarito onde ele erra — `line/motion-value` INTEGROU (2026-08-10, 42 commits, 123 arquivos, +18.616/−2.174; plano [doc 89](docs/Motion%20Nodes/89_plano_conferencia_dos_nos.md) + as 17 folhas de família em [`89_conferencia/`](docs/Motion%20Nodes/89_conferencia/), handoff [`docs/Motion Nodes/handoffs/HANDOFF_INTEGRACAO_line_motion_value_conferencia_2026-08-09.md`](docs/Motion%20Nodes/handoffs/HANDOFF_INTEGRACAO_line_motion_value_conferencia_2026-08-09.md)):** seis blocos. **(A)** o plano e as folhas — params lidos do **`MANIFEST`** (nunca do doc), faixas e unidades lidas do `register_*`, e **toda cadeia de expressibilidade TENTADA contra o catálogo real** em vez de julgada de memória. **(B)** a coluna VETOR é lida pista a pista · um número computado vira a **MÁSCARA** que cinco famílias leem · os três geradores consomem `accel` ⇒ a família `force.*` alcança simulação · e **o teto do boids era o da CPU**. **(C) W3 COR:** o catálogo de formas do editor vira o do grafo (**8 → 43**) e **35 das 43 eram INALCANÇÁVEIS** — o censo do teto de opções. **(D)** a cauda (`motion.morph` descartava o stream inteiro menos `P` · **dois tetos MEDIDOS shipavam INERTES**). **(E) W5** emissão/distribuição, incluindo o BURST. **(F) W6 PULSE** — `pulse.level`, o canal **Falloff** (o 8º do `READ_CHANNELS`) e a cena `=23`. **Medido na árvore COMBINADA, não auto-relatado:** **119 nós · 443 params · 427 com hint · 127 com unidade** · contrato congelado **3/3 + 4/4** · `PROJECT_SCHEMA` **69 INTOCADO** · registro do `ph2d-ecs` **intocado** · **nenhum ADR** ⇒ a linha ficou **fora de toda disputa de número** · `Cargo.lock` com **um** pacote novo, a própria crate (`ph2d-node-pulse-level`, folha drop-in, glob member). ⚠️ **`MAX_ENUM_OPTIONS` 8 → 48** e de `pub(crate)` a `pub` — seis vezes o teto que o `main` trazia, com o `CHANNELS_EXTRA_BASE` passando a ser **DERIVADO** dele. ⚠️ **E é o §0 mordendo em casa:** o doc do `READ_CHANNELS` citava *"seven of them + Custom = 8 = the segmented selector's ceiling"* — **verdade no dia em que foi escrita**, e falsa **dentro desta mesma linha**, na wave das formas, que subiu o teto por outro motivo sem ninguém reconciliar a frase. *Quem move o número que tornava algo inalcançável tem de reconferir a nota.* **A integração conferiu a propagação:** a frase só sobrevive **onde a própria linha a marcou como errada** (`value-attribute/src/lib.rs`), e nenhuma outra linha a copiou (`git grep "segmented selector's ceiling"`). ⚠️ **A família `pulse.*` é CPU-only e o censo agora DIZ isso** (`[demo=23] HYBRID/CPU — boundaries: pulse.counter [no-kernel]`) — **não é omissão a fechar**: um pulso é evento POR LINHA com memória de borda no `pre`, não um mapa por texel; antes desta wave o censo era **cego à família inteira**, a mesma ausência que ele já pagou duas vezes (deformers e vizinhança). ⚠️ **Os `pre` self-loops são escritos à MÃO** — o editor os plumba ao SOLTAR um nó, e um documento montado por `add_node` não os ganha; três nós da cena `=23` dependem disso. ⚠️ **Integrar não é aprovar: as waves E e F NÃO foram smokadas.** Smokes (`--release`): **`PH2D_GPU_COOK_DEMO=23`** (⚠️ **pegue a ferramenta Motion no rail** — o auto-play é edge-triggered na entrada; um losango pisca a cada 0,5 s e **fora dele NADA acontece, nunca**, e *essa metade é a feature*) · **`PH2D_LENS_SMOKE=1`** · **`=17..22`**, que **têm de continuar iguais** (o canal Falloff não as toca). **⬛ O `value.noise` GANHOU UM CATÁLOGO DE KERNELS — e o SIMPLEX foi RECUSADO POR MEDIÇÃO (2026-08-13, `line/motion-value`, cena `=37`, pendente de smoke; folha [15_value](docs/Motion%20Nodes/89_conferencia/15_value.md)):** o P0 da folha 15 pedia um param `type` com Perlin/Simplex/Value/Cellular. ⚠️ **A célula acertou o diagnóstico e errou o NOME, e é o nome que decide o desenho:** o `motion.noise` já gasta `type` na **LEI fractal** (fBm · Turbulence · Ridged, a folha `ph2d-fbm`) e a folha 15 pede o **RUÍDO DE BASE** — duas perguntas ortogonais, e *a mesma palavra a selecionar coisas diferentes em dois nós irmãos é como um menu passa a mentir*; toda referência que tem as duas as separa (Cavalry `Type` + `Octaves/Lacunarity/Gain` · VFX Graph `Noise Type` + os knobs de fractal · Houdini `Noise Type` + `Turbulence`) ⇒ o param é **`kernel`**, rótulo **"Pattern"**. **Três kernels:** `Value` (o que sempre shipou, índice **0**, byte-idêntico) · `Perlin` (gradiente 2002 — o campo é exactamente **zero em cada ponto da grade**, medido: pico do Value na grade **0,49** contra **< 1e-6**) · `Cellular` (Worley, com **`feature`** Cells/Cracks e **`jitter`**, os dois escondidos por `ParamGate` fora do kernel que os lê). ⛔ **O Simplex foi recusado com NÚMERO, não com gosto:** o que ele compra em 2D é **isotropia**, e a anisotropia do Perlin-2002 que temos é **1,83%** (correlação a lag 0,35 por 24 direções: min 0,5692 · max 0,5798) — **entre** o **0,78%** do Value e o **1,95%** do Cellular, os dois kernels que o artista já escolhe no mesmo menu; *o defeito que ele existe para curar é menor que a diferença entre dois vizinhos da própria lista*. ⚠️ **A LEI passou a vir da folha e o RUÍDO DE BASE não** — e a assimetria é o ADR-0075: uma crate-nó **não pode depender de outra crate-nó**, então o `hash2` continua espelhado (o mesmo do `motion.wiggle`, do `force.curl` e do `force.wind` — cinco espelhos) enquanto a **4ª cópia da lei fractal MORREU**. ⚠️ **E a adoção é byte-idêntica por ARITMÉTICA, não por promessa:** o `ph2d_fbm::eval` escala a **COORDENADA** (`px *= 2`) e o laço local escalava a **FREQUÊNCIA** (`x * freq`) — com lacunaridade **2** os dois são a mesma potência de dois e o IEEE-754 não arredonda nenhum (gate `adopting_the_fractal_law_leaf_is_byte_identical`, `to_bits()` sobre 400 pontos × 8 oitavas × 5 rugosidades). ⚠️ **O TETO de oitavas fica no NÓ**, porque o `eval` faz `octaves.max(1)` e **não tem cap** — um `f32` não confiável dirigiria o laço. ⚠️ **TRÊS defeitos de gate, e a MUTAÇÃO achou os três:** **(1)** colapsar a tabela de despacho do `base` **SOBREVIVEU** à suíte inteira, porque **todo gate chamava `perlin_2d`/`cellular_2d` DIRETO** e nenhum passava pela porta que roteia ⇒ gate novo `the_base_door_routes_each_index_to_its_own_kernel`. **(2)** a fixture de paridade CPU×GPU **cozinhava `kernel = 0`** ⇒ os kernels novos concordariam **por VÁCUO**; a nova varre os quatro pares `(kernel, feature)` com `jitter = 0,83`, exigindo `is_fully_gpu()` e `worst < 1e-4`. **(3)** e o gate da CENA era **verde por vácuo**: ele afirmava *"as quatro grades desenham quatro campos"* comparando os quatro streams — mas a cena põe cada grade num `dx` próprio e o ruído é **ESPACIAL**, então elas diferem **por POSIÇÃO** com ou sem kernel (forçar `kernel = 0.0` nas quatro deixava o gate VERDE). O oráculo que isola o kernel da posição é **a MESMA grade, no mesmo lugar, com o param trocado** ⇒ três gates hoje (a cena AUTORA os quatro pares · trocar o kernel MOVE o campo · toda grade fica VISÍVEL), **3 mutações, 3 sangram, uma em cada** — eles não são redundantes. ⚠️ **E a wave achou defeito num nó que ela NÃO possui:** o doc do `NORM` do `motion.noise` afirmava *"o pico empírico desta construção é ~1,49, então `1/1.5` mapeia-o com folga"* — varrido por CÉLULA (180×180 × sub-grid 24²) o pico cru é **1,5111** e a saída chega a **1,0074**, **0,74% FORA**; e o *"golden test"* dele **roda a 4 oitavas**, onde um pico de oitava única é estruturalmente inalcançável ⇒ ele era **cego ao próprio número**. A prosa foi corrigida nos dois lugares e **a CONSTANTE ficou** — apertá-la move 1,3% de toda arte já feita, e é decisão do dono daquele nó. ⚠️ **O `CELL_SPAN = 1.0` é MEDIDO, e a medição matou a régua por-feature que eu ia escrever:** as DUAS features alcançam **1,0000** cru (F1 a jitter 1,0 · F2−F1 em todo jitter); o que difere é a **MÉDIA** (0,43 contra 0,27), que é a assimetria honesta do Worley e não um defeito de escala. ⚠️ **E a cena nasceu com um defeito que a sonda apanhou ANTES do smoke:** ela dirigia o **TAMANHO** — canal sem metade negativa — com o campo **BIPOLAR** ⇒ tamanhos até **−0,26** e a grade do Cracks (a de média mais baixa) quase invisível; com `amplitude 0,5 / offset 0,5` as quatro medem **[0,107 … 0,438]**, todas positivas e com faixa. **Zero schema** (`PROJECT_SCHEMA` **70** intocado) · contrato congelado **3/3** (os três params vivem no `MANIFEST` e os `ParamGate` são side-metadata do registry) · **1 `Cargo.toml`** (`ph2d-fbm` na `ph2d-node-value-noise`) e **nenhum pacote externo novo** — o `Cargo.lock` ganha **uma linha**, a aresta interna. LOC: o `lib.rs` cruzou 700 ⇒ split por responsabilidade em **`kernel.rs`** (*o WGSL que o device roda*) contra o que sobra (*o que o nó É*), o precedente do `motion.noise`. Smoke: **`env PH2D_GPU_COOK_DEMO=37 cargo run -p ph2d-host-desktop --release`** — quatro grades lado a lado (Value · Perlin · Cells · Cracks), e ⚠️ **a pergunta é de OLHO**: as quatro têm de desenhar padrões DIFERENTES, com o Perlin **sem** a grade que o Value mostra e o Cracks com as veias entre células. **⬛ E O ÚLTIMO P0 ACIONÁVEL DA FILA FECHOU — `source.object` ganhou um OFFSET DE TEMPO (2026-08-13, pendente de smoke; folha [14_source](docs/Motion%20Nodes/89_conferencia/14_source.md)):** a tile de um Flip era assada no SHELL, **uma por objeto, no quadro atual do app** ⇒ dois `source.object` do mesmo Flip recebiam o MESMO desenho e nenhum nó a jusante podia pedir outro; uma cascata de cópias cada uma num desenho diferente era **inexprimível**. ⚠️ **A porta é `ph2d_nodegraph::external::appearance_of`**, ao lado do `is_reserved` que guarda o namespace que ela cunha — e o precedente está escrito na própria `external.rs`: `position_of` e `curve_of` nasceram porque eram *a mesma nome, outra pergunta*; esta é a **quarta**, e a primeira que é sobre **QUANDO**. ⚠️ **Zero devolve o NOME CRU**, e é a neutralidade inteira (nenhuma chave, nenhum external, nenhuma tile, nenhum canal a mais — não existe um estado *deslocado de nada* para manter de acordo com o não-deslocado, porque é a **mesma string**; `-0.0` idem). ⚠️ **O offset é de RELÓGIO, não de contagem de desenhos** — cada Flip carrega o próprio `fps`, então um offset em QUADROS deslocaria dois objetos da mesma cena por tempos diferentes; quantos desenhos ele pula é `fps × offset`, **consequência e não um segundo controle**, e a lei mora em `FlipObject::frame_at_shifted` com o `frame_at` de hoje **DELEGANDO** nela (somar `0.0` a um `f64` finito é a identidade ⇒ uma lei, não duas). ⚠️ **O PERIGO QUE DECIDIU O DESENHO:** um param f32 pode ser **DIRIGIDO POR FIO** (doc 58, sem porta), então um `value.lfo` no `time_offset` varreria offsets contínuos e uma cache por-OFFSET cunharia **uma tile por quadro de app**, sem nunca acertar — a cura **não é um cap**: a cache do bake passa a ser chaveada pelo **CONTEÚDO**, então dois offsets que caem no mesmo desenho **são a mesma tile**, e o despejo por quadro deixa o conjunto vivo limitado pela **contagem de nós**, que é uma coisa que o artista põe na tela com a mão. ⚠️ **E a 1ª versão chaveou pelo QUADRO RESOLVIDO, o que o smoke reprovou (*"piscando o tempo todo"*) — o defeito e a medição ficam escritos porque a distinção é fina:** entre duas chaves de animação o desenho está **SEGURADO**, então os quadros 0/1/2 são a MESMA figura com números diferentes ⇒ entradas diferentes ⇒ **despejo + re-bake doze vezes por segundo com a imagem idêntica** (a sonda `probe_animated_flip_bake_churn` dirige o bake REAL quadro a quadro: os `texture_id` subiam **1,2 → 3,4 → 5,6 … → 17,18 em 40 quadros**). *Um quadro é o que se PEDE; uma imagem é o que se ASSA*, e é a imagem que endereça a tile. ⚠️ **E a `=7` é a PRIMEIRA cena do repo com um Flip ANIMADO** — todos os outros modos montam um objeto de UM desenho, cuja chave de conteúdo nunca muda, então o caminho de **re-bake nunca tinha sido percorrido**: a cena fez o defeito existir para ser visto. Medido depois da cura, os tid ficam **estáveis no hold** e — melhor que só parar de piscar — passam a ser **REUSADOS entre as duas cadeias** (o tid 1 migra de *+0,25s* para *agora* quando a fase avança: a mesma figura é a mesma tile). ⚠️ **E o smoke seguinte achou a SEGUNDA metade do mesmo piscar — o DESPEJO —, cuja assimetria É o diagnóstico** (*"o flip do grid à esquerda pisca uma vez por quadro"*): o quadro do app tem **UM de profundidade** — `publish_objects` entrega os `texture_id` deixados pelo bake **ANTERIOR**, o cook constrói as instâncias com eles, e só então `bake_flip_objects` corre —, então despejar ali um id que o cook deste quadro já referenciou faz o `bind_group` devolver `None`, **o lote é PULADO**, e o objeto some por exactamente um quadro. Com duas cadeias do mesmo Flip separadas por **um desenho**, a da FRENTE só **ASSA** (a de trás ainda vai querer a tile dela) e a de TRÁS só **DESPEJA** (a tile de que precisa já está na cache) ⇒ **só o rastro pisca**. Medido pela sonda `probe_the_published_tile_survives_the_bake`, que reproduz a ordem real: **2 mortes em 40 quadros na cadeia não-deslocada, ZERO na deslocada** — e zero nas duas depois da cura. ⚠️ **A carência de um quadro não é folga, é a PROFUNDIDADE do pipeline**, e a aritmética fecha: poupar em `N` o que `N−1` resolveu cobre exactamente as instâncias que o cook de `N` construiu, e o que saiu em `N−1` e não voltou morre em `N+1`, quando o último desenho que o usou já terminou (*zero quadros apaga uma tile debaixo do desenho que a referencia; dois seriam VRAM sem consumidor*); o custo é limitado pela **CONTAGEM** de nós do documento. O gate é GPU-free sobre a decisão pura (`evictable`) e a fixture traz as **três** espécies — pedida agora · publicada no quadro anterior (tem de **sobreviver**) · velha nos dois (tem de **morrer**) —, sendo a terceira o que impede *"nunca despejar nada"* de passar. Gates sem GPU (o oráculo é o **hash**, não o relógio): *um desenho segurado tem a mesma chave em todo quadro em que é segurado* + o **CONTROLE** (*desenhos diferentes dão chaves diferentes* — sem ele um `content_key` constante passaria e a cache colapsaria a animação inteira numa tile, o defeito oposto); a mutação que devolve a chave ao quadro **reproduz o sintoma na sonda**. ⚠️ **A membrana escreve DUAS vezes, nesta ordem:** a **cópia TRANSPARENTE** do canal cru (um sprite, um vetor, um grupo não têm animação própria ⇒ *meio segundo à frente* tem a mesma resposta que *agora*; **sem ela o objeto SUMIRIA da cena**, que é a forma mais cara de um param novo estar errado, porque parece a feature funcionando noutro objeto) e depois a **tile do Flip deslocado, que VENCE** — e se o bake não produziu geometria naquele quadro a cópia FICA (*pulado, nunca adivinhado*). **Os números da faixa são DERIVADOS do domínio:** o passo `1/120` é um quadro no fps mais rápido que a tira oferece (`[1, 120]`) e o teto digitável **999 s** é a exposição máxima de uma chave (`HOLD_MAX = 999` quadros) no fps mais lento — *a magnitude não consome recurso nenhum; o recurso é a CONTAGEM de tiles, e ela é limitada por construção*. ⚠️ **Um gate meu nasceu VERMELHO contra código correto, por FIXTURE:** ele varria tempo negativo com `Playhead::seek`, que **CLAMPA em zero** ⇒ media o quadro zero achando que media um passado; o negativo é alcançável pelo **offset**, e há gate que o exercita por essa via. **17 gates** (6 na porta · 3 na lei do frame · 4 no nó · 4 na membrana) · **zero schema** (`PROJECT_SCHEMA` **70** intocado — um offset é param de nó, e o grafo viaja como TEXTO) · contrato congelado **3/3** (o `NodeManifest` do `source.object` sai de `params: &[]` para **um** param, o que é o manifesto do NÓ e não a superfície congelada) · **1 `Cargo.toml`** (a shell ganha `ph2d-node-source-object` em `[dependencies]`, e ⚠️ **não é dep de gate** como a do `motion.output`: a membrana tem de varrer o documento atrás dos `time_offset`, e o nome do tipo e o do param são do NÓ — um literal aqui seria a segunda cópia de uma string que o nó já declara, e no dia em que uma delas mudasse a varredura ficaria **VAZIA em silêncio**) · **nenhum pacote externo novo**. LOC: **três cortes por ASSUNTO** — o canal deslocado saiu com os próprios gates (`motion_bridge_objects_shift{,_tests}.rs`) e o bake de Flip separou *como uma tile é PRODUZIDA* de *como se PEDE por uma* (`motion_flip_bake_query.rs`). Smoke: **`env PH2D_MOTION_OBJ_SMOKE=7 cargo run -p ph2d-host-desktop --release`** — o MESMO Flip (12 fps, quatro desenhos em `0/3/6/9`) trazido **duas vezes**: a grade da esquerda em `time_offset = 0` e a da direita em **+0,25 s**, que a 12 fps são exatamente **três desenhos à frente**. ⚠️ **A cena traz a própria fixture ANIMADA, e é isso que a torna válida** — os outros modos do mesmo smoke montam um Flip de UM desenho, e sobre um desenho só um offset é **indistinguível de nenhum offset**: a cena diria *"funciona"* sobre um param inerte (há gate afirmando que a fixture tem quatro desenhos DISTINTOS). **O que olhar:** as duas grades desenham a mesma arte e o quadrado da direita fica **sempre um passo à frente** — se andarem juntos o offset não chegou ao bake, se a direita **SUMIR** o canal deslocado não foi publicado; pare o play e faça scrub, a diferença de fase tem de se manter em qualquer quadro. ⚠️ **A varredura que produziu este número corrigiu QUATRO contagens que sobreviveram ao próprio fechamento** — a minha primeira versão desta linha dizia *"2 P0"* e omitia que o `motion.output`/**blend** já fechou (cena `=36`, as duas rotas gateadas); a `Contagem` da folha 10 dizia `P0 = 3` com os três **riscados** na tabela logo acima; e a prosa da folha 08 dizia que *"o `cull` carrega um P0"* contra a tabela da própria folha, que o marca **⛔ REFUTADO**. Os dois itens de **RIG** (`skin_deformer` peso por-osso · o *Manipulator*) seguem **`P0` ⏸ — família DEFERIDA**, e o segundo é **UI, não param**. **⬛ E A DIREÇÃO FECHOU A LINHA QUE CINCO FAMÍLIAS CITARAM — o `value.attribute` ganhou o irmão do `Speed` (2026-08-14, cena `=38`, **smoke OK 2026-08-14**):** com o P0 vazio, a fila passa a ser o que a **§10.0 do plano** manda perguntar — *quem pode ESCREVER uma coluna, e como?* —, e a tabela dela **envelheceu**: das quatro linhas, o `falloff` fechou com o `CH_FALLOFF`, o que a linha do `vel` causava fechou dentro do `sim.step`, e a do **`rot` a partir de uma tangente** (famílias 1·4·5·6·15) **estava aberta pelo lado errado** — o `rot` sempre foi escrevível; o que faltava era a **LEITURA**: o `value.attribute` lia `vel` como **`Speed`** (`MODE_LENGTH`, a magnitude) e descartava *para onde*. ⚠️ **Medido antes de construir, e o vão era real:** **nenhum** nó lê `vel` e escreve `rot` · o `motion.look_at` mira em **Point·Object·Cursor**, nunca em *"para onde este elemento vai"* · `value.math` faz aritmética, `value.unary` não tem trigonometria inversa, e o parser de `motion.expression` tem `sin`/`cos` e **não tem `atan2`** (com `ph2d-expr` **FROZEN** por ADR-0039) ⇒ duas lanes soltas nunca viram um ângulo. **A cura é o espelho exacto do `MODE_LENGTH`** — `MODE_ANGLE` + canal **`Direction`** — e por isso é **COMPOSIÇÃO e não nó novo**: `value.attribute(Direction) → motion.drive(Rotation)` **é** o *align to velocity*, o mesmo desenho que fez de `value.noise(World) → motion.drive(Falloff)` o campo de ruído. ⚠️ **Quatro decisões, cada uma medida:** **GRAUS** (a coluna `rot` é em graus e o lowering só cruza para radianos na borda do render — radianos erraria por **57×** exactamente na costura que o canal existe para servir) · **`MODE_ANGLE = -1`** (o espaço positivo está falado, as lanes são abertas em `BASE + k`, e o `mode` é um **param que o GRAFO GUARDA**: renumerar re-apontaria em silêncio todo documento salvo ⇒ reduções crescem para BAIXO) · **`libm::atan2f`** (o do `std` chama a libm da PLATAFORMA e este número atravessa o cozido; a física já o recusou pelo mesmo motivo) · e **vetor zero responde `0`** (quem não se move não tem direção, e não pode girar). ⚠️ **E o braço entra na exclusão do arm ESCALAR junto com o `MODE_LENGTH`** — pedir a direção de um escalar cai no **miss ORDINÁRIO** (zeros); sem essa metade, `Direction` sobre `age` devolveria o próprio `age` como se fosse um ângulo. ⚠️ **A wave corrige uma afirmação que JÁ SHIPAVA:** o doc do `MODE_COMPONENT_BASE` dizia ter destravado *"turn to face where you're going"* — a lane tornou as **COMPONENTES** alcançáveis e o **ÂNGULO** continuava inalcançável; *uma afirmação de ter fechado um vão é a forma mais cara de nota errada, porque faz a varredura seguinte pular a linha*. ⚠️ **A escada é replayada no DEVICE** (`StreamOp::Project` — o `lowerings: Cpu` do nó **não** significa CPU-only), então o braço tem espelho em `encode_project` + um pipeline `angle` em WGSL, e ele é **o único braço transcendental da escada**, logo o único cuja paridade é por **épsilon** (a CPU corre a libm do MUSL, o device o `atan2` do vendedor). **Gates:** 5 no nó — a lei em graus · **a unidade do CONSUMIDOR** (o número levado pela conversão do lowering aponta ao longo de `vel`; é ela que separa graus de radianos) · o vetor parado · o escalar sem direção · a escada velha intacta — mais a **constante duplicada** na cerca do `[dev-dependencies]` e a **paridade CPU×GPU na RTX** (1225 instâncias). **4 mutações, 4 sangram**, incluindo a do **device** (`atan2` trocado), que é o que prova que o comparador **VÊ** a rotação. **Cena `PH2D_GPU_COOK_DEMO=38`:** o MESMO redemoinho duas vezes, **676 peças cada**, e só a da direita tem os dois nós — ⚠️ **as peças são TRAÇOS de propósito** (um quadrado rodado 90° é o mesmo quadrado, e a cena não provaria nada); medido, o controle **não escreve `rot` de todo** e o alinhado cobre **−177,6..177,9°**. ⚠️ **E o gate da cena nasceu ACUSANDO o canal de um defeito da FIXTURE:** sem `advance_tick` a aresta `pre` nunca carrega estado, a velocidade fica zero e as duas nuvens ficam paradas — e a metade do CONTROLE foi reescrita como **AUSÊNCIA**, porque `all()` sobre um vetor vazio é verdade por **vácuo**. **Deps:** `libm` (mesmo pin das outras cinco crates) na `value-attribute`, e a shell ganha a `value-attribute` para a cena autorar o `mode` **pelo NOME** — um literal seria a segunda cópia de um número que o nó declara, e no dia em que o degrau se movesse a cena desenharia traços **não-alinhados afirmando demonstrar alinhamento**; **nenhum pacote externo novo**, `PROJECT_SCHEMA` **70 intocado**, contrato congelado **3/3**. **⬛ E O TEXTO ENTROU NO GRAFO — `source.text`, UMA INSTÂNCIA POR GLIFO (2026-08-14, cena `=39`, **smoke OK 2026-08-14**; folha [14_source](docs/Motion%20Nodes/89_conferencia/14_source.md) §3 item 1):** com a §10.0 fechada, a fila voltou ao P0 — e a varredura das 17 folhas mediu que **só um item acionável sobrava**: a linha 1 da folha SOURCE, *"não há nó de texto, e embora `ph2d-vector-font` exista como foundational, **nada no grafo o alcança**"*, fechando com *"metade do mograph do mundo é texto animado por caractere"*. ⚠️ **A metade CARA já estava paga, e isso decidiu o desenho:** o módulo Vector construiu texto vetorial inteiro (glifo → `VecPath`, eixos variáveis, refluxo, texto em caminho) ⇒ a wave é **FIAÇÃO, não geometria** — literalmente a frase que o `source.shape` escreveu no próprio doc (*"35 formas que o editor já desenhava eram inalcançáveis de um grafo: a geometria nunca faltou, a fiação faltava"*), uma família adiante. ⚠️ **UM GLIFO POR INSTÂNCIA é a wave inteira numa linha:** um bloco como UMA instância desenharia a mesma imagem e **não seria a feature** — o que a referência entrega (Cavalry *Sub-Mesh* · AE *Text Animators* · Blender *String to Curves*, que devolve **uma instância de curva POR CARACTERE**) é a letra como ELEMENTO, e uma linha por glifo faz **a biblioteca `motion.*` inteira agir por caractere de graça** (stagger, wave, jitter, campos, `drive`, `falloff`, `duplicator`) sem um nó novo a jusante. **A porta é a do `source.shape` e não é escolha** (o nó recebe params, entradas e o playhead, logo não alcança a fonte nem a biblioteca de vetor): o shell faz o layout, interna cada glifo no `VecPathStore` e publica `(P, geometry_id)` sob a chave de conteúdo. ⚠️ **`register_live_vector_source` é obrigatório** — sem ele o documento não recusa o cook GPU e as letras saem como **quadrados brancos do atlas** (ADR-0154). ⚠️ **O LAYOUT tem UMA porta:** `vec_glyph::walk_glyphs` saiu de dentro do `text_to_vec_paths` (refactor puro) e serve os DOIS consumidores — um segundo laço no shell responderia *"onde cai cada letra?"* uma segunda vez, e as duas respostas divergiriam no dia em que alguém mexesse no alinhamento, com **o texto do canvas e o do grafo a desenhar coisas diferentes dos mesmos números**. **O PIVÔ sai do próprio laço, sem estado** (`Center`, o default, desloca a geometria `−adv/2` e o `P` `+adv/2`, com o avanço que o layout já tem na mão) e ⚠️ **os dois pivôs desenham a MESMA imagem em repouso** — a escolha é invisível até alguém animar, e é gate. ⚠️ **E UMA DIVERGÊNCIA FOI FECHADA ANTES DE NASCER:** um nó que resolvesse `None → "Text"` desenharia *Text* no canvas enquanto o painel — que lê o override e não acha nenhum — mostrava o campo **VAZIO**, duas respostas à mesma pergunta lado a lado no primeiro quadro. O texto de fábrica passa a ser **SEMEADO**: o nó **DECLARA** (`register_text_defaults`, side-metadata no registry, o irmão exato do `register_param_ui`) e o **editor APLICA ao soltar**, no mesmo `reconcile` onde o `pre` self-loop já é plumbado e pelo mesmo motivo declarado (*um nó tem de chegar VIVO*) — ausente volta a significar ausente em toda parte, e a semeadura só toca chave **AUSENTE** (um vazio que o artista digitou sobrevive). ⚠️ **DUAS lições de gate, as duas minhas:** o `different_letters_get_different_geometry` nasceu com pivô `Center` e **PASSOU com o caractere fora da chave** — sob Center o deslocamento é metade do AVANÇO, que já difere entre A e B, então a chave desambigua sozinha ⇒ *verde por acidente do pivô*; com `Pen` o shift é zero nos dois e o caractere é o **único** discriminante · e o harness da cena **publicava num `Cook` e cozia noutro**, então o externo não existia no que ele media e ele acusava o produto de **zero instâncias**, que é a assinatura exata da feature quebrada. **19 gates** (7 no nó · 11 na membrana · 1 na cena), **8 mutações, 8 sangram**. **Zero schema** (`PROJECT_SCHEMA` **70** intocado — o grafo viaja como TEXTO e o texto/fonte são **text params**, que moram no `Graph`) · contrato congelado **3/3** · **1 crate nova** (`ph2d-node-source-text`, folha drop-in, glob member) · **nenhuma dep externa nova**. Censo re-medido na árvore: **122 nós · 508 params · 489 com hint · 143 com unidade**. Smoke: **`env PH2D_GPU_COOK_DEMO=39 cargo run -p ph2d-host-desktop --release`** — a MESMA palavra duas vezes; ⚠️ **é o LEQUE que prova a wave, não o texto aparecer**: em cima a palavra assenta reta, em baixo cada letra roda um pouco mais que a anterior (medido: 6 letras, **0,0..55,0 graus**, progressivo), e um bloco emitido como uma instância giraria **rigidamente**, com todas as letras no mesmo ângulo. ⚠️ **Aberto, e não é o P0:** shaping complexo (o layout é advance-only, decisão escrita no `vec_glyph`) · o refluxo (o `wrap_width` existe no `TextLayout` e o nó ainda não o expõe) · texto em CAMINHO no grafo (o Vector já o tem no canvas) · e o *Background Shape*. **⬛ E O SMOKE DO `=39` ACHOU UM DEFEITO MAIOR QUE O REPORT — a faixa de uma magnitude segue o CANAL, e a tabela que decidia isso MORREU (2026-08-14):** Enio — *"Scale de drive não aceita mais que 4 em sua caixa de texto e 4 não é quase nada para rot"*. ⚠️ **Medido antes de mexer:** o `scale` é multiplicação **PURA** (nem a CPU nem o WGSL o clampam) e o canal Rotation honra **1e7 graus com erro `0.000e0`** ⇒ o 4 era **teto de UI sem recurso nenhum atrás**, exatamente o que o §0 manda não escrever. ⚠️ **E o defeito era maior que o report:** o mecanismo por-canal **já existia** (`channel_range_override`) e o doc dele registra *"exatamente o bug que o Enio pegou com o Stagger no canal Rotation"* — **é o MESMO report, um nó depois**; ele era uma **TABELA DE NOMES DE NÓ no shell**, e a varredura mediu o preço: **cobria TRÊS dos SEIS** nós que precisavam dela, com `motion.drive`, `motion.step` e `motion.noise` a shipar sem, cada um à espera do próprio report. ⚠️ **A cura não é uma 4ª linha:** o nó passa a **DECLARAR** quais dos seus params são magnitudes na unidade do canal (`register_param_channel_range`, side-metadata no registry, o irmão do `register_param_ui`), **ao lado dos próprios hints** — a divisão de saber é a que já existia (*o nó diz quais params são magnitudes · o shell diz quais canais são angulares*, o `channel_unit` já o dizia), e um nó que nasça amanhã mostra a ausência **no próprio arquivo**. ⚠️ **Alargar a faixa cria uma OBRIGAÇÃO, e ela era uma SEGUNDA tabela com os mesmos três ausentes:** um valor em graus não pode sobreviver a uma troca de canal (360 graus viram **360 unidades de mundo** e jogam as instâncias para fora do quadro, num slider que não os alcança) ⇒ os presets por-nó ficam (escolhem um valor BONITO) e atrás deles corre uma **regra DERIVADA** que traz todo param de faixa declarada para dentro da faixa do canal novo — **no-op para os três que já tinham preset**, porque o valor que eles escrevem já está dentro. ⚠️ **O teto digitável NOMEIA o recurso, e é PRECISÃO DE REPRESENTAÇÃO:** **`2^24 = 16 777 216`** é o primeiro `f32` em que `x + 1.0 == x` (medido) — acima dele um passo de UM grau **não move o número**, o controle deixa de controlar, e é aí que *o disfuncional começa*; o piso espelha (um `scale` negativo **inverte** o drive). ⚠️ **E o gate universal VARRE o registry em vez de listar** — o irmão antigo chamava-se `every_…` e exercitava **um** nó; a mutação que apaga a regra derivada sangra **nomeando `motion.step.step`**, um dos três que a tabela esquecera. **12 mutações, 12 sangram** · zero schema · contrato congelado intacto · nenhuma dep nova. **⬛ E O ÚLTIMO P0 DA CONFERÊNCIA FECHOU — o SOM dirige a geometria, e a nota que o salvou era a que o punha FORA do escopo (2026-08-15, cena `=40`, **smoke OK 2026-08-15**; [doc 63 §3](docs/Motion%20Nodes/63_pesquisa_industria_2026_e_plano_estado_da_arte.md) + folha [14_source](docs/Motion%20Nodes/89_conferencia/14_source.md) §3 item 6):** com a fila por-folha vazia, a varredura achou **um** item vivo — e ele não era P0 de folha nenhuma. A folha SOURCE apenas o NOMEAVA, *"só para o consolidador não o perder na fronteira entre famílias"*, com o P0 morando no doc 63; ⚠️ **uma varredura que confiasse só nas 17 folhas o teria perdido**, e a linha que o registrou como fora do próprio escopo é a razão de ele existir hoje. ⚠️ **A metade CARA já estava paga, a terceira vez seguida:** o doc 63 §D6 mediu-a em 2026-07 como *"`ph2d-audio-spectral` JÁ tem FFT; falta só a ponte"* ⇒ **era FIAÇÃO, não DSP** — a mesma frase do TEXTO e do `source.shape`, uma família adiante. **A cerca decidiu o desenho e é ESTRUTURAL, não disciplinar:** o doc 63 §6 escreveu *"FFT NUNCA entra no cook"* antes de existir código, e uma regra num doc vale enquanto alguém a lembra — a crate `ph2d-node-audio-bands` **não depende de crate de áudio nenhuma**, então ela *não CONSEGUE* decodificar nem transformar. ⚠️ **O gate é uma lista-BRANCA e não uma lista de proibidos** (`the_fft_never_reaches_the_cook`, o molde do `the_event_core_is_a_leaf`): enumerar *"nem realfft, nem symphonia…"* apodrece na primeira crate pesada que ninguém previu; dizer **quem PODE entrar** não envelhece. O que o nó POSSUI é a **LEI** — o corte do eixo em bandas (Linear/Log/Mel), o **PICO** dentro da banda (uma média enterra o evento que a barra existe para mostrar), a normalização pelo piso em dB e a ponderação **A** —, porque isso é vocabulário de MOTION; o shell faz a transformada e chama a MESMA função, então os dois lados concordam **por construção**. ⚠️ **É um campo de VALOR, não uma fonte de geometria, e a referência decide:** Cavalry **Sound** mapeia bandas → índices (*Use Index Context*), C4D **Sound Effector** é um efetor sobre clones que já existem e MOPs **Audio** é um falloff — **nenhuma delas CRIA objetos**. Emitindo a coluna `v` (o tipo do `value.instance_field`), ele entra em `motion.drive`, em todo `field.*` e no resto da biblioteca **sem um nó novo a jusante**. **A regra é uma:** o elemento `i` toma a banda `i % count`; ligado, `N` vem da geometria; solto, `N` **é o próprio `count`** — *as bandas elas mesmas*, que é o analisador de espectro (⚠️ diverge do `value.instance_field`, que degenera para 1, **porque o dado diverge**). ⚠️ **O SUAVIZADO é um filtro sobre a GRAVAÇÃO, nunca sobre a sessão:** um one-pole por-quadro é **ESTADO** e quebraria o scrub — que é exatamente a promessa que o doc 63 faz (*bandas são função do ARQUIVO + playhead ⇒ scrub-exato*); aqui ele corre ao longo das **colunas do arquivo** quando a análise é construída, e voltar a régua dá o mesmo número **AO BIT** (gate). ⚠️ **A chave NÃO carrega o instante**, e é o desenho: ela nomeia *quais bandas deste arquivo*, o shell reescreve o mesmo nome a cada quadro, e quem diz ao cook que o valor se move é o **`Effect::Temporal`** do nó — declarado `Pure`, o cook estaria autorizado a memoizá-lo e **as barras congelariam com tudo verde**. ⚠️ **A fonte é um CAMINHO (text param), e não o clipe aberto no editor de áudio:** nomear o clipe faria o grafo depender de estado de EDITOR — reabrir o projeto devolveria silêncio, com nada no arquivo salvo a dizer o que dirigiu a animação; o preço (mover o arquivo quebra o vínculo) é o *missing footage* que todo DCC tem e sabe nomear. ⚠️ **E a wave MOVEU uma fronteira de features:** o decodificador (`decode_any`) e o espectro deixaram de ser **opcionais** na shell — eram privados do painel de áudio por acidente de quem precisou primeiro, e gatear a membrana naquela feature deixaria o nó **registrado e MUDO** num build sem o editor, que é um controle morto. **`ParamUnit` ganhou `Hertz` e `Decibel`** (variantes apendadas, side-metadata do registry). ⚠️ **TRÊS defeitos de gate, os três meus, e os três achados por medição ou mutação:** o da chave nasceu afirmando **o CONTRÁRIO** da propriedade (pedia que `key("a:b")` diferisse de `key("a")+":b"`, e as duas SÃO a mesma string — *é isso que "o caminho vai por último" significa*; virou injetividade) · o do clamp de banda estreita **não sangrava**, e a medição explicou: com `lo < hi` vale `floor(lo/h) < ceil(ceil hi/h)` **sempre**, então o piso é inerte numa faixa real e **load-bearing só na DEGENERADA** (`min_hz == max_hz` numa fronteira exata), que ganhou gate próprio · e a ponderação A era medida **DIRETO**, então trocar o centro geométrico pelo aritmético **dentro do `fold`** sobrevivia — o gate novo passa pelo `fold`. ⚠️ **E uma MUTAÇÃO expôs um gate meu que era verde por vácuo:** `v[i] == v[i % 8]` é satisfeito por **qualquer campo constante**, então um nó que devolvesse sempre a banda 0 passava; a metade que faltava é *as oito bandas diferem entre si*. **16 gates no nó + 8 na membrana + 2 na cena + 2 no espectrograma · 9 mutações, 9 sangram.** **Zero schema** (`PROJECT_SCHEMA` **70** intocado — o grafo viaja como TEXTO e o caminho é text param) · contrato congelado **3/3** · **1 crate nova** (`ph2d-node-audio-bands`, folha drop-in, glob member) · **nenhuma dep externa nova** (o `Cargo.lock` ganha a própria crate; `realfft`/`symphonia` já estavam no build default). Censo re-medido na árvore: **123 nós · 516 params · 497 com hint · 146 com unidade**. Smoke: **`env PH2D_GPU_COOK_DEMO=40 cargo run -p ph2d-host-desktop --release`** — a MESMA fileira de **24 barras** duas vezes; ⚠️ **é o movimento COM O TEMPO que prova a wave, não as barras diferirem** (um campo por-índice também as deixaria desiguais, e ficaria PARADO): dê **PLAY** e a onda tem de correr da esquerda para a direita, porque a cena escreve em disco um **varrimento** de 6 s (60 Hz → 12 kHz) — medido, faixa de tamanho **1,00..3,46** e delta no tempo **2,46**. ⚠️ **A cena escreve a própria fixture** (não há asset de áudio no repo) e um **varrimento e não música**, porque a pergunta é *cada barra responde à FAIXA dela?* — com música, uma fileira desalinhada e uma certa parecem as duas plausíveis. **Aberto:** nenhum *file picker* na row (o caminho é colado) · `audio.probe` (as caixas no espectro do C4D) e `field.audio` (o falloff do MOPs) seguem **P1** no doc 63 · e a análise não é despejada (o conjunto vivo é limitado pela CONTAGEM de nós, com o número no doc do cache). **⬛ E A SEGUNDA VOLTA COMEÇOU PELO BANDO — o `motion.boids` ganha OLHOS e um PISO, e DUAS células da tabela tinham envelhecido (2026-08-15, folha [03_simulacao](docs/Motion%20Nodes/89_conferencia/03_simulacao.md), **smoke OK 2026-08-15**):** com a fila de P0 vazia, a fila passa a ser **P1 por família** — e a escolha da família se MEDE: o cluster do boids é o único em que os P1 são as peças que faltam de **um modelo publicado** (Reynolds, *Steering Behaviors*, GDC 1999), em vez de knobs soltos. ⚠️ **E antes de uma linha ser escrita, o §0 mordeu a própria tabela:** das cinco células do cluster, **duas eram falsas** — o `max_force` **já existe** como param (desde `2ec7dc552`) e o *wander* deixou de estar bloqueado quando o **W1-A** fez os três geradores consumirem `accel` (a célula ainda dizia *"boids não lê `accel`"*, e o `gpu.rs` traz `ColumnAccess::Consume` naquela coluna). *Construir a partir da tabela sem a reconferir teria produzido dois params que já existiam.* **(A) O CONE DE VISÃO** (`fov`, rótulo *View Cone*, graus, default **360**): a segunda metade da vizinhança de Reynolds — *a vizinhança é distância **E** ângulo* —, e a folha nomeou o preço da ausência (*"um boid que enxerga atrás de si não vira bando, vira nuvem"*). ⚠️ **O 360 é byte-idêntico por CONSTRUÇÃO, não por promessa:** `cos_half_fov(deg >= 360)` devolve **`-1` LITERAL** e o teste angular é **PULADO**, nunca satisfeito — deixá-lo correr e sempre passar pagaria **uma raiz por vizinho** em toda cena que não usa o cone (mesmo desenho, outro relógio); e o literal dos dois lados é também o que impede o `cos` da `libm` e o do vendedor de discordarem no caso comum, deixando a paridade por ε **só** onde o artista de facto pediu um cone. ⚠️ **Um agente PARADO enxerga tudo, e é decisão** (sem heading não há cone; cegá-lo faria dele um retardatário permanente, que nunca reencontraria o bando). **(B) O PISO DE VELOCIDADE** (`speed_floor`, default `0,2`): o número existia como a const **escondida** `MIN_SPEED_FRAC` e não tinha porta. ⚠️ **FRAÇÃO e não velocidade absoluta, e a razão é byte-identidade** — o piso vivo sempre foi `max_speed × 0,2`, então expô-lo em unidades de mundo pediria um default **que depende de outro param** e mudaria o desenho de todo documento cujo `max_speed` não fosse o de fábrica, em silêncio; o rótulo evita *Min Speed* justamente para o `0,2` não ser lido como m/s. ⚠️ **A prova de que os defaults reduzem são os NOVE gates que já existiam** naquele arquivo: eles foram escritos contra o mundo pré-cone e passam **sem uma edição**. ⚠️ **E a lista de params do KERNEL não é derivada do manifesto** — um param novo compila, coza na CPU, e o device **recusa o shader** (`invalid field accessor`); é falha alta e não silêncio, mas o sítio se esquece, e agora está nomeado no próprio arquivo. ⚠️ **DOIS defeitos de gate, os dois achados pelo CONTROLE e pela MUTAÇÃO:** a fixture partilhada `boids_graph` devolvia **só o nó de OUTPUT**, então a minha paridade do cone escrevia `set_param(out, "fov", …)` — **um param no nó errado, ignorado em silêncio** — e nascia **verde por vácuo**, comparando dois bandos que nunca usaram o cone; quem a pegou foi o gate de controle ao lado (*o cone move o bando*), e a assinatura da fixture passou a devolver os dois nós · e a fixture do agente parado usava velocidade **exatamente zero**, onde a comparação vira `0 < 0` e é falsa nos dois mundos ⇒ apagar a guarda **não sangrava**; o caso real é o agente que desacelerou até um rastejo (sub-EPS e não zero). ⚠️ **E as mutações ACUMULARAM numa corrida** porque o diretório de snapshot em `/tmp` evaporou entre elas — cada resultado media a soma dos anteriores; re-rodadas uma a uma com restauro, **5 mutações, 5 sangram**. **Medido:** o cone de 110° move o bando **8,72e-2**, paridade CPU×GPU do cone **2,38e-7** (barra 2e-3), e a suíte do nó fecha em **14 gates**. **Zero schema** (`PROJECT_SCHEMA` **70**) · contrato congelado **3/3** · **1 `Cargo.toml`** (`libm` no MESMO pin `=0.2.16` de outras cinco crates — **aresta interna, não pacote novo**) · nenhum ADR. ⚠️ **E O SMOKE APROVOU O CONTROLE E REFUTOU A PREMISSA (2026-08-15):** Enio — *"View cone está bom em comportamento, mas fica coordenado com valores altos e agitado com valores baixos"*. A folha abriu este P1 dizendo *"um boid que enxerga atrás de si não vira bando, vira nuvem"*, e na régua canônica de *"isto é um bando?"* — o parâmetro de ordem de Vicsek — **o disco de 360° é o ponto MAIS polarizado da varredura**. Sonda nova `measure_what_the_view_cone_does_to_the_flock` (48 agentes, 600 ticks, média de **8 sementes**): fov **360 → 30** dá polarização **0,7145 → 0,2238**, agitação **60,0 → 135,4 °/s** e spread **2,280 → 1,121** ⇒ *estreitar o cone APERTA e AGITA o bando; ele não o coordena*. ⚠️ **A causa é a vizinhança que ele ENCOLHE, e o CONTROLE mede quanto:** encolher o `radius` — uma borda de DISTÂNCIA, que **não gira com o agente** — reproduz a maior parte do movimento (churn **60,0 → 112,1** de `r` 2,0 a 0,5), então a agitação **não é** a descontinuidade angular que eu ia consertar; mas as duas **não colapsam numa curva**: à spread casada (~1,75) o cone cobra **127,6** contra **81,2** do raio e polariza **0,28 contra 0,45**, o resíduo que a borda angular acrescenta por girar junto com quem olha. ⚠️ **E a PRIMEIRA corrida, com UMA semente, dizia EMPATE** (97,5 × 100,8) e teria encerrado a investigação na conclusão oposta — num bando de 48 agentes a polarização salta **0,16 → 0,78** entre pontos vizinhos da MESMA varredura; *um número que não reproduz não é achado, é ruído com casas decimais*, e a média sobre sementes é o que torna as duas tabelas legíveis. **O knob FICA, o rótulo também** (*View Cone* é o que o número É — batizá-lo pelo EFEITO amarraria o nome a uma medição que a próxima mudança de pesos move); o que estava errado era a **prosa**, corrigida no nó, no `ui.rs` e na folha. **⬛ E A CORDA TEM ESPESSURA — o P1 seguinte do cluster fechou POR MEDIÇÃO, sem uma linha de código (2026-08-15, gate `rope_thickness.rs`):** a folha 03 marcava a colisão da `motion.verlet_rope` como *"**PROVAVELMENTE SIM, e ninguém sabe** — o teste decisivo mais barato desta conferência"*, com o veredito bifurcado (`omissão` se falhasse, `ergonomia` se funcionasse) e a confissão **⚠️ *"não rodei"*** ao lado. Rodado: `rope.out --pre--> motion.collide.in` + `collide.out --> rope.state` **FUNCIONA** — numa corda chicoteada de 24 nós com colisor de raio 0,5, os nós NÃO-vizinhos ficam a **1,0516** (o próprio diâmetro) contra **0,6811** da mesma corda sem ele, que se atravessa ⇒ **`P1` → `P2`**: não falta capacidade, falta o GESTO (dois nós e duas arestas à mão, com o `pre` na aresta que ENTRA no colisor — é ela que quebra o ciclo, e o colisor é `Pure`, logo não carimba `sim_t`). ⚠️ **E a fixture reprovou DUAS vezes antes de medir coisa alguma, as duas por não conter o fenômeno:** (a) a corda PENDURADA nunca se dobra — o nó tem **uma âncora só** (não há pino de cauda, que é outro P1 do mesmo nó) e o controle acusou `min_far = 1,0435`, que é **exatamente `4 × length/(count−1)`**, a pose RETA; (b) com a âncora chicoteada mas **sem `advance_tick`** a aresta `pre` nunca carrega estado, a corda ficou em repouso (`tail = head + length`, `y = 0,0` sob gravidade **9,8**) e as duas cordas saíram **bit a bit idênticas** — *uma cadeia que nunca integra faz todo gate de feedback ser verde por vácuo*, e o número idêntico nas duas colunas foi o que denunciou. **3 mutações, 3 sangram** (tirar o colisor do laço · raio ~0 · tirar o `advance_tick`), e a régua é o **DIÂMETRO** e não o raio (dois nós de raio `r` sobrepõem-se abaixo de `2r`; comparar com o raio pediria o dobro da dobra e reprovaria sobre uma corda que de facto se atravessa). **Aberto no cluster:** o **`lookahead`/avoid obstacle** pede geometria de obstáculo alcançável de dentro do nó (wave própria) · e a coluna **`neighbours`** — o número **JÁ É COMPUTADO e jogado fora nas duas rotas** (`var neighbours` no WGSL, `inv_n` na CPU), que esta investigação passou a QUERER: ela é o discriminador exato entre *cone* e *raio* que a sonda teve de proxiar pelo `spread`, e emiti-la custa uma coluna na CPU **mais um harness de paridade que leia COLUNAS** (o gate de hoje compara `world_pos` de instâncias) ⇒ é wave com cena própria, não rodapé. **⬛ E A SEGUNDA VOLTA PASSOU A ANDAR EM GRUPOS — o grupo A da conferência fecha CINCO linhas P1 de uma vez, com UMA cena (2026-08-15, cena `=41`, **smoke OK**; folha [15_value](docs/Motion%20Nodes/89_conferencia/15_value.md)):** ordem do Enio — *"se continuarmos nesse ritmo nunca acabaremos. Vamos implementar em grupos de nós. A cada grupo uma cena de smoke"*. O grupo A é a **ARITMÉTICA do domínio de valor**: os cinco nós irmãos que respondem *"que número sai deste número?"* — `value.math` ganha **`Modulo` (6) e `Floored Modulo` (7)** · `value.quantize` o **`Truncate` (3)** · `value.step` o **`Smoother` (2)** · `value.map_range` o dropdown **`interpolation`** (Linear/Stepped/Smooth/Smoother) mais `steps` · e `value.mix` os **nove `blend`** (Mix/Add/Subtract/Multiply/Screen/Difference/Darken/Lighten/Overlay). O placar da folha 15 vai de **16 para 11** P1. ⚠️ **Os cinco defaults reduzem LITERALMENTE ao mundo de antes**, com um gate **byte a byte por nó** contra a expressão que shipava, escrita à mão como oráculo (chamar a função sob teste seria o gate sempre-verde que esta casa já documentou três vezes). ⚠️ **OS DOIS MÓDULOS, e não um:** o sinal que cada um segue (dividendo × divisor) é a razão de existirem, e **acima de zero eles COINCIDEM** — uma fixture positiva não os separa. E os dois são computados na forma **DERIVADA** (`a − b·trunc(a/b)`), **nunca por `%`**: o `%` de `f32` é uma chamada a **`fmod` da libm** (a mesma classe de custo que a secagem do Wet Paint mediu em 2,51 ns contra 0,54) **e é EXATO**, enquanto o WGSL não o tem — derivar dos DOIS lados torna a paridade bit-a-bit por construção em vez de por ulp. ⚠️ **O `value.mix` NÃO ganhou uma segunda porta do `value.math`:** a lei é `lerp(a, f(a,b), t)` — *`a` desvanecido ATÉ a operação* —, que a aritmética não sabe dizer sem um segundo nó atrás dela; com `t = 1` os dois coincidem, e **é essa coincidência que prova a lei** em vez de a acusar de cópia. ⚠️ **A lei DEGENERADA do `value.step`** (largura zero colapsa no degrau duro) mora numa função só (`band_t`) e o modo novo a **HERDA** — uma 2ª cópia concordaria hoje e divergiria ao primeiro ajuste da guarda. **O teto de `steps` é DERIVADO e nomeia o recurso:** `2^24`, o último inteiro que um `f32` resolve — acima dele `floor(t·(steps+1))` não separa dois níveis vizinhos e o controle deixa de controlar; o slider para em 32 porque é onde a mão trabalha, e o `steps` esconde-se por `ParamGate` fora do modo que o lê. **A cena `=41` é um GRÁFICO:** dez fileiras de 48 peças cuja **posição Y É o valor**, então cada fileira desenha o PERFIL da função — e ⚠️ **nenhuma está sozinha**, cada modo novo tem o vizinho do MESMO nó sobre a MESMA entrada, porque a pergunta não é *"apareceu alguma coisa?"* e sim *"apareceu coisa DIFERENTE?"*. **⚠️ E O SWEEP DE PARIDADE NASCEU VERMELHO NUM MODO QUE JÁ SHIPAVA HÁ MESES** (`quantize` Floor, `max |d| = 5e-1` — exactamente um degrau), com o diagnóstico impresso pelo próprio gate: **`i = 575`, UM de 576**, o último elemento da rampa, onde `t = 1` exactamente. **O mecanismo não é defeito de kernel:** a rampa é `i/(N−1)` e no device o último elemento sai **um ulp abaixo de 1,0** — o que o ADR-0126 admite e todo gate de paridade absorve com ε —, mas **`floor` é DESCONTÍNUA**, um ulp abaixo de `4,0` devolve `3`, e o degrau inteiro atravessa o ε que existia para absorver o ulp. *Uma tolerância de VALOR não sobrevive a uma função que transforma valor em ÍNDICE.* ⚠️ **E a primeira tentativa de dodge falhou também, por erro MEU de aritmética** (passo `0,3` pôs `i = 460`, `t = 0,8` exacto, noutra fronteira: eu "provara" que `287,5 + 86,25k` nunca é inteiro **conferindo só `k` múltiplo de 4**). **A regra que ficou é estrutural:** os **EXTREMOS** do alcance são sempre candidatos a fronteira (`t = 0` e `t = 1` mapeiam exactamente para `out_lo`/`out_hi`), então escolhe-se um alcance cujos extremos **não** sejam múltiplos do passo. ⚠️ **E é por isto que o gate irmão de `value.quantize` era verde:** a fixture dele dirige um **LFO**, cujos valores não pousam em fronteira nenhuma — verde por **sorte de fixture**, não por prova. ⚠️ **E um gate MEU nasceu vermelho sobre código correto:** a barra única de `0,05` para *"os modos diferem"* é **INATINGÍVEL** no par cúbica × quíntica — a separação máxima entre `3t²−2t³` e `6t⁵−15t⁴+10t³` é **0,0527** do alcance, e a fileira escala por 0,8 ⇒ o teto **FÍSICO** daquele par é `0,042` (medido: **4,29e-2**); a barra passou a ser **por par**, derivada daquele número. *Uma barra que código correto não consegue satisfazer não é rigor, é um gate partido.* **Medido na RTX: 12/12, pior delta 3,8e-6** contra separações de modo de **1,1e-1 a 1,5e0**; **47 gates de unidade + 7 da cena, 6 mutações, 6 sangram**; censo re-medido **123 nós · 521 params · 502 com hint · 148 com unidade**. **Zero schema** (`PROJECT_SCHEMA` **70** intocado) · contrato congelado **3/3** (os params novos vivem no `MANIFEST` do NÓ, e o `ParamGate` é side-metadata do registry) · **zero `Cargo.toml`**, **nenhuma dep nova**, **nenhum ADR**. Smoke: **`env PH2D_GPU_COOK_DEMO=41 cargo run -p ph2d-host-desktop --release`** — ⚠️ **a cena imprime as dez fileiras nomeadas; se a lista não aparecer, PARE.** As três leituras: os dois **dentes de serra** diferem só na metade ESQUERDA · as duas **escadas** só no MEIO (o Truncate tem um degrau de largura **DUPLA** sobre a origem) · e as fileiras 5-7 são a **MESMA** rampa como reta, escada e S. **⬛ O GRUPO B — O RUÍDO E O RELÓGIO: quatro P1 em dois nós, e a metade cara já estava paga pela TERCEIRA vez (2026-08-15, cena `=42`, **smoke OK**; folha [15_value](docs/Motion%20Nodes/89_conferencia/15_value.md)):** os dois geradores **TEMPORAIS** do domínio de valor — `value.noise` ganha **`lacunarity`**, **`loop_period`** e o **`pan`** (x,y); `value.lfo` ganha a régua **BPM**. Placar da folha 15: **11 → 7** P1. **Os quatro defaults reduzem ao mundo anterior, cada um por um MECANISMO diferente** — a lacunarity porque a folha `ph2d-fbm` já recebia o número pelo `Spec` (mudou de ONDE ele vem, nunca a multiplicação), o laço porque `loop_times` devolve `(t, t, 0)` e a segunda amostra **nem é avaliada**, o pan porque somar `0.0` a uma coordenada não a move, e o BPM porque `time_mode = Seconds` lê o `period` de sempre; é por isso que são quatro gates de identidade e não um. ⚠️ **A METADE CARA JÁ ESTAVA PAGA, a terceira vez seguida nesta conferência** (depois do TEXTO e do SOM): `ph2d_fbm::loop_times` **existe e TRÊS nós já a consomem** (`motion.noise` · `force.curl` · `force.wind`) — este era o único da família a ler `t` linearmente —, e o **vocabulário do BPM** (`time_mode` + `bpm`) já é o do irmão `motion.oscillator`, de quem esta wave espelha a faixa (20..300, *a de uma MÚSICA e não a de um período*), o `ParamGate` (*só a régua escolhida aparece: dois números na tela sobre a mesma grandeza são pior que um botão morto*) e a conversão — ⚠️ **RECÍPROCA, e é isso que faz de "BPM" a mesma palavra nos dois nós:** ele fala Hz e faz `bpm/60`, este fala segundos-por-ciclo e faz `60/bpm`. ⚠️ **O piso do BPM é o `MIN_PERIOD` que o nó já tinha** e não um `MIN_BPM` novo (*um segundo guarda é um segundo lugar onde a mesma degenerescência é decidida*): um BPM zero dá `inf`, e `t/inf` é 0 — fase congelada, valor finito, **nunca NaN**. ⚠️ **O `pan` mede RETICULADO e não MUNDO**, e o gate `a_pan_of_one_is_a_seed_of_one` prova-o **byte a byte** — a alternativa (`(px+pan)·frequency`) faria o mesmo controlo medir duas grandezas diferentes conforme o `Sample` fosse Index ou World. ⚠️ **E a consequência honesta é que `pan_y` e o `seed` são o MESMO eixo:** o que os separa é o **GESTO** (passo 1 e widget `Seed` contra passo 0,01 e slider), não a matemática; quem é genuinamente novo é o **`pan_x`**, o eixo do TEMPO, onde não existe seed nenhum — e é por isso que o pan é um **VETOR** e não meio. ⚠️ **DOIS números saíram da MEDIÇÃO e derrubaram a minha aritmética:** a parte **FRACIONÁRIA** de uma coordenada de reticulado morre em **2²³** (onde o ULP de um `f32` chega a 1,0 e `x − floor(x)` é zero em todo ponto), **não em 2²⁴** como eu tinha escrito — *o ruído deixa de ser coerente uma oitava antes de deixar de ser um número* —, e o tecto da **lacunarity é função da COORDENADA** (`~9,8` perto da origem, já **4** numa grade grande), então ela **NÃO leva `ParamHardMax`**: um único número digitável seria certo numa cena e errado na seguinte, e 4 é o que se sustenta na PIOR. ⚠️ **E o comentário do `loop_len` no irmão `motion.noise` prometia um `ParamHardMax` que ele NUNCA registrou** — medido, o shell resolve `param_hard_max(..).unwrap_or(max)`, logo a caixa **PARA nos 30 s** e um ciclo mais longo é hoje inalcançável ali; corrigido para dizer o que é verdade (subi-lo é decisão do dono daquele nó). **A costura fecha ao BIT no device** (`0e0` medido, contra **5,22** de deriva no controle sem laço). ⚠️ **E o gate do FECHO não bastava — a mutação achou o buraco:** neutralizar a **MISTURA** sobreviveu a ele, porque o **wrap sozinho já fecha o VALOR** (em `t = L` a folha devolve `u = 0` ⇒ `campo(0)`); o que a mistura acrescenta é a **DERIVADA**, e sem ela chega-se à costura com `campo'(L)` e sai-se com `campo'(0)` — *um tranco a cada volta*. ⚠️ **E o primeiro oráculo que escrevi para isso REPROVAVA o produto:** ele comparava as duas inclinações por diferenças **unilaterais**, e as duas diferenças unilaterais de uma função lisa já diferem por `δ·f''` (medido: 2,5e-1 com quatro oitavas) — *eu estava a medir a curvatura do campo e a chamar-lhe costura*. O oráculo que ficou não tem número escolhido (`the_seam_is_not_special`): compara o salto de inclinação NA costura com o maior salto que o mesmo campo produz no interior da volta — **55,3 contra 0,46** sob a mutação. ⚠️ **E o SWEEP de paridade nasceu VERMELHO no próprio CONTROLE** (lacunarity 2, o mundo que já shipava, `1,3e-4` contra um ε de `1e-4`) — e **não é um ramo errado**: sobre um campo de amplitude 9,58 aquilo é **1,36e-5 RELATIVO**, acumulação de FMA sobre cinco oitavas. *O ε absoluto dos irmãos era certo **para a fixture deles**, e a minha moveu a amplitude* ⇒ o orçamento mudou de **FORMA** (relativo, `2e-4` = o pior medido com 3,4× de folga), e ⚠️ **isso NÃO revoga a regra do irmão** (*"não conserte alargando o ε"*): lá o erro era um **degrau inteiro** de uma função descontínua, aqui a separação que o gate procura está **três ordens** acima do orçamento. ⚠️ **E o pior caso ser a `lacunarity 3` é a MESMA medição vista do outro lado** — uma lacunarity maior põe o topo da pilha num ponto maior do reticulado, onde há menos bits fraccionários; *a degradação é do param, não do kernel*. **A cena `=42` julga-se por PLAY, e é a primeira desta conferência que não cabe numa foto:** as fileiras 1-2 são o par do laço (a de baixo volta à MESMA forma a cada 2 s, a de cima nunca) e as outras oito estão **CONGELADAS de propósito** — *uma comparação de FORMA não pode ser também uma comparação de instante*. ⚠️ **A terceira leitura que eu tinha escrito para ela era FALSA, e quem a derrubou foi um gate MEU:** ela dizia *"pan contra seed — deslize contra re-sorteio"*, e o `a_pan_of_one_is_a_seed_of_one` prova que são o mesmo eixo ⇒ eles **não podem** desenhar coisas diferentes; a cena mostra agora o pan nos seus DOIS eixos. ⚠️ **Dois defeitos de oráculo na mesma cena:** o gate do lock-step nasceu vermelho por **1,15**, que é o `ROW_GAP` da própria cena (ele comparava Y **CRU**, e cada fileira é levantada pelo seu `offset_y` — *comparar duas fileiras cruas mede o LAYOUT, não o campo*); e um terceiro caso virou **propriedade em vez de falha** — em `t = 0` o termo `t/period` **desaparece**, então nenhuma régua pode importar, e o instante degenerado ganhou gate próprio em vez de ser escondido. ⚠️ **O pan é mostrado ESTATICAMENTE de propósito:** um param **dirigido por fio EXCLUI o nó da GPU** (`eligible` recusa), então a demonstração animada teria posto três fileiras na CPU — as dez são reivindicadas de ponta a ponta, com gate headless. **Medido: sweep 10/10 + a costura, 32 gates de unidade + 10 de cena, 8 mutações, 8 sangram** (1,4e-1 a 5,1e-1 relativos) · censo **123 nós · 527 params · 508 com hint · 149 com unidade**. **Zero schema** (`PROJECT_SCHEMA` **70** intocado, `project.rs` com diff vazio) · contrato congelado **3/3** · **zero `Cargo.toml`**, **nenhuma dep nova**, **nenhum ADR**. LOC: `value-noise/lib.rs` bateu **814 > 700** ⇒ `mod tests` para o irmão `lib_tests.rs`, o precedente dos dois que já existiam. Smoke: **`env PH2D_GPU_COOK_DEMO=42 cargo run -p ph2d-host-desktop --release`** — ⚠️ **a cena imprime as dez fileiras nomeadas; se a lista não aparecer, PARE** — e ⚠️ **DÊ PLAY**: as fileiras 1-2 são a leitura que uma foto não responde (a de baixo repete a cada 2 s), a 9 tem de andar em **LOCK-STEP** com a 8 (0,5 s por ciclo e 120 BPM são o mesmo número em duas réguas) e a 10 visivelmente mais rápida. **⬛ O GRUPO C — AS ESTATÍSTICAS: seis linhas fechadas, uma REFUTADA por medição, e a recusa do device como decisão de CORREÇÃO (2026-08-15, cena `=43`, pendente de smoke; folha [15_value](docs/Motion%20Nodes/89_conferencia/15_value.md)):** os dois nós que respondem *o que este CONJUNTO é* — `value.reduce` ganha **`Range` · `Variance` · `StdDev` · `Median`** e as duas portas opcionais **`mask`** (quem é CONTADO) e **`group`** (um agregado por bin); `value.smooth` ganha o **`weight`** da janela (Box · Triangle · Smooth). Placar da folha 15: **7 → 6** P1 — fecha cinco P1 e um P2, e **refuta** o sexto. **Os defaults reduzem ao mundo anterior, e no `smooth` isso é ARITMÉTICA e não promessa:** os pesos do `Box` são `1.0`, `1.0 * x` é exactamente `x` em IEEE-754, e `Σ 1.0` sobre `2r+1` taps é exactamente o divisor inteiro que shipava ⇒ **bit a bit**, gateado contra o filtro CONGELADO. ⚠️ **A decisão central da wave é CORREÇÃO acima de VELOCIDADE, e ela tem um número:** a variância de UM passo (`E[v²] − E[v]²`) é a única exprimível no canal de redução — as reduções correm todas ANTES do kernel e nenhuma lê o resultado da outra —, e ela foi **construída, MEDIDA e descartada**: sobre um campo de desvio 1 ela reporta **0,50** em média `1e3` e **55,4** em `1e5`, e num campo **CONSTANTE** de magnitude `1e5` — onde a resposta certa é a mais óbvia que existe — reporta desvio **71,6**. ⇒ `Variance`/`StdDev` passam a `Σ(v−μ)²/N` em **dois passos** e **RECUSAM o device** (`applicable`) ao lado da `Median`, que não é monóide; *um número errado apresentado como certo é pior que um caminho mais lento*, e o caminho de device continua a existir por composição (a cadeia de cinco nós que a própria folha já media). ⚠️ **As duas portas novas não custaram canal nenhum:** elas são `ColumnAccess::RefuseIfPresent`, o mecanismo que o `motion.integrate` estreou — ligadas, o nó recua para a CPU; **desligadas, `input_edge` devolve `None` e nada muda**, o que é o CONTROLE que impede a leitura preguiçosa *"declare a recusa e pronto"* (uma binding na porta errada tiraria do device a família inteira). E `in` continua na porta **0**: as duas são APENDADAS, então toda aresta salva pousa onde pousava. ⚠️ **A máscara escolhe quem é CONTADO, nunca quem é RESPONDIDO** — o agregado é difundido a TODAS as peças, porque a razão de existir do nó é a cadeia `reduce → math(Subtract)` sobre o campo inteiro; e um conjunto vazio devolve **0** em todos os modos, porque a identidade do `Min` é `+∞` e uma máscara que não selecciona nada a difundiria por N elementos. ⚠️ **E a linha `iterations` do `smooth` foi REFUTADA com a relação INVERTIDA:** a folha supunha que a forma do peso *cavalga* nas iterações (*"3 passes de box ≈ gaussiana"*), e a aritmética é que `N` convoluções de um box são uma **B-spline de grau N−1**, ou seja um PESO — logo o peso é o parâmetro geral. **Medido:** o raio que melhor reproduz `N` passes de raio `r` é **o SUPORTE deles, `r·N`** (`2×3 → 6` · `4×3 → 12` · `6×4 → 23` · `8×3 → 25`), a **1-2% da amplitude**, contra **5,3×** mais erro de uma janela Box no mesmo alvo. E o peso tem o que a contagem não pode ter: **um passe, logo o device** (`N` passes precisariam de `N` dispatches, cada um lendo o resultado inteiro do anterior). ⚠️ **O que tornou a equivalência ALCANÇÁVEL foi o `ParamHardMax` novo do `radius`** — MEDIDO em **0,76-1,06 ns por tap**, com `r = 512` num campo de 10 000 elementos a custar **10,15 ms**, o quadro de 60 fps inteiro ⇒ teto **512**, um recurso de TEMPO e não de precisão; sem um teto digitável acima do slider de 16, `r·N` ficaria fora da UI. **⚠️ TRÊS gates meus nasceram reprovando código CORRETO, e os três eram fixture:** o dos perfis do `smooth` comparava o **CENTRO** de um pico, onde Triangle e Smooth dão **exactamente o mesmo número** (os dois perfis são partições da unidade, então `Σw` sai igual nos dois) — a divergência mora no OMBRO · o do `Range` comparava a altura CRUA de uma lane com o vão do campo, e uma altura carrega o `offset_y` da BANDA (o mesmo erro que o lock-step do grupo B pagou como `1,15`) · e o da cena media **CURVATURA** afirmando `Box > Triangle > Smooth`, quando o medido é **`Box 0,100 · Smooth 0,040 · Triangle 0,027`** — um S *tem* de curvar mais no meio para ser chato nas pontas; o que ordena os três é o **ARRANQUE da rampa** (`1,00 · 0,14 · 0,055`), que é também o que o olho lê. **Medido: paridade 9/9 na RTX, pior delta 8,6e-6 relativo** contra um orçamento de 2e-4 (23× de folga), com gaps de modo de 0,18 a 22,9 · **37 gates de unidade + 9 de cena + 2 de PLANO** (estes rodam **sem adapter**, e são a metade mais importante: uma recusa não honrada não daria erro — o `switch` cairia no `default` e o campo inteiro receberia a SOMA) · **10 mutações, 10 sangram** · censo re-medido **123 nós · 528 params · 509 com hint · 149 com unidade**. **Zero schema** (`PROJECT_SCHEMA` **70**, `project.rs` com diff vazio) · contrato congelado **3/3 + 4/4** · **zero `Cargo.toml`**, **nenhuma dep nova**, **nenhum ADR**. LOC: o `value-reduce/lib.rs` cedeu a LEI ao irmão `stats.rs` (*o que um conjunto de números É* contra *o que o nó FAZ com ele*) e os dois `mod tests` foram para irmãos. Smoke: **`env PH2D_GPU_COOK_DEMO=43 cargo run -p ph2d-host-desktop --release`** — ⚠️ **a cena imprime as oito BANDAS nomeadas; se a lista não aparecer, PARE**, e ela **julga-se PARADA** (nada aqui depende do relógio). ⚠️ **A forma é diferente das duas anteriores de propósito:** ali a comparação era de FORMA, que sobrevive a um deslocamento vertical; aqui é de **ALTURA**, então as cadeias de uma banda partilham a base — as peças PEQUENAS são o campo, as GRANDES são a estatística sobre ele. As quatro leituras: **(1)** as retas da `Mean` e da `Median` **não coincidem** (o campo é enviesado de propósito: num simétrico elas cairiam no mesmo lugar e a banda não diria nada) · **(2)** ligar a `mask` **SOBE** a reta · **(3)** ligar o `group` transforma a reta numa **ESCADA de quatro degraus** · **(4)** o `Range` mede o vão inteiro e o `Std Dev` a dispersão; e as bandas 6-8 filtram o MESMO degrau com o mesmo raio, com a de cima a arrancar **de repente** e a de baixo num **S**.
 **⬛ O GRUPO D — A TABELA E A SEMENTE: o que NÃO cabe num punhado de f32, e nenhuma das duas metades custou um canal novo (2026-08-15, cena `=44`, pendente de smoke; folha [15_value](docs/Motion%20Nodes/89_conferencia/15_value.md)):** o **W-E** que a própria folha propunha. `value.pattern` ganha a **TABELA** — a lista inteira digitada como **text param**, sem o teto de oito — e `value.instance_field` o **`unique_per_node`**, que decorrelaciona dois nós de mesma semente. Placar da folha 15: **6 → 4** P1. ⚠️ **As duas linhas são a MESMA pergunta:** o `NodeManifest` é f32-only por contrato congelado (ADR-0039), então uma LISTA de comprimento arbitrário e a IDENTIDADE do próprio nó são exatamente o que não cabe ali. ⚠️ **E a metade cara já estava paga nas duas, a quarta vez nesta conferência** (depois do TEXTO, do SOM e das formas): o **`LutSpec`** — construído em 25/07 para a curva — **já lê um text param e leva um vetor ao DEVICE**, então a tabela é FIAÇÃO e não infraestrutura; e o uniforme da identidade é **DERIVADO do kernel** (`codegen::declares_node_key`, o molde exato de `declares_window`/`broadcasts_anything`) em vez de um 7º canal do `KernelResolver` — *se o corpo menciona `params.node_key`, o campo existe*, e as duas metades (a que DECLARA e a que PREENCHE) chamam a MESMA função. ⚠️ **O kernel da tabela lê o BUFFER direto, nunca o `vp_table_sample(t)` que o gerador também emite:** aquele acessor **LERPA** entre vizinhos — certo para uma curva, errado para um sequenciador, cujos passos são autorados um a um; um `t` derivado do índice passaria por `(i/last)*last`, que em `f32` não devolve `i` para todo par. ⚠️ **A CONTAGEM viaja no slot 0** porque `arrayLength` devolve a capacidade e não quantos valores o artista escreveu, e um param f32 paralelo seria a segunda cópia de um número que a string já determina. **`len == 0` é o sinal de *nada autorado*** — o mesmo que a lista vazia dá à CPU, então os dois caminhos concordam sem cada um ter a própria regra. ⚠️ **Os dois defaults reduzem LITERALMENTE**: tabela ausente ⇒ os nove controles legados byte a byte (e o `ParamGateText` os **esconde** quando há tabela — o precedente do `motion.spline_wrap`); `unique_per_node = 0` ⇒ `decorrelate` devolve a semente intacta. Ligar a decorrelação por default mudaria a aleatoriedade de **todo grafo já salvo** (lei 6). ⚠️ **Somar o `node_key` CRU seria colisão fácil** — `(semente 5, id 3)` e `(semente 3, id 5)` cairiam na mesma semente efetiva —, então a identidade é multiplicada pelo golden-ratio antes de somar, o mesmo espalhamento que o hash já faz um degrau adiante. ⚠️ **O teto da tabela diz de que RECURSO é, e foi MEDIDO:** o buffer da LUT é **4100 bytes por nó, CONSTANTE** (a `resolution` de um `LutSpec` é uma const), e o `fill` cobra **0,08 / 0,55 / 2,02 / 7,94 µs** em 8 / 64 / 256 / 1024 entradas — o de antes era **8**, e vinha de *"params são f32"*, não de recurso nenhum. Acima do teto a lista **TRUNCA** (o artista mantém os primeiros mil), e um token malformado **recusa a string inteira** (a política do `ph2d_curve::parse`: pular o token apagaria um passo em silêncio). ⚠️ **Um gate de CONVENÇÃO pegou o NOME que eu escolhi:** o param nasceu `use_node_as_seed` (o nome da feature no Cavalry, e o que a folha escreve) e o `a_seed_wears_the_seed_widget` reprovou — um scanner por nome lê `*_seed` como *uma semente* e exige o widget dela, quando este é um **booleano SOBRE** a semente. O rótulo do painel já dizia a verdade (*Unique Per Node*); o identificador passou a dizê-la também. **Medido na RTX: paridade 6/6 com `max |d| = 0e0` em TODOS os casos** (a tabela é lookup indexado e a semente é hash inteiro — não há aritmética que divirja, ao contrário dos 8,6e-6 do grupo C) · **42 gates de unidade + 6 de cena + 4 de substrato de layout + 4 do `node_key`** · **11 mutações, 11 sangram**. ⚠️ **Duas fixtures minhas nasceram VÁCUAS e as duas eram a mesma doença — o CONTROLE não podia discriminar:** o gate da tabela usava valores `0..10`, que **CONTÊM** os oito dos slots, então *"o legado não contribuiu"* era insatisfazível (os conjuntos têm de ser disjuntos: tabela fracionária, slots inteiros); e o gate *"o `node_key` é o último do layout"* passava `grid: None`, ou seja **não existia campo que pudesse vir depois** — a mutação que o move para antes da grade **SOBREVIVEU** até a fixture declarar uma grade. ⚠️ **E o gate dos gêmeos da cena reprovou por comparar Y CRU entre fileiras**, medindo o `BAND_GAP` de 1,05 em vez do campo: é a **terceira vez** que este repo paga a lição (o `1,15` do grupo B, o `offset_y` do `Range` no grupo C) e a cura é a mesma — o oráculo lê a **SILHUETA**, com a barra derivada do ULP da própria normalização (1e-5, cem vezes acima do ruído e cinco ordens abaixo da amplitude). **Zero schema** (`PROJECT_SCHEMA` **70**, `project.rs` com diff vazio) · contrato congelado **3/3** (o param novo vive no `MANIFEST` do NÓ; a tabela é text param, que mora no `Graph`) · **1 `Cargo.toml`** (a shell ganha `ph2d-node-value-pattern` para a cena autorar a chave pelo NOME — **aresta interna de path**, o precedente do `value-attribute` na cena `=38`) · **nenhuma dep externa nova** · **nenhum ADR**. Censo re-medido **123 nós · 529 params · 510 com hint · 149 com unidade**. LOC: `cook.rs` cruzou 700 com o campo novo ⇒ split por responsabilidade em **`cook_eval_ctx.rs`** (*o que um NÓ vê* contra *o motor que o alimenta*), com os campos em `pub(super)` — quem CONSTRÓI um `EvalCtx` é o motor, e mais ninguém. Smoke: **`env PH2D_GPU_COOK_DEMO=44 cargo run -p ph2d-host-desktop --release`** — ⚠️ **a cena imprime as seis fileiras nomeadas; se a lista não aparecer, PARE**, e ela **julga-se PARADA**. As duas leituras: **(1)** o dente de serra da fileira 2 é **quatro vezes mais largo** que o da 1 (doze passos contra três) · **(2)** as fileiras **3 e 4 TÊM de ser idênticas** — é o defeito, e sem ele à vista *"ligado eles diferem"* não provaria nada — enquanto as **5 e 6** não podem ser. ⚠️ **E a FACE dessa row foi CONSTRUÍDA e REVERTIDA no mesmo dia, por veredito do Enio ao olhar para ela (*"ficou pior. Volte como estava antes"*):** a lista de números virou por algumas horas uma **faixa de barras arrastáveis** (`ParamWidget::Steps`, a gêmea numérica do `Palette`), com a LEI mudada para a folha `ph2d-steps` e um `format` inverso exato de `parse` para que um arrasto reescrevesse **só aquele passo**; o índice viajava partido nos dois `u8` do `CurvePoint` e o campo cru ficava atrás de um checkbox `Type`. **Revertida INTEIRA** — a row volta a ser `ParamWidget::Text` e a lei volta para `ph2d-node-value-pattern::table`, e a crate-folha **saiu junto porque a própria razão dela morreu com o strip** (ela existia para *dois* consumidores independentes, e sem a faixa há um). ⚠️ **O veredito é de PRODUTO e nenhum mecanismo foi nomeado**, então uma segunda tentativa **começa perguntando o que ficou pior** — e não reconstruindo isto: a árvore inteira sobrevive no commit **`ae35416bd`** (e nos três abaixo dele), com 13 gates e 8 mutações já escritos. **⬛ O GRUPO E — AS DUAS PERGUNTAS QUE O GRAFO NÃO SABIA FAZER, e uma CÉLULA que envelheceu (2026-08-15, cena `=45`, **smoke OK**; folha [15_value](docs/Motion%20Nodes/89_conferencia/15_value.md)):** ele não sabia perguntar ***"a é maior que b?"*** num nó só, e não sabia **DIZER** que um nome de coluna não resolve. Placar da folha 15: **4 → 1** P1 — e o que sobra é pergunta de **CONTRATO CONGELADO** (`value.switch`, N entradas: `NodeManifest.inputs` é `&'static [PortSpec]`), logo fora de qualquer wave de params. **(A) A COMPARAÇÃO** — `value.math` ganha os **seis modos do Compare do Blender, na ordem dele**, apendados em 8..13, com a saída sendo uma **MÁSCARA 0/1** (o que cinco famílias deste grafo já consomem) e a tolerância num param **`epsilon`** gateado aos dois ops de igualdade. ⚠️ **No `value.math` e não num nó novo, e a razão é o que ele JÁ possui:** a regra de broadcast 1→N, a lei de contagem `max(len_a, len_b)`, o guard do divisor e o layout de bindings de dois campos que fazem broadcast — um irmão precisaria de **tudo** copiado, e duas portas para *"combine dois campos"* divergem em silêncio; a referência **SEPARA** (Blender tem Math e Compare; o TD põe a comparação no Logic CHOP) e a divergência fica **NOMEADA** no doc do kernel. ⚠️ **Paridade com o device `max |d| = 0e0` nos cinco casos** — bit a bit **por construção** e não por afinação: os operandos são os mesmos bits, o IEEE-754 especifica cada predicado, e os dois lados produzem os literais `0.0`/`1.0`. ⚠️ **`Not Equal` é a comparação DIRETA, nunca a negação de `Equal`** — as duas formas só divergem no **NaN**, e ali a negação daria respostas OPOSTAS nos dois lados da fronteira CPU/WGSL. **(B) O NOME QUE NÃO RESOLVE** — crate `ph2d-motion-diagnose` ganha `unresolved_reads` (módulo **próprio**, `reads.rs`) + badge ⚠ + explicação que **CITA o nome**. ⚠️ **A rota ESTRUTURAL foi construída em pensamento, MEDIDA e descartada:** a união dos nomes que os `ColumnBinding` do repo **declaram** escrever tem **20** entradas contra **29** que o CPU de facto escreve — faltam `texture_id`, `geometry_id`, `uv_rect`, `nrm`, que são as **CONVENÇÕES de stream** que esta §5 documenta ⇒ um badge em `texture_id`, que é o falso positivo que o **ADR-0155 já pagou uma vez** (o `MissingSource("P")` do Boids). A resposta vem do **stream COZIDO**, o mesmo objecto que o `eval` lê ⇒ **zero falsos positivos por construção**, com a regra **DERIVADA do `StreamOp::Project`** (nó novo com essa forma nasce coberto). ⚠️ **Módulo próprio e não um `Deficit` novo, e a distinção é o ponto:** todo diagnóstico do ADR-0155 é **puro** (grafo + registry), este precisa de um **cook** — misturá-los no mesmo enum esconderia exactamente isso. ⚠️ **E a porta das colunas devolve `Option`, não `Vec`:** *não sabemos* (nó não cozido, porta > 0 num quadro de device) e *sabemos, e não há colunas* levam a decisões **opostas**; para o *column picker* — que já tinha esta escada dentro do painel de params e agora **DELEGA** — as duas dão a mesma lista de chips, e é por isso que ele viveu sem a distinção. **(C) A IDADE NORMALIZADA — RECONFERIDA e DERRUBADA por medição:** a célula marcava P1 dizendo *"`life` não é coluna legível ⇒ a vida é digitada à mão e desincroniza do emissor em silêncio"*, e **as duas metades são falsas** — o `motion.emitter` escreve `life` na CPU **e** no device, e o `value.attribute` a oferece **no picker** ao lado de `Age`; a cadeia é de **três nós, exacta**, e o gate mede a metade que a célula nega (**dobrar o `life` do emissor METADE o resultado**, o que um número digitado à mão não faz). ⇒ **P1 → P2** (ergonomia). ⚠️ **É a TERCEIRA célula desta folha a envelhecer antes de alguém voltar a ela** — *o que se perde ao não reconferir não é tempo, é construir o que já existe.* **15 gates novos, 8 mutações, 8 sangram** — ⚠️ e **DUAS lições de fixture, as duas minhas:** o gate da FRONTEIRA da igualdade nasceu **VERMELHO** com `1,1` e `0,1`, porque `(1.0f32 − 1.1).abs()` vale `0.100000024` e é **MAIOR** que `0.1` (ele media a representação do decimal, não o predicado — um gate de fronteira precisa de operandos cuja diferença seja **exactamente** o número que ele afirma); e o *"a ordem ignora a tolerância"* **SOBREVIVEU** à mutação `a > b + eps` porque **os três pares da fixture tinham todos `a <= b`** — somar epsilon só muda a resposta onde a comparação era VERDADEIRA. ⚠️ **E uma mutação sobreviveu por DEFESA EM CAMADAS:** o guard de `node_help_enabled` dentro do helper é redundante *através dos dois chamadores de hoje*, que já saem cedo ⇒ fica, com o doc a dizer o que a medição mediu, e com **gate próprio** exercitando a camada directamente. ⚠️ **E o gate de CONVENÇÃO pegou a faixa que EU escolhi:** o slider do `epsilon` nascia em `0..1` com default `0.001`, e um pixel de arrasto (track de 154 px) movia **6× o próprio default** — a faixa é **DERIVADA** (`span/154 ≤ default` ⇒ `max ≤ 0,154` ⇒ **0,1**) e o teto digitável vai num `ParamHardMax`, que é a prescrição que a própria mensagem do gate traz. ⚠️ **E eu SOBRESCREVI um arquivo existente ao cortar LOC** — o `motion_bridge.rs` estava a **UMA linha** do teto de 600 (dívida latente herdada) e o módulo novo o cruzou; o corte foi para um `motion_bridge_clock.rs` que **já existia** (o `ticks_owed`), recuperado do `HEAD` e depois usado como o destino CERTO (é o mesmo assunto). **`PROJECT_SCHEMA` 70 intocado** · contrato congelado **3/3** · censo **123 nós · 530 params · 511 com hint · 149 com unidade** · **zero `Cargo.toml`**, **nenhuma dep nova**, **nenhum ADR**. Smoke: **`env PH2D_GPU_COOK_DEMO=45 cargo run -p ph2d-host-desktop --release`** — ⚠️ **a cena imprime as cinco bandas nomeadas; se a lista não aparecer, PARE**, e ela **julga-se PARADA**. As três leituras: **(1)** os dois degraus de `Greater`/`Less` **PARTICIONAM** a fileira (24 e 24 de 48 medidos) · **(2)** as duas bandas de `Equal` diferem **só** no epsilon e medem **4 peças contra 18** · **(3)** ⚠️ **a banda 5 sai PLANA de propósito** — o que a wave acrescenta **não está no canvas**: abra o **painel de GRAFO**, o nó tem um badge ⚠, e clicar nele diz o nome que você escreveu. **⬛ O GRUPO F — O ENVELOPE TEM FORMA, e o defeito de side-metadata era QUATRO vezes maior que a célula dizia (2026-08-15, cena `=46`, pendente de smoke; folha [07_tempo_estilisticos](docs/Motion%20Nodes/89_conferencia/07_tempo_estilisticos.md)):** os dois nós que respondem *que forma tem uma coisa que acende e apaga* — o `motion.strobe` a partir de um **PULSO**, o `motion.delay` a partir de um **DEGRAU**. Placar da folha 07: **11 → 5** P1. **(A) O `motion.strobe` ganha ATTACK · HOLD · SHAPE · PROBABILITY**, num nó que se auto-declarava *"the minimal ADSR"*: subida instantânea e queda exponencial fixa. ⚠️ **E é a FASE que os torna exprimíveis, não mais um multiplicador:** um decaimento geométrico é *sem memória*, mas *"estou subindo ou caindo, e há quanto tempo?"* não sai de um número só — um `glow` de 0,5 pode ser uma subida a meio caminho ou uma queda já avançada. O estado ganha a **idade**, e o envelope passa a ser escolhido por ela (`age < attack` ⇒ a rampa · `age <= attack+hold` ⇒ o platô · senão ⇒ a recorrência de sempre). ⚠️ **Com `attack = hold = 0` a escada reduz LITERALMENTE** — `0 < 0` falso, `0 <= 0` verdadeiro ⇒ o tick do pulso emite `1.0` e o seguinte cai na recorrência com `glow_anterior = 1.0` —, **byte a byte** e sem um `powf` no meio: derivar a queda da idade (`rate^age`) daria a MESMA curva matemática e **não os mesmos bits**, porque `n` multiplicações não são uma potência. **Os ONZE gates que aquele arquivo já tinha passam SEM UMA EDIÇÃO de asserção**, e é isso que o prova. ⚠️ **A CURVA mora no LOOK e a coluna `glow` continua a ser o envelope CRU** — ao lado do `size_boost` e do `flash_amount`, que são os outros dois controles de aparência que não tocam a memória (a distinção que o nó já fazia com o `falloff`): se ela entrasse no estado, aplicá-la `n` vezes seria **`c(c(c(…)))`**, o *produto sobre a lista* que esta casa já curou quatro vezes no relevo do Painter. E a ordem é **`curva(envelope) × máscara`**, nunca o contrário — o `falloff` é peso ESPACIAL e a curva é forma no TEMPO; mascarar primeiro faria um ponto a meia influência ler um ponto **diferente** da curva desenhada (o gate dedicado existe porque os gates de `falloff` usam `1.0` e `0.0`, onde as duas ordens **coincidem**). ⚠️ **A pista do sorteio avança em todo pulso que CHEGA, não em todo pulso que ACENDE** — se ela só andasse nos aceitos, um recusado re-sortearia **o mesmo número** e a instância nunca mais acenderia; *determinístico e travado é pior que aleatório*. ⚠️ **Sem `seed`, de propósito:** dentro de um nó a pista `(linha, ordinal)` já decorrelaciona tudo, e um seed serve para separar DOIS nós — cuja resposta canônica desta casa é o `unique_per_node` do `value.instance_field`, derivado da identidade do nó; aqui ele seria um controle **inerte no default**. **(B) O `motion.delay` ganha `ticks_down`** — o *Lag up / Lag down* do **Lag CHOP** do TD, e o que faz de um smoother um envelope. ⚠️ Vale **só no `Blend`** (o único modo em que o número é uma TAXA; o `Delay` é consulta ao passado e o `Average` um boxcar), com **gate que prova a LEI e não só a UI** — um documento pode carregar o param num nó em `Average`, e o kernel tem de o ignorar. ⚠️ **`0` = *"o mesmo da subida"*, e nada fica inalcançável:** a lei do one-pole já divide por `max(1.0)`, então **`ticks_down = 1` JÁ É a descida instantânea** (medido, com gate) — o par alternativo (um toggle *Same | Custom* mais o número, o idioma do `Mass: Auto | Manual` da física) seria dois params para o que a sentinela exprime sem perda. E a direção é decidida **POR COMPONENTE**: uma cor que clareia no vermelho e escurece no azul usa os dois tempos no mesmo tick (medido: vermelho **0,9995** subindo, verde **0,3267** descendo, da MESMA origem). **(C) O defeito de side-metadata era QUATRO vezes maior que a célula dizia** — ela marcava o `motion.strobe` (*"~4 linhas"*), e o canal do ADR-0155 tem duas metades: um nó com kernel de GPU **DERIVA** o papel da própria `ColumnBinding`, um CPU-only tem de **DECLARAR**. Medido: **23 derivados · 5 declarados · QUATRO sem canal nenhum** (`strobe` · `trail` · `color_array` · `cull`), e um campo ligado só a um deles era reportado como **INERTE**. ⚠️ **O caso do `motion.cull` é de outra espécie e fica nomeado no arquivo:** ele **TEM** binding de `falloff`, e ela é invisível à derivação — a leitura mora no `GPU_PREDICATE` do `StreamOp::Compact`, e o kernel que o registry guarda é o `PASSTHROUGH` (ADR-0136), enquanto o `consumes` do diagnoser só consulta `gpu_kernel(ty).bindings`. ⚠️ **E o gate não ENUMERA quem precisa declarar** — ele deriva a pergunta do fonte por duas regras que são a definição que o diagnoser usa (*LÊ a coluna* × *não a RE-PRODUZ*), com o `value.attribute` de fora **com motivo**: ele lê a coluna que o **ARTISTA** nomeou, e uma declaração estática seria falsa em sete dos oito canais. ⚠️ **E A CENA ACHOU UM DEFEITO REAL ANTES DO SMOKE:** um strobe com `attack > 0` **INCHAVA SOZINHO no nascimento** — um estado recém-nascido é zero em toda coluna, e `age = 0` significa *"um pulso acabou de acender"*, indistinguível de *"nenhum pulso jamais chegou"*; com `attack = 0` os dois colapsam, e é por isso que o defeito só nasceu com esta wave. A sentinela `NEVER` (`f32::MAX`) **satura** sob `+ 1.0`, então quem nunca acendeu fica para sempre no ramo da queda sobre um brilho zero. **7 mutações no strobe + 4 no delay + 3 na cena, TODAS sangram** — e a do platô exclusivo derruba **treze** gates, que é a byte-identidade a fazer o trabalho. ⚠️ **QUATRO defeitos de gate, os quatro meus, e as mutações acharam os quatro:** o *"a sentinela é o mesmo da subida"* comparava **param AUSENTE contra param explicitamente `0`** — os dois resolvem ao MESMO número, então era o nó comparado consigo mesmo, verde sob qualquer lei que a sentinela pudesse ter · o do ATTACK media *"está abaixo de 75% do pico a meio caminho"*, e isso é verdade tanto para quem **ainda não chegou** como para quem **já passou** (com as duas bandas iguais a de baixo já decaiu a 2% naquele tick) ⇒ o discriminante é **QUANDO** o pico acontece · o do SHAPE tinha uma barra **ABSOLUTA que reprovava produto correto** (o maior passo de uma exponencial é o PRIMEIRO, e uma cauda de 20 ticks larga 25% logo ali: **0,1379** contra uma barra de 0,114 que eu escolhi) ⇒ virou **razão** entre as duas quedas, sem número a calibrar · e o da PROBABILITY confundia *"a batida"* com *"estar aceso"*, recolhendo **seis retratos da MESMA batida** e acusando o sorteio de ter travado. ⚠️ **E a contagem do sorteio foi MEDIDA até ao fim antes de a cena ser afinada, com a conclusão de NÃO a afinar:** as quatro pistas são individualmente justas (`0,2505 · 0,2450 · 0,2452 · 0,2505` sobre 4000 linhas cada) e a média global fecha em **0,2523** — o que a cena calha é um **canto de 1 em 2000** (só uma janela em duas mil acende 37 ou mais das 96). *Alargar a fileira até o número PARECER o pedido seria afinar a demonstração para lisonjear a feature*; o que um sorteio por-peça entrega é uma contagem que **balança**, e é isso que a cena mostra e a mensagem diz. **`PROJECT_SCHEMA` 70 intocado** · contrato congelado **3/3** (os params novos vivem no `MANIFEST` do NÓ; a curva é text param, e o `ParamGate`/`Coupling` são side-metadata do registry) · **1 `Cargo.toml`** (`ph2d-curve` na `ph2d-node-motion-strobe` — **aresta interna de path**, o mesmo motor do `value.curve` e do contour Curve do `field.remap`; uma segunda resposta a *"como esta curva é amostrada?"* divergiria do editor que a desenha) · **nenhum pacote externo novo** · **nenhum ADR**. LOC: o `lib.rs` do strobe cruzou 700 ⇒ `mod tests` para o irmão `lib_tests.rs` por `#[path]` (segue FILHO, então `use super::*` alcança os privados). Smoke: **`env PH2D_GPU_COOK_DEMO=46 cargo run -p ph2d-host-desktop --release`** — ⚠️ **a cena imprime as dez bandas nomeadas; se a lista não aparecer, PARE**, e ⚠️ **DÊ PLAY**: um envelope é uma forma no TEMPO, e uma foto de um instante mostra dois tamanhos sem dizer como se chegou a eles. Os cinco pares: **1-2** a de cima POPA e a de baixo INCHA (picos medidos nos ticks **0** e **30**) · **3-4** a de baixo fica CHEIA meio segundo e **só então** cai · **5-6** a mesma queda, uma que desvanece e outra que **CORTA** (0,1379 contra **0,5700** num tick) · **7-8** a fileira INTEIRA contra ~um quarto **sorteado, e peças DIFERENTES a cada batida** (se forem sempre as mesmas, o sorteio travou) · **9-10** o MESMO degrau, um simétrico e outro que **SALTA e escorre** (subida idêntica **0,7600**, descida **0,4750** contra **0,7422**). **⬛ O GRUPO G — PARA ONDE ISTO VAI: a coluna `vel` ganha um PRODUTOR, e o teto que ninguém mediu era TRÊS nós (2026-08-15, cena `=47`, **smoke OK**; folha [07_tempo_estilisticos](docs/Motion%20Nodes/89_conferencia/07_tempo_estilisticos.md)):** duas frentes da mesma folha. Placar: **5 → 3** P1. **(A) `motion.velocity`, o *compute velocity* do MOPs** — crate nova, folha drop-in. ⚠️ **A metade CARA já estava paga, a quinta vez nesta conferência** (depois do texto, do som, das formas e da tabela): o CONSUMO já estava todo aqui — `value.attribute` **Speed** (a magnitude) · **Direction** (o ângulo, `→ motion.drive(Rotation)` **é** o *align to velocity*) · **lane** (`vel.x`/`vel.y`) · `force.drag` · `force.buoyancy` · `motion.integrate` —, e o canal **Speed** é **OFERECIDO no picker** devolvendo **ZEROS** num stream que nenhum simulador tocou, indistinguível de um nome mal digitado. O que faltava não era a leitura, era o PRODUTOR. ⚠️ **E ele fecha um item de OUTRA família:** a folha 04 §A.2 nomeia o *Squash and Stretch / Motion Stretch* como *"clássico de animação!"* e o deixa **fora de escopo** com o motivo exato — *"é um deformer dirigido pela VELOCIDADE, e nenhum dos sete lê a coluna"*. ⚠️ **NÓ PRÓPRIO e não um campo no `motion.trail`**, embora a célula o sugerisse (*"o rastro tem `P` de dois ticks na mão"*): as linhas que ele emite são **ECOS**, e um eco não tem velocidade própria — pô-lo lá faria *"quero a velocidade"* exigir *"quero um rastro"*, e a referência nomeia a ausência do mesmo jeito (*"o **compute velocity como serviço** falta"*). **A LEI:** `vel = (P − P_anterior) / dt` em **unidades por SEGUNDO** (a régua que o `motion.integrate` fala, `d += v·dt`), pareada por **`id`** quando o stream tem identidade e posicionalmente quando não tem — a mesma regra do integrador, e pelo mesmo motivo: num emitter a linha `i` de hoje não é a linha `i` de ontem. ⚠️ **Quem não tem passado tem velocidade ZERO, nunca uma inventada** (a partícula recém-nascida · o 1º tick de um cook · o tick seguinte a um reset), e reportar outra coisa seria um motion-stretch a esticar um elemento que ainda não se moveu. ⚠️ **NÃO lê `falloff`:** a lei da família vale para quem MODIFICA, e este nó **MEDE** — uma medição mascarada mente, e o `value.attribute(Speed)` a jusante não teria como saber. ⚠️ **E NÃO declara `Coupling::Produces("vel")`**, embora escreva a coluna: o consumidor canônico é o `value.attribute`, que lê a coluna que o **ARTISTA nomeou num text param** e por isso não declara `Consumes` nenhum (uma declaração estática seria falsa em sete dos oito canais dele) ⇒ declarar a produção marcaria como **INERTE** exatamente a cadeia que o nó existe para servir — *a afirmação seria o próprio bug*, a lição que o `MissingSource("P")` do Boids já pagou. O **`smooth`** é o one-pole do `Blend` do `motion.delay` sobre a VELOCIDADE, nunca sobre a posição (filtrar `P` moveria a arte, que é trabalho do delay), com `0` = a diferença crua **byte a byte** e **sem `ParamHardMax`** — medido, um one-pole de constante enorme é *lento*, nunca quebrado, e há gate a afirmar a ausência para a próxima varredura não "completar" a tabela. **(B) O `MAX_INSTANCES` que nunca foi medido, e a célula nomeou UM nó** — medido, **`fx.drop_shadow` e `fx.rgb_split` carregam o MESMO literal `65_536`** pelo mesmo recurso, com modos de falha **OPOSTOS e os três SILENCIOSOS**: o rastro **CLAMPA** as gerações (a cauda encurta), os dois fx se **DESLIGAM** (devolvem a entrada verbatim). ⚠️ A justificativa escrita era uma **CONTAGEM** (*"4096 vivas × 32 ecos já é 131k quads"*), não um custo — no módulo cuja `line/gpu-nodes` mediu **4,19 M partículas em 3,6 ms**. **Medido pela porta do produto** (sonda com fonte a MEXER-SE): a linha emitida custa **~10-28 ns** e o custo é **LINEAR sem joelho** até um milhão ⇒ `65 536` = **1,24-1,83 ms (7-11% de um quadro)** · **`262 144` = ~4-5 ms (~30%)**, o teto novo · `1 048 576` = **~21 ms, acima de um quadro inteiro**. O antigo estava a um **décimo** do que um nó pode gastar. ⚠️ **E a consequência prática é o caso que o próprio comentário citava:** 4096 × 32 = 131 072 linhas era **clampado em silêncio para 16 gerações** e hoje é HONRADO — com o gate a medi-lo **COZINHANDO o grafo**, porque uma comparação de consts é dobrada pelo compilador e seria cega ao `MAX_LENGTH` privado do nó. A const é copiada nas três crates (drop-crates não dependem umas das outras, ADR-0075, como o `falloff_at` das behaviours) e o que a mantém honesta é o gate `the_three_instance_ceilings_agree`, que as compara contra um **QUARTO literal** — compará-las entre si ficaria verde no dia em que alguém as movesse todas juntas por engano. ⚠️ **A SONDA REPROVOU A 1ª VERSÃO DA CENA antes do smoke:** ela punha as seis bandas num CÍRCULO, e a velocidade sobre um círculo é **constante em magnitude** — a banda do tamanho media `[0,4341 … 0,4382]`, as dezoito peças do MESMO tamanho, só maiores que o controle; a cena mostraria *"tudo cresceu"* e não *"incha onde acelera"*, que é a frase que a mensagem promete. As bandas de tamanho passaram a um **VAIVÉM** (rapidez máxima no meio, **zero nas pontas**) e as de direção ficaram no círculo, onde um vaivém **não serviria** (sobre X a direção alterna `0°`/`180°`, e um traço rodado de meia volta **é o mesmo traço**). ⚠️ **E uma mutação "sobrevivente" era o meu HARNESS:** o `assert` do python falhava dentro de uma função de shell que **não propagava o exit status**, então a mutação nunca era aplicada e o verde era *"nada rodou"* — refeita com a âncora conferida, ela sangra **quatro** gates [[feedback_a_negative_search_needs_a_positive_control]]. **Censo re-medido: 124 nós · 535 params · 516 com hint · 153 com unidade** · `PROJECT_SCHEMA` **70 INTOCADO** · contrato congelado **3/3** · **1 crate nova** (folha, glob member, 2 deps internas) · **nenhum pacote externo novo** · **nenhum ADR**. Smoke: **`env PH2D_GPU_COOK_DEMO=47 cargo run -p ph2d-host-desktop --release`** — ⚠️ **a cena imprime as seis bandas nomeadas; se a lista não aparecer, PARE**, e ⚠️ **DÊ PLAY** (uma velocidade é a diferença entre DOIS instantes). Os três pares: **1-2** a de cima tem **um tamanho só** (0,170 em toda peça — literalmente o que o canal Speed desenhava) e a de baixo **incha no meio do vaivém** (0,203 … 0,435) · **3-4** os traços de baixo **apontam para onde vão**, com a tangente a varrer o círculo (**−162,1 a +178,2 graus**) contra um controle **sem `rot` nenhuma** · **5-6** o MESMO tremor, a crua **PISCA** e a suavizada **RESPIRA** (agitação por tick **0,0274 contra 0,0095**). **⬛ O GRUPO H — O DISCO TEM O TAMANHO QUE SE VÊ, e o BLOQUEADOR era do CATÁLOGO (2026-08-15, cena `=48`, **smoke OK**; folha [03_simulacao](docs/Motion%20Nodes/89_conferencia/03_simulacao.md), linhas 60 e 62):** o `motion.collide` passa a ler as **duas** entradas que lhe faltavam. Placar da folha 03: **17 → 15** P1. **(A) O RAIO POR ELEMENTO** (`r_i + r_j`) mora na coluna **`size`** — a MESMA que o `lower_to_instances` desenha. ⚠️ **Não é capacidade nova: é o nó a cumprir a promessa que o próprio doc dele faz** (*"cada instância é um disco de raio `radius`, e pares sobrepostos são afastados até apenas se tocarem"*), e uma instância desenhada com o dobro do tamanho e empacotada como unitária **sobrepõe visivelmente**. `max(|x|,|y|)` porque o disco **CONTÉM** a arte; `abs` porque uma extensão não tem sinal (o contraste deliberado com o `offset` do collider da física, onde o sinal É a lateralidade porque ali o número é uma POSIÇÃO). ⚠️ **E o BLOQUEADOR que a célula nomeava era do CATÁLOGO, não do maquinário:** ela dizia *"nenhum kernel do repo combina `register_grid` com `reduces()`, então é wave própria"* — medido, o sequenciador do `ph2d-gpu-cook` **já roda `run_reduces` DENTRO do laço de sweeps**, ao lado do `build_grid`, com o comentário a explicar por quê (*uma varredura move a própria coluna que a redução lê*). Este é o primeiro kernel a usar os dois, e o `Max` que limita o alcance é **bit-exato em qualquer ordem** (o doc do `ReduceOp` o diz), o que deixa a paridade ser afirmada sobre um conjunto de tamanhos mistos. **(B) O `falloff`** — a espinha MOPs que **~50 nós** já honram e este solver não honrava. ⚠️ **A distinção é o desenho inteiro:** `falloff = 0` torna o disco **TRANSPARENTE** (nem empurra nem é empurrado), que é precisamente o que `inv_mass = 0` **não** significa (pinado é imóvel **e obstáculo**) — o peso do PAR é o produto `f_i · f_j`, então mutar qualquer lado faz o par desaparecer. ⛔ **MEDIDO E REJEITADO, não refaça: a tally da média PONDERADA.** Pôr o peso do par no numerador **e** no divisor o faz **CANCELAR** num par isolado — `falloff = 0,5` desenhava exatamente o mesmo que `1,0` (**0,6 e 0,6**), e o knob seria um controle decorativo; a tally é uma **CONTAGEM**, e o resíduo (*um par a um fio de zero ainda ocupa um slot*, `n/(n−1)`) está NOMEADO no `push_apart` e move-se na direção inofensiva. **Ausente em qualquer um dos dois ⇒ BYTE-IDÊNTICO** (`x · 1.0` e `r + r` são exatos em IEEE-754), com o gate a comparar as duas rotas em vez de acreditar. ⚠️ **E o maquinário de reduções tinha um defeito latente que só uma redução sobre coluna AUSENTE podia expor:** o `map_module` promete *"a binding da fonte FICA — uma forma de bind group, um caminho"*, e o `create_pipeline` passa `layout: None` ⇒ o wgpu **DERIVA** a layout por reflexão, e uma binding que o shader nunca toca **não está nela** (erro real do device: *`Number of bindings in bind group descriptor (3) does not match ... (2)`*). As seis reduções que existiam dobram `P`, que nunca falta. ⚠️ **A metade load-bearing são as ENTRIES, medido por mutação** — forçar a declaração no WGSL a existir deixa os oito gates de device **VERDES**, porque a reflexão a descarta de qualquer jeito; a declaração fica condicional por honestidade, e o doc que dizia o contrário foi corrigido. ⚠️ **E o `value` de um `ReduceSpec` é uma EXPRESSÃO WGSL escrita à mão que a varredura do naga NUNCA via** (ela valida o módulo do KERNEL, e o map pass é outro módulo): até aqui essas expressões só encontravam um compilador numa máquina com adapter. O sweep passou a cobrir as **duas formas** de toda redução registrada (mutação: um `v.z` num `Vec2` ⇒ **RED**). **Medido na RTX: 8/8**, e ⚠️ **a barra é `1e-5` e não zero** — a primeira versão do gate afirmava zero (*"`Max` é exato em qualquer ordem, e o resto é a mesma aritmética"*) e falhou por **2,98e-8**: a redução é bit-exata, mas o `delta` de cada disco é uma **SOMA** sobre vizinhos que as duas rotas visitam em ordens diferentes, e a média é `delta · (1/c)` num lado e `delta / c` no outro — o ε que o ADR-0140 D4 nomeia. ⚠️ **E a paridade precisou de uma fixture NOVA porque a irmã seria verde por VÁCUO:** toda fixture daquele arquivo alimenta o collide a partir de um `motion.grid` puro, que **não emite `size`** ⇒ os dois lados leem a identidade e a lei nova nunca é exercida. **24 gates no nó** (11 novos) + 1 de paridade com tamanhos VARIÁVEIS; **8 mutações, 8 sangram**, e **uma sobreviveu com o achado a ser meu** (a declaração condicional no WGSL não é load-bearing). ⚠️ **E a fixture do cook do próprio nó carregava `size = [0,4]` como prop de PASSTHROUGH** — ela virou entrada SEMÂNTICA e mediu **0,24000002**, que é exatamente `2 × 0,3 × 0,4`; passou a ser o CONTROLE, na identidade. **`PROJECT_SCHEMA` 70 intocado** · contrato congelado **3/3** · **zero `Cargo.toml`**, nenhuma dep nova, nenhum ADR. Smoke: **`env PH2D_GPU_COOK_DEMO=48 cargo run -p ph2d-host-desktop --release`** — ⚠️ **a cena imprime as seis bandas nomeadas; se a lista não aparecer, PARE**, e ela **julga-se PARADA** (o nó é `Effect::Pure`). Os três pares: **1-2** o vão de cima é o mesmo em toda a fileira (0,4095 … 0,4179) e o de baixo **CRESCE com o disco** (0,4441 … 0,8851) — *ponha os olhos nos VÃOS, não nas peças* · **3-4** um `field.box` cobre o miolo e as três peças de dentro **se ATRAVESSAM** (0,22, meio diâmetro) enquanto as de fora seguem a tocar (0,42) · **5-6** a MESMA peça e o MESMO nó de constraint, só muda para onde o número vai: mutada, uma vizinha passa **por dentro** dela (0,2093); pinada, ninguém a atravessa e a fileira empacota em volta. ⚠️ **A fixture da cena NÃO continha o fenômeno, e a causa era a própria wave** (o `motion.scale(0,2)` passou a **encolher** o raio para `0,048` contra um passo de `0,16` ⇒ as seis fileiras saíam byte a byte iguais), e depois **não CONVERGIA** (a relaxação de Jacobi propaga uma peça por varredura ao longo de uma corrente: quinze discos paravam com vãos entre 0,229 e 0,397). ⚠️ **E um gate meu pedia o alvo EXATO, que um solver iterativo não promete em passos finitos** — as réguas viraram RAZÕES entre as duas fileiras, e o gate da caixa, que era **SIMÉTRICO** (*"algum par sobrepõe e algum toca"* é satisfeito com o miolo mudo **ou** com as pontas mudas — a mutação do `invert` SOBREVIVEU), passou a dizer **ONDE**. ⚠️ **E a cena nasceu SEM `motion.output`** (achado pelo Enio ao ler o documento, não por gate): o laço de render **não usa** a lista de sinks que o construtor devolve — ele a **re-resolve a cada quadro** a partir dos nós de saída do grafo (`motion_bridge.rs`: `motion.sinks = output_nodes(&doc.graph)`), então as seis bandas terminadas no `motion.collide` cozinhavam certo, satisfaziam os seis gates e **desenhavam NADA na tela**, que é indistinguível da feature quebrada; era a **única das onze** cenas de conferência sem Output. Como o `motion.output` é passthrough puro (copia contagem + toda coluna; kernel de GPU `PASSTHROUGH`), os números que a mensagem do smoke cita ficam **idênticos** — re-medidos pela sonda. Dois gates, os dois mutação-provados: o **local** (o `type_name` do sink É `motion.output`) e a **rede da FAMÍLIA** (`tests/every_demo_scene_ends_in_an_output_node.rs`, que varre `motion_state_conferencia_demos*.rs` **por diretório** com controle positivo de ≥ 10 cenas — *a cena que nascer amanhã entra sozinha, que é o que uma enumeração não faz*). **⬛ A FILA DE P0 DA CONFERÊNCIA ESVAZIOU E A SEGUNDA VOLTA ANDOU EM GRUPOS — `line/motion-value` INTEGROU (2026-08-16, 166 commits, 350 arquivos, +50.408/−5.087; handoff MESTRE [`docs/Motion Nodes/handoffs/HANDOFF_INTEGRACAO_line_motion_value_MESTRE_2026-08-16.md`](docs/Motion%20Nodes/handoffs/HANDOFF_INTEGRACAO_line_motion_value_MESTRE_2026-08-16.md)):** quatro blocos — o mecanismo de cada wave está nas entradas acima e não foi copiado. **(A) A cauda do plano 13, e o W7 fechou:** o `sim.spawn` nasce por **PULSO** (mais a porta `reset`) · o `sim.lifetime` emite a **MORTE** como evento · o colisor sabe o **TAMANHO** do que colide (um ponto era o raio sempre errado) e o plano dele deixa de ser horizontal · a colisão deixa de ser invisível (o canal **`hit`**) · o `sim.step` ganha um teto que capa a **DISTÂNCIA**, não o número · e o **SUBSTEP por-zona**, ⚠️ que é **ESCOPO** e não um número global. **(B) As 17 folhas foram percorridas e a fila acionável FECHOU** — de a família `force.*` herdando o cluster de ruído até o **`audio.bands`**, o som como campo de valor com a FFT **fora do cook por cerca ESTRUTURAL** (a crate não depende de crate de áudio nenhuma, então ela *não consegue* transformar). **(C) A segunda volta andou em GRUPOS, uma cena por grupo** (ordem do Enio: *"se continuarmos nesse ritmo nunca acabaremos"*): **A** a aritmética `=41` · **B** o ruído e o relógio `=42` · **C** as estatísticas `=43` · **D** a tabela e a semente `=44` · **E** a comparação `=45` · **F** o envelope `=46` · **G** a velocidade `=47` · **H** o disco que tem o tamanho que se vê `=48`. **(D) E o que a MEDIÇÃO recusou fica escrito para ninguém reconstruir:** o **Simplex** (a anisotropia que ele cura é **menor que a diferença entre dois vizinhos do próprio menu**) · a **variância de um passo** (num campo constante de magnitude `1e5` ela reporta desvio **71,6**) · o `iterations` do `smooth` (a relação estava **INVERTIDA**: `N` box são uma B-spline, logo o **peso** é o parâmetro geral) · a **faixa de barras** do `value.pattern` (construída, smokada e **REVERTIDA inteira** por veredito do Enio — a árvore sobrevive em `ae35416bd`, com 13 gates e 8 mutações já escritos) · e a *idade normalizada*, que já era exprimível. **A superfície de colisão, MEDIDA pelo integrador na árvore combinada:** `PROJECT_SCHEMA` **84 INTOCADO** — a linha **não toca** `project*.rs`, então não há degrau a CONTAR, não há a colisão **MUDA** (duas linhas escrevendo o mesmo literal, com o git sem opinião sobre o que o número significa) e ⚠️ **o corte do `project.rs` que a `line/physics` fez em 15/08 não a alcança** · contrato congelado **3/3 + 4/4** (`git diff` vazio em `node.rs` e `tool.rs`) · registro do `ph2d-ecs` **INTOCADO** ⇒ os **três** espelhos também · `ph2d-i18n` **INTOCADO** ⇒ a cadeia `vector::tr(k).or_else(sculpt3d::tr)` que a integração de 10/08 instalou fica intacta · **nenhum ADR** ⇒ a linha ficou **FORA de toda disputa de número** · **7 crates novas**, todas folha/drop-in · **ZERO pacote externo novo** (os únicos `+name` do `Cargo.lock` são as sete) · **20 `Cargo.toml`** (as 7 novas + 13 arestas internas) · censo **124 nós · 535 params · 516 com hint · 153 com unidade** · cenas de smoke **1..48 contínuas e sem duplicata** (o `main` ia a 23; ⚠️ **próxima livre: 49**, e o roteador é um `match` sobre literais, então um número repetido é `unreachable pattern` do compilador e não uma cena inalcançável em silêncio). ⚠️ **O rebase de 163 commits teve DOIS conflitos, os dois no `Cargo.lock` e os dois do MESMO par de vizinhos alfabéticos:** o `main` trouxe `ph2d-spring` (da `line/Vector`) e a linha trouxe `ph2d-steps`. O primeiro é **UNIÃO** — os dois lados só acrescentam. ⚠️ **O segundo é o commit em que a própria linha REVERTE o `ph2d-steps`, e ali ficar com um lado erra dos DOIS:** `theirs` apagaria o `ph2d-spring` do `main` (o lock perderia um pacote que a workspace tem) e `ours` manteria um pacote cuja crate deixou de existir. *Um lado escolhido não é um conflito resolvido.* ⚠️ **E o gate achou TRÊS vermelhos-latentes que nenhum `cargo test -p` alcança — os três só o SHIP vê.** **(1)** O tip da linha estava **fmt-VERMELHO em três arquivos**, e ⚠️ **o §6.1 do handoff mandava não os tocar:** ele descreve o `ph2d-node-pulse-signal/src/tests.rs` sujo na worktree como *"um diff de formatação pura de OUTRO dono, deixado deliberadamente fora dos 162 commits"*. Medido, o veredito **INVERTE** — aquela crate é uma das **sete novas da própria linha**, a versão **COMMITADA reprova** em `rustfmt --check` e a **suja PASSA**, e o `rustfmt` reproduz o arquivo sujo **byte a byte**: não era estilo alheio, era a correção que faltava ao tip. Os outros dois estão no `ph2d-motion-diagnose`, onde um `mod tests` extraído para irmão por `#[path]` manteve a indentação do bloco. **(2)** ⚠️ **O `main` TAMBÉM estava fmt-vermelho — 9 hunks em 4 arquivos, e não desta linha:** os quatro têm como último tocador o `bd2fbbd05`, a wave do ROUGH da `line/Painter` integrada em 15/08. *Por que ficou latente:* o gate da árvore combinada daquela janela — e o da integração da `line/sculpt3d` no dia seguinte — é check+nextest+clippy+codegen e **não inclui fmt**, que só aparece no `scripts/ship.sh`; **um vermelho que só o ship vê é invisível entre integrações**. O diff é **reordenamento puro** (o `reorder_modules` do rustfmt), conferido hunk a hunk: os dois `#[cfg(test)]` seguiram com os seus módulos e o doc-comment das leis de linha não ficou órfão quando o `spec_frame` passou na frente. **(3)** O clippy na forma do ship (`-D warnings`) reprovava com **três `unused Result that must be used`** em `tests/normalized_age.rs` — e ⚠️ **a cura NÃO é o `let _ =` que o próprio compilador sugere:** o §6.6 daquele handoff diz que *toda fixture que mede simulação precisa do `advance_tick`, senão a aresta `pre` nunca carrega estado e o gate fica VERDE sobre uma cena morta*, e silenciar aquele `Result` silencia exatamente a chamada que faz a fixture conter o fenômeno. Virou `.expect("advance_tick")`, a convenção que o próprio arquivo já usa para o `cook` ao lado. **O gate da árvore combinada, todo no tip rebasado:** `cargo fmt --all -- --check` **EXIT 0** na árvore inteira · `cargo check --workspace --all-targets` **EXIT 0** · clippy `--workspace --all-targets --features ph2d-spike/bevy_ecs -- -D warnings` **EXIT 0, zero warnings** · `cargo nextest run --workspace --cargo-profile ci-test` **16.096 de 16.097, 1.559 skipped** · gates de GPU do `ph2d-gpu-cook` na RTX **com o adapter encontrado** (zero skip gracioso — *skip não é verde*). ⚠️ **As DUAS falhas são pré-existentes e cada uma tem CONTROLE:** o `the_brush_snapshot_costs_the_same_on_a_canvas_sixteen_times_bigger` (`ph2d-tool-painter`) é gate de RAZÃO sobre **microssegundos** (1,8 contra 16,6 µs) — a crate tem **diff VAZIO** contra o `main` e ele passa **3/3 isolado**, a classe de flake que esta casa já registrou duas vezes; e o `value_slope_kernel_matches_the_cpu_on_the_device` mede **1,05023384e-4** contra uma barra de `1e-4` e **reprova no `main` com o número idêntico a todos os dígitos** ⇒ pré-existente, e `#[ignore]`, logo **fora do ship**. ⚠️ A anotação que o explica — atribuída por **ABLAÇÃO** pela própria linha, que o encontrou sem ser dela — **chega ao `main` com esta integração**; ele não a tinha. ⚠️ **Integrar não é aprovar: as cenas `=43`, `=44` e `=46` NÃO foram smokadas** (as demais o Enio julgou à medida que fecharam). **Aberto, com o preço ao lado:** a folha 03 tem **15 P1** — ⚠️ e a primeira coisa é **MEDIR se a composição já os exprime** (com o `size` a dirigir o raio, *Scale* pode ser `value.attribute → motion.drive(Size)` e *Hide* um `motion.cull`, o que **dissolveria** o `motion.push_apart` que o doc 63 propõe como nó novo) · a coluna **`neighbours`**, cujo número **já é computado e jogado fora nas duas rotas** (`var neighbours` no WGSL, `inv_n` na CPU) e cuja emissão custa uma coluna **mais um harness de paridade que leia COLUNAS**, ⇒ wave com cena própria · `verlet_rope` (sub-passos e pino de índice arbitrário) · `soft_body` (forma inicial e peso por-partícula) · `spring` (massa por-elemento e o `motion.lag`) · `pin_constraint` (limiar de ruptura) · a folha 07 com 3 P1 · o `motion.collide` com espessura de corda **FECHADA por medição** (o P1 virou **P2**: o gesto existe, falta o atalho) · e ⚠️ **a faixa de barras do `value.pattern` foi revertida por veredito de PRODUTO sem mecanismo nomeado** — uma segunda tentativa **começa perguntando o que ficou pior**, não reconstruindo. **⬛ O GRUPO I — A VIZINHANÇA VIRA UM NÚMERO, e os dois modos que a folha pedia como PARAM saem por COMPOSIÇÃO (2026-08-16, cena `=49`, **smoke OK 2026-08-16**; folha [03_simulacao](docs/Motion%20Nodes/89_conferencia/03_simulacao.md), linhas 42 e 61):** crate nova **`ph2d-node-motion-proximity`** (folha drop-in, glob member) — o nó que **MEDE** e publica **duas** colunas por elemento: **`neighbours`** (quantos discos o tocam) e **`overlap`** (a pior penetração ∈ [0,1]). Placar da folha 03: **15 → 13** P1. ⚠️ **A LEI é a do `motion.collide`, VERBATIM** (`r_i = radius · max(|size.x|, |size.y|)`; um par se toca com `d < r_i + r_j`) — e partilhar a lei é a wave inteira numa linha: é ela que faz *o que a proximidade REPORTA* ser igual a *o que o collide EMPURRARIA*. ⚠️ **E é por isso que a célula 42, que pedia o número ao `motion.boids`, foi fechada por OUTRO nó:** o número que o bando já computa e joga fora é a vizinhança **DELE** (raio próprio, cone, pesos), e emiti-lo de lá teria dado a um consumidor genérico uma resposta que só o boids sabe interpretar. ⚠️ **A COMPOSIÇÃO dissolveu as DUAS prescrições da folha:** o *Push Apart* do C4D tem Push · Scale · Hide, a linha 61 pedia um param `mode` **dentro do collide** e o doc 63 §2.2 propunha o nó novo **`motion.push_apart`** — com a coluna publicada, **Push** continua sendo o `motion.collide`, *Scale* é `proximity → value.attribute(Overlap) → value.map_range(0..1 → 1..0) → motion.drive(Size, Multiply)` e *Hide* é `… → motion.drive(Falloff, Set) → motion.cull(Falloff, invert)`. **A cadeia entrega MAIS que o param teria:** entre o `attribute` e o `drive` cabe qualquer nó do domínio de valor, e o mesmo `overlap` alimenta cor, rotação e opacidade — coisa que um `mode` dentro do collide não alcançaria. ⚠️ **Medir e empurrar ficam em DOIS nós, e é escolha:** o `motion.collide` **RELAXA** (varreduras iterativas que movem a própria coluna que leem) e a proximidade é **uma medição de UM passo sobre a entrada** — fundi-los faria *"quantos vizinhos eu tinha?"* depender de quantas varreduras alguém pediu. ⚠️ **UMA CONTAGEM É UMA DECISÃO, UMA FRAÇÃO É UM VALOR ARITMÉTICO — e é essa distinção que a paridade com o device mede:** `max` e contar são independentes de ordem, mas o `dot(d, d)` do WGSL pode ser **FUNDIDO num `fma`** pelo compilador de shader (mais exacto que os dois arredondamentos da CPU, e por isso diferente) ⇒ **`neighbours` é comparada por IGUALDADE** e **`overlap` carrega ε** — medido na RTX, **contagem EXACTA nos dois casos**, fracção `0e0` com tamanhos uniformes e **`5,96e-8` = 2⁻²⁴ = UM ULP** com tamanhos mistos (barra `4e-7`, ~3 ULP). ⚠️ **E a minha primeira prosa AFIRMAVA bit-identidade** *("as duas operações são `max` e uma contagem, logo nada depende da ordem")* — verdade sobre o REDUTOR e falsa sobre o VALOR que o alimenta; corrigida nos três doc-comments em vez de o ε ser escondido. ⚠️ **O custo que a fila anotava estava METADE errado:** ela dizia que emitir a coluna custa *"uma coluna MAIS um harness de paridade que leia COLUNAS"* e fechava com *"é wave com cena própria, não rodapé"* — medido, **o harness JÁ existia** (`GpuCook::retain_streams_for_debug` + `read_column`), e só a coluna era trabalho. ⚠️ **As duas colunas ficam FORA do `TRANSIENT_COLUMNS` do diagnoser, de propósito:** o consumidor canônico é o `value.attribute`, que lê a coluna que o **ARTISTA nomeou num text param** e por isso não declara `Consumes` nenhum ⇒ pô-las lá marcaria como **INERTE** exactamente a cadeia que o nó existe para servir — o falso positivo que o `MissingSource("P")` do Boids já pagou (ADR-0155), e o mesmo raciocínio pelo qual o `motion.velocity` não declara `Produces("vel")`. ⚠️ **O `radius` NÃO leva `ParamHardMax`, e a ausência é MEDIDA:** ele é ao mesmo tempo o raio do disco e a **célula da grade**, então o número de células varridas por elemento fica constante quando ele cresce; o que cresce é a densidade da cena, limitada por `n` — a mesma razão pela qual o `motion.collide` também não tem, com gate a afirmar a ausência para a próxima varredura não "completar" a tabela. ⚠️ **E a CENA achou um confundidor que teria adoecido uma banda inteira:** o gradiente de tamanho **não** usa `motion.falloff` + `motion.scale`, porque aquele par deixa a coluna `falloff` escrita como uma rampa `0..1` e o **`motion.drive` MASCARA todo efeito pelo falloff** — na banda da CONTAGEM a metade esquerda ficaria com `falloff ≈ 0` e o medidor **não escreveria nada ali**, exactamente onde a leitura *"livre ⇒ pequeno"* mora; o gradiente vem de `value.instance_field(Ramp)`, que não toca coluna nenhuma além da própria `v` ⇒ `falloff` fica **AUSENTE** (lido como `1`) em toda parte. ⚠️ **A fixture do gate de composição nasceu VÁCUA pela doença de sempre — o CONTROLE não podia discriminar:** sobre uma grade uniformemente amontoada **TODO** elemento se sobrepõe, o Hide apagava a cena inteira, e *"apaga quem se sobrepõe"* ficava indistinguível de *"apaga tudo"*; a fileira passou a carregar as **DUAS espécies** (tamanho de `0,4` a `2,4` sobre um passo FIXO ⇒ **quatro** livres à esquerda, **nove** amontoadas à direita), e isso é hoje o primeiro gate da cena. **10 gates no nó + 4 na cadeia cross-crate + 2 de paridade na RTX + 6 na cena · 5 mutações, 5 sangram** — ⚠️ e a 5ª (**trocar as duas colunas uma pela outra**) sangra **UM** gate só, que é exactamente o certo: o oráculo do medidor é o único que pina **QUAL coluna carrega QUAL grandeza**, e o Hide e o Scale passam por acidente aritmético (o limiar de `1e-4` e o clamp do `map_range` aceitam os dois dados). ⚠️ **E uma mutação minha "passou" sem nunca ter sido aplicada** — a âncora do `python` tinha a indentação errada e o `assert` a apanhou, mas o `cargo test` que veio a seguir na MESMA linha de comando imprimiu VERDE: *um verde depois de uma substituição que falhou é "nada rodou"*, e é a segunda vez que esta casa paga isso [[feedback_a_negative_search_needs_a_positive_control]]. **Zero schema** (`PROJECT_SCHEMA` **84** intocado, `project*.rs` com diff vazio) · contrato congelado **3/3** (o param único vive no `MANIFEST` do NÓ) · **1 crate nova** e **nenhum pacote externo novo** (o único `+name` do `Cargo.lock` é ela) · **2 `Cargo.toml`** — a `ph2d-node-registry-init` (o glob) e a `ph2d-gpu-cook` em **`[dev-dependencies]`**, porque a crate-nó só existe ali para o gate de paridade e o `src/` não a usa ⇒ **machete-safe**, o precedente das cinco crates-nó da `line/gpu-nodes` · **nenhum ADR**. Censo re-medido na árvore: **125 nós · 536 params · 517 com hint · 154 com unidade**. **LOC:** o `motion_state_demo_router.rs` cruzou 600 ⇒ as **nove** cenas de GRUPO da conferência (`=41..=49`) saíram para o irmão `motion_state_demo_conferencia.rs`, **uma função por cena** — ⚠️ e o irmão **não tem `match` nenhum, de propósito**: o roteador continua a ser a ÚNICA lista de níveis, porque dois `match` em dois arquivos deixariam um nível reivindicado duas vezes passar **em silêncio** (o compilador só vê `unreachable pattern` dentro de um mesmo `match`), que é o defeito que o cabeçalho do roteador já nomeia. Smoke: **`env PH2D_GPU_COOK_DEMO=49 cargo run -p ph2d-host-desktop --release`** — ⚠️ **a cena imprime as seis bandas nomeadas; se a lista não aparecer, PARE**, e ela **julga-se PARADA** (o nó é `Effect::Pure`). As três leituras, medidas: **(1)** o pior vão vai de **−0,448 a −0,000** e a maior peça de **2,40 a 1,04**, com os **três primeiros vãos IDÊNTICOS** aos de cima (quem já estava livre não foi tocado — se a fileira inteira encolher, isso é um `motion.scale` global) · **(2)** o Hide deixa **QUATRO das treze**, e são as quatro da esquerda (a fileira encolhe de **4,08 para 1,02**) · **(3)** na contagem as peças passam a ter **QUATRO tamanhos só** (0,60 · 1,00 · 1,40 · 1,80), um por número de vizinhos tocados — se a fileira 6 desenhar o mesmo gradiente da 5, o `neighbours` não chegou. ⚠️ **Próxima cena livre: conte-a no `match` do `motion_state_demo_router.rs`** (um literal aqui envelhece; um número repetido lá é `unreachable pattern` do compilador). **⬛ O GRUPO J — O PINO ALCANÇA AS SIMULAÇÕES, e a cerca tinha uma porta que ninguém tentou (2026-08-16, cena `=50`, **smoke OK 2026-08-16** ⚠️ *a 1ª rodada REPROVOU e a entrada seguinte (`=51`) É a cura dela*; folha [03_simulacao](docs/Motion%20Nodes/89_conferencia/03_simulacao.md), linhas 39, 51 e 77):** os três geradores — `motion.verlet_rope` · `motion.soft_body` · `motion.boids` — passam a ler **`inv_mass`** da **CADEIA DE ESTADO**, e um `motion.pin_constraint` no laço de feedback prega qualquer um deles. Placar da folha 03: **13 → 10** P1. ⚠️ **A cerca estava escrita em DOIS docs e as duas frases eram falsas do mesmo jeito:** o doc do pino dizia *"são geradores (…) nenhum stream de instâncias entra, então um pino a montante não tem fio por onde os alcançar"* e o doc 34 §7 repetia *"hoje não há como um stream entrar neles"* — **verdade sobre a porta `in`, FALSO sobre a cadeia de estado**, que é um fio e que **já era o fio pelo qual o `accel` entra** (`rope.out --pre--> force.wind --> rope.state` está documentado no próprio `accel_col` da corda desde o W1-A). Os dois docs foram corrigidos, porque *um comentário que contradiz o código shipado é pior que comentário nenhum*. ⚠️ **Medido ANTES de uma linha ser escrita**, com o pino no laço e os três nós a ignorá-lo: **pior deslocamento `0,000000`** nos três ⇒ o vão era real, e a cura é **uma leitura de coluna por nó** — nenhum kernel novo, nenhuma porta nova, nenhum schema. ⚠️ **E a FÓRMULA PBD ESTAVA ESCONDIDA na tabela de quatro braços da relaxação da corda** (`(true,false) => (0.0, 1.0)` etc.): ela é a forma booleana de `w_i/(w_i+w_j)` com pesos em `{0,1}`, então a substituição é **byte-idêntica por ARITMÉTICA e não por promessa** — `0/1`, `1/1` e `1/2` são todos exactos em IEEE-754, e o par degenerado cai no guard. A prova de que a troca não move nada são os **52 gates que já existiam** nos três arquivos, que passam **sem uma edição de asserção**; e o gate `a_pin_that_selects_nothing_is_byte_identical` é mais forte que a ausência — `count = 0` escreve `inv_mass = [1,0; n]`, a coluna EXISTE e o leitor RODA com o valor neutro, então ele prova o **RAMO** e não só o `else`. ⚠️ **DUAS espécies de pino, e a diferença é o ALVO:** o intrínseco (a cabeça da corda, a linha de topo do corpo) é clampado a um alvo **ANIMADO** que uma `value.lfo` varre; o genérico segura **onde está**, que é o que *pregar um índice* significa quando não há alvo nenhum a que o prender — as duas coexistem e compõem. ⚠️ **A MASSA INVERSA ESCALA A ACELERAÇÃO E NÃO A INÉRCIA, e sem isso um pino PARCIAL é INVISÍVEL:** medido, `strength = 0,5` desenhava **exactamente** o mesmo que `0,0` numa corda em repouso (y = −3,0688 nos dois) — sob PBD puro o peso só distribui correções, e num equilíbrio as restrições já estão satisfeitas. A cura é `a = F/m` com a gravidade lida como força por massa de referência (a leitura que o `motion.integrate` já faz e que o doc do pino promete — *"deixar um elemento mais pesado meramente RESISTIR"*), com `(c − pv)` intocado porque é **MOMENTO**, e escalá-lo faria um ponto pesado perder velocidade. ⚠️ **Consumida, nunca emitida** — o pino **MULTIPLICA** no que já está no stream, então um gerador que reemitisse `inv_mass` faria um pino parcial decair `0,5 → 0,25 → 0,125` a cada tique: o *produto sobre a lista* que esta casa já curou quatro vezes no relevo do Painter. ⚠️ **E o BANDO tem kernel de GPU, o que a wave quase deixou passar:** medido, um bando pinado **SEGUE reivindicando o device** (`fully_gpu = true`, `boundaries = []`) ⇒ sem o porte do WGSL o agente **seguraria na CPU e voaria na GPU** — *dois produtores lendo dados diferentes* —, com **todos os dez gates de paridade existentes VERDES porque nenhum deles põe um pino no laço**. O kernel ganhou a binding (⚠️ **identidade `1.0` e não zero**: um `[0.0; 4]` congelaria todo bando que nenhum pino alcança, o modo de falha mais caro que uma binding pode ter) e o early-out; **paridade na RTX 11/11, o bando pinado a `2,38e-7`**, o mesmo ε dos irmãos. ⚠️ **A corda e o corpo mole NÃO têm kernel de device** (conferido: não há `gpu.rs` neles), então ali não há segunda rota a divergir. **10 gates novos** (6 de cadeia cross-crate + 4 de unidade) + 2 de paridade + 4 na cena; **8 mutações, 8 sangram** — ⚠️ e **DUAS sobreviveram primeiro, as duas por FIXTURE que não continha o fenômeno**: apagar o early-out da corda passava porque, com a aceleração já escalada por `w`, um ponto que chega ao pino **PARADO** não se move de qualquer maneira (e na fixture de cadeia o pino chega no tique 1, logo a seguir à semeadura, onde `pos == prev`) ⇒ gate de unidade novo que prega um ponto **EM VOO**, com o movimento **PERPENDICULAR** ao eixo (⚠️ *ao longo* dele a restrição de distância desfaz a inércia no mesmo passe, e o controle media `x = 1` EXACTO — um gate que não pode falhar pelo motivo que alega); e neutralizar a escala no WGSL passava porque com `strength = 1` o peso é **0 ou 1**, então o early-out apanha o zero e a multiplicação por um é a identidade ⇒ gate novo com pino **PARCIAL**, mais o controle *meio-pino não é nem livre nem pregado*. ⚠️ **E a linha 39 fechou POR MEDIÇÃO, sem código:** a célula do *wander* dizia *"mas boids não lê `accel`, então a força é escrita e descartada"* — ele **LÊ** (`read_state_accel(i)` no `gpu.rs`, `accel_col` na CPU) desde o W1-A, e medido pela porta do produto o `force.curl` na cadeia leva a dispersão de **2,5471 para 5,3244** com pior deslocamento **10,6910**. É a **terceira** célula desta conferência a envelhecer antes de alguém voltar a ela. **Zero schema** (`PROJECT_SCHEMA` **84**, `project*.rs` com diff vazio) · contrato congelado **3/3** (nenhum `MANIFEST` ganhou param: a wave é toda leitura de coluna) · **zero crate nova**, **zero `Cargo.toml`**, **zero dep** · **nenhum ADR**. Censo **inalterado**: 125 nós · 536 params · 517 com hint · 154 com unidade. Smoke: **`env PH2D_GPU_COOK_DEMO=50 cargo run -p ph2d-host-desktop --release`** — ⚠️ **a cena imprime as seis bandas nomeadas; se a lista não aparecer, PARE**, e ⚠️ **DÊ PLAY** (as três famílias são `Effect::Temporal`, e uma foto de um instante não distingue *segurou* de *ainda não caiu*). Os três pares, medidos: **1-2** a corda da direita **DOBRA sobre o ponto 12**, que não é ponta nenhuma — maior desvio **14,3716**, com aquele ponto a sair de `[-6,71, 8,90]` para `[7,63, 9,00]` · **3-4** o corpo da direita **PENDE** da primeira linha em vez de cair (topo **−11,5710** contra **1,7500**) · **5-6** três agentes da direita andam **0,000000** enquanto o resto do bando anda até **3,5084**, e o bando **CONTORNA-OS** (um agente de massa infinita continua a ser VISTO pelos vizinhos, porque o laço lê o estado de ENTRADA). ⚠️ **A âncora chicoteada da banda 1-2 não é decoração, é a fixture:** uma corda pendurada nunca se dobra (o nó tem uma âncora só) e um pino no meio dela seria indistinguível de um nó de passagem. **Próxima cena livre: conte-a no `match` do `motion_state_demo_router.rs`** (um literal aqui envelhece; um número repetido lá é `unreachable pattern` do compilador). **⬛ E O SMOKE DA `=50` ACHOU DOIS DEFEITOS QUE NÃO ERAM O MESMO — o pino que não PRESCREVE e o bando que não sabe o tamanho que desenha (2026-08-16, cena `=51`, pendente de smoke; folha [03_simulacao](docs/Motion%20Nodes/89_conferencia/03_simulacao.md)):** report do Enio em duas frases, e **MEDIDO antes de qualquer hipótese** elas viraram duas frentes independentes. **(A) *"o que está pinado, se eu mover `spacing` em Soft Body, não muda de posição até apertar reset"*** — ⚠️ **a afirmação geral é FALSA e o exemplo é EXACTO**: mover o `spacing` com a sim rodando move o corpo **3,8740** contra um controle de 0,0017, e a linha **PINADA** anda **0,0000**. A causa é o desenho do Grupo J, uma wave atrás: o `motion.pin_constraint` escreve `inv_mass = 0` e o ramo `w <= 0.0` do soft body segurava `pred[i] = pos[i]`. ⚠️ **Segurar ONDE ESTÁ é um FREEZE de espaço de mundo, não um pino** — nem o `spacing`, nem `rows`/`cols`, nem a **ÂNCORA** o alcançavam (medido: mover a âncora levava o pino intrínseco **3,0000** e o genérico **0,0000**, e a âncora é a metade que o report não nomeava). **A LEI que substitui:** *uma partícula de massa infinita segue a pose que o NÓ sabe PRESCREVER* — o soft body sabe (`anchor + rest[i]`, função viva de `anchor`/`rows`/`cols`/`spacing`), a corda e o bando **não têm forma de repouso posicional** de onde prescrever, e lá `pos[i]` continua a ser a resposta certa; é o que Blender Cloth (segue a malha original), Vellum (`pintoanimation` segue a geometria animada) e nCloth (*constrain to transform*) fazem. ⚠️ **O ramo da PROJEÇÃO é `pull <= 0.0` e não `is_pinned`, de propósito:** com `inv_mass = 0` o peso é zero e `x + (g − x)·0` **é** `x` para todo `g` FINITO — mas **NaN** para um `g` infinito; sair cedo mantém a aritmética fora do caminho. **Os 27 gates que aquele arquivo já tinha passam SEM UMA EDIÇÃO de asserção**, e é isso que prova que a mudança só alcança o ramo de massa infinita. ⚠️ **E o harness é a metade que faltava:** o `pose()` constrói um `Cook` **NOVO** a cada chamada, então um param mudado entre duas chamadas é uma simulação inteira desde a semente — e o report é sobre **arrastar um slider com a sim RODANDO**; a mutação que apaga a prescrição **SOBREVIVEU** até o `settle_edit_settle` (um `Cook`, param mudado no meio) existir. Medido depois: `spacing` **0,0000 → 2,4749** e âncora **0,0000 → 3,0000**, byte-idêntico ao intrínseco. **(B) *"não há parâmetro que estabeleça o grau de sobreposição entre os indivíduos; ou a imagem desenhada é maior do que deveria"*** — ⚠️ **a SEGUNDA metade é a certa:** um agente é desenhado a **1,0** (o `SIZE_IDENTITY` que o shell passa ao `lower_to_instances`) e o bando empacota-os a **0,795** de mediana, **70% sobrepostos** no `separation` da cena. ⚠️ **E o único knob era um PESO com equilíbrio COMPRIMIDO:** 32× o peso compra **2,4×** a distância, então zerar a sobreposição de 40 agentes pedia `separation = 51,2` contra um slider que para em **6,0**. ⚠️ **Havia um defeito de ACOPLAMENTO por trás:** separação e percepção partilhavam **UM** `radius`, então um bando mais LOCAL sobrepunha **MAIS** (`radius 0,5` ⇒ **100%** sobrepostos) — Reynolds/Shiffman separam os dois (`desiredseparation` × `neighbordist`), o POP Steer Separate da Houdini tem raio próprio, Unity e Unreal idem. **`separation_radius`** (default `0,0` = o mundo de antes, **byte-idêntico** e gateado por `to_bits()`) é o alcance do empurrão, independente da percepção. ⚠️ **O ESPAÇO PESSOAL É FÍSICO, NÃO PERCEPTUAL** — ele corre **antes** do gate de percepção e **antes** do cone de visão: dois corpos que não se veem ainda não podem ocupar o mesmo lugar (gate próprio com dois agentes cegos um para o outro, a `radius 1,0` e `sep_radius 4,0`). ⛔ **MEDIDO E REJEITADO, não refaça: a MOLA DE COMPRESSÃO** (`~(r − d)` em vez de `1/d²`) foi construída e mede **PIOR** — mediana **0,387 contra 0,748** —, e o mecanismo fica no doc-comment: *um equilíbrio entre forças FINITAS cai sempre ABAIXO do raio onde a repulsão se anula*; para o vão pousar num número é precisa repulsão **INFINITA** ali, ou seja uma **RESTRIÇÃO** — e essa já existe, é o `motion.collide` (mediana medida **0,9878** de um diâmetro 1,0). ⚠️ **A GPU RECUSA o device quando o espaço pessoal passa a percepção** (`applicable`), porque a grade é construída com `cell_param: "radius"` e varrida 3×3 — ela literalmente **não consegue ver** mais longe; recusar é a alternativa a **divergir em silêncio** entre as duas rotas, que é o que o `inv_mass` do Grupo J quase produziu. Paridade CPU×GPU na RTX: 400 agentes, `sep_radius 1,7 < radius 2,3`, **max |Δpos| = 2,3841858e-7** (barra 2e-3), **12/12**. ⚠️ **E a sonda que decidiu tudo isto nasceu com o número ERRADO:** `DRAWN_SIZE` saiu das fixtures de GPU (**0,4**) e o shell passa **1,0** — a primeira medição dizia *"5% sobrepostos"* e **invertia o veredito do report (B)**; com o número do produto são **82%**. *Uma régua herdada de uma fixture não é a régua do produto.* **Zero schema** (`PROJECT_SCHEMA` **84** intocado, `project*.rs` com diff vazio) · contrato congelado **3/3** (o param novo vive no `MANIFEST` do NÓ e o `ParamUiHint`/`ParamUnit` são side-metadata do registry) · **zero crate nova**, **zero `Cargo.toml`**, **zero dep** · **nenhum ADR**. Censo re-medido na árvore: **125 nós · 537 params · 518 com hint · 155 com unidade**. **6 mutações, 6 sangram** — ⚠️ e **uma "sobreviveu" por SHELL**: um `echo` com crases dentro fez o comando morrer (`command not found`) e o `cargo test` seguinte imprimiu VERDE sobre a árvore **não-mutada**; refeita com **controle positivo** (contagem de `grep` do comentário removido = 0), ela sangra [[feedback_a_negative_search_needs_a_positive_control]]. Smoke: **`env PH2D_GPU_COOK_DEMO=51 cargo run -p ph2d-host-desktop --release`** — ⚠️ **a cena imprime as quatro bandas nomeadas; se a lista não aparecer, PARE**, e ⚠️ **DÊ PLAY nas bandas 1-2** (o mastro varre com o tempo) **e olhe as 3-4 PARADAS**. As duas leituras: **(1)** as duas bandeiras **TÊM de desenhar a mesma coisa** — medido, excursão **5,0000** nas duas e pose relativa ao pino a diferir menos de **0,001**; se divergirem, a lei voltou a ser duas · **(2)** o bando de cima empacota a **0,803** de mediana com **34/40 SOBREPOSTOS** e o de baixo a **1,614** com **0/40** — *ponha os olhos nos VÃOS, não nas peças*. ⚠️ **A âncora CHICOTEADA das bandas 1-2 é a fixture, não decoração:** um corpo mole pendurado em repouso não mexe, e ali *segurar onde está* e *seguir a prescrição* são indistinguíveis. **⬛ O GRUPO K — O PESO QUE O PAPER JÁ TINHA E O PASSO QUE NINGUÉM DIVIDIA (2026-08-16, cena `=52`, **smoke OK 2026-08-16**; folha [03_simulacao](docs/Motion%20Nodes/89_conferencia/03_simulacao.md), linhas 49 e 58):** duas waves independentes que a folha marcava como `P1` no mesmo cluster — `motion.soft_body` ganha o **PESO POR PARTÍCULA** e `motion.verlet_rope` os **SUB-PASSOS**. Placar da folha 03: **10 → 8** P1. **(A) O PESO — o `wᵢ` que o nó CITAVA e fixava em 1.** O `motion.soft_body` é shape matching de **Müller et al. 2005**, e o paper carrega um peso por partícula nos centroides e em `A_pq`/`A_qq`; o doc-comment deste nó **declarava** tê-los pinado em `1` ⇒ a wave é *o nó a cumprir a citação que ele próprio faz*, não capacidade inventada. ⚠️ **Medido ANTES de uma linha ser escrita** (um `field.index_range` no laço de estado, com o corpo a ignorá-lo): **pior deslocamento `0,000000`** — o vão era real, e a cura é **uma leitura de coluna**. ⚠️ **É UMA ideia com DUAS consequências, e escrever só a segunda seria pior que nada:** pertencer ao corpo significa **DEFINIR** a forma (entrar nos centroides e nas duas matrizes) e **SER PUXADO** de volta a ela (o `pull`) — uma partícula solta que ainda definisse o referencial **arrastaria o corpo inteiro atrás dela**. ⚠️ **E o neutro é byte-idêntico por ARITMÉTICA, não por um ramo:** `1.0 × x` **é** `x`, `x − 0.0` **é** `x` (o `−0.0` incluído) e `Σ 1.0` sobre `n < 2²⁴` é exactamente `n` em IEEE-754 ⇒ **a rota uniforme É a lei ponderada com argumentos neutros** — uma lei, não duas —, e a prova são os **27 gates** que aquele arquivo já tinha, que passam **sem uma edição de asserção**. ⚠️ **O `c0` literal zero NÃO é aproximação, e a medição decidiu o desenho:** o centroide de repouso não-ponderado de uma malha 8×8 de espaçamento 0,7 mede **−1,192e-7**, não zero — computá-lo *seria mais correcto* e **moveria toda arte já autorada por ~1e-7 por tique**; é por isso que `falloff_col` devolve `Option` e não um vector de uns (*uma coluna presente torna o centroide **calculado** em vez de assumido zero*). ⚠️ **O peso atravessa a PARTIÇÃO** dos clusters (`cluster_goals_weighted` computa um `centre` ponderado por região e o passa como `c0` em vez de pré-mutar o `sub_rest` — a mesma subtracção, na mesma ordem ⇒ bit-idêntico em peso 1) e **um `total <= 1e-6` numa região faz `continue`**: uma região inteira de peso zero não tem frame a que voltar. ⚠️ **DOIS defeitos de gate, os dois achados por MUTAÇÃO, e os dois eram meus:** **M1** (tirar os pesos do FIT, deixando só o `pull`) **SOBREVIVEU** porque os dois gates de lei **chamavam o kernel DIRETO** e o `pull` sozinho já solta a cauda — *um gate de unidade é cego à fiação*, e só um gate de **PRODUTO** a matou (sob ela a linha do meio é arrastada **20,2455**); **M5** (tirar o `Coupling::Consumes("falloff")`) sobreviveu porque o `falloff_declaration` casava o literal `"falloff"` e o `get(` **na MESMA linha**, cego à convenção de **const nomeada** que este repo usa — corrigido com `reads_via_const`, o universo dele foi de **32 para 34** leitores, e ele apanhou que o **`motion.collide` também nunca estivera lá**. **8 gates, 5 mutações, 5 sangram.** **(B) OS SUB-PASSOS — e eles NÃO são mais iterações.** O achado é o do *Small Steps in Physics Simulation* (Macklin et al., SCA 2019): uma iteração **propaga uma correcção já computada**, um sub-passo **volta a perguntar onde a corda está**. Medido a orçamento igual (24 nós, gravidade 98, 24 correcções no total): `1 × 24` estica **8,3008%** · `2 × 12` **4,5722** · `4 × 6` **2,1513** · `8 × 3` **1,1745** · `24 × 1` **0,5253%** — quinze vezes menos. ⚠️ **E as `iterations` sozinhas NÃO alcançam, que é o gate que prova que o param novo não é um sinónimo:** `1 × 128` (**5,3× o custo**) estica **1,1266%** e ainda perde para `4 × 24`, que estica **0,4772%**. ⚠️ **A INÉRCIA ATRAVESSA A FRONTEIRA DO TIQUE, e são DUAS metades — a segunda foi um defeito MEU que a sonda achou:** a **ENTRADA** (o `prev` que chega codifica um deslocamento de um TIQUE, então o primeiro sub-passo levaria `N ×` a inércia sem re-escala) e a **SAÍDA** (o tique seguinte tem de ler a velocidade do **ÚLTIMO** sub-passo, esticada de volta a um tique). ⚠️ **A viagem é CEGA à metade da saída** — com velocidade constante o deslocamento **MÉDIO** sobre o tique é igual ao **FINAL**, então devolver a pose de entrada passa por lá —, e o oráculo dela é a **queda LIVRE** (`iterations = 0` de propósito: uma restrição rígida puxa o atrasado de volta e **esconde** o erro), onde devolver a pose de entrada comia a velocidade a cada fronteira e um segundo de queda media **−2,65 m com dezasseis sub-passos** contra os **−4,97** analíticos. Corrigido, a queda converge (**−4,98 → −4,91**, o analítico é 4,90) e a viagem fica **plana** (2,9500 em 1 e em 16). **O TETO é 16 e o recurso é TEMPO**, medido pela porta do produto: `count 24 ×16 = 0,1343 ms/tique` (7% de um quadro de 2 ms) · `count 256 ×16 = 1,2321` (62%) e `×32 = 2,3829` (**119%, estoura**) · `count 2048 ×4 = 2,4811` (**124%**); o slider para em **8**, que é onde a mão trabalha, e o teto digitável é o `ParamHardMax`. ⚠️ **E a medição corrigiu uma AFIRMAÇÃO minha:** eu escrevi que o caminho literal de `substeps = 1` existe porque `a − (a − b)` não devolve `b` em IEEE-754 — verdade, mas **não no regime da corda**, onde o lema de **Sterbenz** o torna EXATO (**144 de 144** pares realistas medidos); a mutação que troca o ramo pela fórmula **SOBREVIVE**, e ela fica **documentada em vez de escondida** — o ramo existe pelo **custo** (uma alocação e um laço por tique que não fazem nada no default) e como **guarda para o `prev` degenerado**, cujos dois casos estão medidos (`a = 100, b = 1e-6` devolve **0**; o zero negativo perde os bits). **5 gates, 4 mutações, 4 sangram** (a 5ª é a acima), e os **14 gates** que o arquivo já tinha passam **sem uma edição**. **Zero schema** (`PROJECT_SCHEMA` **84** intocado, `project*.rs` com diff vazio) · contrato congelado **3/3** (os params novos vivem no `MANIFEST` do NÓ; o `falloff` é leitura de coluna e o `Coupling` é side-metadata do registry) · **zero crate nova**, **zero `Cargo.toml`**, **zero dep** · **nenhum ADR**. Censo re-medido na árvore: **125 nós · 538 params · 519 com hint · 155 com unidade** — ⚠️ **um param novo, não dois:** o `substeps` entra no `MANIFEST` da corda e o **`falloff` do corpo NÃO é param** (é leitura de coluna), que é precisamente o que o torna componível com todo `field.*` a montante. Smoke: **`env PH2D_GPU_COOK_DEMO=52 cargo run -p ph2d-host-desktop --release`** — ⚠️ **a cena imprime as quatro bandas nomeadas; se a lista não aparecer, PARE**, e ⚠️ **DÊ PLAY** (as duas metades são simulações, e uma foto de um instante não distingue *ainda não caiu* de *nunca vai cair*). As duas leituras, medidas: **(1)** a cauda de peso zero **DESPRENDE** — a extensão do corpo vai de **3,53 para 14,12** — e a linha do MEIO anda **0,0099** relativa ao pino (se o corpo inteiro descer atrás dela, o peso chegou ao puxão e **não** ao ajuste da forma) · **(2)** a MESMA corda chicoteada, com as MESMAS 3 iterações, estica **51,7%** com um sub-passo e **0,6%** com oito — *ponha os olhos nos VÃOS entre nós*. ⚠️ **A cauda cai RÍGIDA, e isso é CORRETO:** no shape matching **não existe restrição de distância entre partículas** — o único fio é o puxão ao goal, e peso zero é fio nenhum —, então ela mantém o próprio espaçamento; **ela não se deforma, ela SAI**. ⚠️ **E o gate da cena nasceu medindo a coisa errada, pela QUARTA vez neste repo:** comparar a linha do meio em coordenada **CRUA** entre as duas bandas media **8,9901**, que é o `BAND_DY` de **9,0** que a própria cena põe entre elas (o `1,15` do grupo B, o `offset_y` do C e o `BAND_GAP` do D são os três anteriores) — o oráculo passou a ser **relativo à linha pinada da própria banda**. **3 gates de cena + 1 sonda, 3 mutações, 3 sangram.** ⚠️ **Próxima cena livre: `53`, e o número se CONTA lendo o `match` do `motion_state_demo_router.rs`** — nunca uma nota: a linha equivalente da física já envelheceu em onze cenas uma vez, e aqui o roteador é um `match` sobre literais, então um número repetido é `unreachable pattern` do compilador em vez de uma cena inalcançável em silêncio.
 **⬛ O GRUPO L — O TETO DA TAXA, E O `motion.lag` COMO NÓ NOVO FOI REFUTADO (2026-08-16, cena `=53`, **smoke OK 2026-08-16**; folha [03_simulacao](docs/Motion%20Nodes/89_conferencia/03_simulacao.md), linhas 65 e 67):** o `motion.delay` ganha **`max_step`** e **`max_accel`** — o *Max Slope* e o *Max Accel* do **Lag CHOP** do TouchDesigner, a metade daquele CHOP que este nó não tinha. Placar da folha 03: **8 → 6** P1. ⚠️ **A célula pedia um NÓ NOVO e metade dela já estava obsoleta quando foi escrita:** ela lista *"modos Average / Blend / Spring + clamp de slope/aceleração"* e manda construí-los num **`motion.lag` próprio** (doc 63 §2.2, **P0**) — medido antes de uma linha, os modos **`Average` e `Blend` EXISTEM no `motion.delay` desde que ele nasceu**, o doc dele diz por que o terceiro não está lá (*"os nossos são Delay, Average e Blend, **porque já temos uma mola e ela é melhor**"*), e o **`ticks_down` do grupo F já era o *Lag up/down* do MESMO CHOP**. *Construir o nó a partir da célula teria produzido dois modos que já existiam* — a quarta célula desta conferência a envelhecer antes de alguém voltar a ela. ⚠️ **A RÉGUA É A MAGNITUDE DO ELEMENTO, NUNCA A COMPONENTE:** um teto por componente deixa o movimento diagonal ser **`√2` vezes mais rápido** que o axial ⇒ a taxa passaria a depender da orientação dos eixos, que é a anisotropia que o `sim.step::limit_speed` já recusou e cuja lei esta wave espelha. ⚠️ **A ACELERAÇÃO CORRE ANTES DO PASSO, e a ordem só é OBSERVÁVEL num gesto REAL — a mutação me corrigiu:** com a velocidade a partir de zero as duas ordens respeitam o teto **POR INDUÇÃO** (aproximar-se de um alvo que já cabe nunca sai dele), e a mutação da ordem trocada **SOBREVIVEU** à primeira fixture; o que as separa é o artista **APERTAR** o slider com o canal já em movimento, onde a ordem trocada devolve `prev_vel ± max_accel` — muito acima do número acabado de digitar. *A minha primeira justificativa era mais forte que o facto*, e o gate passou a montar esse mundo (o teto entra no tique 12 sobre uma saída que já corre, com o CONTROLE a exigir que ela corra **mais depressa que o teto que vem aí** — um número menor deixaria as duas ordens concordarem e o gate não separaria nada). ⚠️ **A UNIDADE É POR TIQUE, e é a régua do NÓ:** o `ticks` já é uma contagem de tiques, então um teto em unidades por SEGUNDO seria **a segunda régua no mesmo painel** — e o preço fica NOMEADO (como o lag, o teto acompanha a taxa de quadros). ⚠️ **Mas a FAIXA segue o CANAL** (`register_param_channel_range`), e a prescrição já estava escrita naquele arquivo **antes de o segundo caso existir**: o doc do `PARAM_UNITS` explica que `ticks` é `Count` e **não** `FromChannel` porque *"uma duração não muda de unidade: oito tiques são oito tiques quer ela atrase um ponto, um ângulo ou uma cor"* — e uma MAGNITUDE muda (unidades de mundo em Position, graus em Rotation), que é a definição exacta daquele variant. ⚠️ **Sem `ParamHardMax`, e a ausência é deliberada** (o precedente é o `radius` do `motion.collide`): um teto de taxa é magnitude pura — o kernel não o clampa, não há recurso que ele consuma, e o §0 proíbe escrever um número sem nomear de que recurso ele é. ⚠️ **DESLIGADO O PASSE NEM CORRE**, e a coluna de estado da velocidade **não nasce** ⇒ byte-idêntico **por CONSTRUÇÃO no valor E no objeto cozido** — um grafo salvo antes destes params cozinha o mesmo stream, não só a mesma imagem. ⚠️ **A velocidade é GUARDADA e não derivada de duas saídas passadas** (`dl_vel`, escrita só com um teto armado): derivá-la pediria uma segunda linha de `PREV_OUT`, e as duas responderiam *"a que velocidade isto ia?"* — a segunda resposta divergiria no primeiro tique em que alguém mexesse no teto, que é precisamente quando a pergunta importa; e **o recém-nascido entra com velocidade ZERO** (ao contrário do `prev_out`, cujo fallback é o valor vivo), senão um elemento novo arranca com o impulso de um estranho, que é o oposto do que um teto de aceleração existe para impedir. **5 gates, 5 mutações, 5 sangram.** **E A LINHA 65 CAIU POR MEDIÇÃO, SEM UMA LINHA DE CÓDIGO:** ela marcava *"POR ELEMENTO: gap real"* no `motion.spring`, supondo que só reinterpretar o `inv_mass` como massa daria molas diferentes no mesmo conjunto — **a composição já as dá**, porque cada `motion.spring` tem os SEUS `tension`/`friction` e o `falloff` torna uma mola **transparente** onde vale zero. Medido (alvo que salta 0 → 5): o elemento da mola RÁPIDA sai em **7,0213** — **idêntico ao controle de uma mola só, ao dígito**, que é a prova de que a segunda é transparente ali — e o da LENTA em **2,0119**, ainda a subir. ⚠️ **E a cerca do `inv_mass` não devia ser derrubada de qualquer modo:** ali `0` significa *segue o alvo RIGIDAMENTE* (documentado e gateado), que é a lei do grupo J lida **correctamente** — *massa infinita segue a pose que o nó sabe PRESCREVER*, e o que este nó prescreve é o alvo cru; sob a lei de massa o mesmo `0` **CONGELARIA** o valor, a resposta oposta. ⇒ **P1 → P2** (ergonomia: N nós para N massas, como a linha 68 do mesmo nó já registra), com a massa CONTÍNUA por elemento **nomeada e não construída**. **Zero schema** (`PROJECT_SCHEMA` **84** intocado, `project*.rs` com diff vazio) · contrato congelado **3/3** (os params novos vivem no `MANIFEST` do NÓ; `ParamUnit`/`ParamChannelRange` são side-metadata do registry) · **zero crate nova, zero `Cargo.toml`, zero dep** · **nenhum ADR**. LOC: o `lib.rs` do delay cruzou 700 ⇒ split por responsabilidade em **`rate.rs`** (*quão DEPRESSA a saída pode mover-se* contra *quão ATRASADA ela chega*), e o harness do `tests.rs` — que está no teto — ganhou um SEGUNDO consumidor e por isso é `pub(crate)` em vez de uma fixture duplicada. Smoke: **`env PH2D_GPU_COOK_DEMO=53 cargo run -p ph2d-host-desktop --release`** — ⚠️ **a cena imprime as quatro bandas nomeadas; se a lista não aparecer, PARE**, e ⚠️ **DÊ PLAY** (a pergunta é *quão DEPRESSA*, e uma foto mostra quatro fileiras a alturas diferentes sem dizer porquê). Os dois pares, medidos: **1-2** a de baixo **não alcança os picos** (excursão **9,6234 → 3,2184**) e o caminho dela deixa de ser senoide para virar um vaivém de lados **RETOS**, porque uma taxa constante desenha uma reta · **3-4** a de baixo **PARTE devagar** (**0,1200** nos doze primeiros tiques contra **0,3509**) e **depois acompanha** (**0,2600**) — se ela ficar lenta o tempo todo, isto virou um teto de passo. ⚠️ **ABERTO e NOMEADO, não escondido numa barra folgada:** o teto é honrado **ao dígito na rampa** (`0,0800` em todo tique) e sobe a **`0,1678` no tique 70** — exactamente onde o alvo INVERTE —, com vizinhos irregulares (`−0,0800 · 0,0256 · 0,1678 · 0,0955 · 0,0800`). **A lei do kernel não pode produzir isso** (ela clampa `|out − prev|` por construção, e os cinco gates de unidade do nó, que dirigem o produto pelo `Cook`, sangram sob mutação) ⇒ a diferença tem de estar **entre o kernel e o que esta cena monta**, e o candidato nomeado é o `prev_out` que o gather entrega no tique da inversão. O gate fica **`#[ignore]` com o número e o mecanismo escritos** — o precedente dos dois `watercolor_app_params_incremental` do Painter: *uma barra afrouxada sobre um mecanismo que ninguém entendeu é um gate que deixou de perguntar*. **3 gates de cena verdes + 1 aberto + 1 sonda.** Censo re-medido na árvore: **125 nós · 540 params · 521 com hint · 157 com unidade** — e ele **RECONCILIA** com o do grupo K (`125 · 538 · 519 · 155`) exactamente pelos dois params desta wave, os dois com hint e com unidade. ⚠️ **A primeira leitura, no fecho da wave, dizia `124 · 535` e NÃO reconciliava** — era um binário velho; o número ficou **fora** desta entrada até fechar, porque *um censo que não bate com o anterior não é medição, é ruído com casas decimais*. ⚠️ **Próxima cena livre: `55`, e o número se CONTA lendo o `match` do `motion_state_demo_router.rs`** — nunca uma nota. **⬛ O GRUPO M — A CONTAGEM DA CONFERÊNCIA DEIXA DE SER ESCRITA À MÃO (2026-08-16, sem código de produto; ferramenta [`placar_conferencia.py`](docs/Motion%20Nodes/ferramentas/placar_conferencia.py) + §11.0 do [plano 89](docs/Motion%20Nodes/89_plano_conferencia_dos_nos.md)):** com a fila de P0 vazia, a pergunta *"quanto falta?"* passou a ser a que decide a próxima wave — e **o plano não tinha um total confiável**. ⚠️ **A varredura achou o estrago antes de qualquer número:** **SETE das dezassete folhas não tinham linha de Contagem nenhuma** · a folha 13 dizia **`3 P0 · 7 P1`** com a própria prosa **três parágrafos abaixo** a dizer *"a folha 13 não tem item aberto"* · e a §11 do plano — que é o **ACHADO ORIGINAL** e não o estado — continuava a ser lida como estado, o que *manda construir o construído*, exactamente o erro que este plano existe para não repetir. ⚠️ **A cura não é reconferir com mais cuidado, é a contagem deixar de ser ESCRITA:** a ferramenta lê a coluna `P` de cada linha da tabela canónica de sete colunas, e a linha de `**Contagem:**` de cada folha passou a ser **gerada por ela** e a dizer isso de si mesma. ⚠️ **A varredura tem DUAS decisões que os dados reais impuseram:** ela acha a tabela pelo **CABEÇALHO** e não pela contagem de colunas (a folha 03 tem outra tabela de sete), e lê **o primeiro campo que classifica como veredito** a partir do 5º — porque uma célula pode conter `|` dentro de um code span e aí o índice fixo aponta para o meio de outra frase (catorze linhas quebravam o índice, todas conferidas à mão). E ela **sai vermelha** se não souber ler uma linha: *uma linha que a varredura não entende é uma linha que a próxima contagem erra em silêncio*. **O placar de hoje, DERIVADO:** **P0 = 0 · P1 = 70 · P2 = 119 · ⏸ RIG = 18 · ✅ 118 · ⛔ 119, em 450 linhas de conferência.** ⚠️ **E a reconciliação achou a SEXTA célula envelhecida:** a folha 06 marcava `**P0/P1**` no *"a POSIÇÃO como variável"* do `motion.expression` — e as **lanes `x`/`y` existem** (`lib.rs:119-125`), com gate próprio afirmando a ORDEM que as torna corretas (*um stream que de facto carregue uma coluna escalar chamada `x` significa **aquela** coluna, e a resposta mais específica tem de vencer*). ⚠️ **A metade dela sobre o `motion.drive` continua de pé**, e é o que sobra da §10.0: *ler componente* fechou, **escrever coluna arbitrária não** — os dois P0 que o placar mostra são esse e o `motion.wave` sem N produtores. **⬛ O GRUPO N — O TREMOR GANHA TEXTURA (E UMA VOLTA): o `motion.wiggle` ganha as OITAVAS, o multiplicador e o LAÇO (2026-08-16, cena `=54`, **smoke OK 2026-08-16**; folha [06_animadores](docs/Motion%20Nodes/89_conferencia/06_animadores.md), linhas 32 e 33):** o nó mais **magro da família** — `channel·amplitude·frequency·seed` — passa a ter a **assinatura da própria referência**, `wiggle(freq, amp, octaves = 1, amp_mult = 0.5)` do After Effects, mais o *Looping + Loop Length* da Cavalry. Placar da folha 06: **12 → 10** P1. ⚠️ **A metade CARA já estava paga, a SEXTA vez nesta conferência** (depois do texto, do som, das formas, da tabela e da velocidade): a **LEI fractal** é a folha `ph2d-fbm`, que já tem três consumidores (`motion.noise` · `force.curl` · `force.wind`), e adotá-la **não muda a aparência** porque ela carrega a lei e **recebe o ruído de base por closure** — o `hash2` daqui continua sendo o daqui (ADR-0075 proíbe uma crate-nó depender de outra, mas a folha não é crate-nó), e a **4ª cópia da lei não nasceu**. ⚠️ **O default reduz por ARITMÉTICA e não por promessa:** com uma oitava o `eval` devolve `sum/total` com `total = 1.0`, e `n / 1.0` é `n` em IEEE-754; o deslocamento de oitava é `o = 0` ⇒ `x + 0.0`, também exacto ⇒ **os DOZE gates que aquele arquivo já tinha passam sem uma edição de asserção**, que é o que o prova. ⚠️ **A MUTAÇÃO ME CORRIGIU, a segunda vez em dois grupos:** eu escrevera que o deslocamento de oitava tinha de ir no eixo **X** senão dois elementos tremeriam *exactamente juntos* (porque `y` aqui é a IDENTIDADE do elemento, uma linha de ruído por instância) — medido, **falso**: o `eval` escala as **duas** coordenadas por oitava, então a linha de cada elemento já se separa sozinha, e a mutação do eixo **SOBREVIVEU aos cinco gates**. ⚠️ **O que o deslocamento de facto compra é o CANTO DEGENERADO, e ali ele é grande:** em `t = 0` com `seed = 0` o elemento `0` tem `px = 0` **e** `py = 0`, e multiplicar zero por dois é zero — todas as oitavas caem no MESMO canto da grade, a soma colapsa numa oitava só e o valor fica preso em **`-1.000000`**, o extremo do alcance (é literalmente `hash2(0,0)`); com o deslocamento, **`-0.521581`**. *Uma peça atirada ao extremo no instante zero de toda reprodução é visível*, e a fixture do gate **tem de estar no canto** — em qualquer outro ponto a mutação sobrevive. O eixo fica sendo o X por **convenção com motivo** (X é o tempo, que este nó rola; Y é a identidade, que ele não deve mexer), não por correção. ⚠️ **E A PARIDADE APANHOU UM DEFEITO REAL QUE SÓ EXISTIA PORQUE A FIXTURE PASSOU A CONTER O FENÔMENO:** o gate de device do wiggle cozinhava **`octaves = 1`**, o default, então o laço fBm e a costura do laço no tempo **não eram percorridos por lado nenhum** — os dois concordariam **por VÁCUO**, a mesma armadilha que a fixture do `value.noise` já documentou quando cozinhava `kernel = 0`. Com `octaves = 4`, `amp_mult = 0.63` e `loop_len = 3.7` ela reprovou na RTX com **`|dif| 0,668`** — da ordem da amplitude, não de um ulp: **o meu laço WGSL escalava só o X e a folha escala as DUAS coordenadas**. Curado, **1/1 verde**. ⚠️ **E uma frase de OUTRO gate deixou de ser verdade com esta wave:** o doc do `value_noise_kernel_matches_the_cpu_on_the_device` dizia que ele exercitava *"the fBm octave loop, **the piece wiggle never exercises**"* — corrigido no mesmo commit, com o que sobra de exclusivo lá nomeado (a ROTA valor→`drive`, não o laço). ⚠️ **Três higienes que a wave arrastou:** os **três variants de WGSL carregavam `wgsl_lib` LITERAL** (três blocos byte-idênticos de 1527 caracteres — *toda lei nova de ruído teria de ser escrita três vezes e podia divergir em duas*), colapsados num `WG_LIB` como o `motion.noise` e o `motion.oscillator` já faziam · a **lacunaridade NÃO vira param aqui** (o `wiggle()` da referência não a tem e o irmão `motion.noise` já a expõe — *dois nós com o mesmo knob é o que faz um artista perguntar qual dos dois manda*) · e o **rótulo é o da REFERÊNCIA e não o do irmão** (*Amp Multiplier*, do AE, contra o *Roughness* do `motion.noise`: quem procura o wiggle procura a palavra do AE). ⚠️ **A unidade do `loop_len` é `Seconds` e NÃO `FromChannel`** — o precedente é o `ticks` do `motion.delay`: *uma duração não muda de unidade com o que ela fecha*. **5 gates novos + os 12 que já existiam · 4 mutações, 4 sangram** · **zero schema** (`PROJECT_SCHEMA` **84** intocado) · contrato congelado **3/3** (os três params vivem no `MANIFEST` do NÓ) · **1 `Cargo.toml`** (`ph2d-fbm` na crate do wiggle — **aresta interna de path**, e o `Cargo.lock` não ganha um `+name`) · **nenhum ADR**. Smoke: **`env PH2D_GPU_COOK_DEMO=54 cargo run -p ph2d-host-desktop --release`** — ⚠️ **a cena imprime as quatro bandas nomeadas; se a lista não aparecer, PARE**, e ⚠️ **DÊ PLAY**. ⚠️ **As quatro têm a MESMA amplitude e a MESMA frequência de propósito** — se diferissem, *"a de baixo mexe mais"* responderia por qualquer coisa. Os dois pares, medidos: **1-2** a de cima ondula LISA e a de baixo ondula **E TREME**, com a rugosidade a subir de **0,00189 para 0,01933** (10×) e a excursão a **ficar na mesma ordem** (3,887 contra 2,494) — *se a de baixo só parecer MAIOR, isto virou um segundo controle de amplitude* · **3-4** o laço de 2 s, e ⚠️ **é o único par desta cena que precisa de PACIÊNCIA** (um campo que fecha e um que não fecha são indistinguíveis num instante): medido na volta de 2 s, o controle desvia **3,855** e a fileira com laço **0,000002**. **⬛ O GRUPO O — A FORMA DA ONDA E O DESLIZE DA RAMPA, e as três cópias de WGSL do oscilador JÁ TINHAM DIVERGIDO (2026-08-16, cena `=55`, **smoke OK 2026-08-16**; folha [06_animadores](docs/Motion%20Nodes/89_conferencia/06_animadores.md), linhas 26 e 30):** o `motion.oscillator` ganha o **`pulse_width`** e o `motion.stagger` o **`offset`**. Placar da folha 06: **10 → 8** P1. ⚠️ **UM knob para o que a referência dá em DOIS:** o TouchDesigner tem *Pulse Width* (a fatia do ciclo em que a Square fica em cima) e *Bias* (onde o pico da Triangle/Saw se senta), e eles são **o mesmo número** — *dois nomes para um número é como um artista aprende que são coisas diferentes*. A lei é um **WARP DE FASE** aplicado a `f` **antes** da forma, então as **CINCO** formas o herdam de uma vez (a Square ganha o ciclo de trabalho, a Triangle e a Saw o *bias*, a senoide a versão enviesada). ⚠️ **O neutro é a IDENTIDADE por aritmética:** `0.5 / 0.5` é exactamente `1.0` ⇒ o primeiro ramo é `f * 1.0`; e no segundo `f − 0.5` é exacto (Sterbenz, os dois dentro de um fator de dois) e `0.5 + (f − 0.5)` reconstrói `f` **ao bit**. ⚠️ **E A WAVE ACHOU UM DEFEITO VIVO NO DEVICE, que é o preço exacto da duplicação:** o oscilador tem **três** variants de WGSL (um por coluna escrita) e cada um carregava a biblioteca **LITERAL** — medido, **só o de `P` tinha o `osc_cycles_per_second`**; os de `rot` e `size` liam `params.frequency` **direto** e ignoravam `time_mode`/`bpm`, que estão no uniforme dos três. Na CPU a régua vale para todo canal ⇒ **um grafo a dirigir a ROTAÇÃO em BPM corria a `frequency` no device e a `bpm/60` na CPU**, sem erro e sem aviso. ⚠️ **Nenhum gate o via, e a razão é estrutural:** o sweep de canais ao lado **nunca liga o `time_mode`** e os gates de matemática só usam o canal `P` — a fixture não continha o fenômeno. Gate **red-first** `the_bpm_ruler_reaches_every_oscillator_channel` (RED com `|dif| 0,0397` na Rotation, Y verde), e a cura foi **extrair a biblioteca para UM lugar**, que a resolve por construção. ⚠️ **A MUTAÇÃO ME CORRIGIU PELA TERCEIRA VEZ EM DOIS GRUPOS:** o doc do `PW_MIN` dizia que os extremos são *uma divisão por zero* — **não são**: com `p = 0` o ramo que divide por `p` é **inalcançável** (`f < 0` é sempre falso) e com `p = 1` o outro também (`frac` vive em `[0,1)`), e apagar o clamp deixava tudo **finito e os quatro gates VERDES**. O que ele compra é a onda continuar a **ser uma onda**: o param é `f32`, logo **dirigível por fio** (doc 58), e um `pw = 7` sem clamp comprime o ciclo inteiro nos primeiros 7% do domínio da forma ⇒ a excursão colapsa. O gate passou a medir isso. ⚠️ **E o `offset` do stagger tem a sua própria armadilha de PONTA:** o último elemento da rampa senta **exactamente em `1.0`**, e `frac(1.0)` é `0.0` — um `frac` aplicado sempre mandaria a peça do fim para o começo **com o knob no neutro**, e só ela notaria ⇒ o `frac` **só corre com o knob armado**. ⚠️ **Duas afirmações minhas caíram nos gates dele, e as duas são a mesma:** *"uma volta inteira é o neutro"* e *"o alcance não encolhe"* — uma rampa **INCLUSIVA não é um ciclo** (rolar trata `0` e `1` como o mesmo ponto, então a ponta cai em cima do começo); o que é verdade, e o que o artista vê, é que ela **ROLA**, afirmado ponto a ponto. ⚠️ **E a mutação *"o offset é ignorado"* SOBREVIVEU à primeira fixture** porque ela **re-implementava a lei** em vez de cozer o nó — o oráculo-espelho; reescrita pelo `Cook`, ela sangra. **8 gates novos (4 no warp + 3 no offset + 1 de paridade) · 5 mutações, 5 sangram** · **zero schema** (`PROJECT_SCHEMA` **84** intocado) · contrato congelado **3/3** · **zero `Cargo.toml`**, nenhuma dep, **nenhum ADR**. Smoke: **`env PH2D_GPU_COOK_DEMO=55 cargo run -p ph2d-host-desktop --release`** — ⚠️ **a cena imprime as quatro bandas nomeadas; se a lista não aparecer, PARE**, e ⚠️ **ela tem DUAS naturezas de propósito:** o par 1-2 **precisa de PLAY** (é uma onda no tempo — medido, o controle fica **0,556** do ciclo em cima e o estreito **0,222**) e o par 3-4 **julga-se PARADO** (o `motion.stagger` não lê o relógio — a peça mais alta sai do índice **23** para o **14**, e a rampa cai **UMA** vez, que é a emenda; *se ela serrilhar, isto embaralhou em vez de rolar*). **⬛ O GRUPO P — O ESCRITOR DE COLUNA NOMEADA, e a §10.0 do plano FECHA (2026-08-16, cena `=56`, **smoke OK 2026-08-16**; folha [06_animadores](docs/Motion%20Nodes/89_conferencia/06_animadores.md) linha 38):** o `motion.drive` ganha o canal **`Custom…`** — ele escreve num **nome que o artista batiza**, e não só nas nove colunas que o enum conhece. ⚠️ **É a assimetria que SEIS famílias citaram e que o doc 63 marcava como *"TEMOS"* apontando para o LEITOR:** o `value.attribute` **LÊ** qualquer coluna por nome (o escape *"Custom…"* + text param `attr`) e este nó só **ESCREVIA** nas do enum — *um item marcado TEMOS que não existe não manda construir o construído: manda NÃO construir o que a §10.0 identificou como o gargalo*. ⚠️ **O índice é o ÚLTIMO (9) e isso não é arrumação:** o `channel` é um param que o **documento GUARDA**, então renumerar o que já existe re-aponta em silêncio todo grafo salvo — o espaço positivo cresce para cima, como o `CH_HUE` fez. O NOME viaja num **text param** (`column`), o padrão canônico para param não-f32 (o manifesto é f32-only por contrato congelado), no mesmo assento da fórmula do `motion.expression`. ⚠️ **ELE RECUSA O DEVICE, e o motivo é ESTRUTURAL, não preguiça:** uma `ColumnBinding` carrega o nome como `&'static str` — o sequenciador precisa dele **antes** de cozer, para montar o bind group — e um nome que o artista digita só existe **em tempo de cook**; o recuo passa pela porta `applicable`, a mesma que a `Median` do `value.reduce` usa. *Um caminho mais lento é honesto; um nome escrito no buffer errado não é.* ⚠️ **E isto RE-PRECIFICA a W0-B-genérico do plano**, que previa *"o análogo de ESCRITA do `StreamOp::Project` — maquinaria de sequenciador com pipes e paridade próprios, não um apêndice"*: medido, **isso é o preço de o Custom correr no DEVICE, e não o de ele EXISTIR** — a metade que desbloqueia as famílias cabe num canal e num recuo. ⚠️ **A recusa de ESCRITA é feita ao STREAM, nunca a uma lista de nomes proibidos:** escrever um `Scalar` por cima de um `P` (`Vec2`) ou de um `tint` (`Vec4`) **muda o TIPO** da coluna, e todo leitor a jusante passa a ler outra coisa — a pergunta é *a coluna que já está lá é escalar?*, porque *uma lista apodrece na décima convenção* e esta casa já tem `texture_id`, `geometry_id`, `uv_rect` e `nrm`. Nome vazio e tipo trocado devolvem a **entrada verbatim**. ⚠️ **A LEI é a MESMA dos nove canais do enum** — o `blend` por `falloff`, o broadcast do campo de valor, o `scale` — partilhada por helpers e não copiada; o que difere é só *onde o número pousa*. ⚠️ **E a fixture não continha a MÁSCARA:** sem uma coluna `falloff`, `falloff_at` devolve `1.0` e `blend(x, driven, 1)` **é** `driven` ⇒ a mutação que apagava a mistura **SOBREVIVEU**; com a máscara na fonte desde o começo (e um gate que pergunta pelo elemento de `falloff = 0`), ela sangra. **8 gates novos · 4 mutações, 4 sangram** · **zero schema** (`PROJECT_SCHEMA` **84**) · contrato congelado **3/3** (o canal é um índice do param `channel` e o nome é text param — o `NodeManifest` não é tocado) · **zero `Cargo.toml`**, nenhuma dep, **nenhum ADR**. Smoke: **`env PH2D_GPU_COOK_DEMO=56 cargo run -p ph2d-host-desktop --release`** — ⚠️ **a cena imprime as quatro bandas nomeadas; se a lista não aparecer, PARE**, e ela **julga-se PARADA**. ⚠️ **Ela é um CIRCUITO, não um efeito:** o drive escreve `heat`, o `value.attribute` lê `heat`, um segundo drive põe no TAMANHO — e se qualquer elo faltar a fileira sai toda do mesmo tamanho. Os dois pares: **1-2** a mesma cadeia **sem** o escritor é plana (vão **0,0000**) e com ele cresce (**0,7000**) · **3-4** o par do **NOME**: ler `cold`, que ninguém escreveu, volta a ser plano — *uma coluna é uma palavra, e a palavra errada não é um erro, é o SILÊNCIO*. ⚠️ **E a primeira versão desta cena saiu com as QUATRO bandas planas** — a assinatura exacta da feature quebrada, sobre um nó cujos oito gates de unidade passavam: o `value.instance_field` tem **porta de entrada** (sem ela o campo sai vazio) e o `mode = 0` é o índice **CRU**, não a rampa `0..1`. *O defeito era da fixture, e só a sonda da cena o mostrou.* Censo re-medido na árvore: **125 nós · 545 params · 526 com hint · 158 com unidade** ⚠️ e ele **RECONCILIA** com o do grupo L (`125 · 540 · 521 · 157`) exactamente pelos cinco params das waves N e O, das quais só o `loop_period` carrega unidade. ⚠️ **Próxima cena livre: `57`, e o número se CONTA lendo o `match` do `motion_state_demo_router.rs`** — nunca uma nota.
 **⬛ E A AUDITORIA MULTIAGÊNTICA DA JORNADA (2026-08-16, ordem do Enio: *"auditoria completa multiagêntica"*):** oito lentes de leitura em paralelo (disciplina de gate · correção de kernel · paridade CPU×GPU · neutro/byte-identidade · superfície de colisão · doc-contra-código · cenas de smoke · costura de UI) → dedup → **catorze céticos** com `refuted: true` por omissão → síntese. ⚠️ **A divisão que fez a auditoria valer é *o que se LÊ* contra *o que só um build SERIAL mede*:** o fan-out achou 36 candidatos, e as duas coisas que **nenhuma leitura podia achar** saíram da medição — o `--ignored` **CANCELA na primeira falha** e por isso as duas suítes de GPU NOVAS da jornada (`gpu_proximity`/`gpu_boids`) **nunca tinham corrido** (`--no-fail-fast` + `--test <nome>`: 38/38), e o `value_slope` vermelho é **pré-existente e confirmado a todos os dígitos**. **Quatro achados sobreviveram aos céticos e DOIS dissolveram na verificação** — *é o resultado honesto de uma varredura, e está escrito para ninguém os re-abrir*: a sanitização do `inv_mass` no device é o **espelho exacto** da CPU (`w <= 0.0` → early-out nos dois), e as duas *"afirmações que contradizem o próprio gate"* eram uma correção **já narrada** no teste do stagger e uma mola de compressão que **não existe** no fonte do boids. **As quatro reais:** o WGSL do boids **elevava ao quadrado um param com sinal** que a CPU sanitiza (o quadrado apaga o sinal, logo a sanidade tem de vir ANTES dele) · o teto de taxa do `motion.delay` era **INERTE** numa posição alcançável do slider · o `motion.drive(Custom…)` podia **sobrescrever uma coluna de escrituração** (`is_bookkeeping_column`, porta nova na `ph2d-nodegraph` ao lado do `VALUE_COLUMN` — ⚠️ *não* a lista `INTERNAL` do picker, que responde outra pergunta: `falloff` é lida **e** escrita) · e o gate de sub-passos da corda era **TAUTOLÓGICO** (comparava `Params { substeps: 1 }` consigo mesmo em vez de passar pela porta de LEITURA). ⚠️ **E DOIS defeitos foram MEDIDOS e NÃO curados, porque a cura mudaria um smoke já aprovado:** os sub-passos compõem com o `damping` (a cena `=52` não escreve `damping`, então o smoke aprovado já continha a composição) e a `=51` **diferia em DUAS coisas** — corrigida aqui, com a banda tratada **byte-idêntica à aprovada** e só o CONTROLE a ficar honesto. ⚠️ **E a barra do gate da `=51` desceu de 1,4× para 1,25×, o que NÃO é afrouxar:** ela só era satisfazível porque a banda tratada carregava também o peso maior, e com o peso nos DOIS lados o controle empacota a 1,1824 contra 1,6137 do tratado (razão medida **1,365**) — *uma barra que código correto não consegue satisfazer não é rigor, é um gate partido*; a metade categórica da wave é a de cima (**11 sobrepostos contra ZERO**), e o raciocínio está escrito no próprio gate, não numa nota. **A superfície de colisão, MEDIDA pelo integrador na árvore combinada:** `PROJECT_SCHEMA` **84 INTOCADO** — ⚠️ conferido nos **três** sítios (`project.rs` · o irmão `project_schema.rs`, que a `line/physics` cortou em 15/08 · a tripla **`(84, 13, 14)`**), e a razão de conferir os três mesmo numa linha que não bumpa é que *um degrau escrito no arquivo antigo funde LIMPO e evapora* · contrato congelado **INTOCADO** (`git diff` vazio em `ph2d-nodegraph/src/node.rs` e `ph2d-core/src/tool.rs`) · registro do `ph2d-ecs` **INTOCADO** ⇒ os **três** espelhos também (**57 / 58 / 58**, re-medidos pós-merge) · `ph2d-i18n` **INTOCADO** ⇒ a cadeia `vector::tr(k).or_else(sculpt3d::tr)` fica intacta · **nenhum ADR** ⇒ a linha ficou **FORA de toda disputa de número** · **1 crate nova** (`ph2d-node-motion-proximity`, folha drop-in, glob member) e **ZERO pacote externo novo** (o único `+name` do `Cargo.lock` é ela) · **4 `Cargo.toml`**, e ⚠️ **a crate-nó entra em `[dev-dependencies]` da `ph2d-gpu-cook`** (o `src/` não a referencia — conferido por grep ⇒ **machete-safe**, o precedente das cinco da `line/gpu-nodes`) · cenas de smoke **1..56 contínuas, 56 distintas, zero duplicata** ⇒ **próxima livre: 57** · censo re-medido na árvore combinada: **125 nós · 545 params · 526 com hint · 158 com unidade**. ⚠️ **O rebase de 55 commits teve ZERO conflitos, e a razão é MEDÍVEL:** a interseção *arquivos da linha ∩ arquivos que o `main` moveu nesta janela* é de **UM** — o `CLAUDE.md` —, e as duas edições caem a ~88 linhas uma da outra (o `main` trazia a entrada do **Vector**, esta linha escreve na do **Motion Nodes**); conferido **nos dois sentidos depois do rebase** (as duas entradas sobreviveram), mais varredura de marcadores **incluindo `|||||||`** (diff3) sobre os 98 arquivos. ⚠️ **O ponto de merge sensível é para quem vier DEPOIS:** o `motion_state_demo_router.rs` cruzou 600 LOC e as nove cenas de grupo saíram para o irmão `motion_state_demo_conferencia.rs`, que **não tem `match` nenhum, de propósito** (conferido: **0**) — o roteador continua a ser a **ÚNICA** lista de níveis, porque dois `match` em dois arquivos deixariam um nível reivindicado duas vezes passar **em silêncio** (o compilador só vê `unreachable pattern` dentro de um mesmo `match`); **nenhuma outra linha viva toca esses dois arquivos hoje**. **Gate da árvore combinada, rodado pelo integrador:** `cargo fmt --all -- --check` **EXIT 0** · `typos` **EXIT 0** — ⚠️ conferido pelo **ESTADO** e nunca pelo exit de um pipe, que é sempre o do `tail` · clippy `--workspace --all-targets --features ph2d-spike/bevy_ecs -- -D warnings` **EXIT 0, zero warnings** · codegen sync **no-op** (o script de integração não produziu commit de regen: os 55 que pousaram são exatamente os da linha) · `nextest --workspace --cargo-profile ci-test --no-fail-fast` **16.227 correram, 16.227 passaram, 1.592 skipped** ⇒ **zero vermelho e ZERO flake nesta corrida** (⚠️ as duas que a linha exonerou por três testemunhas — `the_cost_of_depth_is_linear_not_explosive` e `the_fit_rebuilds_the_neighbourhood_not_the_whole_stroke` — **passaram aqui com a máquina em `load 18`**, o que é a assinatura de carga a confirmar-se pelo outro lado) · gates de GPU do `ph2d-gpu-cook` **em SÉRIE** (`--run-ignored all --test-threads=1`, RTX 5060 Ti com o adapter **ENCONTRADO** — *skip gracioso não é verde*): **265 correram, 264 passaram**, com **ZERO `Out of Memory` do wgpu** — que é a razão de o `--test-threads=1` não ser gosto (em paralelo cada binário abre o próprio device e a linha mediu **três** a morrerem assim). ⚠️ **A única falha é o `value_slope_kernel_matches_the_cpu_on_the_device`, e ela é PRÉ-EXISTENTE, exonerada por TRÊS testemunhas e nunca por opinião:** o `git diff` de `ph2d-node-value-slope` contra o `main` é **VAZIO** · o número medido é **`max |d| = 1,05023384e-4`, idêntico a todos os dígitos** ao que esta §5 já registrava como reprovando no `main` · e ele é **`#[ignore]`**, logo fora do ship. ⚠️ **E as DUAS suítes de GPU NOVAS da jornada correram de facto** (`gpu_proximity` · `gpu_boids`) — a auditoria tinha descoberto que o `--ignored` **CANCELA na primeira falha** e que por isso elas **nunca tinham corrido**; ⚠️ os seis probes de escala do `gpu_boids_scale` são a parte cara e o número fica escrito para quem vier (**867 s** de suíte, com o `where_does_the_flock_settle` sozinho a varrer **1 M de agentes por 9600 tiques** e a levar mais de seis minutos) · e as outras quatro crates de GPU (`ph2d-render` · `ph2d-flip-render` · `ph2d-paint-gpu` · `ph2d-mesh-render`) têm **diff VAZIO** ⇒ fora do alcance desta linha. ⚠️ **Integrar não é aprovar, e aqui a distinção tem NOME:** as oito cenas foram aprovadas pelo Enio **à medida que fecharam**, mas a **auditoria correu DEPOIS delas** — as quatro correções reais e a `=51` re-armada **não passaram por smoke**. **Re-smoke é barato e é decisão do Enio.** **Aberto, com o preço ao lado:** ⚠️ um gate **`#[ignore]` NOVO com o número e o mecanismo escritos** (`the_ceiling_is_honoured_on_every_tick_including_the_turn`, cena `=53`) — o teto vale **ao dígito na rampa** (`0,0800`) e sobe a **`0,1678` no tique da inversão**; a lei do kernel **não pode** produzir isso (ela clampa `|out − prev|` por construção, e cinco gates de unidade sangram sob mutação) ⇒ a diferença mora **entre o kernel e o que a CENA monta**, com o `prev_out` do gather como candidato nomeado, e ⛔ **não afrouxe a barra** (o precedente é o par `watercolor_app_params_incremental` do Painter) · ⛔ **a composição sub-passos × `damping`** da `motion.verlet_rope` está **MEDIDA e NÃO curada de propósito** (a cena `=52` não escreve `damping`, então o smoke aprovado já continha a composição; as duas curas candidatas moram no doc-comment do param com um ⛔ explícito) · a folha 03 tem **6 P1** e a folha 07 tem **3** · ⚠️ **e a primeira coisa de toda wave desta conferência é MEDIR se a composição já exprime o item** — **seis** células envelheceram antes de alguém voltar a elas nesta jornada (o `max_force`, o *wander*, a idade normalizada, o `motion.lag` inteiro, a posição como variável do `motion.expression` e a colisão da corda): *o que se perde ao não reconferir não é tempo, é construir o que já existe.*
