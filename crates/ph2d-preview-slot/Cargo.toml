[package]
name = "ph2d-preview-slot"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
license.workspace = true
authors.workspace = true

[lib]

# ⭐ ZERO dependências, e isso é a medida do assunto: a ranhura é dados puros
# (`u32`×3 · `usize` · `u64`). Nenhum tipo de render, de ECS ou de ferramenta
# atravessa esta fronteira.
[dependencies]
