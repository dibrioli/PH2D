[package]
name = "ph2d-node-motion-wiggle"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
license.workspace = true
authors.workspace = true

[lib]

[dependencies]
ph2d-nodegraph = { path = "../ph2d-nodegraph" }
ph2d-node-registry = { path = "../ph2d-node-registry" }
# A LEI fractal, não o ruído — a folha carrega como as oitavas se somam e
# recebe o ruído de base por closure, então adotá-la NÃO muda a aparência
# deste nó (o `hash2` daqui continua sendo o daqui). É o quarto consumidor,
# depois do `motion.noise`, do `force.curl` e do `force.wind`.
ph2d-fbm = { path = "../ph2d-fbm" }
