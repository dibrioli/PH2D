//! (Empty after ADR-0040 TG-C — padding vocab moved into
//! `ph2d_tool_padding::params`, joining bgremoval which migrated in TG-B.)
//!
//! All tool implementations and their UI/action vocabulary now live in
//! satellite crates (`ph2d-tool-*`). This module is kept only as a
//! placeholder until TG-D removes `pub mod tools` from `lib.rs` and
//! deletes the file entirely (no remaining content to host).
